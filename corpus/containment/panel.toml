# corpus seed — containment. Default audit.Rules(); no [rules] override, and in
# particular no declared spill: a seed that needs a loosened rule proves nothing
# about the shipped predicate.
[probe]
root = ".panel"

[probe.containers]
plate = ".plate"

[probe.parts]
blk = ".blk"
