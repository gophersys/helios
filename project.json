{
  "name": "workspace",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "root": ".",
  "targets": {
    "setup": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/setup-workspace.mjs"
      },
      "cache": false
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash scripts/check-workspace.sh"
      },
      "cache": false
    }
  }
}
