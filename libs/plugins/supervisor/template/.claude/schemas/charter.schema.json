{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://eden.gophersys.dev/supervisor/charter.schema.json",
  "title": "Charter",
  "description": "The product charter the supervisor drafts. Written to init/charter/charter.md (front-matter is this object; body is prose). Frozen on ratification.",
  "type": "object",
  "required": ["title", "problem", "outcomes", "non_goals", "constraints", "stakeholders"],
  "additionalProperties": false,
  "properties": {
    "title": { "type": "string", "minLength": 3, "maxLength": 120 },
    "problem": { "type": "string", "minLength": 20, "description": "The problem being solved, in plain language." },
    "outcomes": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 5 },
      "description": "The measurable outcomes that define success."
    },
    "non_goals": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 3 },
      "description": "Explicit out-of-scope items, so scope cannot creep silently."
    },
    "constraints": {
      "type": "array",
      "items": { "type": "string", "minLength": 3 },
      "description": "Hard constraints (budget, deadline, technology, regulatory)."
    },
    "stakeholders": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 2 },
      "description": "Named roles or people who own outcomes."
    }
  }
}
