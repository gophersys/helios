{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://eden.gophersys.dev/supervisor/audit-entry.schema.json",
  "title": "AuditEntry",
  "description": "One newline-delimited JSON record appended to audit/log.ndjson by every transition command. The append-only audit trail of the supervisor's deterministic decisions.",
  "type": "object",
  "required": ["at", "transition", "from", "to", "artifact", "guard"],
  "additionalProperties": false,
  "properties": {
    "at": { "type": "string", "format": "date-time" },
    "transition": { "type": "string", "minLength": 1 },
    "from": { "type": "string", "minLength": 1 },
    "to": { "type": "string", "minLength": 1 },
    "artifact": { "type": "string", "minLength": 1 },
    "guard": { "type": "string", "description": "The guard id that was satisfied to admit this transition." },
    "by": { "type": "string", "description": "Audit identity that drove the transition." },
    "detail": { "type": "string" }
  }
}
