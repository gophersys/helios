{
  "name": "protos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "",
  "projectType": "application",
  "targets": {
    "create": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "chmod +x ./libs/protos/ctl.sh && ./libs/protos/ctl.sh generate"
        ],
        "cwd": "{workspaceRoot}"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "chmod +x ./libs/protos/ctl.sh && ./libs/protos/ctl.sh clean"
        ],
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
