# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Product(bases.BaseProduct):
    """A hardware product that Concord tests: Sigma5, Theta, Alpha, etc.
    Everything flows from here -- fixtures, tests, sessions, builds.
    """

    id: _str
    name: _str
    """"Alpha B0", "Sigma5 C0"
    """

    slug: Optional[_str] = None
    """"alpha_b0", "sigma5_c0" -- URL-safe identifier
    """

    description: Optional[_str] = None
    active: _bool
    buildConfig: Optional['fields.Json'] = None
    """structured build configuration (overlays, post-build steps)
    """

    metadata: Optional['fields.Json'] = None
    """additional product-specific config
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    targets: Optional[List['models.ProductTarget']] = None
    boards: Optional[List['models.Board']] = None
    firmwareBuilds: Optional[List['models.FirmwareBuild']] = None
    fixtures: Optional[List['models.Fixture']] = None
    tests: Optional[List['models.Test']] = None
    sessions: Optional[List['models.Session']] = None
    buildJobs: Optional[List['models.BuildJob']] = None
    pipelineRuns: Optional[List['models.PipelineRun']] = None
    deployments: Optional[List['models.Deployment']] = None
    stageConfigs: Optional[List['models.ProductStageConfig']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductKeys']] = None,
        exclude: Optional[Iterable['types.ProductKeys']] = None,
        required: Optional[Iterable['types.ProductKeys']] = None,
        optional: Optional[Iterable['types.ProductKeys']] = None,
        relations: Optional[Mapping['types.ProductRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Product_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Product_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Product_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Product_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Product_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Product_relational_fields:
                        raise errors.UnknownRelationalFieldError('Product', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Product / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Product',
            }
        )
        _created_partial_types.add(name)


class ProductTarget(bases.BaseProductTarget):
    """A firmware target within a product (e.g. "comms" on nRF9151, "app" on nRF52840).
    Each target has a role, SoC, and unique appId used for OTA addressing.
    """

    id: _str
    productId: _str
    role: _str
    """"comms", "app" -- functional role
    """

    soc: _str
    """"nRF9151", "nRF52840" -- SoC identifier
    """

    appId: _int
    """CoreCloud application ID (108, 109)
    """

    product: Optional['models.Product'] = None
    firmwareBuilds: Optional[List['models.FirmwareBuild']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductTargetKeys']] = None,
        exclude: Optional[Iterable['types.ProductTargetKeys']] = None,
        required: Optional[Iterable['types.ProductTargetKeys']] = None,
        optional: Optional[Iterable['types.ProductTargetKeys']] = None,
        relations: Optional[Mapping['types.ProductTargetRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductTargetKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductTarget_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductTarget_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductTarget_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductTarget_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductTarget_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductTarget_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductTarget', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductTarget / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductTarget',
            }
        )
        _created_partial_types.add(name)


class Board(bases.BaseBoard):
    """A distinct PCB design within a product (e.g. "Main Board", "Sensor Board").
    Each product has exactly one board, linked to a ck_boards definition.
    """

    id: _str
    productId: _str
    name: _str
    """"Main Board", "Sensor Board"
    """

    ckBoardsName: _str
    """west board name from ck_boards repo, e.g. "alpha_b0"
    """

    ckBoardsBranch: _str
    """branch in ck_boards repo
    """

    vendor: _str
    description: Optional[_str] = None
    active: _bool
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    revisions: Optional[List['models.BoardRevision']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BoardKeys']] = None,
        exclude: Optional[Iterable['types.BoardKeys']] = None,
        required: Optional[Iterable['types.BoardKeys']] = None,
        optional: Optional[Iterable['types.BoardKeys']] = None,
        relations: Optional[Mapping['types.BoardRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BoardKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Board_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Board_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Board_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Board_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Board_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Board_relational_fields:
                        raise errors.UnknownRelationalFieldError('Board', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Board / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Board',
            }
        )
        _created_partial_types.add(name)


class BoardRevision(bases.BaseBoardRevision):
    """A board revision (e.g. A0, B0, B1) -- a specific version of a Board PCB.
    """

    id: _str
    boardId: _str
    version: _str
    """e.g. "A0", "B0", "B1"
    """

    status: 'enums.LifecycleStatus'
    notes: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    board: Optional['models.Board'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BoardRevisionKeys']] = None,
        exclude: Optional[Iterable['types.BoardRevisionKeys']] = None,
        required: Optional[Iterable['types.BoardRevisionKeys']] = None,
        optional: Optional[Iterable['types.BoardRevisionKeys']] = None,
        relations: Optional[Mapping['types.BoardRevisionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BoardRevisionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BoardRevision_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BoardRevision_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BoardRevision_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BoardRevision_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BoardRevision_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BoardRevision_relational_fields:
                        raise errors.UnknownRelationalFieldError('BoardRevision', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BoardRevision / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BoardRevision',
            }
        )
        _created_partial_types.add(name)


class FirmwareBuild(bases.BaseFirmwareBuild):
    """An uploaded firmware build file with version metadata.
    """

    id: _str
    productId: _str
    targetId: Optional[_str] = None
    """FK to ProductTarget (optional for legacy builds)
    """

    version: _str
    """Semantic version string "1.0.0"
    """

    isManufacturing: _bool
    status: 'enums.LifecycleStatus'
    storageKey: _str
    """MinIO object key
    """

    filename: _str
    sizeBytes: _int
    checksum: _str
    """SHA-256 hex digest
    """

    contentType: Optional[_str] = None
    modemStorageKey: Optional[_str] = None
    """MinIO key for modem firmware zip (modem targets only)
    """

    modemFilename: Optional[_str] = None
    modemSizeBytes: Optional[_int] = None
    modemChecksum: Optional[_str] = None
    """SHA-256 hex digest of modem file
    """

    notes: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    target: Optional['models.ProductTarget'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FirmwareBuildKeys']] = None,
        exclude: Optional[Iterable['types.FirmwareBuildKeys']] = None,
        required: Optional[Iterable['types.FirmwareBuildKeys']] = None,
        optional: Optional[Iterable['types.FirmwareBuildKeys']] = None,
        relations: Optional[Mapping['types.FirmwareBuildRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FirmwareBuildKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FirmwareBuild_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FirmwareBuild_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FirmwareBuild_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FirmwareBuild_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FirmwareBuild_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FirmwareBuild_relational_fields:
                        raise errors.UnknownRelationalFieldError('FirmwareBuild', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FirmwareBuild / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FirmwareBuild',
            }
        )
        _created_partial_types.add(name)


class ProductStageConfig(bases.BaseProductStageConfig):
    """Configuration for a single validation stage (1-5) within a product.
    Defines what firmware to build, what tests to run, and queue priority.
    """

    id: _str
    productId: _str
    stage: _int
    """1=Smoke, 2=Silicon, 3=Integration, 4=Nightly, 5=FUOTA
    """

    name: _str
    """Display name: "Smoke", "Silicon", "Integration", "Nightly", "FUOTA"
    """

    enabled: _bool
    buildScript: Optional[_str] = None
    """Bash build script content
    """

    buildTarget: Optional[_str] = None
    """West board name ("alpha_b0") or "native_sim"
    """

    fwRepoUrl: Optional[_str] = None
    """Firmware repo SSH URL
    """

    fwRepoBranch: Optional[_str] = None
    """Branch to build from
    """

    mfgRepoUrl: Optional[_str] = None
    """Manufacturing firmware repo (stages 4-6)
    """

    mfgRepoBranch: Optional[_str] = None
    """Manufacturing firmware branch
    """

    buildVariant: Optional[_str] = None
    """Default variant: "debug", "release", "mfg", "test"
    """

    configFlags: Optional['fields.Json'] = None
    """Build-time flags: {"harness": true, "debug": false}
    """

    buildMatrix: Optional['fields.Json'] = None
    """Stage-specific build definitions (array of BuildDef)
    """

    testDirectory: Optional[_str] = None
    """Pytest path: "tests/stage5/"
    """

    testMarker: Optional[_str] = None
    """Pytest marker: "-m fuota"
    """

    testTimeout: _int
    """Max seconds for test execution
    """

    priority: _int
    """Higher = more urgent (FUOTA=100, Nightly=50)
    """

    blocksMerge: _bool
    requiresFuota: _bool
    requiresBench: _bool
    """Stage 1 doesn't need hardware
    """

    maxDurationSec: _int
    """Hard timeout for entire stage
    """

    description: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    pipelineRuns: Optional[List['models.PipelineRun']] = None
    queueEntries: Optional[List['models.ValidationQueueEntry']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductStageConfigKeys']] = None,
        exclude: Optional[Iterable['types.ProductStageConfigKeys']] = None,
        required: Optional[Iterable['types.ProductStageConfigKeys']] = None,
        optional: Optional[Iterable['types.ProductStageConfigKeys']] = None,
        relations: Optional[Mapping['types.ProductStageConfigRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductStageConfigKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductStageConfig_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductStageConfig_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductStageConfig_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductStageConfig_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductStageConfig_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductStageConfig_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductStageConfig', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductStageConfig / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductStageConfig',
            }
        )
        _created_partial_types.add(name)


class ValidationQueueEntry(bases.BaseValidationQueueEntry):
    """A queued validation run waiting for a fixture.
    Priority queue: higher priority entries are assigned first.
    """

    id: _str
    pipelineRunId: _str
    stageConfigId: Optional[_str] = None
    stage: _int
    """Validation stage (1-5)
    """

    priority: _int
    """From stage config, but overridable
    """

    status: 'enums.QueueEntryStatus'
    fixtureId: Optional[_str] = None
    """Assigned fixture (null until ASSIGNED)
    """

    sessionId: Optional[_str] = None
    reason: Optional[_str] = None
    """"PR #42", "nightly 2026-03-11", "manual"
    """

    errorMessage: Optional[_str] = None
    jobName: Optional[_str] = None
    """K8s job name when triggered
    """

    requestedAt: datetime.datetime
    assignedAt: Optional[datetime.datetime] = None
    startedAt: Optional[datetime.datetime] = None
    completedAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    pipelineRun: Optional['models.PipelineRun'] = None
    stageConfig: Optional['models.ProductStageConfig'] = None
    fixture: Optional['models.Fixture'] = None
    session: Optional['models.Session'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ValidationQueueEntryKeys']] = None,
        exclude: Optional[Iterable['types.ValidationQueueEntryKeys']] = None,
        required: Optional[Iterable['types.ValidationQueueEntryKeys']] = None,
        optional: Optional[Iterable['types.ValidationQueueEntryKeys']] = None,
        relations: Optional[Mapping['types.ValidationQueueEntryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ValidationQueueEntryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ValidationQueueEntry_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ValidationQueueEntry_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ValidationQueueEntry_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ValidationQueueEntry_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ValidationQueueEntry_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ValidationQueueEntry_relational_fields:
                        raise errors.UnknownRelationalFieldError('ValidationQueueEntry', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ValidationQueueEntry / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ValidationQueueEntry',
            }
        )
        _created_partial_types.add(name)


class PipelineRun(bases.BasePipelineRun):
    """Groups multiple builds (e.g., alpha_fw + alpha_mfg_fw) triggered by same commit.
    Tracks the overall build+validate workflow for a product.
    """

    id: _str
    name: Optional[_str] = None
    """e.g., "alpha-main-abc1234"
    """

    productId: _str
    """FK to Product
    """

    board: _str
    """Board variant (alpha_b0)
    """

    branch: _str
    commitSha: Optional[_str] = None
    status: 'enums.PipelineStatus'
    triggerType: _str
    """manual, webhook, scheduled
    """

    triggerData: Optional['fields.Json'] = None
    """Webhook payload or trigger config
    """

    stage: Optional[_int] = None
    """Validation stage (1-5) this pipeline targets
    """

    stageConfigId: Optional[_str] = None
    """FK to ProductStageConfig
    """

    expectedBuilds: _int
    """How many builds to wait for
    """

    completedBuilds: _int
    buildMatrix: Optional['fields.Json'] = None
    """Matrix config: {mode: "stage4", labels: [...], mainRef: "main", prRef: "feature/x"}
    """

    matrixMode: Optional[_str] = None
    """"legacy" (4 builds) or "stage4" (8 builds)
    """

    autoValidate: _bool
    """Auto-trigger validation when all builds pass
    """

    validationRunId: Optional[_str] = None
    """Links to Session when validation starts
    """

    startedAt: Optional[datetime.datetime] = None
    finishedAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    stageConfig: Optional['models.ProductStageConfig'] = None
    builds: Optional[List['models.BuildJob']] = None
    sessions: Optional[List['models.Session']] = None
    queueEntries: Optional[List['models.ValidationQueueEntry']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PipelineRunKeys']] = None,
        exclude: Optional[Iterable['types.PipelineRunKeys']] = None,
        required: Optional[Iterable['types.PipelineRunKeys']] = None,
        optional: Optional[Iterable['types.PipelineRunKeys']] = None,
        relations: Optional[Mapping['types.PipelineRunRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PipelineRunKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PipelineRun_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PipelineRun_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PipelineRun_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PipelineRun_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PipelineRun_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PipelineRun_relational_fields:
                        raise errors.UnknownRelationalFieldError('PipelineRun', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PipelineRun / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PipelineRun',
            }
        )
        _created_partial_types.add(name)


class BuildJob(bases.BaseBuildJob):
    """A single firmware build job within a pipeline.
    """

    id: _str
    productId: _str
    """FK to Product (required)
    """

    board: _str
    target: _str
    variant: _str
    mtibRev: _str
    branch: _str
    commitSha: Optional[_str] = None
    status: 'enums.BuildJobStatus'
    versionMajor: Optional[_int] = None
    versionMinor: Optional[_int] = None
    buildNum: _int
    versionString: Optional[_str] = None
    errorMessage: Optional[_str] = None
    buildLog: Optional[_str] = None
    webhookData: Optional['fields.Json'] = None
    matrixLabel: Optional[_str] = None
    """e.g., "MFG_BASE", "FUT_DEBUG_A", "FUT_RELEASE_B"
    """

    matrixIndex: Optional[_int] = None
    """Position in build sequence (0-7 for Stage 4)
    """

    versionBump: _bool
    """True if version-bumped build (same code, N+1)
    """

    baseJobId: Optional[_str] = None
    """If version-bumped, points to the base build job
    """

    buildFingerprint: Optional[_str] = None
    """SHA-256 of (repo+commit+board+variant+configFlags)
    """

    configFlags: Optional['fields.Json'] = None
    """{"harness": false, "debug": true, "mfg": false}
    """

    reusedFromId: Optional[_str] = None
    """If CACHED, points to the original successful build
    """

    pipelineRunId: Optional[_str] = None
    pipelineRun: Optional['models.PipelineRun'] = None
    workerId: Optional[_str] = None
    """Node/host that built this (e.g. "wanda")
    """

    startedAt: Optional[datetime.datetime] = None
    finishedAt: Optional[datetime.datetime] = None
    durationSeconds: Optional[_int] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    artifacts: Optional[List['models.BuildJobArtifact']] = None
    reusedFrom: Optional['models.BuildJob'] = None
    reusedBy: Optional[List['models.BuildJob']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BuildJobKeys']] = None,
        exclude: Optional[Iterable['types.BuildJobKeys']] = None,
        required: Optional[Iterable['types.BuildJobKeys']] = None,
        optional: Optional[Iterable['types.BuildJobKeys']] = None,
        relations: Optional[Mapping['types.BuildJobRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BuildJobKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BuildJob_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BuildJob_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BuildJob_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BuildJob_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BuildJob_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BuildJob_relational_fields:
                        raise errors.UnknownRelationalFieldError('BuildJob', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BuildJob / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BuildJob',
            }
        )
        _created_partial_types.add(name)


class BuildJobArtifact(bases.BaseBuildJobArtifact):
    """A build artifact (hex, bin, elf) stored in MinIO.
    """

    id: _str
    buildJobId: _str
    name: _str
    storageKey: _str
    sizeBytes: _int
    checksum: _str
    role: Optional[_str] = None
    """Artifact role: app, comms, modem, manifest, log
    """

    processor: Optional[_str] = None
    """Target processor: nrf52840, nrf9151, etc.
    """

    artifactType: Optional[_str] = None
    """Artifact type: plaintextHex, encryptedCfw, manifest, log, metadata
    """

    createdAt: datetime.datetime
    buildJob: Optional['models.BuildJob'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.BuildJobArtifactKeys']] = None,
        exclude: Optional[Iterable['types.BuildJobArtifactKeys']] = None,
        required: Optional[Iterable['types.BuildJobArtifactKeys']] = None,
        optional: Optional[Iterable['types.BuildJobArtifactKeys']] = None,
        relations: Optional[Mapping['types.BuildJobArtifactRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.BuildJobArtifactKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _BuildJobArtifact_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _BuildJobArtifact_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _BuildJobArtifact_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _BuildJobArtifact_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _BuildJobArtifact_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _BuildJobArtifact_relational_fields:
                        raise errors.UnknownRelationalFieldError('BuildJobArtifact', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid BuildJobArtifact / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'BuildJobArtifact',
            }
        )
        _created_partial_types.add(name)


class Session(bases.BaseSession):
    """A unified test session: manufacturing or validation.
    Every test execution happens within a session, giving full traceability.

    The count fields are denormalized for dashboard performance.
    They are updated by the API when devices complete testing
    and can always be recomputed from Device statuses.
    """

    id: _str
    name: _str
    type: 'enums.SessionType'
    productId: _str
    status: 'enums.SessionStatus'
    fixtureId: Optional[_str] = None
    pipelineRunId: Optional[_str] = None
    targetCount: Optional[_int] = None
    completedCount: _int
    passedCount: _int
    failedCount: _int
    config: Optional['fields.Json'] = None
    notes: Optional[_str] = None
    errorMessage: Optional[_str] = None
    createdById: Optional[_str] = None
    startedAt: Optional[datetime.datetime] = None
    finishedAt: Optional[datetime.datetime] = None
    durationMs: Optional[_int] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    fixture: Optional['models.Fixture'] = None
    pipeline: Optional['models.PipelineRun'] = None
    createdBy: Optional['models.User'] = None
    devices: Optional[List['models.Device']] = None
    queueEntry: Optional['models.ValidationQueueEntry'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SessionKeys']] = None,
        exclude: Optional[Iterable['types.SessionKeys']] = None,
        required: Optional[Iterable['types.SessionKeys']] = None,
        optional: Optional[Iterable['types.SessionKeys']] = None,
        relations: Optional[Mapping['types.SessionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SessionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Session_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Session_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Session_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Session_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Session_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Session_relational_fields:
                        raise errors.UnknownRelationalFieldError('Session', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Session / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Session',
            }
        )
        _created_partial_types.add(name)


class Device(bases.BaseDevice):
    """An individual device (DUT) going through testing within a session.
    Tracks serial number, overall pass/fail status, and links to all
    test executions run on this device.

    The same serial number may appear in different sessions
    (e.g., a device that failed and was retested later).

    Device status lifecycle:
    PENDING     -> device registered, no tests started
    IN_PROGRESS -> at least one execution running
    PASSED      -> all required tests passed
    FAILED      -> any test failed
    """

    id: _str
    serialNumber: _str
    sessionId: _str
    status: 'enums.DeviceStatus'
    metadata: Optional['fields.Json'] = None
    """IMEI, ICCID, hardware info, calibration data
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    session: Optional['models.Session'] = None
    executions: Optional[List['models.TestExecution']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DeviceKeys']] = None,
        exclude: Optional[Iterable['types.DeviceKeys']] = None,
        required: Optional[Iterable['types.DeviceKeys']] = None,
        optional: Optional[Iterable['types.DeviceKeys']] = None,
        relations: Optional[Mapping['types.DeviceRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DeviceKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Device_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Device_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Device_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Device_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Device_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Device_relational_fields:
                        raise errors.UnknownRelationalFieldError('Device', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Device / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Device',
            }
        )
        _created_partial_types.add(name)


class FixtureDesign(bases.BaseFixtureDesign):
    """A versioned fixture hardware design -- the PCB/wiring spec for testing a product.
    Example: "alpha-fixture-v1.2" describes REV1.2 MTIB carrier with specific GPIO wiring.

    The profileTemplate JSON contains the default fixture profile for fixtures using this design.
    Individual Fixture records can override specific fields via profileOverrides.
    """

    id: _str
    name: _str
    """"alpha-fixture-v1.2"
    """

    product: _str
    """"alpha" -- which product this fixture tests
    """

    revision: _str
    """"1.2" -- fixture hardware revision
    """

    capabilities: List[_str]
    """["button", "peltier", "charger_relay"]
    """

    profileTemplate: 'fields.Json'
    """Default fixture profile JSON for this design
    """

    schematicUrl: Optional[_str] = None
    """Link to schematic PDF/CAD files
    """

    bomUrl: Optional[_str] = None
    """Link to BOM spreadsheet
    """

    assemblyGuide: Optional[_str] = None
    """Link to assembly instructions
    """

    notes: Optional[_str] = None
    """Build notes, known issues, changelog
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    fixtures: Optional[List['models.Fixture']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('capabilities', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FixtureDesignKeys']] = None,
        exclude: Optional[Iterable['types.FixtureDesignKeys']] = None,
        required: Optional[Iterable['types.FixtureDesignKeys']] = None,
        optional: Optional[Iterable['types.FixtureDesignKeys']] = None,
        relations: Optional[Mapping['types.FixtureDesignRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FixtureDesignKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FixtureDesign_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FixtureDesign_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FixtureDesign_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FixtureDesign_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FixtureDesign_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FixtureDesign_relational_fields:
                        raise errors.UnknownRelationalFieldError('FixtureDesign', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FixtureDesign / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FixtureDesign',
            }
        )
        _created_partial_types.add(name)


class Fixture(bases.BaseFixture):
    """A physical test fixture for a product. Unified model for both
    manufacturing fixtures (multi-panel arrays) and validation benches
    (individual units with MTIB + DUT).
    """

    id: _str
    name: _str
    """"Theta MFG Fixture 1", "Alpha B0 Bench 1"
    """

    stationId: Optional[_str] = None
    """"station-33", matches fixture profile
    """

    productId: _str
    type: 'enums.NodeType'
    """MANUFACTURING or VALIDATION
    """

    designId: Optional[_str] = None
    """FK to FixtureDesign
    """

    status: 'enums.FixtureStatus'
    lockedBy: Optional[_str] = None
    """Pipeline/job ID holding the lock
    """

    lockedAt: Optional[datetime.datetime] = None
    profileOverrides: Optional['fields.Json'] = None
    """Fixture-specific profile overrides (merged with design template)
    """

    description: Optional[_str] = None
    active: _bool
    metadata: Optional['fields.Json'] = None
    """physical location, rack position, etc.
    """

    lastHealthCheck: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    design: Optional['models.FixtureDesign'] = None
    slots: Optional[List['models.FixtureSlot']] = None
    sessions: Optional[List['models.Session']] = None
    queueEntries: Optional[List['models.ValidationQueueEntry']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FixtureKeys']] = None,
        exclude: Optional[Iterable['types.FixtureKeys']] = None,
        required: Optional[Iterable['types.FixtureKeys']] = None,
        optional: Optional[Iterable['types.FixtureKeys']] = None,
        relations: Optional[Mapping['types.FixtureRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FixtureKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Fixture_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Fixture_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Fixture_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Fixture_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Fixture_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Fixture_relational_fields:
                        raise errors.UnknownRelationalFieldError('Fixture', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Fixture / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Fixture',
            }
        )
        _created_partial_types.add(name)


class FixtureSlot(bases.BaseFixtureSlot):
    """A physical slot/position in a fixture. Each slot maps to one
    MTIB node. The @unique on nodeId ensures a node can only be
    assigned to one slot at a time (NULL is exempt from uniqueness,
    so empty slots are fine).
    """

    id: _str
    fixtureId: _str
    slotIndex: _int
    """0-based position in the fixture
    """

    label: Optional[_str] = None
    """human-readable: "Slot 1", "Stand Alone"
    """

    nodeId: Optional[_str] = None
    active: _bool
    jlinkAppSerial: Optional[_str] = None
    """J-Link SNR for nRF52840 app processor
    """

    jlinkCommsSerial: Optional[_str] = None
    """J-Link SNR for nRF9151 comms (if separate)
    """

    uartAppPath: Optional[_str] = None
    """"/dev/verdin-uart2"
    """

    uartCommsPath: Optional[_str] = None
    """"/dev/verdin-uart1"
    """

    dutDeviceId: Optional[_str] = None
    """"70B3D584C01E1FCC"
    """

    dutSnr: Optional[_str] = None
    """"0964" -- J-Link probe serial
    """

    dutImei: Optional[_str] = None
    """"355025931735979"
    """

    dutIccids: List[_str]
    """["89148000009808558441", "89457300000037582833"]
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    fixture: Optional['models.Fixture'] = None
    node: Optional['models.Node'] = None
    testExecutions: Optional[List['models.TestExecution']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('dutIccids', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FixtureSlotKeys']] = None,
        exclude: Optional[Iterable['types.FixtureSlotKeys']] = None,
        required: Optional[Iterable['types.FixtureSlotKeys']] = None,
        optional: Optional[Iterable['types.FixtureSlotKeys']] = None,
        relations: Optional[Mapping['types.FixtureSlotRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FixtureSlotKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FixtureSlot_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FixtureSlot_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FixtureSlot_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FixtureSlot_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FixtureSlot_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FixtureSlot_relational_fields:
                        raise errors.UnknownRelationalFieldError('FixtureSlot', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FixtureSlot / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FixtureSlot',
            }
        )
        _created_partial_types.add(name)


class Node(bases.BaseNode):
    """A compute node managed by Concord -- typically an MTIB
    (Verdin iMX8MM + Mallow + MTIB carrier board) running
    as an edge node in the K3s cluster.

    Nodes with TestExecution history cannot be deleted
    (default Restrict behavior). Decommission by setting
    status = MAINTENANCE and unassigning from fixture slot.
    """

    id: _str
    name: _str
    hostname: _str
    """K8s node name / OS hostname
    """

    type: 'enums.NodeType'
    status: 'enums.NodeStatus'
    ipAddress: Optional[_str] = None
    hardwareRevision: Optional[_str] = None
    """"REV1.1", "REV1.2"
    """

    metadata: Optional['fields.Json'] = None
    """K8s labels, capabilities, gRPC port, etc.
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    fixtureSlot: Optional['models.FixtureSlot'] = None
    """one-to-one via @unique on FixtureSlot.nodeId
    """

    testExecutions: Optional[List['models.TestExecution']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.NodeKeys']] = None,
        exclude: Optional[Iterable['types.NodeKeys']] = None,
        required: Optional[Iterable['types.NodeKeys']] = None,
        optional: Optional[Iterable['types.NodeKeys']] = None,
        relations: Optional[Mapping['types.NodeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.NodeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Node_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Node_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Node_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Node_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Node_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Node_relational_fields:
                        raise errors.UnknownRelationalFieldError('Node', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Node / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Node',
            }
        )
        _created_partial_types.add(name)


class IcleDevice(bases.BaseIcleDevice):
    """An ICLE (In-Circuit Loop-back Equipment) device -- ESP32-based
    power monitoring tool for DUT validation. Devices register
    themselves via heartbeat and receive commands in the response.
    """

    id: _str
    deviceId: _str
    """Hardware ID from ZMS
    """

    name: Optional[_str] = None
    ipAddress: Optional[_str] = None
    macAddress: Optional[_str] = None
    firmwareVersion: Optional[_str] = None
    status: 'enums.IcleDeviceStatus'
    registered: _bool
    """false = discovered, true = registered via UI
    """

    lastHeartbeat: Optional[datetime.datetime] = None
    lastStatusData: Optional['fields.Json'] = None
    """Latest telemetry snapshot (uptime, heap, rssi, etc.)
    """

    pendingConfig: Optional['fields.Json'] = None
    """Config to push on next heartbeat
    """

    metadata: Optional['fields.Json'] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    commands: Optional[List['models.IclePendingCommand']] = None
    logs: Optional[List['models.IcleLog']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.IcleDeviceKeys']] = None,
        exclude: Optional[Iterable['types.IcleDeviceKeys']] = None,
        required: Optional[Iterable['types.IcleDeviceKeys']] = None,
        optional: Optional[Iterable['types.IcleDeviceKeys']] = None,
        relations: Optional[Mapping['types.IcleDeviceRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.IcleDeviceKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _IcleDevice_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _IcleDevice_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _IcleDevice_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _IcleDevice_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _IcleDevice_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _IcleDevice_relational_fields:
                        raise errors.UnknownRelationalFieldError('IcleDevice', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid IcleDevice / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'IcleDevice',
            }
        )
        _created_partial_types.add(name)


class IclePendingCommand(bases.BaseIclePendingCommand):
    """A pending command to be delivered to an ICLE device on its
    next heartbeat. Commands are acknowledged by the device and
    then marked as such.
    """

    id: _str
    deviceId: _str
    commandType: _str
    """config_update, ota_trigger, log_upload, reboot
    """

    payload: 'fields.Json'
    priority: _int
    """Higher priority = delivered first
    """

    expiresAt: Optional[datetime.datetime] = None
    acknowledged: _bool
    createdAt: datetime.datetime
    device: Optional['models.IcleDevice'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.IclePendingCommandKeys']] = None,
        exclude: Optional[Iterable['types.IclePendingCommandKeys']] = None,
        required: Optional[Iterable['types.IclePendingCommandKeys']] = None,
        optional: Optional[Iterable['types.IclePendingCommandKeys']] = None,
        relations: Optional[Mapping['types.IclePendingCommandRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.IclePendingCommandKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _IclePendingCommand_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _IclePendingCommand_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _IclePendingCommand_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _IclePendingCommand_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _IclePendingCommand_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _IclePendingCommand_relational_fields:
                        raise errors.UnknownRelationalFieldError('IclePendingCommand', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid IclePendingCommand / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'IclePendingCommand',
            }
        )
        _created_partial_types.add(name)


class IcleLog(bases.BaseIcleLog):
    """A log file uploaded from an ICLE device, stored in MinIO.
    """

    id: _str
    deviceId: _str
    filename: _str
    storageKey: _str
    """MinIO object key
    """

    sizeBytes: _int
    format: _str
    """"binary" or "csv"
    """

    uploadedAt: datetime.datetime
    device: Optional['models.IcleDevice'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.IcleLogKeys']] = None,
        exclude: Optional[Iterable['types.IcleLogKeys']] = None,
        required: Optional[Iterable['types.IcleLogKeys']] = None,
        optional: Optional[Iterable['types.IcleLogKeys']] = None,
        relations: Optional[Mapping['types.IcleLogRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.IcleLogKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _IcleLog_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _IcleLog_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _IcleLog_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _IcleLog_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _IcleLog_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _IcleLog_relational_fields:
                        raise errors.UnknownRelationalFieldError('IcleLog', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid IcleLog / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'IcleLog',
            }
        )
        _created_partial_types.add(name)


class Deployment(bases.BaseDeployment):
    """A software deployment tracked by Concord -- test runners,
    operators, MTIB servers, etc. The actual K8s state is
    queried live; this table records intent and history.
    """

    id: _str
    name: _str
    productId: Optional[_str] = None
    status: 'enums.DeploymentStatus'
    config: Optional['fields.Json'] = None
    """K8s manifest, image tags, env overrides
    """

    version: Optional[_str] = None
    createdById: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    createdBy: Optional['models.User'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DeploymentKeys']] = None,
        exclude: Optional[Iterable['types.DeploymentKeys']] = None,
        required: Optional[Iterable['types.DeploymentKeys']] = None,
        optional: Optional[Iterable['types.DeploymentKeys']] = None,
        relations: Optional[Mapping['types.DeploymentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DeploymentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Deployment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Deployment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Deployment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Deployment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Deployment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Deployment_relational_fields:
                        raise errors.UnknownRelationalFieldError('Deployment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Deployment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Deployment',
            }
        )
        _created_partial_types.add(name)


class Test(bases.BaseTest):
    """A test definition for a product. Defines WHAT to test,
    not a specific run. Tests are grouped by category
    (e.g., "electrical", "firmware", "post") and ordered
    by sortOrder for sequential execution.

    Tests with execution history cannot be deleted (default
    Restrict). Disable with enabled = false instead.
    """

    id: _str
    name: _str
    productId: _str
    description: Optional[_str] = None
    category: Optional[_str] = None
    """"electrical", "firmware", "post", etc.
    """

    sortOrder: _int
    config: Optional['fields.Json'] = None
    """default config template (can be overridden per-execution)
    """

    enabled: _bool
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    executions: Optional[List['models.TestExecution']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TestKeys']] = None,
        exclude: Optional[Iterable['types.TestKeys']] = None,
        required: Optional[Iterable['types.TestKeys']] = None,
        optional: Optional[Iterable['types.TestKeys']] = None,
        relations: Optional[Mapping['types.TestRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TestKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Test_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Test_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Test_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Test_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Test_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Test_relational_fields:
                        raise errors.UnknownRelationalFieldError('Test', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Test / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Test',
            }
        )
        _created_partial_types.add(name)


class TestExecution(bases.BaseTestExecution):
    """A single run of a test on a node for a device.

    Links to: Test (what), Node (where), Device (who/what DUT),
    FixtureSlot (which slot), User (triggered by).

    deviceId is optional for system-level or ad-hoc tests that
    don't target a specific device.

    On cascade behavior:
    - test/node: Restrict (cannot delete test/node with history)
    - device:    Cascade  (session cleanup cascades through)
    - slot/user: SetNull  (preserve execution if slot/user removed)
    """

    id: _str
    testId: _str
    nodeId: _str
    deviceId: Optional[_str] = None
    slotId: Optional[_str] = None
    status: 'enums.TestExecutionStatus'
    config: Optional['fields.Json'] = None
    """execution-specific config overrides
    """

    triggeredById: Optional[_str] = None
    startedAt: Optional[datetime.datetime] = None
    finishedAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    test: Optional['models.Test'] = None
    node: Optional['models.Node'] = None
    device: Optional['models.Device'] = None
    slot: Optional['models.FixtureSlot'] = None
    triggeredBy: Optional['models.User'] = None
    steps: Optional[List['models.TestStep']] = None
    logs: Optional[List['models.Log']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TestExecutionKeys']] = None,
        exclude: Optional[Iterable['types.TestExecutionKeys']] = None,
        required: Optional[Iterable['types.TestExecutionKeys']] = None,
        optional: Optional[Iterable['types.TestExecutionKeys']] = None,
        relations: Optional[Mapping['types.TestExecutionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TestExecutionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _TestExecution_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _TestExecution_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _TestExecution_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _TestExecution_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _TestExecution_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _TestExecution_relational_fields:
                        raise errors.UnknownRelationalFieldError('TestExecution', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid TestExecution / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'TestExecution',
            }
        )
        _created_partial_types.add(name)


class TestStep(bases.BaseTestStep):
    """A single step within a test execution. Steps provide sub-test
    granularity for live progress tracking and detailed diagnostics.
    Replaces the former TestResult model with richer fields.
    """

    id: _str
    executionId: _str
    stepIndex: _int
    name: _str
    status: 'enums.TestExecutionStatus'
    passed: Optional[_bool] = None
    errorMessage: Optional[_str] = None
    measurements: Optional['fields.Json'] = None
    logOutput: Optional[_str] = None
    logStorageKey: Optional[_str] = None
    startedAt: Optional[datetime.datetime] = None
    finishedAt: Optional[datetime.datetime] = None
    durationMs: Optional[_int] = None
    createdAt: datetime.datetime
    execution: Optional['models.TestExecution'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TestStepKeys']] = None,
        exclude: Optional[Iterable['types.TestStepKeys']] = None,
        required: Optional[Iterable['types.TestStepKeys']] = None,
        optional: Optional[Iterable['types.TestStepKeys']] = None,
        relations: Optional[Mapping['types.TestStepRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TestStepKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _TestStep_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _TestStep_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _TestStep_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _TestStep_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _TestStep_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _TestStep_relational_fields:
                        raise errors.UnknownRelationalFieldError('TestStep', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid TestStep / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'TestStep',
            }
        )
        _created_partial_types.add(name)


class User(bases.BaseUser):
    """A platform user, authenticated via Google OAuth.
    """

    id: _str
    email: _str
    name: _str
    externalId: Optional[_str] = None
    """Google sub ID, set on first login
    """

    permissionSetId: Optional[_str] = None
    active: _bool
    lastSeenAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    permissionSet: Optional['models.PermissionSet'] = None
    apiKeys: Optional[List['models.ApiKey']] = None
    sessions: Optional[List['models.Session']] = None
    deployments: Optional[List['models.Deployment']] = None
    testExecutions: Optional[List['models.TestExecution']] = None
    auditLogs: Optional[List['models.AuditLog']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserKeys']] = None,
        exclude: Optional[Iterable['types.UserKeys']] = None,
        required: Optional[Iterable['types.UserKeys']] = None,
        optional: Optional[Iterable['types.UserKeys']] = None,
        relations: Optional[Mapping['types.UserRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _User_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _User_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _User_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _User_relational_fields:
                        raise errors.UnknownRelationalFieldError('User', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid User / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'User',
            }
        )
        _created_partial_types.add(name)


class PermissionSet(bases.BasePermissionSet):
    """A named set of permissions assignable to users.
    """

    id: _str
    name: _str
    description: Optional[_str] = None
    permissions: List[_str]
    """e.g. ["Concord.Firmware.AppID.Create", "Concord.Cluster.Read"]
    """

    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    users: Optional[List['models.User']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('permissions', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PermissionSetKeys']] = None,
        exclude: Optional[Iterable['types.PermissionSetKeys']] = None,
        required: Optional[Iterable['types.PermissionSetKeys']] = None,
        optional: Optional[Iterable['types.PermissionSetKeys']] = None,
        relations: Optional[Mapping['types.PermissionSetRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PermissionSetKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PermissionSet_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PermissionSet_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PermissionSet_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PermissionSet_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PermissionSet_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PermissionSet_relational_fields:
                        raise errors.UnknownRelationalFieldError('PermissionSet', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PermissionSet / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PermissionSet',
            }
        )
        _created_partial_types.add(name)


class ApiKey(bases.BaseApiKey):
    """A programmatic API key linked to a user. Keys are stored as
    SHA-256 hashes; the plaintext is only shown once at creation.
    """

    id: _str
    name: _str
    keyHash: _str
    """SHA-256 hash of the actual key (never store plaintext)
    """

    keyPrefix: _str
    """First 8 chars of key for display ("ck_live_a1b2...")
    """

    userId: _str
    expiresAt: Optional[datetime.datetime] = None
    lastUsedAt: Optional[datetime.datetime] = None
    createdAt: datetime.datetime
    user: Optional['models.User'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ApiKeyKeys']] = None,
        exclude: Optional[Iterable['types.ApiKeyKeys']] = None,
        required: Optional[Iterable['types.ApiKeyKeys']] = None,
        optional: Optional[Iterable['types.ApiKeyKeys']] = None,
        relations: Optional[Mapping['types.ApiKeyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ApiKeyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ApiKey_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ApiKey_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ApiKey_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ApiKey_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ApiKey_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ApiKey_relational_fields:
                        raise errors.UnknownRelationalFieldError('ApiKey', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ApiKey / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ApiKey',
            }
        )
        _created_partial_types.add(name)


class AuditLog(bases.BaseAuditLog):
    """Append-only log of every significant mutation in the system.
    userId is nullable for system-initiated actions.
    """

    id: _str
    userId: Optional[_str] = None
    action: _str
    """dot-notation verb: "session.create", "node.update", etc.
    """

    entityType: _str
    """model name: "Session", "Node", "Test", etc.
    """

    entityId: Optional[_str] = None
    details: Optional['fields.Json'] = None
    """freeform context -- before/after snapshots, params, etc.
    """

    ipAddress: Optional[_str] = None
    createdAt: datetime.datetime
    user: Optional['models.User'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AuditLogKeys']] = None,
        exclude: Optional[Iterable['types.AuditLogKeys']] = None,
        required: Optional[Iterable['types.AuditLogKeys']] = None,
        optional: Optional[Iterable['types.AuditLogKeys']] = None,
        relations: Optional[Mapping['types.AuditLogRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AuditLogKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AuditLog_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AuditLog_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AuditLog_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AuditLog_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _AuditLog_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _AuditLog_relational_fields:
                        raise errors.UnknownRelationalFieldError('AuditLog', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AuditLog / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AuditLog',
            }
        )
        _created_partial_types.add(name)


class Setting(bases.BaseSetting):
    """Key-value configuration store for system-wide settings
    (thresholds, feature flags, UI preferences, etc.).
    """

    key: _str
    value: _str
    description: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SettingKeys']] = None,
        exclude: Optional[Iterable['types.SettingKeys']] = None,
        required: Optional[Iterable['types.SettingKeys']] = None,
        optional: Optional[Iterable['types.SettingKeys']] = None,
        relations: Optional[Mapping['types.SettingRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SettingKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Setting_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Setting_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Setting_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Setting_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "Setting" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Setting / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Setting',
            }
        )
        _created_partial_types.add(name)


class Log(bases.BaseLog):
    """System and execution logs. Can be free-standing (system
    events) or linked to a specific test execution.
    """

    id: _str
    level: 'enums.LogLevel'
    message: _str
    source: Optional[_str] = None
    """service name, module path, etc.
    """

    executionId: Optional[_str] = None
    createdAt: datetime.datetime
    execution: Optional['models.TestExecution'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.LogKeys']] = None,
        exclude: Optional[Iterable['types.LogKeys']] = None,
        required: Optional[Iterable['types.LogKeys']] = None,
        optional: Optional[Iterable['types.LogKeys']] = None,
        relations: Optional[Mapping['types.LogRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.LogKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Log_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Log_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Log_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Log_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Log_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Log_relational_fields:
                        raise errors.UnknownRelationalFieldError('Log', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Log / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Log',
            }
        )
        _created_partial_types.add(name)



_Product_relational_fields: Set[str] = {
        'targets',
        'boards',
        'firmwareBuilds',
        'fixtures',
        'tests',
        'sessions',
        'buildJobs',
        'pipelineRuns',
        'deployments',
        'stageConfigs',
    }
_Product_fields: Dict['types.ProductKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"Alpha B0", "Sigma5 C0"''',
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"alpha_b0", "sigma5_c0" -- URL-safe identifier''',
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('active', {
            'name': 'active',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildConfig', {
            'name': 'buildConfig',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''structured build configuration (overlays, post-build steps)''',
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''additional product-specific config''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('targets', {
            'name': 'targets',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductTarget\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('boards', {
            'name': 'boards',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Board\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('firmwareBuilds', {
            'name': 'firmwareBuilds',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FirmwareBuild\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('fixtures', {
            'name': 'fixtures',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Fixture\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('tests', {
            'name': 'tests',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Test\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('sessions', {
            'name': 'sessions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Session\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('buildJobs', {
            'name': 'buildJobs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BuildJob\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('pipelineRuns', {
            'name': 'pipelineRuns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PipelineRun\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('deployments', {
            'name': 'deployments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Deployment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('stageConfigs', {
            'name': 'stageConfigs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductStageConfig\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductTarget_relational_fields: Set[str] = {
        'product',
        'firmwareBuilds',
    }
_ProductTarget_fields: Dict['types.ProductTargetKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"comms", "app" -- functional role''',
        }),
        ('soc', {
            'name': 'soc',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"nRF9151", "nRF52840" -- SoC identifier''',
        }),
        ('appId', {
            'name': 'appId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''CoreCloud application ID (108, 109)''',
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('firmwareBuilds', {
            'name': 'firmwareBuilds',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FirmwareBuild\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Board_relational_fields: Set[str] = {
        'product',
        'revisions',
    }
_Board_fields: Dict['types.BoardKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"Main Board", "Sensor Board"''',
        }),
        ('ckBoardsName', {
            'name': 'ckBoardsName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''west board name from ck_boards repo, e.g. "alpha_b0"''',
        }),
        ('ckBoardsBranch', {
            'name': 'ckBoardsBranch',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''branch in ck_boards repo''',
        }),
        ('vendor', {
            'name': 'vendor',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('active', {
            'name': 'active',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('revisions', {
            'name': 'revisions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BoardRevision\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BoardRevision_relational_fields: Set[str] = {
        'board',
    }
_BoardRevision_fields: Dict['types.BoardRevisionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('boardId', {
            'name': 'boardId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('version', {
            'name': 'version',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''e.g. "A0", "B0", "B1"''',
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.LifecycleStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('board', {
            'name': 'board',
            'is_list': False,
            'optional': True,
            'type': 'models.Board',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FirmwareBuild_relational_fields: Set[str] = {
        'product',
        'target',
    }
_FirmwareBuild_fields: Dict['types.FirmwareBuildKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('targetId', {
            'name': 'targetId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''FK to ProductTarget (optional for legacy builds)''',
        }),
        ('version', {
            'name': 'version',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Semantic version string "1.0.0"''',
        }),
        ('isManufacturing', {
            'name': 'isManufacturing',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.LifecycleStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('storageKey', {
            'name': 'storageKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''MinIO object key''',
        }),
        ('filename', {
            'name': 'filename',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sizeBytes', {
            'name': 'sizeBytes',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('checksum', {
            'name': 'checksum',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''SHA-256 hex digest''',
        }),
        ('contentType', {
            'name': 'contentType',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('modemStorageKey', {
            'name': 'modemStorageKey',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''MinIO key for modem firmware zip (modem targets only)''',
        }),
        ('modemFilename', {
            'name': 'modemFilename',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('modemSizeBytes', {
            'name': 'modemSizeBytes',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('modemChecksum', {
            'name': 'modemChecksum',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''SHA-256 hex digest of modem file''',
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('target', {
            'name': 'target',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductTarget',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductStageConfig_relational_fields: Set[str] = {
        'product',
        'pipelineRuns',
        'queueEntries',
    }
_ProductStageConfig_fields: Dict['types.ProductStageConfigKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stage', {
            'name': 'stage',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''1=Smoke, 2=Silicon, 3=Integration, 4=Nightly, 5=FUOTA''',
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Display name: "Smoke", "Silicon", "Integration", "Nightly", "FUOTA"''',
        }),
        ('enabled', {
            'name': 'enabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildScript', {
            'name': 'buildScript',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Bash build script content''',
        }),
        ('buildTarget', {
            'name': 'buildTarget',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''West board name ("alpha_b0") or "native_sim"''',
        }),
        ('fwRepoUrl', {
            'name': 'fwRepoUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Firmware repo SSH URL''',
        }),
        ('fwRepoBranch', {
            'name': 'fwRepoBranch',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Branch to build from''',
        }),
        ('mfgRepoUrl', {
            'name': 'mfgRepoUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Manufacturing firmware repo (stages 4-6)''',
        }),
        ('mfgRepoBranch', {
            'name': 'mfgRepoBranch',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Manufacturing firmware branch''',
        }),
        ('buildVariant', {
            'name': 'buildVariant',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Default variant: "debug", "release", "mfg", "test"''',
        }),
        ('configFlags', {
            'name': 'configFlags',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Build-time flags: {"harness": true, "debug": false}''',
        }),
        ('buildMatrix', {
            'name': 'buildMatrix',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Stage-specific build definitions (array of BuildDef)''',
        }),
        ('testDirectory', {
            'name': 'testDirectory',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Pytest path: "tests/stage5/"''',
        }),
        ('testMarker', {
            'name': 'testMarker',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Pytest marker: "-m fuota"''',
        }),
        ('testTimeout', {
            'name': 'testTimeout',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Max seconds for test execution''',
        }),
        ('priority', {
            'name': 'priority',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Higher = more urgent (FUOTA=100, Nightly=50)''',
        }),
        ('blocksMerge', {
            'name': 'blocksMerge',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('requiresFuota', {
            'name': 'requiresFuota',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('requiresBench', {
            'name': 'requiresBench',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': '''Stage 1 doesn't need hardware''',
        }),
        ('maxDurationSec', {
            'name': 'maxDurationSec',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Hard timeout for entire stage''',
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('pipelineRuns', {
            'name': 'pipelineRuns',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PipelineRun\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('queueEntries', {
            'name': 'queueEntries',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ValidationQueueEntry\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ValidationQueueEntry_relational_fields: Set[str] = {
        'pipelineRun',
        'stageConfig',
        'fixture',
        'session',
    }
_ValidationQueueEntry_fields: Dict['types.ValidationQueueEntryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('pipelineRunId', {
            'name': 'pipelineRunId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stageConfigId', {
            'name': 'stageConfigId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stage', {
            'name': 'stage',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Validation stage (1-5)''',
        }),
        ('priority', {
            'name': 'priority',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''From stage config, but overridable''',
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.QueueEntryStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('fixtureId', {
            'name': 'fixtureId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Assigned fixture (null until ASSIGNED)''',
        }),
        ('sessionId', {
            'name': 'sessionId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('reason', {
            'name': 'reason',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"PR #42", "nightly 2026-03-11", "manual"''',
        }),
        ('errorMessage', {
            'name': 'errorMessage',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobName', {
            'name': 'jobName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''K8s job name when triggered''',
        }),
        ('requestedAt', {
            'name': 'requestedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('assignedAt', {
            'name': 'assignedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('completedAt', {
            'name': 'completedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('pipelineRun', {
            'name': 'pipelineRun',
            'is_list': False,
            'optional': True,
            'type': 'models.PipelineRun',
            'is_relational': True,
            'documentation': None,
        }),
        ('stageConfig', {
            'name': 'stageConfig',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductStageConfig',
            'is_relational': True,
            'documentation': None,
        }),
        ('fixture', {
            'name': 'fixture',
            'is_list': False,
            'optional': True,
            'type': 'models.Fixture',
            'is_relational': True,
            'documentation': None,
        }),
        ('session', {
            'name': 'session',
            'is_list': False,
            'optional': True,
            'type': 'models.Session',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PipelineRun_relational_fields: Set[str] = {
        'product',
        'stageConfig',
        'builds',
        'sessions',
        'queueEntries',
    }
_PipelineRun_fields: Dict['types.PipelineRunKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''e.g., "alpha-main-abc1234"''',
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''FK to Product''',
        }),
        ('board', {
            'name': 'board',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Board variant (alpha_b0)''',
        }),
        ('branch', {
            'name': 'branch',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('commitSha', {
            'name': 'commitSha',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.PipelineStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('triggerType', {
            'name': 'triggerType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''manual, webhook, scheduled''',
        }),
        ('triggerData', {
            'name': 'triggerData',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Webhook payload or trigger config''',
        }),
        ('stage', {
            'name': 'stage',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Validation stage (1-5) this pipeline targets''',
        }),
        ('stageConfigId', {
            'name': 'stageConfigId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''FK to ProductStageConfig''',
        }),
        ('expectedBuilds', {
            'name': 'expectedBuilds',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''How many builds to wait for''',
        }),
        ('completedBuilds', {
            'name': 'completedBuilds',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildMatrix', {
            'name': 'buildMatrix',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Matrix config: {mode: "stage4", labels: [...], mainRef: "main", prRef: "feature/x"}''',
        }),
        ('matrixMode', {
            'name': 'matrixMode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"legacy" (4 builds) or "stage4" (8 builds)''',
        }),
        ('autoValidate', {
            'name': 'autoValidate',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': '''Auto-trigger validation when all builds pass''',
        }),
        ('validationRunId', {
            'name': 'validationRunId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Links to Session when validation starts''',
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedAt', {
            'name': 'finishedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('stageConfig', {
            'name': 'stageConfig',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductStageConfig',
            'is_relational': True,
            'documentation': None,
        }),
        ('builds', {
            'name': 'builds',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BuildJob\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('sessions', {
            'name': 'sessions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Session\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('queueEntries', {
            'name': 'queueEntries',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ValidationQueueEntry\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BuildJob_relational_fields: Set[str] = {
        'pipelineRun',
        'product',
        'artifacts',
        'reusedFrom',
        'reusedBy',
    }
_BuildJob_fields: Dict['types.BuildJobKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''FK to Product (required)''',
        }),
        ('board', {
            'name': 'board',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('target', {
            'name': 'target',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('variant', {
            'name': 'variant',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('mtibRev', {
            'name': 'mtibRev',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('branch', {
            'name': 'branch',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('commitSha', {
            'name': 'commitSha',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.BuildJobStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('versionMajor', {
            'name': 'versionMajor',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('versionMinor', {
            'name': 'versionMinor',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildNum', {
            'name': 'buildNum',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('versionString', {
            'name': 'versionString',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('errorMessage', {
            'name': 'errorMessage',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildLog', {
            'name': 'buildLog',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('webhookData', {
            'name': 'webhookData',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('matrixLabel', {
            'name': 'matrixLabel',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''e.g., "MFG_BASE", "FUT_DEBUG_A", "FUT_RELEASE_B"''',
        }),
        ('matrixIndex', {
            'name': 'matrixIndex',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Position in build sequence (0-7 for Stage 4)''',
        }),
        ('versionBump', {
            'name': 'versionBump',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': '''True if version-bumped build (same code, N+1)''',
        }),
        ('baseJobId', {
            'name': 'baseJobId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''If version-bumped, points to the base build job''',
        }),
        ('buildFingerprint', {
            'name': 'buildFingerprint',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''SHA-256 of (repo+commit+board+variant+configFlags)''',
        }),
        ('configFlags', {
            'name': 'configFlags',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''{"harness": false, "debug": true, "mfg": false}''',
        }),
        ('reusedFromId', {
            'name': 'reusedFromId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''If CACHED, points to the original successful build''',
        }),
        ('pipelineRunId', {
            'name': 'pipelineRunId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('pipelineRun', {
            'name': 'pipelineRun',
            'is_list': False,
            'optional': True,
            'type': 'models.PipelineRun',
            'is_relational': True,
            'documentation': None,
        }),
        ('workerId', {
            'name': 'workerId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Node/host that built this (e.g. "wanda")''',
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedAt', {
            'name': 'finishedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('durationSeconds', {
            'name': 'durationSeconds',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('artifacts', {
            'name': 'artifacts',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BuildJobArtifact\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('reusedFrom', {
            'name': 'reusedFrom',
            'is_list': False,
            'optional': True,
            'type': 'models.BuildJob',
            'is_relational': True,
            'documentation': None,
        }),
        ('reusedBy', {
            'name': 'reusedBy',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.BuildJob\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_BuildJobArtifact_relational_fields: Set[str] = {
        'buildJob',
    }
_BuildJobArtifact_fields: Dict['types.BuildJobArtifactKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildJobId', {
            'name': 'buildJobId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('storageKey', {
            'name': 'storageKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sizeBytes', {
            'name': 'sizeBytes',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('checksum', {
            'name': 'checksum',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Artifact role: app, comms, modem, manifest, log''',
        }),
        ('processor', {
            'name': 'processor',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Target processor: nrf52840, nrf9151, etc.''',
        }),
        ('artifactType', {
            'name': 'artifactType',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Artifact type: plaintextHex, encryptedCfw, manifest, log, metadata''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('buildJob', {
            'name': 'buildJob',
            'is_list': False,
            'optional': True,
            'type': 'models.BuildJob',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Session_relational_fields: Set[str] = {
        'product',
        'fixture',
        'pipeline',
        'createdBy',
        'devices',
        'queueEntry',
    }
_Session_fields: Dict['types.SessionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': 'enums.SessionType',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.SessionStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('fixtureId', {
            'name': 'fixtureId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('pipelineRunId', {
            'name': 'pipelineRunId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('targetCount', {
            'name': 'targetCount',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('completedCount', {
            'name': 'completedCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('passedCount', {
            'name': 'passedCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('failedCount', {
            'name': 'failedCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('config', {
            'name': 'config',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('errorMessage', {
            'name': 'errorMessage',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdById', {
            'name': 'createdById',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedAt', {
            'name': 'finishedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('durationMs', {
            'name': 'durationMs',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('fixture', {
            'name': 'fixture',
            'is_list': False,
            'optional': True,
            'type': 'models.Fixture',
            'is_relational': True,
            'documentation': None,
        }),
        ('pipeline', {
            'name': 'pipeline',
            'is_list': False,
            'optional': True,
            'type': 'models.PipelineRun',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdBy', {
            'name': 'createdBy',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('devices', {
            'name': 'devices',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Device\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('queueEntry', {
            'name': 'queueEntry',
            'is_list': False,
            'optional': True,
            'type': 'models.ValidationQueueEntry',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Device_relational_fields: Set[str] = {
        'session',
        'executions',
    }
_Device_fields: Dict['types.DeviceKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('serialNumber', {
            'name': 'serialNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sessionId', {
            'name': 'sessionId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.DeviceStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''IMEI, ICCID, hardware info, calibration data''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('session', {
            'name': 'session',
            'is_list': False,
            'optional': True,
            'type': 'models.Session',
            'is_relational': True,
            'documentation': None,
        }),
        ('executions', {
            'name': 'executions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TestExecution\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FixtureDesign_relational_fields: Set[str] = {
        'fixtures',
    }
_FixtureDesign_fields: Dict['types.FixtureDesignKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"alpha-fixture-v1.2"''',
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"alpha" -- which product this fixture tests''',
        }),
        ('revision', {
            'name': 'revision',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"1.2" -- fixture hardware revision''',
        }),
        ('capabilities', {
            'name': 'capabilities',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': '''["button", "peltier", "charger_relay"]''',
        }),
        ('profileTemplate', {
            'name': 'profileTemplate',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Default fixture profile JSON for this design''',
        }),
        ('schematicUrl', {
            'name': 'schematicUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Link to schematic PDF/CAD files''',
        }),
        ('bomUrl', {
            'name': 'bomUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Link to BOM spreadsheet''',
        }),
        ('assemblyGuide', {
            'name': 'assemblyGuide',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Link to assembly instructions''',
        }),
        ('notes', {
            'name': 'notes',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Build notes, known issues, changelog''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('fixtures', {
            'name': 'fixtures',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Fixture\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Fixture_relational_fields: Set[str] = {
        'product',
        'design',
        'slots',
        'sessions',
        'queueEntries',
    }
_Fixture_fields: Dict['types.FixtureKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"Theta MFG Fixture 1", "Alpha B0 Bench 1"''',
        }),
        ('stationId', {
            'name': 'stationId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"station-33", matches fixture profile''',
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': 'enums.NodeType',
            'is_relational': False,
            'documentation': '''MANUFACTURING or VALIDATION''',
        }),
        ('designId', {
            'name': 'designId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''FK to FixtureDesign''',
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.FixtureStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('lockedBy', {
            'name': 'lockedBy',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Pipeline/job ID holding the lock''',
        }),
        ('lockedAt', {
            'name': 'lockedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('profileOverrides', {
            'name': 'profileOverrides',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Fixture-specific profile overrides (merged with design template)''',
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('active', {
            'name': 'active',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''physical location, rack position, etc.''',
        }),
        ('lastHealthCheck', {
            'name': 'lastHealthCheck',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('design', {
            'name': 'design',
            'is_list': False,
            'optional': True,
            'type': 'models.FixtureDesign',
            'is_relational': True,
            'documentation': None,
        }),
        ('slots', {
            'name': 'slots',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FixtureSlot\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('sessions', {
            'name': 'sessions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Session\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('queueEntries', {
            'name': 'queueEntries',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ValidationQueueEntry\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FixtureSlot_relational_fields: Set[str] = {
        'fixture',
        'node',
        'testExecutions',
    }
_FixtureSlot_fields: Dict['types.FixtureSlotKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('fixtureId', {
            'name': 'fixtureId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slotIndex', {
            'name': 'slotIndex',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''0-based position in the fixture''',
        }),
        ('label', {
            'name': 'label',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''human-readable: "Slot 1", "Stand Alone"''',
        }),
        ('nodeId', {
            'name': 'nodeId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('active', {
            'name': 'active',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('jlinkAppSerial', {
            'name': 'jlinkAppSerial',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''J-Link SNR for nRF52840 app processor''',
        }),
        ('jlinkCommsSerial', {
            'name': 'jlinkCommsSerial',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''J-Link SNR for nRF9151 comms (if separate)''',
        }),
        ('uartAppPath', {
            'name': 'uartAppPath',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"/dev/verdin-uart2"''',
        }),
        ('uartCommsPath', {
            'name': 'uartCommsPath',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"/dev/verdin-uart1"''',
        }),
        ('dutDeviceId', {
            'name': 'dutDeviceId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"70B3D584C01E1FCC"''',
        }),
        ('dutSnr', {
            'name': 'dutSnr',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"0964" -- J-Link probe serial''',
        }),
        ('dutImei', {
            'name': 'dutImei',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"355025931735979"''',
        }),
        ('dutIccids', {
            'name': 'dutIccids',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': '''["89148000009808558441", "89457300000037582833"]''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('fixture', {
            'name': 'fixture',
            'is_list': False,
            'optional': True,
            'type': 'models.Fixture',
            'is_relational': True,
            'documentation': None,
        }),
        ('node', {
            'name': 'node',
            'is_list': False,
            'optional': True,
            'type': 'models.Node',
            'is_relational': True,
            'documentation': None,
        }),
        ('testExecutions', {
            'name': 'testExecutions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TestExecution\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Node_relational_fields: Set[str] = {
        'fixtureSlot',
        'testExecutions',
    }
_Node_fields: Dict['types.NodeKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('hostname', {
            'name': 'hostname',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''K8s node name / OS hostname''',
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': 'enums.NodeType',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.NodeStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('ipAddress', {
            'name': 'ipAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('hardwareRevision', {
            'name': 'hardwareRevision',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"REV1.1", "REV1.2"''',
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''K8s labels, capabilities, gRPC port, etc.''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('fixtureSlot', {
            'name': 'fixtureSlot',
            'is_list': False,
            'optional': True,
            'type': 'models.FixtureSlot',
            'is_relational': True,
            'documentation': '''one-to-one via @unique on FixtureSlot.nodeId''',
        }),
        ('testExecutions', {
            'name': 'testExecutions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TestExecution\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_IcleDevice_relational_fields: Set[str] = {
        'commands',
        'logs',
    }
_IcleDevice_fields: Dict['types.IcleDeviceKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deviceId', {
            'name': 'deviceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Hardware ID from ZMS''',
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('ipAddress', {
            'name': 'ipAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('macAddress', {
            'name': 'macAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('firmwareVersion', {
            'name': 'firmwareVersion',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.IcleDeviceStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('registered', {
            'name': 'registered',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': '''false = discovered, true = registered via UI''',
        }),
        ('lastHeartbeat', {
            'name': 'lastHeartbeat',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastStatusData', {
            'name': 'lastStatusData',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Latest telemetry snapshot (uptime, heap, rssi, etc.)''',
        }),
        ('pendingConfig', {
            'name': 'pendingConfig',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''Config to push on next heartbeat''',
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('commands', {
            'name': 'commands',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.IclePendingCommand\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('logs', {
            'name': 'logs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.IcleLog\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_IclePendingCommand_relational_fields: Set[str] = {
        'device',
    }
_IclePendingCommand_fields: Dict['types.IclePendingCommandKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deviceId', {
            'name': 'deviceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('commandType', {
            'name': 'commandType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''config_update, ota_trigger, log_upload, reboot''',
        }),
        ('payload', {
            'name': 'payload',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('priority', {
            'name': 'priority',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': '''Higher priority = delivered first''',
        }),
        ('expiresAt', {
            'name': 'expiresAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('acknowledged', {
            'name': 'acknowledged',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('device', {
            'name': 'device',
            'is_list': False,
            'optional': True,
            'type': 'models.IcleDevice',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_IcleLog_relational_fields: Set[str] = {
        'device',
    }
_IcleLog_fields: Dict['types.IcleLogKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deviceId', {
            'name': 'deviceId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('filename', {
            'name': 'filename',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('storageKey', {
            'name': 'storageKey',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''MinIO object key''',
        }),
        ('sizeBytes', {
            'name': 'sizeBytes',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('format', {
            'name': 'format',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"binary" or "csv"''',
        }),
        ('uploadedAt', {
            'name': 'uploadedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('device', {
            'name': 'device',
            'is_list': False,
            'optional': True,
            'type': 'models.IcleDevice',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Deployment_relational_fields: Set[str] = {
        'product',
        'createdBy',
    }
_Deployment_fields: Dict['types.DeploymentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.DeploymentStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('config', {
            'name': 'config',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''K8s manifest, image tags, env overrides''',
        }),
        ('version', {
            'name': 'version',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdById', {
            'name': 'createdById',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdBy', {
            'name': 'createdBy',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Test_relational_fields: Set[str] = {
        'product',
        'executions',
    }
_Test_fields: Dict['types.TestKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''"electrical", "firmware", "post", etc.''',
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('config', {
            'name': 'config',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''default config template (can be overridden per-execution)''',
        }),
        ('enabled', {
            'name': 'enabled',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('executions', {
            'name': 'executions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TestExecution\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_TestExecution_relational_fields: Set[str] = {
        'test',
        'node',
        'device',
        'slot',
        'triggeredBy',
        'steps',
        'logs',
    }
_TestExecution_fields: Dict['types.TestExecutionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('testId', {
            'name': 'testId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('nodeId', {
            'name': 'nodeId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deviceId', {
            'name': 'deviceId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slotId', {
            'name': 'slotId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.TestExecutionStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('config', {
            'name': 'config',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''execution-specific config overrides''',
        }),
        ('triggeredById', {
            'name': 'triggeredById',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedAt', {
            'name': 'finishedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('test', {
            'name': 'test',
            'is_list': False,
            'optional': True,
            'type': 'models.Test',
            'is_relational': True,
            'documentation': None,
        }),
        ('node', {
            'name': 'node',
            'is_list': False,
            'optional': True,
            'type': 'models.Node',
            'is_relational': True,
            'documentation': None,
        }),
        ('device', {
            'name': 'device',
            'is_list': False,
            'optional': True,
            'type': 'models.Device',
            'is_relational': True,
            'documentation': None,
        }),
        ('slot', {
            'name': 'slot',
            'is_list': False,
            'optional': True,
            'type': 'models.FixtureSlot',
            'is_relational': True,
            'documentation': None,
        }),
        ('triggeredBy', {
            'name': 'triggeredBy',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('steps', {
            'name': 'steps',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TestStep\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('logs', {
            'name': 'logs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Log\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_TestStep_relational_fields: Set[str] = {
        'execution',
    }
_TestStep_fields: Dict['types.TestStepKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('executionId', {
            'name': 'executionId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stepIndex', {
            'name': 'stepIndex',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.TestExecutionStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('passed', {
            'name': 'passed',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('errorMessage', {
            'name': 'errorMessage',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('measurements', {
            'name': 'measurements',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('logOutput', {
            'name': 'logOutput',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('logStorageKey', {
            'name': 'logStorageKey',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('finishedAt', {
            'name': 'finishedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('durationMs', {
            'name': 'durationMs',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('execution', {
            'name': 'execution',
            'is_list': False,
            'optional': True,
            'type': 'models.TestExecution',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_User_relational_fields: Set[str] = {
        'permissionSet',
        'apiKeys',
        'sessions',
        'deployments',
        'testExecutions',
        'auditLogs',
    }
_User_fields: Dict['types.UserKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('externalId', {
            'name': 'externalId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''Google sub ID, set on first login''',
        }),
        ('permissionSetId', {
            'name': 'permissionSetId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('active', {
            'name': 'active',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastSeenAt', {
            'name': 'lastSeenAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('permissionSet', {
            'name': 'permissionSet',
            'is_list': False,
            'optional': True,
            'type': 'models.PermissionSet',
            'is_relational': True,
            'documentation': None,
        }),
        ('apiKeys', {
            'name': 'apiKeys',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ApiKey\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('sessions', {
            'name': 'sessions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Session\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('deployments', {
            'name': 'deployments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Deployment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('testExecutions', {
            'name': 'testExecutions',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.TestExecution\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('auditLogs', {
            'name': 'auditLogs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.AuditLog\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PermissionSet_relational_fields: Set[str] = {
        'users',
    }
_PermissionSet_fields: Dict['types.PermissionSetKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('permissions', {
            'name': 'permissions',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': '''e.g. ["Concord.Firmware.AppID.Create", "Concord.Cluster.Read"]''',
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.User\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ApiKey_relational_fields: Set[str] = {
        'user',
    }
_ApiKey_fields: Dict['types.ApiKeyKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('keyHash', {
            'name': 'keyHash',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''SHA-256 hash of the actual key (never store plaintext)''',
        }),
        ('keyPrefix', {
            'name': 'keyPrefix',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''First 8 chars of key for display ("ck_live_a1b2...")''',
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiresAt', {
            'name': 'expiresAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastUsedAt', {
            'name': 'lastUsedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_AuditLog_relational_fields: Set[str] = {
        'user',
    }
_AuditLog_fields: Dict['types.AuditLogKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('action', {
            'name': 'action',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''dot-notation verb: "session.create", "node.update", etc.''',
        }),
        ('entityType', {
            'name': 'entityType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': '''model name: "Session", "Node", "Test", etc.''',
        }),
        ('entityId', {
            'name': 'entityId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('details', {
            'name': 'details',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': '''freeform context -- before/after snapshots, params, etc.''',
        }),
        ('ipAddress', {
            'name': 'ipAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Setting_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_Setting_fields: Dict['types.SettingKeys', PartialModelField] = OrderedDict(
    [
        ('key', {
            'name': 'key',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('value', {
            'name': 'value',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_Log_relational_fields: Set[str] = {
        'execution',
    }
_Log_fields: Dict['types.LogKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('level', {
            'name': 'level',
            'is_list': False,
            'optional': False,
            'type': 'enums.LogLevel',
            'is_relational': False,
            'documentation': None,
        }),
        ('message', {
            'name': 'message',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('source', {
            'name': 'source',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': '''service name, module path, etc.''',
        }),
        ('executionId', {
            'name': 'executionId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('execution', {
            'name': 'execution',
            'is_list': False,
            'optional': True,
            'type': 'models.TestExecution',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Product)
model_rebuild(ProductTarget)
model_rebuild(Board)
model_rebuild(BoardRevision)
model_rebuild(FirmwareBuild)
model_rebuild(ProductStageConfig)
model_rebuild(ValidationQueueEntry)
model_rebuild(PipelineRun)
model_rebuild(BuildJob)
model_rebuild(BuildJobArtifact)
model_rebuild(Session)
model_rebuild(Device)
model_rebuild(FixtureDesign)
model_rebuild(Fixture)
model_rebuild(FixtureSlot)
model_rebuild(Node)
model_rebuild(IcleDevice)
model_rebuild(IclePendingCommand)
model_rebuild(IcleLog)
model_rebuild(Deployment)
model_rebuild(Test)
model_rebuild(TestExecution)
model_rebuild(TestStep)
model_rebuild(User)
model_rebuild(PermissionSet)
model_rebuild(ApiKey)
model_rebuild(AuditLog)
model_rebuild(Setting)
model_rebuild(Log)
