# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ProductActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Product]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Product.prisma().query_raw(
            'SELECT * FROM Product WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Product
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Product.prisma().query_first(
            'SELECT * FROM Product WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductCreateInput,
        include: Optional[types.ProductInclude] = None
    ) -> _PrismaModelT:
        """Create a new Product record.

        Parameters
        ----------
        data
            Product record data
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The created Product record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Product record from just the required fields
        product = Product.prisma().create(
            data={
                # data to create a Product record
                'name': 'ggciceaie',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Product records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Product record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Product.prisma().create_many(
            data=[
                {
                    # data to create a Product record
                    'name': 'bbehjachib',
                },
                {
                    # data to create a Product record
                    'name': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Product record.

        Parameters
        ----------
        where
            Product filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The deleted Product record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().delete(
            where={
                'id': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Product record.

        Parameters
        ----------
        where
            Product filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The found Product record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().find_unique(
            where={
                'id': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Product record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Product filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The found Product record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().find_unique_or_raise(
            where={
                'id': 'eigcfgbif',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
        include: Optional[types.ProductInclude] = None,
        order: Optional[Union[types.ProductOrderByInput, List[types.ProductOrderByInput]]] = None,
        distinct: Optional[List[types.ProductScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Product records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Product records returned
        skip
            Ignore the first N results
        where
            Product filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Product model
        order
            Order the returned Product records by any field
        distinct
            Filter Product records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Product]
            The list of all Product records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Product records
        products = Product.prisma().find_many(take=10)

        # find the first 5 Product records ordered by the slug field
        products = Product.prisma().find_many(
            take=5,
            order={
                'slug': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
        include: Optional[types.ProductInclude] = None,
        order: Optional[Union[types.ProductOrderByInput, List[types.ProductOrderByInput]]] = None,
        distinct: Optional[List[types.ProductScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Product record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Product filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Product model
        order
            Order the returned Product records by any field
        distinct
            Filter Product records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Product
            The first Product record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Product record ordered by the description field
        product = Product.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
        include: Optional[types.ProductInclude] = None,
        order: Optional[Union[types.ProductOrderByInput, List[types.ProductOrderByInput]]] = None,
        distinct: Optional[List[types.ProductScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Product record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Product filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Product model
        order
            Order the returned Product records by any field
        distinct
            Filter Product records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Product
            The first Product record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Product record ordered by the active field
        product = Product.prisma().find_first_or_raise(
            skip=1,
            order={
                'active': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductUpdateInput,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Product record.

        Parameters
        ----------
        data
            Product record data specifying what to update
        where
            Product filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The updated Product record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        product = Product.prisma().update(
            where={
                'id': 'bagcfbhiig',
            },
            data={
                # data to update the Product record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductWhereUniqueInput,
        data: types.ProductUpsertInput,
        include: Optional[types.ProductInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Product filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The created or updated Product record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().upsert(
            where={
                'id': 'cghideieh',
            },
            data={
                'create': {
                    'id': 'cghideieh',
                    'name': 'cadfabfehe',
                },
                'update': {
                    'name': 'cadfabfehe',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductUpdateManyMutationInput,
        where: types.ProductWhereInput,
    ) -> int:
        """Update multiple Product records

        Parameters
        ----------
        data
            Product data to update the selected Product records to
        where
            Filter to select the Product records to update

        Returns
        -------
        int
            The total number of Product records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Product records
        total = Product.prisma().update_many(
            data={
                'buildConfig': Json({'biabhbdai': True})
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Product records present in the database

        Parameters
        ----------
        select
            Select the Product fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Product filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Product.prisma().count()

        # results: prisma.types.ProductCountAggregateOutput
        results = Product.prisma().count(
            select={
                '_all': True,
                'metadata': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
    ) -> types.ProductCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
    ) -> Union[int, types.ProductCountAggregateOutput]:
        """Count the number of Product records present in the database

        Parameters
        ----------
        select
            Select the Product fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Product filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Product.prisma().count()

        # results: prisma.types.ProductCountAggregateOutput
        results = Product.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductWhereInput] = None
    ) -> int:
        """Delete multiple Product records.

        Parameters
        ----------
        where
            Optional Product filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Product records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Product records
        total = Product.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductScalarFieldKeys'],
        *,
        where: Optional['types.ProductWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductAvgAggregateInput'] = None,
        sum: Optional['types.ProductSumAggregateInput'] = None,
        min: Optional['types.ProductMinAggregateInput'] = None,
        max: Optional['types.ProductMaxAggregateInput'] = None,
        having: Optional['types.ProductScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductGroupByOutput']:
        """Group Product records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Product fields to group records by
        where
            Product filter to select records
        take
            Limit the maximum number of Product records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductGroupByOutput]
            A list of dictionaries representing the Product record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Product records by updatedAt values
        # and count how many records are in each group
        results = Product.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ProductTargetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ProductTarget]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ProductTarget.prisma().query_raw(
            'SELECT * FROM ProductTarget WHERE id = $1',
            'idghgaicb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ProductTarget
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ProductTarget.prisma().query_first(
            'SELECT * FROM ProductTarget WHERE boardRevisionId = $1',
            'fjfddhigg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductTargetCreateInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> _PrismaModelT:
        """Create a new ProductTarget record.

        Parameters
        ----------
        data
            ProductTarget record data
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The created ProductTarget record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ProductTarget record from just the required fields
        producttarget = ProductTarget.prisma().create(
            data={
                # data to create a ProductTarget record
                'boardRevisionId': 'hjaecfifb',
                'role': 'cbbbjbfcii',
                'soc': 'bbejhfidcb',
                'appId': 1644289366,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductTargetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ProductTarget records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ProductTarget record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ProductTarget.prisma().create_many(
            data=[
                {
                    # data to create a ProductTarget record
                    'boardRevisionId': 'bdiicjafbj',
                    'role': 'bgehebiafc',
                    'soc': 'bghffegacj',
                    'appId': 1767274722,
                },
                {
                    # data to create a ProductTarget record
                    'boardRevisionId': 'dcgchcbbf',
                    'role': 'bdedcabahc',
                    'soc': 'ghfhiafcb',
                    'appId': 744964398,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ProductTarget record.

        Parameters
        ----------
        where
            ProductTarget filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The deleted ProductTarget record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().delete(
            where={
                'id': 'bjgjgibgbf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ProductTarget record.

        Parameters
        ----------
        where
            ProductTarget filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The found ProductTarget record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().find_unique(
            where={
                'id': 'bbbgbhfjge',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ProductTarget record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ProductTarget filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The found ProductTarget record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().find_unique_or_raise(
            where={
                'id': 'igbehcbab',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
        include: Optional[types.ProductTargetInclude] = None,
        order: Optional[Union[types.ProductTargetOrderByInput, List[types.ProductTargetOrderByInput]]] = None,
        distinct: Optional[List[types.ProductTargetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ProductTarget records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ProductTarget records returned
        skip
            Ignore the first N results
        where
            ProductTarget filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductTarget model
        order
            Order the returned ProductTarget records by any field
        distinct
            Filter ProductTarget records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ProductTarget]
            The list of all ProductTarget records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ProductTarget records
        producttargets = ProductTarget.prisma().find_many(take=10)

        # find the first 5 ProductTarget records ordered by the role field
        producttargets = ProductTarget.prisma().find_many(
            take=5,
            order={
                'role': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
        include: Optional[types.ProductTargetInclude] = None,
        order: Optional[Union[types.ProductTargetOrderByInput, List[types.ProductTargetOrderByInput]]] = None,
        distinct: Optional[List[types.ProductTargetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ProductTarget record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductTarget filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductTarget model
        order
            Order the returned ProductTarget records by any field
        distinct
            Filter ProductTarget records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductTarget
            The first ProductTarget record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductTarget record ordered by the soc field
        producttarget = ProductTarget.prisma().find_first(
            skip=1,
            order={
                'soc': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
        include: Optional[types.ProductTargetInclude] = None,
        order: Optional[Union[types.ProductTargetOrderByInput, List[types.ProductTargetOrderByInput]]] = None,
        distinct: Optional[List[types.ProductTargetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ProductTarget record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductTarget filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductTarget model
        order
            Order the returned ProductTarget records by any field
        distinct
            Filter ProductTarget records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductTarget
            The first ProductTarget record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductTarget record ordered by the appId field
        producttarget = ProductTarget.prisma().find_first_or_raise(
            skip=1,
            order={
                'appId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductTargetUpdateInput,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ProductTarget record.

        Parameters
        ----------
        data
            ProductTarget record data specifying what to update
        where
            ProductTarget filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The updated ProductTarget record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().update(
            where={
                'id': 'bdadaadhag',
            },
            data={
                # data to update the ProductTarget record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductTargetWhereUniqueInput,
        data: types.ProductTargetUpsertInput,
        include: Optional[types.ProductTargetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ProductTarget filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The created or updated ProductTarget record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().upsert(
            where={
                'id': 'bgiggdidbf',
            },
            data={
                'create': {
                    'id': 'bgiggdidbf',
                    'boardRevisionId': 'dcgchcbbf',
                    'role': 'bdedcabahc',
                    'soc': 'ghfhiafcb',
                    'appId': 744964398,
                },
                'update': {
                    'boardRevisionId': 'dcgchcbbf',
                    'role': 'bdedcabahc',
                    'soc': 'ghfhiafcb',
                    'appId': 744964398,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductTargetUpdateManyMutationInput,
        where: types.ProductTargetWhereInput,
    ) -> int:
        """Update multiple ProductTarget records

        Parameters
        ----------
        data
            ProductTarget data to update the selected ProductTarget records to
        where
            Filter to select the ProductTarget records to update

        Returns
        -------
        int
            The total number of ProductTarget records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ProductTarget records
        total = ProductTarget.prisma().update_many(
            data={
                'id': 'caaaedabfc'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ProductTarget records present in the database

        Parameters
        ----------
        select
            Select the ProductTarget fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductTarget filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductTargetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductTarget.prisma().count()

        # results: prisma.types.ProductTargetCountAggregateOutput
        results = ProductTarget.prisma().count(
            select={
                '_all': True,
                'boardRevisionId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductTargetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
    ) -> types.ProductTargetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductTargetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
    ) -> Union[int, types.ProductTargetCountAggregateOutput]:
        """Count the number of ProductTarget records present in the database

        Parameters
        ----------
        select
            Select the ProductTarget fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductTarget filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductTargetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductTarget.prisma().count()

        # results: prisma.types.ProductTargetCountAggregateOutput
        results = ProductTarget.prisma().count(
            select={
                '_all': True,
                'role': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductTargetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductTargetWhereInput] = None
    ) -> int:
        """Delete multiple ProductTarget records.

        Parameters
        ----------
        where
            Optional ProductTarget filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ProductTarget records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ProductTarget records
        total = ProductTarget.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductTargetScalarFieldKeys'],
        *,
        where: Optional['types.ProductTargetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductTargetAvgAggregateInput'] = None,
        sum: Optional['types.ProductTargetSumAggregateInput'] = None,
        min: Optional['types.ProductTargetMinAggregateInput'] = None,
        max: Optional['types.ProductTargetMaxAggregateInput'] = None,
        having: Optional['types.ProductTargetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductTargetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductTargetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductTargetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductTargetGroupByOutput']:
        """Group ProductTarget records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ProductTarget fields to group records by
        where
            ProductTarget filter to select records
        take
            Limit the maximum number of ProductTarget records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductTargetGroupByOutput]
            A list of dictionaries representing the ProductTarget record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ProductTarget records by soc values
        # and count how many records are in each group
        results = ProductTarget.prisma().group_by(
            ['soc'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BoardActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Board]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Board.prisma().query_raw(
            'SELECT * FROM Board WHERE id = $1',
            'bigibebcib',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Board
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Board.prisma().query_first(
            'SELECT * FROM Board WHERE productId = $1',
            'bigaiehgcc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BoardCreateInput,
        include: Optional[types.BoardInclude] = None
    ) -> _PrismaModelT:
        """Create a new Board record.

        Parameters
        ----------
        data
            Board record data
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The created Board record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Board record from just the required fields
        board = Board.prisma().create(
            data={
                # data to create a Board record
                'productId': 'beeifcbebf',
                'name': 'bgcigfahea',
                'ckBoardsFamily': 'bcejgaggif',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BoardCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Board records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Board record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Board.prisma().create_many(
            data=[
                {
                    # data to create a Board record
                    'productId': 'idfjadbcc',
                    'name': 'hgdhbjhhj',
                    'ckBoardsFamily': 'ecjjjfbae',
                },
                {
                    # data to create a Board record
                    'productId': 'bhhfibbigf',
                    'name': 'ijdbeffgg',
                    'ckBoardsFamily': 'jjfeafhfj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Board record.

        Parameters
        ----------
        where
            Board filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The deleted Board record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().delete(
            where={
                'id': 'cbachdgfce',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Board record.

        Parameters
        ----------
        where
            Board filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The found Board record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().find_unique(
            where={
                'id': 'chbfcacbd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Board record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Board filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The found Board record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().find_unique_or_raise(
            where={
                'id': 'efggddide',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
        include: Optional[types.BoardInclude] = None,
        order: Optional[Union[types.BoardOrderByInput, List[types.BoardOrderByInput]]] = None,
        distinct: Optional[List[types.BoardScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Board records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Board records returned
        skip
            Ignore the first N results
        where
            Board filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Board model
        order
            Order the returned Board records by any field
        distinct
            Filter Board records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Board]
            The list of all Board records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Board records
        boards = Board.prisma().find_many(take=10)

        # find the first 5 Board records ordered by the name field
        boards = Board.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
        include: Optional[types.BoardInclude] = None,
        order: Optional[Union[types.BoardOrderByInput, List[types.BoardOrderByInput]]] = None,
        distinct: Optional[List[types.BoardScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Board record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Board filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Board model
        order
            Order the returned Board records by any field
        distinct
            Filter Board records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Board
            The first Board record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Board record ordered by the ckBoardsFamily field
        board = Board.prisma().find_first(
            skip=1,
            order={
                'ckBoardsFamily': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
        include: Optional[types.BoardInclude] = None,
        order: Optional[Union[types.BoardOrderByInput, List[types.BoardOrderByInput]]] = None,
        distinct: Optional[List[types.BoardScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Board record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Board filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Board model
        order
            Order the returned Board records by any field
        distinct
            Filter Board records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Board
            The first Board record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Board record ordered by the vendor field
        board = Board.prisma().find_first_or_raise(
            skip=1,
            order={
                'vendor': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BoardUpdateInput,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Board record.

        Parameters
        ----------
        data
            Board record data specifying what to update
        where
            Board filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The updated Board record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        board = Board.prisma().update(
            where={
                'id': 'caficfigfb',
            },
            data={
                # data to update the Board record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BoardWhereUniqueInput,
        data: types.BoardUpsertInput,
        include: Optional[types.BoardInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Board filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The created or updated Board record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().upsert(
            where={
                'id': 'bfidgijfjc',
            },
            data={
                'create': {
                    'id': 'bfidgijfjc',
                    'productId': 'bhhfibbigf',
                    'name': 'ijdbeffgg',
                    'ckBoardsFamily': 'jjfeafhfj',
                },
                'update': {
                    'productId': 'bhhfibbigf',
                    'name': 'ijdbeffgg',
                    'ckBoardsFamily': 'jjfeafhfj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BoardUpdateManyMutationInput,
        where: types.BoardWhereInput,
    ) -> int:
        """Update multiple Board records

        Parameters
        ----------
        data
            Board data to update the selected Board records to
        where
            Filter to select the Board records to update

        Returns
        -------
        int
            The total number of Board records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Board records
        total = Board.prisma().update_many(
            data={
                'description': 'ihieecagf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Board records present in the database

        Parameters
        ----------
        select
            Select the Board fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Board filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Board.prisma().count()

        # results: prisma.types.BoardCountAggregateOutput
        results = Board.prisma().count(
            select={
                '_all': True,
                'active': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BoardCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
    ) -> types.BoardCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BoardCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
    ) -> Union[int, types.BoardCountAggregateOutput]:
        """Count the number of Board records present in the database

        Parameters
        ----------
        select
            Select the Board fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Board filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Board.prisma().count()

        # results: prisma.types.BoardCountAggregateOutput
        results = Board.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BoardCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BoardWhereInput] = None
    ) -> int:
        """Delete multiple Board records.

        Parameters
        ----------
        where
            Optional Board filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Board records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Board records
        total = Board.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BoardScalarFieldKeys'],
        *,
        where: Optional['types.BoardWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BoardAvgAggregateInput'] = None,
        sum: Optional['types.BoardSumAggregateInput'] = None,
        min: Optional['types.BoardMinAggregateInput'] = None,
        max: Optional['types.BoardMaxAggregateInput'] = None,
        having: Optional['types.BoardScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BoardCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BoardScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BoardScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BoardGroupByOutput']:
        """Group Board records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Board fields to group records by
        where
            Board filter to select records
        take
            Limit the maximum number of Board records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BoardGroupByOutput]
            A list of dictionaries representing the Board record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Board records by updatedAt values
        # and count how many records are in each group
        results = Board.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BoardRevisionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BoardRevision]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BoardRevision.prisma().query_raw(
            'SELECT * FROM BoardRevision WHERE id = $1',
            'bghfciaafe',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BoardRevision
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BoardRevision.prisma().query_first(
            'SELECT * FROM BoardRevision WHERE boardId = $1',
            'bgchfhgceh',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BoardRevisionCreateInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> _PrismaModelT:
        """Create a new BoardRevision record.

        Parameters
        ----------
        data
            BoardRevision record data
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The created BoardRevision record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BoardRevision record from just the required fields
        boardrevision = BoardRevision.prisma().create(
            data={
                # data to create a BoardRevision record
                'boardId': 'cafeiaccbc',
                'version': 'gaddfhfh',
                'ckBoardsName': 'gieegcbeg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BoardRevisionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BoardRevision records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BoardRevision record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BoardRevision.prisma().create_many(
            data=[
                {
                    # data to create a BoardRevision record
                    'boardId': 'bgcffadich',
                    'version': 'fcbichhci',
                    'ckBoardsName': 'bcggadccgf',
                },
                {
                    # data to create a BoardRevision record
                    'boardId': 'jdcfdcgc',
                    'version': 'cafdaehjid',
                    'ckBoardsName': 'gifdddbia',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BoardRevision record.

        Parameters
        ----------
        where
            BoardRevision filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The deleted BoardRevision record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().delete(
            where={
                'id': 'bchehecef',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BoardRevision record.

        Parameters
        ----------
        where
            BoardRevision filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The found BoardRevision record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().find_unique(
            where={
                'id': 'jeijcbhfe',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BoardRevision record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BoardRevision filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The found BoardRevision record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().find_unique_or_raise(
            where={
                'id': 'bjgejjabff',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
        include: Optional[types.BoardRevisionInclude] = None,
        order: Optional[Union[types.BoardRevisionOrderByInput, List[types.BoardRevisionOrderByInput]]] = None,
        distinct: Optional[List[types.BoardRevisionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BoardRevision records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BoardRevision records returned
        skip
            Ignore the first N results
        where
            BoardRevision filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BoardRevision model
        order
            Order the returned BoardRevision records by any field
        distinct
            Filter BoardRevision records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BoardRevision]
            The list of all BoardRevision records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BoardRevision records
        boardrevisions = BoardRevision.prisma().find_many(take=10)

        # find the first 5 BoardRevision records ordered by the version field
        boardrevisions = BoardRevision.prisma().find_many(
            take=5,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
        include: Optional[types.BoardRevisionInclude] = None,
        order: Optional[Union[types.BoardRevisionOrderByInput, List[types.BoardRevisionOrderByInput]]] = None,
        distinct: Optional[List[types.BoardRevisionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BoardRevision record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BoardRevision filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BoardRevision model
        order
            Order the returned BoardRevision records by any field
        distinct
            Filter BoardRevision records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BoardRevision
            The first BoardRevision record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BoardRevision record ordered by the ckBoardsName field
        boardrevision = BoardRevision.prisma().find_first(
            skip=1,
            order={
                'ckBoardsName': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
        include: Optional[types.BoardRevisionInclude] = None,
        order: Optional[Union[types.BoardRevisionOrderByInput, List[types.BoardRevisionOrderByInput]]] = None,
        distinct: Optional[List[types.BoardRevisionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BoardRevision record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BoardRevision filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BoardRevision model
        order
            Order the returned BoardRevision records by any field
        distinct
            Filter BoardRevision records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BoardRevision
            The first BoardRevision record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BoardRevision record ordered by the socs field
        boardrevision = BoardRevision.prisma().find_first_or_raise(
            skip=1,
            order={
                'socs': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BoardRevisionUpdateInput,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BoardRevision record.

        Parameters
        ----------
        data
            BoardRevision record data specifying what to update
        where
            BoardRevision filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The updated BoardRevision record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().update(
            where={
                'id': 'bcciijbibg',
            },
            data={
                # data to update the BoardRevision record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        data: types.BoardRevisionUpsertInput,
        include: Optional[types.BoardRevisionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BoardRevision filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The created or updated BoardRevision record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().upsert(
            where={
                'id': 'cffcachfd',
            },
            data={
                'create': {
                    'id': 'cffcachfd',
                    'boardId': 'jdcfdcgc',
                    'version': 'cafdaehjid',
                    'ckBoardsName': 'gifdddbia',
                },
                'update': {
                    'boardId': 'jdcfdcgc',
                    'version': 'cafdaehjid',
                    'ckBoardsName': 'gifdddbia',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BoardRevisionUpdateManyMutationInput,
        where: types.BoardRevisionWhereInput,
    ) -> int:
        """Update multiple BoardRevision records

        Parameters
        ----------
        data
            BoardRevision data to update the selected BoardRevision records to
        where
            Filter to select the BoardRevision records to update

        Returns
        -------
        int
            The total number of BoardRevision records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BoardRevision records
        total = BoardRevision.prisma().update_many(
            data={
                'status': enums.LifecycleStatus.DRAFT
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BoardRevision records present in the database

        Parameters
        ----------
        select
            Select the BoardRevision fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BoardRevision filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardRevisionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BoardRevision.prisma().count()

        # results: prisma.types.BoardRevisionCountAggregateOutput
        results = BoardRevision.prisma().count(
            select={
                '_all': True,
                'notes': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BoardRevisionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
    ) -> types.BoardRevisionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BoardRevisionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
    ) -> Union[int, types.BoardRevisionCountAggregateOutput]:
        """Count the number of BoardRevision records present in the database

        Parameters
        ----------
        select
            Select the BoardRevision fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BoardRevision filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardRevisionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BoardRevision.prisma().count()

        # results: prisma.types.BoardRevisionCountAggregateOutput
        results = BoardRevision.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BoardRevisionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BoardRevisionWhereInput] = None
    ) -> int:
        """Delete multiple BoardRevision records.

        Parameters
        ----------
        where
            Optional BoardRevision filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BoardRevision records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BoardRevision records
        total = BoardRevision.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BoardRevisionScalarFieldKeys'],
        *,
        where: Optional['types.BoardRevisionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BoardRevisionAvgAggregateInput'] = None,
        sum: Optional['types.BoardRevisionSumAggregateInput'] = None,
        min: Optional['types.BoardRevisionMinAggregateInput'] = None,
        max: Optional['types.BoardRevisionMaxAggregateInput'] = None,
        having: Optional['types.BoardRevisionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BoardRevisionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BoardRevisionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BoardRevisionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BoardRevisionGroupByOutput']:
        """Group BoardRevision records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BoardRevision fields to group records by
        where
            BoardRevision filter to select records
        take
            Limit the maximum number of BoardRevision records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BoardRevisionGroupByOutput]
            A list of dictionaries representing the BoardRevision record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BoardRevision records by updatedAt values
        # and count how many records are in each group
        results = BoardRevision.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FirmwareBuildActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FirmwareBuild]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FirmwareBuild.prisma().query_raw(
            'SELECT * FROM FirmwareBuild WHERE id = $1',
            'bccdfhdigc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FirmwareBuild
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FirmwareBuild.prisma().query_first(
            'SELECT * FROM FirmwareBuild WHERE productId = $1',
            'febcgjbfj',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FirmwareBuildCreateInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> _PrismaModelT:
        """Create a new FirmwareBuild record.

        Parameters
        ----------
        data
            FirmwareBuild record data
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The created FirmwareBuild record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FirmwareBuild record from just the required fields
        firmwarebuild = FirmwareBuild.prisma().create(
            data={
                # data to create a FirmwareBuild record
                'productId': 'bageiegghg',
                'version': 'faidicegb',
                'storageKey': 'bacecgfhbe',
                'filename': 'ihcahiead',
                'sizeBytes': 22496977152,
                'checksum': 'jbgijghgb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FirmwareBuildCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FirmwareBuild records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FirmwareBuild record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FirmwareBuild.prisma().create_many(
            data=[
                {
                    # data to create a FirmwareBuild record
                    'productId': 'hgjcghfbi',
                    'version': 'icadbcehj',
                    'storageKey': 'jchciaee',
                    'filename': 'deeificjd',
                    'sizeBytes': 13460893560,
                    'checksum': 'bejfijgcfb',
                },
                {
                    # data to create a FirmwareBuild record
                    'productId': 'caifcbgii',
                    'version': 'igaibbfgj',
                    'storageKey': 'bggajdcbbi',
                    'filename': 'fcfhgbjed',
                    'sizeBytes': 8834517552,
                    'checksum': 'ejdjahicb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FirmwareBuild record.

        Parameters
        ----------
        where
            FirmwareBuild filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The deleted FirmwareBuild record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().delete(
            where={
                'id': 'gdjgigfgc',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FirmwareBuild record.

        Parameters
        ----------
        where
            FirmwareBuild filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The found FirmwareBuild record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().find_unique(
            where={
                'id': 'gfeaahdeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FirmwareBuild record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FirmwareBuild filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The found FirmwareBuild record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().find_unique_or_raise(
            where={
                'id': 'bjafcgbffc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
        include: Optional[types.FirmwareBuildInclude] = None,
        order: Optional[Union[types.FirmwareBuildOrderByInput, List[types.FirmwareBuildOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareBuildScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FirmwareBuild records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FirmwareBuild records returned
        skip
            Ignore the first N results
        where
            FirmwareBuild filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model
        order
            Order the returned FirmwareBuild records by any field
        distinct
            Filter FirmwareBuild records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FirmwareBuild]
            The list of all FirmwareBuild records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FirmwareBuild records
        firmwarebuilds = FirmwareBuild.prisma().find_many(take=10)

        # find the first 5 FirmwareBuild records ordered by the targetId field
        firmwarebuilds = FirmwareBuild.prisma().find_many(
            take=5,
            order={
                'targetId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
        include: Optional[types.FirmwareBuildInclude] = None,
        order: Optional[Union[types.FirmwareBuildOrderByInput, List[types.FirmwareBuildOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareBuildScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FirmwareBuild record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model
        order
            Order the returned FirmwareBuild records by any field
        distinct
            Filter FirmwareBuild records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FirmwareBuild
            The first FirmwareBuild record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FirmwareBuild record ordered by the version field
        firmwarebuild = FirmwareBuild.prisma().find_first(
            skip=1,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
        include: Optional[types.FirmwareBuildInclude] = None,
        order: Optional[Union[types.FirmwareBuildOrderByInput, List[types.FirmwareBuildOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareBuildScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FirmwareBuild record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model
        order
            Order the returned FirmwareBuild records by any field
        distinct
            Filter FirmwareBuild records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FirmwareBuild
            The first FirmwareBuild record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FirmwareBuild record ordered by the isManufacturing field
        firmwarebuild = FirmwareBuild.prisma().find_first_or_raise(
            skip=1,
            order={
                'isManufacturing': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FirmwareBuildUpdateInput,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FirmwareBuild record.

        Parameters
        ----------
        data
            FirmwareBuild record data specifying what to update
        where
            FirmwareBuild filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The updated FirmwareBuild record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().update(
            where={
                'id': 'hihegjif',
            },
            data={
                # data to update the FirmwareBuild record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        data: types.FirmwareBuildUpsertInput,
        include: Optional[types.FirmwareBuildInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FirmwareBuild filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The created or updated FirmwareBuild record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().upsert(
            where={
                'id': 'bdjidcidac',
            },
            data={
                'create': {
                    'id': 'bdjidcidac',
                    'productId': 'caifcbgii',
                    'version': 'igaibbfgj',
                    'storageKey': 'bggajdcbbi',
                    'filename': 'fcfhgbjed',
                    'sizeBytes': 8834517552,
                    'checksum': 'ejdjahicb',
                },
                'update': {
                    'productId': 'caifcbgii',
                    'version': 'igaibbfgj',
                    'storageKey': 'bggajdcbbi',
                    'filename': 'fcfhgbjed',
                    'sizeBytes': 8834517552,
                    'checksum': 'ejdjahicb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FirmwareBuildUpdateManyMutationInput,
        where: types.FirmwareBuildWhereInput,
    ) -> int:
        """Update multiple FirmwareBuild records

        Parameters
        ----------
        data
            FirmwareBuild data to update the selected FirmwareBuild records to
        where
            Filter to select the FirmwareBuild records to update

        Returns
        -------
        int
            The total number of FirmwareBuild records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FirmwareBuild records
        total = FirmwareBuild.prisma().update_many(
            data={
                'status': enums.LifecycleStatus.DRAFT
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FirmwareBuild records present in the database

        Parameters
        ----------
        select
            Select the FirmwareBuild fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FirmwareBuildCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FirmwareBuild.prisma().count()

        # results: prisma.types.FirmwareBuildCountAggregateOutput
        results = FirmwareBuild.prisma().count(
            select={
                '_all': True,
                'storageKey': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FirmwareBuildCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
    ) -> types.FirmwareBuildCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FirmwareBuildCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
    ) -> Union[int, types.FirmwareBuildCountAggregateOutput]:
        """Count the number of FirmwareBuild records present in the database

        Parameters
        ----------
        select
            Select the FirmwareBuild fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FirmwareBuildCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FirmwareBuild.prisma().count()

        # results: prisma.types.FirmwareBuildCountAggregateOutput
        results = FirmwareBuild.prisma().count(
            select={
                '_all': True,
                'filename': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FirmwareBuildCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FirmwareBuildWhereInput] = None
    ) -> int:
        """Delete multiple FirmwareBuild records.

        Parameters
        ----------
        where
            Optional FirmwareBuild filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FirmwareBuild records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FirmwareBuild records
        total = FirmwareBuild.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FirmwareBuildScalarFieldKeys'],
        *,
        where: Optional['types.FirmwareBuildWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FirmwareBuildAvgAggregateInput'] = None,
        sum: Optional['types.FirmwareBuildSumAggregateInput'] = None,
        min: Optional['types.FirmwareBuildMinAggregateInput'] = None,
        max: Optional['types.FirmwareBuildMaxAggregateInput'] = None,
        having: Optional['types.FirmwareBuildScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FirmwareBuildCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FirmwareBuildScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FirmwareBuildScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FirmwareBuildGroupByOutput']:
        """Group FirmwareBuild records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FirmwareBuild fields to group records by
        where
            FirmwareBuild filter to select records
        take
            Limit the maximum number of FirmwareBuild records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FirmwareBuildGroupByOutput]
            A list of dictionaries representing the FirmwareBuild record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FirmwareBuild records by sizeBytes values
        # and count how many records are in each group
        results = FirmwareBuild.prisma().group_by(
            ['sizeBytes'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ProductStageConfigActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ProductStageConfig]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ProductStageConfig.prisma().query_raw(
            'SELECT * FROM ProductStageConfig WHERE id = $1',
            'ifgaaagff',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ProductStageConfig
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ProductStageConfig.prisma().query_first(
            'SELECT * FROM ProductStageConfig WHERE productId = $1',
            'befcddgjce',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductStageConfigCreateInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> _PrismaModelT:
        """Create a new ProductStageConfig record.

        Parameters
        ----------
        data
            ProductStageConfig record data
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The created ProductStageConfig record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ProductStageConfig record from just the required fields
        productstageconfig = ProductStageConfig.prisma().create(
            data={
                # data to create a ProductStageConfig record
                'productId': 'bfhdbjjgfd',
                'stage': 2013903098,
                'name': 'faajgfadf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductStageConfigCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ProductStageConfig records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ProductStageConfig record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ProductStageConfig.prisma().create_many(
            data=[
                {
                    # data to create a ProductStageConfig record
                    'productId': 'biaagcedjc',
                    'stage': 2077067425,
                    'name': 'bghcbbcidi',
                },
                {
                    # data to create a ProductStageConfig record
                    'productId': 'jcgghhgdj',
                    'stage': 1447624116,
                    'name': 'bhdiaidiaf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ProductStageConfig record.

        Parameters
        ----------
        where
            ProductStageConfig filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The deleted ProductStageConfig record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().delete(
            where={
                'id': 'deajegcfi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ProductStageConfig record.

        Parameters
        ----------
        where
            ProductStageConfig filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The found ProductStageConfig record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().find_unique(
            where={
                'id': 'gabahhhjf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ProductStageConfig record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ProductStageConfig filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The found ProductStageConfig record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().find_unique_or_raise(
            where={
                'id': 'cjagadcjg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
        include: Optional[types.ProductStageConfigInclude] = None,
        order: Optional[Union[types.ProductStageConfigOrderByInput, List[types.ProductStageConfigOrderByInput]]] = None,
        distinct: Optional[List[types.ProductStageConfigScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ProductStageConfig records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ProductStageConfig records returned
        skip
            Ignore the first N results
        where
            ProductStageConfig filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model
        order
            Order the returned ProductStageConfig records by any field
        distinct
            Filter ProductStageConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ProductStageConfig]
            The list of all ProductStageConfig records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ProductStageConfig records
        productstageconfigs = ProductStageConfig.prisma().find_many(take=10)

        # find the first 5 ProductStageConfig records ordered by the stage field
        productstageconfigs = ProductStageConfig.prisma().find_many(
            take=5,
            order={
                'stage': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
        include: Optional[types.ProductStageConfigInclude] = None,
        order: Optional[Union[types.ProductStageConfigOrderByInput, List[types.ProductStageConfigOrderByInput]]] = None,
        distinct: Optional[List[types.ProductStageConfigScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ProductStageConfig record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model
        order
            Order the returned ProductStageConfig records by any field
        distinct
            Filter ProductStageConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductStageConfig
            The first ProductStageConfig record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductStageConfig record ordered by the name field
        productstageconfig = ProductStageConfig.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
        include: Optional[types.ProductStageConfigInclude] = None,
        order: Optional[Union[types.ProductStageConfigOrderByInput, List[types.ProductStageConfigOrderByInput]]] = None,
        distinct: Optional[List[types.ProductStageConfigScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ProductStageConfig record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model
        order
            Order the returned ProductStageConfig records by any field
        distinct
            Filter ProductStageConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductStageConfig
            The first ProductStageConfig record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductStageConfig record ordered by the enabled field
        productstageconfig = ProductStageConfig.prisma().find_first_or_raise(
            skip=1,
            order={
                'enabled': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductStageConfigUpdateInput,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ProductStageConfig record.

        Parameters
        ----------
        data
            ProductStageConfig record data specifying what to update
        where
            ProductStageConfig filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The updated ProductStageConfig record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().update(
            where={
                'id': 'bifficggej',
            },
            data={
                # data to update the ProductStageConfig record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        data: types.ProductStageConfigUpsertInput,
        include: Optional[types.ProductStageConfigInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ProductStageConfig filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The created or updated ProductStageConfig record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().upsert(
            where={
                'id': 'bgbbaajbic',
            },
            data={
                'create': {
                    'id': 'bgbbaajbic',
                    'productId': 'jcgghhgdj',
                    'stage': 1447624116,
                    'name': 'bhdiaidiaf',
                },
                'update': {
                    'productId': 'jcgghhgdj',
                    'stage': 1447624116,
                    'name': 'bhdiaidiaf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductStageConfigUpdateManyMutationInput,
        where: types.ProductStageConfigWhereInput,
    ) -> int:
        """Update multiple ProductStageConfig records

        Parameters
        ----------
        data
            ProductStageConfig data to update the selected ProductStageConfig records to
        where
            Filter to select the ProductStageConfig records to update

        Returns
        -------
        int
            The total number of ProductStageConfig records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ProductStageConfig records
        total = ProductStageConfig.prisma().update_many(
            data={
                'buildScript': 'eegghdhjb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ProductStageConfig records present in the database

        Parameters
        ----------
        select
            Select the ProductStageConfig fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductStageConfigCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductStageConfig.prisma().count()

        # results: prisma.types.ProductStageConfigCountAggregateOutput
        results = ProductStageConfig.prisma().count(
            select={
                '_all': True,
                'buildTarget': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductStageConfigCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
    ) -> types.ProductStageConfigCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductStageConfigCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
    ) -> Union[int, types.ProductStageConfigCountAggregateOutput]:
        """Count the number of ProductStageConfig records present in the database

        Parameters
        ----------
        select
            Select the ProductStageConfig fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductStageConfigCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductStageConfig.prisma().count()

        # results: prisma.types.ProductStageConfigCountAggregateOutput
        results = ProductStageConfig.prisma().count(
            select={
                '_all': True,
                'fwRepoUrl': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductStageConfigCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductStageConfigWhereInput] = None
    ) -> int:
        """Delete multiple ProductStageConfig records.

        Parameters
        ----------
        where
            Optional ProductStageConfig filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ProductStageConfig records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ProductStageConfig records
        total = ProductStageConfig.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductStageConfigScalarFieldKeys'],
        *,
        where: Optional['types.ProductStageConfigWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductStageConfigAvgAggregateInput'] = None,
        sum: Optional['types.ProductStageConfigSumAggregateInput'] = None,
        min: Optional['types.ProductStageConfigMinAggregateInput'] = None,
        max: Optional['types.ProductStageConfigMaxAggregateInput'] = None,
        having: Optional['types.ProductStageConfigScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductStageConfigCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductStageConfigScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductStageConfigScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductStageConfigGroupByOutput']:
        """Group ProductStageConfig records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ProductStageConfig fields to group records by
        where
            ProductStageConfig filter to select records
        take
            Limit the maximum number of ProductStageConfig records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductStageConfigGroupByOutput]
            A list of dictionaries representing the ProductStageConfig record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ProductStageConfig records by fwRepoBranch values
        # and count how many records are in each group
        results = ProductStageConfig.prisma().group_by(
            ['fwRepoBranch'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ValidationQueueEntryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ValidationQueueEntry]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ValidationQueueEntry.prisma().query_raw(
            'SELECT * FROM ValidationQueueEntry WHERE id = $1',
            'daafgidjg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ValidationQueueEntry.prisma().query_first(
            'SELECT * FROM ValidationQueueEntry WHERE pipelineRunId = $1',
            'gdcgcgagj',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ValidationQueueEntryCreateInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> _PrismaModelT:
        """Create a new ValidationQueueEntry record.

        Parameters
        ----------
        data
            ValidationQueueEntry record data
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The created ValidationQueueEntry record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ValidationQueueEntry record from just the required fields
        validationqueueentry = ValidationQueueEntry.prisma().create(
            data={
                # data to create a ValidationQueueEntry record
                'pipelineRunId': 'bhceabbgja',
                'stage': 470157467,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ValidationQueueEntryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ValidationQueueEntry records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ValidationQueueEntry record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ValidationQueueEntry.prisma().create_many(
            data=[
                {
                    # data to create a ValidationQueueEntry record
                    'pipelineRunId': 'bcajcajjbc',
                    'stage': 1536744465,
                },
                {
                    # data to create a ValidationQueueEntry record
                    'pipelineRunId': 'ececbijji',
                    'stage': 2125632375,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ValidationQueueEntry record.

        Parameters
        ----------
        where
            ValidationQueueEntry filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The deleted ValidationQueueEntry record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().delete(
            where={
                'id': 'fdgjfbhia',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ValidationQueueEntry record.

        Parameters
        ----------
        where
            ValidationQueueEntry filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The found ValidationQueueEntry record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().find_unique(
            where={
                'id': 'jcehcdchh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ValidationQueueEntry record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ValidationQueueEntry filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The found ValidationQueueEntry record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().find_unique_or_raise(
            where={
                'id': 'bgcbjdhjcc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
        include: Optional[types.ValidationQueueEntryInclude] = None,
        order: Optional[Union[types.ValidationQueueEntryOrderByInput, List[types.ValidationQueueEntryOrderByInput]]] = None,
        distinct: Optional[List[types.ValidationQueueEntryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ValidationQueueEntry records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ValidationQueueEntry records returned
        skip
            Ignore the first N results
        where
            ValidationQueueEntry filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model
        order
            Order the returned ValidationQueueEntry records by any field
        distinct
            Filter ValidationQueueEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ValidationQueueEntry]
            The list of all ValidationQueueEntry records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ValidationQueueEntry records
        validationqueueentrys = ValidationQueueEntry.prisma().find_many(take=10)

        # find the first 5 ValidationQueueEntry records ordered by the stageConfigId field
        validationqueueentrys = ValidationQueueEntry.prisma().find_many(
            take=5,
            order={
                'stageConfigId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
        include: Optional[types.ValidationQueueEntryInclude] = None,
        order: Optional[Union[types.ValidationQueueEntryOrderByInput, List[types.ValidationQueueEntryOrderByInput]]] = None,
        distinct: Optional[List[types.ValidationQueueEntryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ValidationQueueEntry record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model
        order
            Order the returned ValidationQueueEntry records by any field
        distinct
            Filter ValidationQueueEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The first ValidationQueueEntry record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ValidationQueueEntry record ordered by the stage field
        validationqueueentry = ValidationQueueEntry.prisma().find_first(
            skip=1,
            order={
                'stage': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
        include: Optional[types.ValidationQueueEntryInclude] = None,
        order: Optional[Union[types.ValidationQueueEntryOrderByInput, List[types.ValidationQueueEntryOrderByInput]]] = None,
        distinct: Optional[List[types.ValidationQueueEntryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ValidationQueueEntry record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model
        order
            Order the returned ValidationQueueEntry records by any field
        distinct
            Filter ValidationQueueEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The first ValidationQueueEntry record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ValidationQueueEntry record ordered by the priority field
        validationqueueentry = ValidationQueueEntry.prisma().find_first_or_raise(
            skip=1,
            order={
                'priority': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ValidationQueueEntryUpdateInput,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ValidationQueueEntry record.

        Parameters
        ----------
        data
            ValidationQueueEntry record data specifying what to update
        where
            ValidationQueueEntry filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The updated ValidationQueueEntry record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().update(
            where={
                'id': 'bieiidcabj',
            },
            data={
                # data to update the ValidationQueueEntry record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        data: types.ValidationQueueEntryUpsertInput,
        include: Optional[types.ValidationQueueEntryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ValidationQueueEntry filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The created or updated ValidationQueueEntry record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().upsert(
            where={
                'id': 'bjcbfcieaa',
            },
            data={
                'create': {
                    'id': 'bjcbfcieaa',
                    'pipelineRunId': 'ececbijji',
                    'stage': 2125632375,
                },
                'update': {
                    'pipelineRunId': 'ececbijji',
                    'stage': 2125632375,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ValidationQueueEntryUpdateManyMutationInput,
        where: types.ValidationQueueEntryWhereInput,
    ) -> int:
        """Update multiple ValidationQueueEntry records

        Parameters
        ----------
        data
            ValidationQueueEntry data to update the selected ValidationQueueEntry records to
        where
            Filter to select the ValidationQueueEntry records to update

        Returns
        -------
        int
            The total number of ValidationQueueEntry records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ValidationQueueEntry records
        total = ValidationQueueEntry.prisma().update_many(
            data={
                'status': enums.QueueEntryStatus.QUEUED
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ValidationQueueEntry records present in the database

        Parameters
        ----------
        select
            Select the ValidationQueueEntry fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ValidationQueueEntryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ValidationQueueEntry.prisma().count()

        # results: prisma.types.ValidationQueueEntryCountAggregateOutput
        results = ValidationQueueEntry.prisma().count(
            select={
                '_all': True,
                'fixtureId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ValidationQueueEntryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
    ) -> types.ValidationQueueEntryCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ValidationQueueEntryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
    ) -> Union[int, types.ValidationQueueEntryCountAggregateOutput]:
        """Count the number of ValidationQueueEntry records present in the database

        Parameters
        ----------
        select
            Select the ValidationQueueEntry fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ValidationQueueEntryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ValidationQueueEntry.prisma().count()

        # results: prisma.types.ValidationQueueEntryCountAggregateOutput
        results = ValidationQueueEntry.prisma().count(
            select={
                '_all': True,
                'sessionId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ValidationQueueEntryCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ValidationQueueEntryWhereInput] = None
    ) -> int:
        """Delete multiple ValidationQueueEntry records.

        Parameters
        ----------
        where
            Optional ValidationQueueEntry filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ValidationQueueEntry records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ValidationQueueEntry records
        total = ValidationQueueEntry.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ValidationQueueEntryScalarFieldKeys'],
        *,
        where: Optional['types.ValidationQueueEntryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ValidationQueueEntryAvgAggregateInput'] = None,
        sum: Optional['types.ValidationQueueEntrySumAggregateInput'] = None,
        min: Optional['types.ValidationQueueEntryMinAggregateInput'] = None,
        max: Optional['types.ValidationQueueEntryMaxAggregateInput'] = None,
        having: Optional['types.ValidationQueueEntryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ValidationQueueEntryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ValidationQueueEntryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ValidationQueueEntryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ValidationQueueEntryGroupByOutput']:
        """Group ValidationQueueEntry records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ValidationQueueEntry fields to group records by
        where
            ValidationQueueEntry filter to select records
        take
            Limit the maximum number of ValidationQueueEntry records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ValidationQueueEntryGroupByOutput]
            A list of dictionaries representing the ValidationQueueEntry record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ValidationQueueEntry records by reason values
        # and count how many records are in each group
        results = ValidationQueueEntry.prisma().group_by(
            ['reason'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PipelineRunActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PipelineRun]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PipelineRun.prisma().query_raw(
            'SELECT * FROM PipelineRun WHERE id = $1',
            'cbaaechiej',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PipelineRun
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PipelineRun.prisma().query_first(
            'SELECT * FROM PipelineRun WHERE name = $1',
            'iejbeaaeg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PipelineRunCreateInput,
        include: Optional[types.PipelineRunInclude] = None
    ) -> _PrismaModelT:
        """Create a new PipelineRun record.

        Parameters
        ----------
        data
            PipelineRun record data
        include
            Specifies which relations should be loaded on the returned PipelineRun model

        Returns
        -------
        prisma.models.PipelineRun
            The created PipelineRun record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PipelineRun record from just the required fields
        pipelinerun = PipelineRun.prisma().create(
            data={
                # data to create a PipelineRun record
                'productId': 'jcibfcbhf',
                'board': 'chdadcaga',
                'branch': 'jicieifbh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PipelineRunCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PipelineRun records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PipelineRun record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PipelineRun.prisma().create_many(
            data=[
                {
                    # data to create a PipelineRun record
                    'productId': 'fbahdheji',
                    'board': 'cbbheiicgh',
                    'branch': 'beabjeejdg',
                },
                {
                    # data to create a PipelineRun record
                    'productId': 'bcjhgahffd',
                    'board': 'fbjeiiffa',
                    'branch': 'jhgidcgbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PipelineRunWhereUniqueInput,
        include: Optional[types.PipelineRunInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PipelineRun record.

        Parameters
        ----------
        where
            PipelineRun filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PipelineRun model

        Returns
        -------
        prisma.models.PipelineRun
            The deleted PipelineRun record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pipelinerun = PipelineRun.prisma().delete(
            where={
                'id': 'bgjgecfejc',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PipelineRunWhereUniqueInput,
        include: Optional[types.PipelineRunInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PipelineRun record.

        Parameters
        ----------
        where
            PipelineRun filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PipelineRun model

        Returns
        -------
        prisma.models.PipelineRun
            The found PipelineRun record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pipelinerun = PipelineRun.prisma().find_unique(
            where={
                'id': 'bgjcgchib',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PipelineRunWhereUniqueInput,
        include: Optional[types.PipelineRunInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PipelineRun record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PipelineRun filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PipelineRun model

        Returns
        -------
        prisma.models.PipelineRun
            The found PipelineRun record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pipelinerun = PipelineRun.prisma().find_unique_or_raise(
            where={
                'id': 'bacdaibgfa',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PipelineRunWhereInput] = None,
        cursor: Optional[types.PipelineRunWhereUniqueInput] = None,
        include: Optional[types.PipelineRunInclude] = None,
        order: Optional[Union[types.PipelineRunOrderByInput, List[types.PipelineRunOrderByInput]]] = None,
        distinct: Optional[List[types.PipelineRunScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PipelineRun records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PipelineRun records returned
        skip
            Ignore the first N results
        where
            PipelineRun filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PipelineRun model
        order
            Order the returned PipelineRun records by any field
        distinct
            Filter PipelineRun records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PipelineRun]
            The list of all PipelineRun records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PipelineRun records
        pipelineruns = PipelineRun.prisma().find_many(take=10)

        # find the first 5 PipelineRun records ordered by the productId field
        pipelineruns = PipelineRun.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PipelineRunWhereInput] = None,
        cursor: Optional[types.PipelineRunWhereUniqueInput] = None,
        include: Optional[types.PipelineRunInclude] = None,
        order: Optional[Union[types.PipelineRunOrderByInput, List[types.PipelineRunOrderByInput]]] = None,
        distinct: Optional[List[types.PipelineRunScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PipelineRun record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PipelineRun filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PipelineRun model
        order
            Order the returned PipelineRun records by any field
        distinct
            Filter PipelineRun records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PipelineRun
            The first PipelineRun record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PipelineRun record ordered by the board field
        pipelinerun = PipelineRun.prisma().find_first(
            skip=1,
            order={
                'board': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PipelineRunWhereInput] = None,
        cursor: Optional[types.PipelineRunWhereUniqueInput] = None,
        include: Optional[types.PipelineRunInclude] = None,
        order: Optional[Union[types.PipelineRunOrderByInput, List[types.PipelineRunOrderByInput]]] = None,
        distinct: Optional[List[types.PipelineRunScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PipelineRun record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PipelineRun filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PipelineRun model
        order
            Order the returned PipelineRun records by any field
        distinct
            Filter PipelineRun records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PipelineRun
            The first PipelineRun record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PipelineRun record ordered by the branch field
        pipelinerun = PipelineRun.prisma().find_first_or_raise(
            skip=1,
            order={
                'branch': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PipelineRunUpdateInput,
        where: types.PipelineRunWhereUniqueInput,
        include: Optional[types.PipelineRunInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PipelineRun record.

        Parameters
        ----------
        data
            PipelineRun record data specifying what to update
        where
            PipelineRun filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PipelineRun model

        Returns
        -------
        prisma.models.PipelineRun
            The updated PipelineRun record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        pipelinerun = PipelineRun.prisma().update(
            where={
                'id': 'dchgibach',
            },
            data={
                # data to update the PipelineRun record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PipelineRunWhereUniqueInput,
        data: types.PipelineRunUpsertInput,
        include: Optional[types.PipelineRunInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PipelineRun filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PipelineRun model

        Returns
        -------
        prisma.models.PipelineRun
            The created or updated PipelineRun record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pipelinerun = PipelineRun.prisma().upsert(
            where={
                'id': 'fchheijjc',
            },
            data={
                'create': {
                    'id': 'fchheijjc',
                    'productId': 'bcjhgahffd',
                    'board': 'fbjeiiffa',
                    'branch': 'jhgidcgbf',
                },
                'update': {
                    'productId': 'bcjhgahffd',
                    'board': 'fbjeiiffa',
                    'branch': 'jhgidcgbf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PipelineRunUpdateManyMutationInput,
        where: types.PipelineRunWhereInput,
    ) -> int:
        """Update multiple PipelineRun records

        Parameters
        ----------
        data
            PipelineRun data to update the selected PipelineRun records to
        where
            Filter to select the PipelineRun records to update

        Returns
        -------
        int
            The total number of PipelineRun records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PipelineRun records
        total = PipelineRun.prisma().update_many(
            data={
                'commitSha': 'cacjdfhejh'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PipelineRunWhereInput] = None,
        cursor: Optional[types.PipelineRunWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PipelineRun records present in the database

        Parameters
        ----------
        select
            Select the PipelineRun fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PipelineRun filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PipelineRunCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PipelineRun.prisma().count()

        # results: prisma.types.PipelineRunCountAggregateOutput
        results = PipelineRun.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PipelineRunCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PipelineRunWhereInput] = None,
        cursor: Optional[types.PipelineRunWhereUniqueInput] = None,
    ) -> types.PipelineRunCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PipelineRunCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PipelineRunWhereInput] = None,
        cursor: Optional[types.PipelineRunWhereUniqueInput] = None,
    ) -> Union[int, types.PipelineRunCountAggregateOutput]:
        """Count the number of PipelineRun records present in the database

        Parameters
        ----------
        select
            Select the PipelineRun fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PipelineRun filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PipelineRunCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PipelineRun.prisma().count()

        # results: prisma.types.PipelineRunCountAggregateOutput
        results = PipelineRun.prisma().count(
            select={
                '_all': True,
                'triggerType': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PipelineRunCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PipelineRunWhereInput] = None
    ) -> int:
        """Delete multiple PipelineRun records.

        Parameters
        ----------
        where
            Optional PipelineRun filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PipelineRun records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PipelineRun records
        total = PipelineRun.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PipelineRunScalarFieldKeys'],
        *,
        where: Optional['types.PipelineRunWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PipelineRunAvgAggregateInput'] = None,
        sum: Optional['types.PipelineRunSumAggregateInput'] = None,
        min: Optional['types.PipelineRunMinAggregateInput'] = None,
        max: Optional['types.PipelineRunMaxAggregateInput'] = None,
        having: Optional['types.PipelineRunScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PipelineRunCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PipelineRunScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PipelineRunScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PipelineRunGroupByOutput']:
        """Group PipelineRun records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PipelineRun fields to group records by
        where
            PipelineRun filter to select records
        take
            Limit the maximum number of PipelineRun records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PipelineRunGroupByOutput]
            A list of dictionaries representing the PipelineRun record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PipelineRun records by triggerData values
        # and count how many records are in each group
        results = PipelineRun.prisma().group_by(
            ['triggerData'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BuildJobActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BuildJob]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BuildJob.prisma().query_raw(
            'SELECT * FROM BuildJob WHERE id = $1',
            'bdbifjhbbi',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BuildJob
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BuildJob.prisma().query_first(
            'SELECT * FROM BuildJob WHERE productId = $1',
            'cbccbbcdfb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BuildJobCreateInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> _PrismaModelT:
        """Create a new BuildJob record.

        Parameters
        ----------
        data
            BuildJob record data
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The created BuildJob record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BuildJob record from just the required fields
        buildjob = BuildJob.prisma().create(
            data={
                # data to create a BuildJob record
                'productId': 'bacejedaca',
                'board': 'bhbhdahfaj',
                'target': 'bfjibceaec',
                'branch': 'ibhgcdbgd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BuildJobCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BuildJob records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BuildJob record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BuildJob.prisma().create_many(
            data=[
                {
                    # data to create a BuildJob record
                    'productId': 'badaffhddg',
                    'board': 'bbdbfcfihd',
                    'target': 'cbagggbji',
                    'branch': 'bchgafhjed',
                },
                {
                    # data to create a BuildJob record
                    'productId': 'heffgjdei',
                    'board': 'dahihgbeb',
                    'target': 'bgheaejbcc',
                    'branch': 'bfcgifeged',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BuildJob record.

        Parameters
        ----------
        where
            BuildJob filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The deleted BuildJob record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().delete(
            where={
                'id': 'jfiahhbae',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BuildJob record.

        Parameters
        ----------
        where
            BuildJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The found BuildJob record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().find_unique(
            where={
                'id': 'bfbdafajcb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BuildJob record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BuildJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The found BuildJob record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().find_unique_or_raise(
            where={
                'id': 'caeghehde',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
        include: Optional[types.BuildJobInclude] = None,
        order: Optional[Union[types.BuildJobOrderByInput, List[types.BuildJobOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BuildJob records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BuildJob records returned
        skip
            Ignore the first N results
        where
            BuildJob filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJob model
        order
            Order the returned BuildJob records by any field
        distinct
            Filter BuildJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BuildJob]
            The list of all BuildJob records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BuildJob records
        buildjobs = BuildJob.prisma().find_many(take=10)

        # find the first 5 BuildJob records ordered by the board field
        buildjobs = BuildJob.prisma().find_many(
            take=5,
            order={
                'board': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
        include: Optional[types.BuildJobInclude] = None,
        order: Optional[Union[types.BuildJobOrderByInput, List[types.BuildJobOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BuildJob record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJob model
        order
            Order the returned BuildJob records by any field
        distinct
            Filter BuildJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildJob
            The first BuildJob record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildJob record ordered by the target field
        buildjob = BuildJob.prisma().find_first(
            skip=1,
            order={
                'target': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
        include: Optional[types.BuildJobInclude] = None,
        order: Optional[Union[types.BuildJobOrderByInput, List[types.BuildJobOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BuildJob record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJob model
        order
            Order the returned BuildJob records by any field
        distinct
            Filter BuildJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildJob
            The first BuildJob record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildJob record ordered by the variant field
        buildjob = BuildJob.prisma().find_first_or_raise(
            skip=1,
            order={
                'variant': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BuildJobUpdateInput,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BuildJob record.

        Parameters
        ----------
        data
            BuildJob record data specifying what to update
        where
            BuildJob filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The updated BuildJob record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().update(
            where={
                'id': 'caghgfbggd',
            },
            data={
                # data to update the BuildJob record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BuildJobWhereUniqueInput,
        data: types.BuildJobUpsertInput,
        include: Optional[types.BuildJobInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BuildJob filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The created or updated BuildJob record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().upsert(
            where={
                'id': 'bbidjbbjaa',
            },
            data={
                'create': {
                    'id': 'bbidjbbjaa',
                    'productId': 'heffgjdei',
                    'board': 'dahihgbeb',
                    'target': 'bgheaejbcc',
                    'branch': 'bfcgifeged',
                },
                'update': {
                    'productId': 'heffgjdei',
                    'board': 'dahihgbeb',
                    'target': 'bgheaejbcc',
                    'branch': 'bfcgifeged',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BuildJobUpdateManyMutationInput,
        where: types.BuildJobWhereInput,
    ) -> int:
        """Update multiple BuildJob records

        Parameters
        ----------
        data
            BuildJob data to update the selected BuildJob records to
        where
            Filter to select the BuildJob records to update

        Returns
        -------
        int
            The total number of BuildJob records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BuildJob records
        total = BuildJob.prisma().update_many(
            data={
                'mtibRev': 'bfijhaejdd'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BuildJob records present in the database

        Parameters
        ----------
        select
            Select the BuildJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildJob.prisma().count()

        # results: prisma.types.BuildJobCountAggregateOutput
        results = BuildJob.prisma().count(
            select={
                '_all': True,
                'branch': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BuildJobCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
    ) -> types.BuildJobCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BuildJobCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
    ) -> Union[int, types.BuildJobCountAggregateOutput]:
        """Count the number of BuildJob records present in the database

        Parameters
        ----------
        select
            Select the BuildJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildJob.prisma().count()

        # results: prisma.types.BuildJobCountAggregateOutput
        results = BuildJob.prisma().count(
            select={
                '_all': True,
                'commitSha': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BuildJobCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BuildJobWhereInput] = None
    ) -> int:
        """Delete multiple BuildJob records.

        Parameters
        ----------
        where
            Optional BuildJob filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BuildJob records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BuildJob records
        total = BuildJob.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BuildJobScalarFieldKeys'],
        *,
        where: Optional['types.BuildJobWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BuildJobAvgAggregateInput'] = None,
        sum: Optional['types.BuildJobSumAggregateInput'] = None,
        min: Optional['types.BuildJobMinAggregateInput'] = None,
        max: Optional['types.BuildJobMaxAggregateInput'] = None,
        having: Optional['types.BuildJobScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BuildJobCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BuildJobScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BuildJobScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BuildJobGroupByOutput']:
        """Group BuildJob records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BuildJob fields to group records by
        where
            BuildJob filter to select records
        take
            Limit the maximum number of BuildJob records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BuildJobGroupByOutput]
            A list of dictionaries representing the BuildJob record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BuildJob records by status values
        # and count how many records are in each group
        results = BuildJob.prisma().group_by(
            ['status'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BuildJobArtifactActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BuildJobArtifact]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BuildJobArtifact.prisma().query_raw(
            'SELECT * FROM BuildJobArtifact WHERE id = $1',
            'bcedehfiji',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BuildJobArtifact
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BuildJobArtifact.prisma().query_first(
            'SELECT * FROM BuildJobArtifact WHERE buildJobId = $1',
            'bdgjicijhb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BuildJobArtifactCreateInput,
        include: Optional[types.BuildJobArtifactInclude] = None
    ) -> _PrismaModelT:
        """Create a new BuildJobArtifact record.

        Parameters
        ----------
        data
            BuildJobArtifact record data
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model

        Returns
        -------
        prisma.models.BuildJobArtifact
            The created BuildJobArtifact record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BuildJobArtifact record from just the required fields
        buildjobartifact = BuildJobArtifact.prisma().create(
            data={
                # data to create a BuildJobArtifact record
                'buildJobId': 'bghifjdeia',
                'name': 'eadfcbbcb',
                'storageKey': 'geihgahba',
                'sizeBytes': 7287884628,
                'checksum': 'begiijahea',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BuildJobArtifactCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BuildJobArtifact records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BuildJobArtifact record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BuildJobArtifact.prisma().create_many(
            data=[
                {
                    # data to create a BuildJobArtifact record
                    'buildJobId': 'gcjadjaaf',
                    'name': 'bcbebgiaic',
                    'storageKey': 'ijigbdcbj',
                    'sizeBytes': 7900538496,
                    'checksum': 'jfegcaafh',
                },
                {
                    # data to create a BuildJobArtifact record
                    'buildJobId': 'bcbeiajjfa',
                    'name': 'baehicaajf',
                    'storageKey': 'bdachdeiga',
                    'sizeBytes': 10716626940,
                    'checksum': 'ciaaiddag',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BuildJobArtifactWhereUniqueInput,
        include: Optional[types.BuildJobArtifactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BuildJobArtifact record.

        Parameters
        ----------
        where
            BuildJobArtifact filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model

        Returns
        -------
        prisma.models.BuildJobArtifact
            The deleted BuildJobArtifact record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjobartifact = BuildJobArtifact.prisma().delete(
            where={
                'id': 'fejggijff',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BuildJobArtifactWhereUniqueInput,
        include: Optional[types.BuildJobArtifactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BuildJobArtifact record.

        Parameters
        ----------
        where
            BuildJobArtifact filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model

        Returns
        -------
        prisma.models.BuildJobArtifact
            The found BuildJobArtifact record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjobartifact = BuildJobArtifact.prisma().find_unique(
            where={
                'id': 'hghjaaai',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BuildJobArtifactWhereUniqueInput,
        include: Optional[types.BuildJobArtifactInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BuildJobArtifact record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BuildJobArtifact filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model

        Returns
        -------
        prisma.models.BuildJobArtifact
            The found BuildJobArtifact record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjobartifact = BuildJobArtifact.prisma().find_unique_or_raise(
            where={
                'id': 'cajicjjdef',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobArtifactWhereInput] = None,
        cursor: Optional[types.BuildJobArtifactWhereUniqueInput] = None,
        include: Optional[types.BuildJobArtifactInclude] = None,
        order: Optional[Union[types.BuildJobArtifactOrderByInput, List[types.BuildJobArtifactOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobArtifactScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BuildJobArtifact records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BuildJobArtifact records returned
        skip
            Ignore the first N results
        where
            BuildJobArtifact filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model
        order
            Order the returned BuildJobArtifact records by any field
        distinct
            Filter BuildJobArtifact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BuildJobArtifact]
            The list of all BuildJobArtifact records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BuildJobArtifact records
        buildjobartifacts = BuildJobArtifact.prisma().find_many(take=10)

        # find the first 5 BuildJobArtifact records ordered by the name field
        buildjobartifacts = BuildJobArtifact.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobArtifactWhereInput] = None,
        cursor: Optional[types.BuildJobArtifactWhereUniqueInput] = None,
        include: Optional[types.BuildJobArtifactInclude] = None,
        order: Optional[Union[types.BuildJobArtifactOrderByInput, List[types.BuildJobArtifactOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobArtifactScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BuildJobArtifact record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildJobArtifact filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model
        order
            Order the returned BuildJobArtifact records by any field
        distinct
            Filter BuildJobArtifact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildJobArtifact
            The first BuildJobArtifact record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildJobArtifact record ordered by the storageKey field
        buildjobartifact = BuildJobArtifact.prisma().find_first(
            skip=1,
            order={
                'storageKey': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobArtifactWhereInput] = None,
        cursor: Optional[types.BuildJobArtifactWhereUniqueInput] = None,
        include: Optional[types.BuildJobArtifactInclude] = None,
        order: Optional[Union[types.BuildJobArtifactOrderByInput, List[types.BuildJobArtifactOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobArtifactScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BuildJobArtifact record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildJobArtifact filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model
        order
            Order the returned BuildJobArtifact records by any field
        distinct
            Filter BuildJobArtifact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildJobArtifact
            The first BuildJobArtifact record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildJobArtifact record ordered by the sizeBytes field
        buildjobartifact = BuildJobArtifact.prisma().find_first_or_raise(
            skip=1,
            order={
                'sizeBytes': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BuildJobArtifactUpdateInput,
        where: types.BuildJobArtifactWhereUniqueInput,
        include: Optional[types.BuildJobArtifactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BuildJobArtifact record.

        Parameters
        ----------
        data
            BuildJobArtifact record data specifying what to update
        where
            BuildJobArtifact filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model

        Returns
        -------
        prisma.models.BuildJobArtifact
            The updated BuildJobArtifact record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        buildjobartifact = BuildJobArtifact.prisma().update(
            where={
                'id': 'cefjaadec',
            },
            data={
                # data to update the BuildJobArtifact record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BuildJobArtifactWhereUniqueInput,
        data: types.BuildJobArtifactUpsertInput,
        include: Optional[types.BuildJobArtifactInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BuildJobArtifact filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BuildJobArtifact model

        Returns
        -------
        prisma.models.BuildJobArtifact
            The created or updated BuildJobArtifact record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjobartifact = BuildJobArtifact.prisma().upsert(
            where={
                'id': 'ibbigdigd',
            },
            data={
                'create': {
                    'id': 'ibbigdigd',
                    'buildJobId': 'bcbeiajjfa',
                    'name': 'baehicaajf',
                    'storageKey': 'bdachdeiga',
                    'sizeBytes': 10716626940,
                    'checksum': 'ciaaiddag',
                },
                'update': {
                    'buildJobId': 'bcbeiajjfa',
                    'name': 'baehicaajf',
                    'storageKey': 'bdachdeiga',
                    'sizeBytes': 10716626940,
                    'checksum': 'ciaaiddag',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BuildJobArtifactUpdateManyMutationInput,
        where: types.BuildJobArtifactWhereInput,
    ) -> int:
        """Update multiple BuildJobArtifact records

        Parameters
        ----------
        data
            BuildJobArtifact data to update the selected BuildJobArtifact records to
        where
            Filter to select the BuildJobArtifact records to update

        Returns
        -------
        int
            The total number of BuildJobArtifact records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BuildJobArtifact records
        total = BuildJobArtifact.prisma().update_many(
            data={
                'checksum': 'bdiiiabbii'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobArtifactWhereInput] = None,
        cursor: Optional[types.BuildJobArtifactWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BuildJobArtifact records present in the database

        Parameters
        ----------
        select
            Select the BuildJobArtifact fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildJobArtifact filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildJobArtifactCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildJobArtifact.prisma().count()

        # results: prisma.types.BuildJobArtifactCountAggregateOutput
        results = BuildJobArtifact.prisma().count(
            select={
                '_all': True,
                'role': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BuildJobArtifactCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobArtifactWhereInput] = None,
        cursor: Optional[types.BuildJobArtifactWhereUniqueInput] = None,
    ) -> types.BuildJobArtifactCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BuildJobArtifactCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobArtifactWhereInput] = None,
        cursor: Optional[types.BuildJobArtifactWhereUniqueInput] = None,
    ) -> Union[int, types.BuildJobArtifactCountAggregateOutput]:
        """Count the number of BuildJobArtifact records present in the database

        Parameters
        ----------
        select
            Select the BuildJobArtifact fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildJobArtifact filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildJobArtifactCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildJobArtifact.prisma().count()

        # results: prisma.types.BuildJobArtifactCountAggregateOutput
        results = BuildJobArtifact.prisma().count(
            select={
                '_all': True,
                'processor': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BuildJobArtifactCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BuildJobArtifactWhereInput] = None
    ) -> int:
        """Delete multiple BuildJobArtifact records.

        Parameters
        ----------
        where
            Optional BuildJobArtifact filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BuildJobArtifact records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BuildJobArtifact records
        total = BuildJobArtifact.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BuildJobArtifactScalarFieldKeys'],
        *,
        where: Optional['types.BuildJobArtifactWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BuildJobArtifactAvgAggregateInput'] = None,
        sum: Optional['types.BuildJobArtifactSumAggregateInput'] = None,
        min: Optional['types.BuildJobArtifactMinAggregateInput'] = None,
        max: Optional['types.BuildJobArtifactMaxAggregateInput'] = None,
        having: Optional['types.BuildJobArtifactScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BuildJobArtifactCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BuildJobArtifactScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BuildJobArtifactScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BuildJobArtifactGroupByOutput']:
        """Group BuildJobArtifact records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BuildJobArtifact fields to group records by
        where
            BuildJobArtifact filter to select records
        take
            Limit the maximum number of BuildJobArtifact records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BuildJobArtifactGroupByOutput]
            A list of dictionaries representing the BuildJobArtifact record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BuildJobArtifact records by artifactType values
        # and count how many records are in each group
        results = BuildJobArtifact.prisma().group_by(
            ['artifactType'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SessionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Session]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Session.prisma().query_raw(
            'SELECT * FROM Session WHERE id = $1',
            'hfcfhhadh',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Session
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Session.prisma().query_first(
            'SELECT * FROM Session WHERE name = $1',
            'bbihggdcji',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.SessionCreateInput,
        include: Optional[types.SessionInclude] = None
    ) -> _PrismaModelT:
        """Create a new Session record.

        Parameters
        ----------
        data
            Session record data
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The created Session record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Session record from just the required fields
        session = Session.prisma().create(
            data={
                # data to create a Session record
                'name': 'hgjgibdgd',
                'type': enums.SessionType.MANUFACTURING,
                'productId': 'bcbecjfice',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.SessionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Session records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Session record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Session.prisma().create_many(
            data=[
                {
                    # data to create a Session record
                    'name': 'bacbebhjjd',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'dfbfaddhe',
                },
                {
                    # data to create a Session record
                    'name': 'bdcbbieibf',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'dgjhdcggi',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Session record.

        Parameters
        ----------
        where
            Session filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The deleted Session record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().delete(
            where={
                'id': 'bbjbcdfabd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Session record.

        Parameters
        ----------
        where
            Session filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The found Session record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().find_unique(
            where={
                'id': 'gchfgbcec',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Session record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Session filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The found Session record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().find_unique_or_raise(
            where={
                'id': 'bihcjfcjah',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Session records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Session records returned
        skip
            Ignore the first N results
        where
            Session filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Session]
            The list of all Session records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Session records
        sessions = Session.prisma().find_many(take=10)

        # find the first 5 Session records ordered by the type field
        sessions = Session.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Session record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Session filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Session
            The first Session record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Session record ordered by the productId field
        session = Session.prisma().find_first(
            skip=1,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Session record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Session filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Session
            The first Session record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Session record ordered by the status field
        session = Session.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.SessionUpdateInput,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Session record.

        Parameters
        ----------
        data
            Session record data specifying what to update
        where
            Session filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The updated Session record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        session = Session.prisma().update(
            where={
                'id': 'bhjdcicaii',
            },
            data={
                # data to update the Session record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.SessionWhereUniqueInput,
        data: types.SessionUpsertInput,
        include: Optional[types.SessionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Session filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The created or updated Session record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().upsert(
            where={
                'id': 'bibedjhcej',
            },
            data={
                'create': {
                    'id': 'bibedjhcej',
                    'name': 'bdcbbieibf',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'dgjhdcggi',
                },
                'update': {
                    'name': 'bdcbbieibf',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'dgjhdcggi',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.SessionUpdateManyMutationInput,
        where: types.SessionWhereInput,
    ) -> int:
        """Update multiple Session records

        Parameters
        ----------
        data
            Session data to update the selected Session records to
        where
            Filter to select the Session records to update

        Returns
        -------
        int
            The total number of Session records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Session records
        total = Session.prisma().update_many(
            data={
                'fixtureId': 'bjcdajabfa'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Session records present in the database

        Parameters
        ----------
        select
            Select the Session fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Session filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Session.prisma().count()

        # results: prisma.types.SessionCountAggregateOutput
        results = Session.prisma().count(
            select={
                '_all': True,
                'pipelineRunId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.SessionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> types.SessionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.SessionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> Union[int, types.SessionCountAggregateOutput]:
        """Count the number of Session records present in the database

        Parameters
        ----------
        select
            Select the Session fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Session filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Session.prisma().count()

        # results: prisma.types.SessionCountAggregateOutput
        results = Session.prisma().count(
            select={
                '_all': True,
                'targetCount': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SessionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.SessionWhereInput] = None
    ) -> int:
        """Delete multiple Session records.

        Parameters
        ----------
        where
            Optional Session filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Session records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Session records
        total = Session.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.SessionScalarFieldKeys'],
        *,
        where: Optional['types.SessionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SessionAvgAggregateInput'] = None,
        sum: Optional['types.SessionSumAggregateInput'] = None,
        min: Optional['types.SessionMinAggregateInput'] = None,
        max: Optional['types.SessionMaxAggregateInput'] = None,
        having: Optional['types.SessionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SessionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SessionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SessionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SessionGroupByOutput']:
        """Group Session records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Session fields to group records by
        where
            Session filter to select records
        take
            Limit the maximum number of Session records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SessionGroupByOutput]
            A list of dictionaries representing the Session record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Session records by completedCount values
        # and count how many records are in each group
        results = Session.prisma().group_by(
            ['completedCount'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DeviceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Device]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Device.prisma().query_raw(
            'SELECT * FROM Device WHERE id = $1',
            'bchhceeeff',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Device
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Device.prisma().query_first(
            'SELECT * FROM Device WHERE serialNumber = $1',
            'bbgaifhdaa',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.DeviceCreateInput,
        include: Optional[types.DeviceInclude] = None
    ) -> _PrismaModelT:
        """Create a new Device record.

        Parameters
        ----------
        data
            Device record data
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The created Device record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Device record from just the required fields
        device = Device.prisma().create(
            data={
                # data to create a Device record
                'serialNumber': 'dgbcdaegb',
                'sessionId': 'beagfbbjig',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.DeviceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Device records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Device record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Device.prisma().create_many(
            data=[
                {
                    # data to create a Device record
                    'serialNumber': 'beicihhijb',
                    'sessionId': 'fgggcdcjg',
                },
                {
                    # data to create a Device record
                    'serialNumber': 'ccjbbjigf',
                    'sessionId': 'bhfaabbaha',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Device record.

        Parameters
        ----------
        where
            Device filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The deleted Device record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().delete(
            where={
                'id': 'ebajedhhf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Device record.

        Parameters
        ----------
        where
            Device filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The found Device record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().find_unique(
            where={
                'id': 'jajacedge',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Device record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Device filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The found Device record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().find_unique_or_raise(
            where={
                'id': 'hffgbabgf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
        include: Optional[types.DeviceInclude] = None,
        order: Optional[Union[types.DeviceOrderByInput, List[types.DeviceOrderByInput]]] = None,
        distinct: Optional[List[types.DeviceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Device records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Device records returned
        skip
            Ignore the first N results
        where
            Device filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Device model
        order
            Order the returned Device records by any field
        distinct
            Filter Device records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Device]
            The list of all Device records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Device records
        devices = Device.prisma().find_many(take=10)

        # find the first 5 Device records ordered by the sessionId field
        devices = Device.prisma().find_many(
            take=5,
            order={
                'sessionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
        include: Optional[types.DeviceInclude] = None,
        order: Optional[Union[types.DeviceOrderByInput, List[types.DeviceOrderByInput]]] = None,
        distinct: Optional[List[types.DeviceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Device record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Device filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Device model
        order
            Order the returned Device records by any field
        distinct
            Filter Device records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Device
            The first Device record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Device record ordered by the status field
        device = Device.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
        include: Optional[types.DeviceInclude] = None,
        order: Optional[Union[types.DeviceOrderByInput, List[types.DeviceOrderByInput]]] = None,
        distinct: Optional[List[types.DeviceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Device record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Device filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Device model
        order
            Order the returned Device records by any field
        distinct
            Filter Device records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Device
            The first Device record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Device record ordered by the metadata field
        device = Device.prisma().find_first_or_raise(
            skip=1,
            order={
                'metadata': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.DeviceUpdateInput,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Device record.

        Parameters
        ----------
        data
            Device record data specifying what to update
        where
            Device filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The updated Device record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        device = Device.prisma().update(
            where={
                'id': 'biacbiieja',
            },
            data={
                # data to update the Device record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.DeviceWhereUniqueInput,
        data: types.DeviceUpsertInput,
        include: Optional[types.DeviceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Device filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The created or updated Device record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().upsert(
            where={
                'id': 'cjejbgbff',
            },
            data={
                'create': {
                    'id': 'cjejbgbff',
                    'serialNumber': 'ccjbbjigf',
                    'sessionId': 'bhfaabbaha',
                },
                'update': {
                    'serialNumber': 'ccjbbjigf',
                    'sessionId': 'bhfaabbaha',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.DeviceUpdateManyMutationInput,
        where: types.DeviceWhereInput,
    ) -> int:
        """Update multiple Device records

        Parameters
        ----------
        data
            Device data to update the selected Device records to
        where
            Filter to select the Device records to update

        Returns
        -------
        int
            The total number of Device records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Device records
        total = Device.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Device records present in the database

        Parameters
        ----------
        select
            Select the Device fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Device filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Device.prisma().count()

        # results: prisma.types.DeviceCountAggregateOutput
        results = Device.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.DeviceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
    ) -> types.DeviceCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.DeviceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
    ) -> Union[int, types.DeviceCountAggregateOutput]:
        """Count the number of Device records present in the database

        Parameters
        ----------
        select
            Select the Device fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Device filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Device.prisma().count()

        # results: prisma.types.DeviceCountAggregateOutput
        results = Device.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DeviceCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.DeviceWhereInput] = None
    ) -> int:
        """Delete multiple Device records.

        Parameters
        ----------
        where
            Optional Device filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Device records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Device records
        total = Device.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.DeviceScalarFieldKeys'],
        *,
        where: Optional['types.DeviceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DeviceAvgAggregateInput'] = None,
        sum: Optional['types.DeviceSumAggregateInput'] = None,
        min: Optional['types.DeviceMinAggregateInput'] = None,
        max: Optional['types.DeviceMaxAggregateInput'] = None,
        having: Optional['types.DeviceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DeviceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DeviceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DeviceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DeviceGroupByOutput']:
        """Group Device records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Device fields to group records by
        where
            Device filter to select records
        take
            Limit the maximum number of Device records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DeviceGroupByOutput]
            A list of dictionaries representing the Device record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Device records by serialNumber values
        # and count how many records are in each group
        results = Device.prisma().group_by(
            ['serialNumber'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FixtureDesignActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FixtureDesign]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FixtureDesign.prisma().query_raw(
            'SELECT * FROM FixtureDesign WHERE id = $1',
            'fgeahddae',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FixtureDesign
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FixtureDesign.prisma().query_first(
            'SELECT * FROM FixtureDesign WHERE name = $1',
            'diageigcf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FixtureDesignCreateInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> _PrismaModelT:
        """Create a new FixtureDesign record.

        Parameters
        ----------
        data
            FixtureDesign record data
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The created FixtureDesign record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FixtureDesign record from just the required fields
        fixturedesign = FixtureDesign.prisma().create(
            data={
                # data to create a FixtureDesign record
                'name': 'badagbgeha',
                'product': 'ibgebbjch',
                'revision': 'baieajjiee',
                'profileTemplate': Json({'bahjhaccfd': True}),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FixtureDesignCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FixtureDesign records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FixtureDesign record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FixtureDesign.prisma().create_many(
            data=[
                {
                    # data to create a FixtureDesign record
                    'name': 'hffhfabhi',
                    'product': 'bbcigiadhb',
                    'revision': 'cfjagbbae',
                    'profileTemplate': Json({'bbbfhdidef': True}),
                },
                {
                    # data to create a FixtureDesign record
                    'name': 'bdadhibhec',
                    'product': 'bfhdjaiejf',
                    'revision': 'bbjfijjadg',
                    'profileTemplate': Json({'hdjacbehh': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FixtureDesign record.

        Parameters
        ----------
        where
            FixtureDesign filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The deleted FixtureDesign record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().delete(
            where={
                'id': 'bhcccbeaba',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FixtureDesign record.

        Parameters
        ----------
        where
            FixtureDesign filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The found FixtureDesign record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().find_unique(
            where={
                'id': 'bcgjbdgjdj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FixtureDesign record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FixtureDesign filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The found FixtureDesign record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().find_unique_or_raise(
            where={
                'id': 'fhdbhifae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
        include: Optional[types.FixtureDesignInclude] = None,
        order: Optional[Union[types.FixtureDesignOrderByInput, List[types.FixtureDesignOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureDesignScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FixtureDesign records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FixtureDesign records returned
        skip
            Ignore the first N results
        where
            FixtureDesign filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureDesign model
        order
            Order the returned FixtureDesign records by any field
        distinct
            Filter FixtureDesign records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FixtureDesign]
            The list of all FixtureDesign records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FixtureDesign records
        fixturedesigns = FixtureDesign.prisma().find_many(take=10)

        # find the first 5 FixtureDesign records ordered by the product field
        fixturedesigns = FixtureDesign.prisma().find_many(
            take=5,
            order={
                'product': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
        include: Optional[types.FixtureDesignInclude] = None,
        order: Optional[Union[types.FixtureDesignOrderByInput, List[types.FixtureDesignOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureDesignScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FixtureDesign record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureDesign filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureDesign model
        order
            Order the returned FixtureDesign records by any field
        distinct
            Filter FixtureDesign records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureDesign
            The first FixtureDesign record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureDesign record ordered by the revision field
        fixturedesign = FixtureDesign.prisma().find_first(
            skip=1,
            order={
                'revision': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
        include: Optional[types.FixtureDesignInclude] = None,
        order: Optional[Union[types.FixtureDesignOrderByInput, List[types.FixtureDesignOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureDesignScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FixtureDesign record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureDesign filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureDesign model
        order
            Order the returned FixtureDesign records by any field
        distinct
            Filter FixtureDesign records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureDesign
            The first FixtureDesign record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureDesign record ordered by the capabilities field
        fixturedesign = FixtureDesign.prisma().find_first_or_raise(
            skip=1,
            order={
                'capabilities': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FixtureDesignUpdateInput,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FixtureDesign record.

        Parameters
        ----------
        data
            FixtureDesign record data specifying what to update
        where
            FixtureDesign filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The updated FixtureDesign record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().update(
            where={
                'id': 'beeacgfcej',
            },
            data={
                # data to update the FixtureDesign record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        data: types.FixtureDesignUpsertInput,
        include: Optional[types.FixtureDesignInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FixtureDesign filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The created or updated FixtureDesign record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().upsert(
            where={
                'id': 'bbifhdiicc',
            },
            data={
                'create': {
                    'id': 'bbifhdiicc',
                    'name': 'bdadhibhec',
                    'product': 'bfhdjaiejf',
                    'revision': 'bbjfijjadg',
                    'profileTemplate': Json({'hdjacbehh': True}),
                },
                'update': {
                    'name': 'bdadhibhec',
                    'product': 'bfhdjaiejf',
                    'revision': 'bbjfijjadg',
                    'profileTemplate': Json({'hdjacbehh': True}),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FixtureDesignUpdateManyMutationInput,
        where: types.FixtureDesignWhereInput,
    ) -> int:
        """Update multiple FixtureDesign records

        Parameters
        ----------
        data
            FixtureDesign data to update the selected FixtureDesign records to
        where
            Filter to select the FixtureDesign records to update

        Returns
        -------
        int
            The total number of FixtureDesign records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FixtureDesign records
        total = FixtureDesign.prisma().update_many(
            data={
                'profileTemplate': Json({'bgjeccejad': True})
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FixtureDesign records present in the database

        Parameters
        ----------
        select
            Select the FixtureDesign fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureDesign filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureDesignCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureDesign.prisma().count()

        # results: prisma.types.FixtureDesignCountAggregateOutput
        results = FixtureDesign.prisma().count(
            select={
                '_all': True,
                'schematicUrl': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FixtureDesignCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
    ) -> types.FixtureDesignCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FixtureDesignCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
    ) -> Union[int, types.FixtureDesignCountAggregateOutput]:
        """Count the number of FixtureDesign records present in the database

        Parameters
        ----------
        select
            Select the FixtureDesign fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureDesign filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureDesignCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureDesign.prisma().count()

        # results: prisma.types.FixtureDesignCountAggregateOutput
        results = FixtureDesign.prisma().count(
            select={
                '_all': True,
                'bomUrl': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FixtureDesignCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FixtureDesignWhereInput] = None
    ) -> int:
        """Delete multiple FixtureDesign records.

        Parameters
        ----------
        where
            Optional FixtureDesign filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FixtureDesign records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FixtureDesign records
        total = FixtureDesign.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FixtureDesignScalarFieldKeys'],
        *,
        where: Optional['types.FixtureDesignWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FixtureDesignAvgAggregateInput'] = None,
        sum: Optional['types.FixtureDesignSumAggregateInput'] = None,
        min: Optional['types.FixtureDesignMinAggregateInput'] = None,
        max: Optional['types.FixtureDesignMaxAggregateInput'] = None,
        having: Optional['types.FixtureDesignScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FixtureDesignCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FixtureDesignScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FixtureDesignScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FixtureDesignGroupByOutput']:
        """Group FixtureDesign records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FixtureDesign fields to group records by
        where
            FixtureDesign filter to select records
        take
            Limit the maximum number of FixtureDesign records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FixtureDesignGroupByOutput]
            A list of dictionaries representing the FixtureDesign record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FixtureDesign records by assemblyGuide values
        # and count how many records are in each group
        results = FixtureDesign.prisma().group_by(
            ['assemblyGuide'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FixtureActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Fixture]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Fixture.prisma().query_raw(
            'SELECT * FROM Fixture WHERE id = $1',
            'bjagdgabbg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Fixture
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Fixture.prisma().query_first(
            'SELECT * FROM Fixture WHERE name = $1',
            'bjbbcffdij',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FixtureCreateInput,
        include: Optional[types.FixtureInclude] = None
    ) -> _PrismaModelT:
        """Create a new Fixture record.

        Parameters
        ----------
        data
            Fixture record data
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The created Fixture record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Fixture record from just the required fields
        fixture = Fixture.prisma().create(
            data={
                # data to create a Fixture record
                'name': 'begcgchdi',
                'productId': 'bhbjceagbb',
                'type': enums.NodeType.MANUFACTURING,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FixtureCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Fixture records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Fixture record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Fixture.prisma().create_many(
            data=[
                {
                    # data to create a Fixture record
                    'name': 'bjeifffjdg',
                    'productId': 'bdidcfdfjd',
                    'type': enums.NodeType.MANUFACTURING,
                },
                {
                    # data to create a Fixture record
                    'name': 'dfeggejja',
                    'productId': 'gehbgghbj',
                    'type': enums.NodeType.MANUFACTURING,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Fixture record.

        Parameters
        ----------
        where
            Fixture filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The deleted Fixture record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().delete(
            where={
                'id': 'dfhaijeie',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Fixture record.

        Parameters
        ----------
        where
            Fixture filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The found Fixture record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().find_unique(
            where={
                'id': 'gbcdjgicb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Fixture record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Fixture filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The found Fixture record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().find_unique_or_raise(
            where={
                'id': 'biaibdagac',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
        include: Optional[types.FixtureInclude] = None,
        order: Optional[Union[types.FixtureOrderByInput, List[types.FixtureOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Fixture records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Fixture records returned
        skip
            Ignore the first N results
        where
            Fixture filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fixture model
        order
            Order the returned Fixture records by any field
        distinct
            Filter Fixture records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Fixture]
            The list of all Fixture records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Fixture records
        fixtures = Fixture.prisma().find_many(take=10)

        # find the first 5 Fixture records ordered by the stationId field
        fixtures = Fixture.prisma().find_many(
            take=5,
            order={
                'stationId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
        include: Optional[types.FixtureInclude] = None,
        order: Optional[Union[types.FixtureOrderByInput, List[types.FixtureOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Fixture record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Fixture filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fixture model
        order
            Order the returned Fixture records by any field
        distinct
            Filter Fixture records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Fixture
            The first Fixture record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Fixture record ordered by the productId field
        fixture = Fixture.prisma().find_first(
            skip=1,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
        include: Optional[types.FixtureInclude] = None,
        order: Optional[Union[types.FixtureOrderByInput, List[types.FixtureOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Fixture record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Fixture filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fixture model
        order
            Order the returned Fixture records by any field
        distinct
            Filter Fixture records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Fixture
            The first Fixture record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Fixture record ordered by the type field
        fixture = Fixture.prisma().find_first_or_raise(
            skip=1,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FixtureUpdateInput,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Fixture record.

        Parameters
        ----------
        data
            Fixture record data specifying what to update
        where
            Fixture filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The updated Fixture record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fixture = Fixture.prisma().update(
            where={
                'id': 'bbfbheibcd',
            },
            data={
                # data to update the Fixture record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FixtureWhereUniqueInput,
        data: types.FixtureUpsertInput,
        include: Optional[types.FixtureInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Fixture filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The created or updated Fixture record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().upsert(
            where={
                'id': 'hiagajie',
            },
            data={
                'create': {
                    'id': 'hiagajie',
                    'name': 'dfeggejja',
                    'productId': 'gehbgghbj',
                    'type': enums.NodeType.MANUFACTURING,
                },
                'update': {
                    'name': 'dfeggejja',
                    'productId': 'gehbgghbj',
                    'type': enums.NodeType.MANUFACTURING,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FixtureUpdateManyMutationInput,
        where: types.FixtureWhereInput,
    ) -> int:
        """Update multiple Fixture records

        Parameters
        ----------
        data
            Fixture data to update the selected Fixture records to
        where
            Filter to select the Fixture records to update

        Returns
        -------
        int
            The total number of Fixture records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Fixture records
        total = Fixture.prisma().update_many(
            data={
                'designId': 'eeejidbif'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Fixture records present in the database

        Parameters
        ----------
        select
            Select the Fixture fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Fixture filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Fixture.prisma().count()

        # results: prisma.types.FixtureCountAggregateOutput
        results = Fixture.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FixtureCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
    ) -> types.FixtureCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FixtureCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
    ) -> Union[int, types.FixtureCountAggregateOutput]:
        """Count the number of Fixture records present in the database

        Parameters
        ----------
        select
            Select the Fixture fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Fixture filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Fixture.prisma().count()

        # results: prisma.types.FixtureCountAggregateOutput
        results = Fixture.prisma().count(
            select={
                '_all': True,
                'lockedBy': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FixtureCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FixtureWhereInput] = None
    ) -> int:
        """Delete multiple Fixture records.

        Parameters
        ----------
        where
            Optional Fixture filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Fixture records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Fixture records
        total = Fixture.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FixtureScalarFieldKeys'],
        *,
        where: Optional['types.FixtureWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FixtureAvgAggregateInput'] = None,
        sum: Optional['types.FixtureSumAggregateInput'] = None,
        min: Optional['types.FixtureMinAggregateInput'] = None,
        max: Optional['types.FixtureMaxAggregateInput'] = None,
        having: Optional['types.FixtureScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FixtureCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FixtureScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FixtureScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FixtureGroupByOutput']:
        """Group Fixture records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Fixture fields to group records by
        where
            Fixture filter to select records
        take
            Limit the maximum number of Fixture records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FixtureGroupByOutput]
            A list of dictionaries representing the Fixture record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Fixture records by lockedAt values
        # and count how many records are in each group
        results = Fixture.prisma().group_by(
            ['lockedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FixtureSlotActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FixtureSlot]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FixtureSlot.prisma().query_raw(
            'SELECT * FROM FixtureSlot WHERE id = $1',
            'efgbahec',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FixtureSlot
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FixtureSlot.prisma().query_first(
            'SELECT * FROM FixtureSlot WHERE fixtureId = $1',
            'hgjaiebfb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FixtureSlotCreateInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> _PrismaModelT:
        """Create a new FixtureSlot record.

        Parameters
        ----------
        data
            FixtureSlot record data
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The created FixtureSlot record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FixtureSlot record from just the required fields
        fixtureslot = FixtureSlot.prisma().create(
            data={
                # data to create a FixtureSlot record
                'fixtureId': 'bddefjjabc',
                'slotIndex': 1116761037,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FixtureSlotCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FixtureSlot records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FixtureSlot record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FixtureSlot.prisma().create_many(
            data=[
                {
                    # data to create a FixtureSlot record
                    'fixtureId': 'bbhcgagaic',
                    'slotIndex': 330014611,
                },
                {
                    # data to create a FixtureSlot record
                    'fixtureId': 'bhgibfgbbc',
                    'slotIndex': 71628745,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FixtureSlot record.

        Parameters
        ----------
        where
            FixtureSlot filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The deleted FixtureSlot record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().delete(
            where={
                'id': 'ffhgghde',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FixtureSlot record.

        Parameters
        ----------
        where
            FixtureSlot filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The found FixtureSlot record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().find_unique(
            where={
                'id': 'ibcadcejf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FixtureSlot record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FixtureSlot filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The found FixtureSlot record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().find_unique_or_raise(
            where={
                'id': 'bdcdfgccdg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
        include: Optional[types.FixtureSlotInclude] = None,
        order: Optional[Union[types.FixtureSlotOrderByInput, List[types.FixtureSlotOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureSlotScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FixtureSlot records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FixtureSlot records returned
        skip
            Ignore the first N results
        where
            FixtureSlot filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureSlot model
        order
            Order the returned FixtureSlot records by any field
        distinct
            Filter FixtureSlot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FixtureSlot]
            The list of all FixtureSlot records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FixtureSlot records
        fixtureslots = FixtureSlot.prisma().find_many(take=10)

        # find the first 5 FixtureSlot records ordered by the slotIndex field
        fixtureslots = FixtureSlot.prisma().find_many(
            take=5,
            order={
                'slotIndex': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
        include: Optional[types.FixtureSlotInclude] = None,
        order: Optional[Union[types.FixtureSlotOrderByInput, List[types.FixtureSlotOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureSlotScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FixtureSlot record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureSlot filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureSlot model
        order
            Order the returned FixtureSlot records by any field
        distinct
            Filter FixtureSlot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureSlot
            The first FixtureSlot record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureSlot record ordered by the label field
        fixtureslot = FixtureSlot.prisma().find_first(
            skip=1,
            order={
                'label': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
        include: Optional[types.FixtureSlotInclude] = None,
        order: Optional[Union[types.FixtureSlotOrderByInput, List[types.FixtureSlotOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureSlotScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FixtureSlot record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureSlot filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureSlot model
        order
            Order the returned FixtureSlot records by any field
        distinct
            Filter FixtureSlot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureSlot
            The first FixtureSlot record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureSlot record ordered by the nodeId field
        fixtureslot = FixtureSlot.prisma().find_first_or_raise(
            skip=1,
            order={
                'nodeId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FixtureSlotUpdateInput,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FixtureSlot record.

        Parameters
        ----------
        data
            FixtureSlot record data specifying what to update
        where
            FixtureSlot filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The updated FixtureSlot record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().update(
            where={
                'id': 'edhjgdfh',
            },
            data={
                # data to update the FixtureSlot record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        data: types.FixtureSlotUpsertInput,
        include: Optional[types.FixtureSlotInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FixtureSlot filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The created or updated FixtureSlot record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().upsert(
            where={
                'id': 'bdeffdadda',
            },
            data={
                'create': {
                    'id': 'bdeffdadda',
                    'fixtureId': 'bhgibfgbbc',
                    'slotIndex': 71628745,
                },
                'update': {
                    'fixtureId': 'bhgibfgbbc',
                    'slotIndex': 71628745,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FixtureSlotUpdateManyMutationInput,
        where: types.FixtureSlotWhereInput,
    ) -> int:
        """Update multiple FixtureSlot records

        Parameters
        ----------
        data
            FixtureSlot data to update the selected FixtureSlot records to
        where
            Filter to select the FixtureSlot records to update

        Returns
        -------
        int
            The total number of FixtureSlot records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FixtureSlot records
        total = FixtureSlot.prisma().update_many(
            data={
                'active': False
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FixtureSlot records present in the database

        Parameters
        ----------
        select
            Select the FixtureSlot fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureSlot filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureSlotCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureSlot.prisma().count()

        # results: prisma.types.FixtureSlotCountAggregateOutput
        results = FixtureSlot.prisma().count(
            select={
                '_all': True,
                'jlinkAppSerial': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FixtureSlotCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
    ) -> types.FixtureSlotCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FixtureSlotCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
    ) -> Union[int, types.FixtureSlotCountAggregateOutput]:
        """Count the number of FixtureSlot records present in the database

        Parameters
        ----------
        select
            Select the FixtureSlot fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureSlot filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureSlotCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureSlot.prisma().count()

        # results: prisma.types.FixtureSlotCountAggregateOutput
        results = FixtureSlot.prisma().count(
            select={
                '_all': True,
                'jlinkCommsSerial': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FixtureSlotCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FixtureSlotWhereInput] = None
    ) -> int:
        """Delete multiple FixtureSlot records.

        Parameters
        ----------
        where
            Optional FixtureSlot filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FixtureSlot records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FixtureSlot records
        total = FixtureSlot.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FixtureSlotScalarFieldKeys'],
        *,
        where: Optional['types.FixtureSlotWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FixtureSlotAvgAggregateInput'] = None,
        sum: Optional['types.FixtureSlotSumAggregateInput'] = None,
        min: Optional['types.FixtureSlotMinAggregateInput'] = None,
        max: Optional['types.FixtureSlotMaxAggregateInput'] = None,
        having: Optional['types.FixtureSlotScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FixtureSlotCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FixtureSlotScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FixtureSlotScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FixtureSlotGroupByOutput']:
        """Group FixtureSlot records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FixtureSlot fields to group records by
        where
            FixtureSlot filter to select records
        take
            Limit the maximum number of FixtureSlot records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FixtureSlotGroupByOutput]
            A list of dictionaries representing the FixtureSlot record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FixtureSlot records by uartAppPath values
        # and count how many records are in each group
        results = FixtureSlot.prisma().group_by(
            ['uartAppPath'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class NodeActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Node]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Node.prisma().query_raw(
            'SELECT * FROM Node WHERE id = $1',
            'iaeihdeei',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Node
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Node.prisma().query_first(
            'SELECT * FROM Node WHERE name = $1',
            'bfggejgfbd',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.NodeCreateInput,
        include: Optional[types.NodeInclude] = None
    ) -> _PrismaModelT:
        """Create a new Node record.

        Parameters
        ----------
        data
            Node record data
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The created Node record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Node record from just the required fields
        node = Node.prisma().create(
            data={
                # data to create a Node record
                'name': 'ifaaaedja',
                'hostname': 'cbajdjjabf',
                'type': enums.NodeType.MANUFACTURING,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.NodeCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Node records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Node record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Node.prisma().create_many(
            data=[
                {
                    # data to create a Node record
                    'name': 'bcicggedea',
                    'hostname': 'cebcdadjh',
                    'type': enums.NodeType.MANUFACTURING,
                },
                {
                    # data to create a Node record
                    'name': 'ehfigdgac',
                    'hostname': 'bhbgccijjf',
                    'type': enums.NodeType.MANUFACTURING,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Node record.

        Parameters
        ----------
        where
            Node filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The deleted Node record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().delete(
            where={
                'id': 'bigjhdgbjc',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Node record.

        Parameters
        ----------
        where
            Node filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The found Node record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().find_unique(
            where={
                'id': 'bfifdebhfd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Node record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Node filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The found Node record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().find_unique_or_raise(
            where={
                'id': 'cjchbjde',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
        include: Optional[types.NodeInclude] = None,
        order: Optional[Union[types.NodeOrderByInput, List[types.NodeOrderByInput]]] = None,
        distinct: Optional[List[types.NodeScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Node records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Node records returned
        skip
            Ignore the first N results
        where
            Node filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Node model
        order
            Order the returned Node records by any field
        distinct
            Filter Node records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Node]
            The list of all Node records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Node records
        nodes = Node.prisma().find_many(take=10)

        # find the first 5 Node records ordered by the hostname field
        nodes = Node.prisma().find_many(
            take=5,
            order={
                'hostname': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
        include: Optional[types.NodeInclude] = None,
        order: Optional[Union[types.NodeOrderByInput, List[types.NodeOrderByInput]]] = None,
        distinct: Optional[List[types.NodeScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Node record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Node filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Node model
        order
            Order the returned Node records by any field
        distinct
            Filter Node records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Node
            The first Node record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Node record ordered by the type field
        node = Node.prisma().find_first(
            skip=1,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
        include: Optional[types.NodeInclude] = None,
        order: Optional[Union[types.NodeOrderByInput, List[types.NodeOrderByInput]]] = None,
        distinct: Optional[List[types.NodeScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Node record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Node filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Node model
        order
            Order the returned Node records by any field
        distinct
            Filter Node records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Node
            The first Node record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Node record ordered by the status field
        node = Node.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.NodeUpdateInput,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Node record.

        Parameters
        ----------
        data
            Node record data specifying what to update
        where
            Node filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The updated Node record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        node = Node.prisma().update(
            where={
                'id': 'bfiibjcehj',
            },
            data={
                # data to update the Node record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.NodeWhereUniqueInput,
        data: types.NodeUpsertInput,
        include: Optional[types.NodeInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Node filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The created or updated Node record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().upsert(
            where={
                'id': 'ijieafghg',
            },
            data={
                'create': {
                    'id': 'ijieafghg',
                    'name': 'ehfigdgac',
                    'hostname': 'bhbgccijjf',
                    'type': enums.NodeType.MANUFACTURING,
                },
                'update': {
                    'name': 'ehfigdgac',
                    'hostname': 'bhbgccijjf',
                    'type': enums.NodeType.MANUFACTURING,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.NodeUpdateManyMutationInput,
        where: types.NodeWhereInput,
    ) -> int:
        """Update multiple Node records

        Parameters
        ----------
        data
            Node data to update the selected Node records to
        where
            Filter to select the Node records to update

        Returns
        -------
        int
            The total number of Node records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Node records
        total = Node.prisma().update_many(
            data={
                'ipAddress': 'hhhegahcf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Node records present in the database

        Parameters
        ----------
        select
            Select the Node fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Node filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NodeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Node.prisma().count()

        # results: prisma.types.NodeCountAggregateOutput
        results = Node.prisma().count(
            select={
                '_all': True,
                'hardwareRevision': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.NodeCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
    ) -> types.NodeCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.NodeCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
    ) -> Union[int, types.NodeCountAggregateOutput]:
        """Count the number of Node records present in the database

        Parameters
        ----------
        select
            Select the Node fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Node filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NodeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Node.prisma().count()

        # results: prisma.types.NodeCountAggregateOutput
        results = Node.prisma().count(
            select={
                '_all': True,
                'metadata': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.NodeCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.NodeWhereInput] = None
    ) -> int:
        """Delete multiple Node records.

        Parameters
        ----------
        where
            Optional Node filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Node records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Node records
        total = Node.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.NodeScalarFieldKeys'],
        *,
        where: Optional['types.NodeWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.NodeAvgAggregateInput'] = None,
        sum: Optional['types.NodeSumAggregateInput'] = None,
        min: Optional['types.NodeMinAggregateInput'] = None,
        max: Optional['types.NodeMaxAggregateInput'] = None,
        having: Optional['types.NodeScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.NodeCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.NodeScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.NodeScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.NodeGroupByOutput']:
        """Group Node records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Node fields to group records by
        where
            Node filter to select records
        take
            Limit the maximum number of Node records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.NodeGroupByOutput]
            A list of dictionaries representing the Node record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Node records by createdAt values
        # and count how many records are in each group
        results = Node.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IcleDeviceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IcleDevice]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = IcleDevice.prisma().query_raw(
            'SELECT * FROM IcleDevice WHERE id = $1',
            'edhijefdi',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IcleDevice
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = IcleDevice.prisma().query_first(
            'SELECT * FROM IcleDevice WHERE deviceId = $1',
            'djddecjhb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.IcleDeviceCreateInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> _PrismaModelT:
        """Create a new IcleDevice record.

        Parameters
        ----------
        data
            IcleDevice record data
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The created IcleDevice record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IcleDevice record from just the required fields
        icledevice = IcleDevice.prisma().create(
            data={
                # data to create a IcleDevice record
                'deviceId': 'bgdicjhie',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.IcleDeviceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IcleDevice records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IcleDevice record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = IcleDevice.prisma().create_many(
            data=[
                {
                    # data to create a IcleDevice record
                    'deviceId': 'ceibfcgij',
                },
                {
                    # data to create a IcleDevice record
                    'deviceId': 'debhbfada',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IcleDevice record.

        Parameters
        ----------
        where
            IcleDevice filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The deleted IcleDevice record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().delete(
            where={
                'id': 'bgjchggecd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IcleDevice record.

        Parameters
        ----------
        where
            IcleDevice filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The found IcleDevice record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().find_unique(
            where={
                'id': 'igggcfjg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IcleDevice record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IcleDevice filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The found IcleDevice record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().find_unique_or_raise(
            where={
                'id': 'bgjhijffjh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
        include: Optional[types.IcleDeviceInclude] = None,
        order: Optional[Union[types.IcleDeviceOrderByInput, List[types.IcleDeviceOrderByInput]]] = None,
        distinct: Optional[List[types.IcleDeviceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IcleDevice records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IcleDevice records returned
        skip
            Ignore the first N results
        where
            IcleDevice filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleDevice model
        order
            Order the returned IcleDevice records by any field
        distinct
            Filter IcleDevice records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IcleDevice]
            The list of all IcleDevice records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IcleDevice records
        icledevices = IcleDevice.prisma().find_many(take=10)

        # find the first 5 IcleDevice records ordered by the name field
        icledevices = IcleDevice.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
        include: Optional[types.IcleDeviceInclude] = None,
        order: Optional[Union[types.IcleDeviceOrderByInput, List[types.IcleDeviceOrderByInput]]] = None,
        distinct: Optional[List[types.IcleDeviceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IcleDevice record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleDevice filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleDevice model
        order
            Order the returned IcleDevice records by any field
        distinct
            Filter IcleDevice records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleDevice
            The first IcleDevice record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleDevice record ordered by the ipAddress field
        icledevice = IcleDevice.prisma().find_first(
            skip=1,
            order={
                'ipAddress': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
        include: Optional[types.IcleDeviceInclude] = None,
        order: Optional[Union[types.IcleDeviceOrderByInput, List[types.IcleDeviceOrderByInput]]] = None,
        distinct: Optional[List[types.IcleDeviceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IcleDevice record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleDevice filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleDevice model
        order
            Order the returned IcleDevice records by any field
        distinct
            Filter IcleDevice records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleDevice
            The first IcleDevice record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleDevice record ordered by the macAddress field
        icledevice = IcleDevice.prisma().find_first_or_raise(
            skip=1,
            order={
                'macAddress': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.IcleDeviceUpdateInput,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IcleDevice record.

        Parameters
        ----------
        data
            IcleDevice record data specifying what to update
        where
            IcleDevice filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The updated IcleDevice record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().update(
            where={
                'id': 'bcigdhache',
            },
            data={
                # data to update the IcleDevice record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        data: types.IcleDeviceUpsertInput,
        include: Optional[types.IcleDeviceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IcleDevice filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The created or updated IcleDevice record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().upsert(
            where={
                'id': 'igefhgdhb',
            },
            data={
                'create': {
                    'id': 'igefhgdhb',
                    'deviceId': 'debhbfada',
                },
                'update': {
                    'deviceId': 'debhbfada',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.IcleDeviceUpdateManyMutationInput,
        where: types.IcleDeviceWhereInput,
    ) -> int:
        """Update multiple IcleDevice records

        Parameters
        ----------
        data
            IcleDevice data to update the selected IcleDevice records to
        where
            Filter to select the IcleDevice records to update

        Returns
        -------
        int
            The total number of IcleDevice records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IcleDevice records
        total = IcleDevice.prisma().update_many(
            data={
                'firmwareVersion': 'ejbiifbae'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IcleDevice records present in the database

        Parameters
        ----------
        select
            Select the IcleDevice fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleDevice filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleDeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleDevice.prisma().count()

        # results: prisma.types.IcleDeviceCountAggregateOutput
        results = IcleDevice.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.IcleDeviceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
    ) -> types.IcleDeviceCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.IcleDeviceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
    ) -> Union[int, types.IcleDeviceCountAggregateOutput]:
        """Count the number of IcleDevice records present in the database

        Parameters
        ----------
        select
            Select the IcleDevice fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleDevice filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleDeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleDevice.prisma().count()

        # results: prisma.types.IcleDeviceCountAggregateOutput
        results = IcleDevice.prisma().count(
            select={
                '_all': True,
                'registered': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IcleDeviceCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.IcleDeviceWhereInput] = None
    ) -> int:
        """Delete multiple IcleDevice records.

        Parameters
        ----------
        where
            Optional IcleDevice filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IcleDevice records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IcleDevice records
        total = IcleDevice.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.IcleDeviceScalarFieldKeys'],
        *,
        where: Optional['types.IcleDeviceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IcleDeviceAvgAggregateInput'] = None,
        sum: Optional['types.IcleDeviceSumAggregateInput'] = None,
        min: Optional['types.IcleDeviceMinAggregateInput'] = None,
        max: Optional['types.IcleDeviceMaxAggregateInput'] = None,
        having: Optional['types.IcleDeviceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IcleDeviceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IcleDeviceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IcleDeviceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IcleDeviceGroupByOutput']:
        """Group IcleDevice records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IcleDevice fields to group records by
        where
            IcleDevice filter to select records
        take
            Limit the maximum number of IcleDevice records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IcleDeviceGroupByOutput]
            A list of dictionaries representing the IcleDevice record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IcleDevice records by lastHeartbeat values
        # and count how many records are in each group
        results = IcleDevice.prisma().group_by(
            ['lastHeartbeat'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IclePendingCommandActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IclePendingCommand]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = IclePendingCommand.prisma().query_raw(
            'SELECT * FROM IclePendingCommand WHERE id = $1',
            'djcfgedjd',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IclePendingCommand
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = IclePendingCommand.prisma().query_first(
            'SELECT * FROM IclePendingCommand WHERE deviceId = $1',
            'bdbjcdegag',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.IclePendingCommandCreateInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> _PrismaModelT:
        """Create a new IclePendingCommand record.

        Parameters
        ----------
        data
            IclePendingCommand record data
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The created IclePendingCommand record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IclePendingCommand record from just the required fields
        iclependingcommand = IclePendingCommand.prisma().create(
            data={
                # data to create a IclePendingCommand record
                'deviceId': 'hbchfebch',
                'commandType': 'bcjjffegfc',
                'payload': Json({'cahaeaicjd': True}),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.IclePendingCommandCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IclePendingCommand records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IclePendingCommand record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = IclePendingCommand.prisma().create_many(
            data=[
                {
                    # data to create a IclePendingCommand record
                    'deviceId': 'ibbjaacbi',
                    'commandType': 'djgacbcch',
                    'payload': Json({'geeeegace': True}),
                },
                {
                    # data to create a IclePendingCommand record
                    'deviceId': 'bbgdigchd',
                    'commandType': 'dajcifgdi',
                    'payload': Json({'ccedhdbj': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IclePendingCommand record.

        Parameters
        ----------
        where
            IclePendingCommand filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The deleted IclePendingCommand record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().delete(
            where={
                'id': 'bjaabjjjce',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IclePendingCommand record.

        Parameters
        ----------
        where
            IclePendingCommand filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The found IclePendingCommand record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().find_unique(
            where={
                'id': 'cafhdcdcjd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IclePendingCommand record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IclePendingCommand filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The found IclePendingCommand record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().find_unique_or_raise(
            where={
                'id': 'bdeebbhbdi',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
        include: Optional[types.IclePendingCommandInclude] = None,
        order: Optional[Union[types.IclePendingCommandOrderByInput, List[types.IclePendingCommandOrderByInput]]] = None,
        distinct: Optional[List[types.IclePendingCommandScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IclePendingCommand records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IclePendingCommand records returned
        skip
            Ignore the first N results
        where
            IclePendingCommand filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model
        order
            Order the returned IclePendingCommand records by any field
        distinct
            Filter IclePendingCommand records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IclePendingCommand]
            The list of all IclePendingCommand records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IclePendingCommand records
        iclependingcommands = IclePendingCommand.prisma().find_many(take=10)

        # find the first 5 IclePendingCommand records ordered by the commandType field
        iclependingcommands = IclePendingCommand.prisma().find_many(
            take=5,
            order={
                'commandType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
        include: Optional[types.IclePendingCommandInclude] = None,
        order: Optional[Union[types.IclePendingCommandOrderByInput, List[types.IclePendingCommandOrderByInput]]] = None,
        distinct: Optional[List[types.IclePendingCommandScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IclePendingCommand record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model
        order
            Order the returned IclePendingCommand records by any field
        distinct
            Filter IclePendingCommand records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IclePendingCommand
            The first IclePendingCommand record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IclePendingCommand record ordered by the payload field
        iclependingcommand = IclePendingCommand.prisma().find_first(
            skip=1,
            order={
                'payload': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
        include: Optional[types.IclePendingCommandInclude] = None,
        order: Optional[Union[types.IclePendingCommandOrderByInput, List[types.IclePendingCommandOrderByInput]]] = None,
        distinct: Optional[List[types.IclePendingCommandScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IclePendingCommand record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model
        order
            Order the returned IclePendingCommand records by any field
        distinct
            Filter IclePendingCommand records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IclePendingCommand
            The first IclePendingCommand record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IclePendingCommand record ordered by the priority field
        iclependingcommand = IclePendingCommand.prisma().find_first_or_raise(
            skip=1,
            order={
                'priority': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.IclePendingCommandUpdateInput,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IclePendingCommand record.

        Parameters
        ----------
        data
            IclePendingCommand record data specifying what to update
        where
            IclePendingCommand filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The updated IclePendingCommand record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().update(
            where={
                'id': 'cafcbdchah',
            },
            data={
                # data to update the IclePendingCommand record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        data: types.IclePendingCommandUpsertInput,
        include: Optional[types.IclePendingCommandInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IclePendingCommand filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The created or updated IclePendingCommand record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().upsert(
            where={
                'id': 'bdffbehbae',
            },
            data={
                'create': {
                    'id': 'bdffbehbae',
                    'deviceId': 'bbgdigchd',
                    'commandType': 'dajcifgdi',
                    'payload': Json({'ccedhdbj': True}),
                },
                'update': {
                    'deviceId': 'bbgdigchd',
                    'commandType': 'dajcifgdi',
                    'payload': Json({'ccedhdbj': True}),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.IclePendingCommandUpdateManyMutationInput,
        where: types.IclePendingCommandWhereInput,
    ) -> int:
        """Update multiple IclePendingCommand records

        Parameters
        ----------
        data
            IclePendingCommand data to update the selected IclePendingCommand records to
        where
            Filter to select the IclePendingCommand records to update

        Returns
        -------
        int
            The total number of IclePendingCommand records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IclePendingCommand records
        total = IclePendingCommand.prisma().update_many(
            data={
                'expiresAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IclePendingCommand records present in the database

        Parameters
        ----------
        select
            Select the IclePendingCommand fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IclePendingCommandCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IclePendingCommand.prisma().count()

        # results: prisma.types.IclePendingCommandCountAggregateOutput
        results = IclePendingCommand.prisma().count(
            select={
                '_all': True,
                'acknowledged': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.IclePendingCommandCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
    ) -> types.IclePendingCommandCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.IclePendingCommandCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
    ) -> Union[int, types.IclePendingCommandCountAggregateOutput]:
        """Count the number of IclePendingCommand records present in the database

        Parameters
        ----------
        select
            Select the IclePendingCommand fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IclePendingCommandCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IclePendingCommand.prisma().count()

        # results: prisma.types.IclePendingCommandCountAggregateOutput
        results = IclePendingCommand.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IclePendingCommandCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.IclePendingCommandWhereInput] = None
    ) -> int:
        """Delete multiple IclePendingCommand records.

        Parameters
        ----------
        where
            Optional IclePendingCommand filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IclePendingCommand records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IclePendingCommand records
        total = IclePendingCommand.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.IclePendingCommandScalarFieldKeys'],
        *,
        where: Optional['types.IclePendingCommandWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IclePendingCommandAvgAggregateInput'] = None,
        sum: Optional['types.IclePendingCommandSumAggregateInput'] = None,
        min: Optional['types.IclePendingCommandMinAggregateInput'] = None,
        max: Optional['types.IclePendingCommandMaxAggregateInput'] = None,
        having: Optional['types.IclePendingCommandScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IclePendingCommandCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IclePendingCommandScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IclePendingCommandScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IclePendingCommandGroupByOutput']:
        """Group IclePendingCommand records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IclePendingCommand fields to group records by
        where
            IclePendingCommand filter to select records
        take
            Limit the maximum number of IclePendingCommand records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IclePendingCommandGroupByOutput]
            A list of dictionaries representing the IclePendingCommand record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IclePendingCommand records by id values
        # and count how many records are in each group
        results = IclePendingCommand.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IcleLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IcleLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = IcleLog.prisma().query_raw(
            'SELECT * FROM IcleLog WHERE id = $1',
            'ieahjgeb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IcleLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = IcleLog.prisma().query_first(
            'SELECT * FROM IcleLog WHERE deviceId = $1',
            'hfeeddceg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.IcleLogCreateInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new IcleLog record.

        Parameters
        ----------
        data
            IcleLog record data
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The created IcleLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IcleLog record from just the required fields
        iclelog = IcleLog.prisma().create(
            data={
                # data to create a IcleLog record
                'deviceId': 'dbecgbbid',
                'filename': 'cchghigae',
                'storageKey': 'ecdjjjhab',
                'sizeBytes': 2163305208,
                'format': 'fhgaibff',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.IcleLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IcleLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IcleLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = IcleLog.prisma().create_many(
            data=[
                {
                    # data to create a IcleLog record
                    'deviceId': 'cadajbcbca',
                    'filename': 'bjheigfcdd',
                    'storageKey': 'bjejigcdcg',
                    'sizeBytes': 22306574484,
                    'format': 'dbjibjdaa',
                },
                {
                    # data to create a IcleLog record
                    'deviceId': 'dgijbdiaf',
                    'filename': 'begfaigba',
                    'storageKey': 'bdjiafcgjb',
                    'sizeBytes': 18989115072,
                    'format': 'cbjjeedcj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IcleLog record.

        Parameters
        ----------
        where
            IcleLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The deleted IcleLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().delete(
            where={
                'id': 'dedgbbhja',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IcleLog record.

        Parameters
        ----------
        where
            IcleLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The found IcleLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().find_unique(
            where={
                'id': 'cabiahchj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IcleLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IcleLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The found IcleLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().find_unique_or_raise(
            where={
                'id': 'cgbeccfce',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
        include: Optional[types.IcleLogInclude] = None,
        order: Optional[Union[types.IcleLogOrderByInput, List[types.IcleLogOrderByInput]]] = None,
        distinct: Optional[List[types.IcleLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IcleLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IcleLog records returned
        skip
            Ignore the first N results
        where
            IcleLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleLog model
        order
            Order the returned IcleLog records by any field
        distinct
            Filter IcleLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IcleLog]
            The list of all IcleLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IcleLog records
        iclelogs = IcleLog.prisma().find_many(take=10)

        # find the first 5 IcleLog records ordered by the filename field
        iclelogs = IcleLog.prisma().find_many(
            take=5,
            order={
                'filename': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
        include: Optional[types.IcleLogInclude] = None,
        order: Optional[Union[types.IcleLogOrderByInput, List[types.IcleLogOrderByInput]]] = None,
        distinct: Optional[List[types.IcleLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IcleLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleLog model
        order
            Order the returned IcleLog records by any field
        distinct
            Filter IcleLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleLog
            The first IcleLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleLog record ordered by the storageKey field
        iclelog = IcleLog.prisma().find_first(
            skip=1,
            order={
                'storageKey': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
        include: Optional[types.IcleLogInclude] = None,
        order: Optional[Union[types.IcleLogOrderByInput, List[types.IcleLogOrderByInput]]] = None,
        distinct: Optional[List[types.IcleLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IcleLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleLog model
        order
            Order the returned IcleLog records by any field
        distinct
            Filter IcleLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleLog
            The first IcleLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleLog record ordered by the sizeBytes field
        iclelog = IcleLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'sizeBytes': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.IcleLogUpdateInput,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IcleLog record.

        Parameters
        ----------
        data
            IcleLog record data specifying what to update
        where
            IcleLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The updated IcleLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().update(
            where={
                'id': 'fcjcagef',
            },
            data={
                # data to update the IcleLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.IcleLogWhereUniqueInput,
        data: types.IcleLogUpsertInput,
        include: Optional[types.IcleLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IcleLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The created or updated IcleLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().upsert(
            where={
                'id': 'bgdhaeacic',
            },
            data={
                'create': {
                    'id': 'bgdhaeacic',
                    'deviceId': 'dgijbdiaf',
                    'filename': 'begfaigba',
                    'storageKey': 'bdjiafcgjb',
                    'sizeBytes': 18989115072,
                    'format': 'cbjjeedcj',
                },
                'update': {
                    'deviceId': 'dgijbdiaf',
                    'filename': 'begfaigba',
                    'storageKey': 'bdjiafcgjb',
                    'sizeBytes': 18989115072,
                    'format': 'cbjjeedcj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.IcleLogUpdateManyMutationInput,
        where: types.IcleLogWhereInput,
    ) -> int:
        """Update multiple IcleLog records

        Parameters
        ----------
        data
            IcleLog data to update the selected IcleLog records to
        where
            Filter to select the IcleLog records to update

        Returns
        -------
        int
            The total number of IcleLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IcleLog records
        total = IcleLog.prisma().update_many(
            data={
                'format': 'caffafcheh'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IcleLog records present in the database

        Parameters
        ----------
        select
            Select the IcleLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleLog.prisma().count()

        # results: prisma.types.IcleLogCountAggregateOutput
        results = IcleLog.prisma().count(
            select={
                '_all': True,
                'uploadedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.IcleLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
    ) -> types.IcleLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.IcleLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
    ) -> Union[int, types.IcleLogCountAggregateOutput]:
        """Count the number of IcleLog records present in the database

        Parameters
        ----------
        select
            Select the IcleLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleLog.prisma().count()

        # results: prisma.types.IcleLogCountAggregateOutput
        results = IcleLog.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IcleLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.IcleLogWhereInput] = None
    ) -> int:
        """Delete multiple IcleLog records.

        Parameters
        ----------
        where
            Optional IcleLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IcleLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IcleLog records
        total = IcleLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.IcleLogScalarFieldKeys'],
        *,
        where: Optional['types.IcleLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IcleLogAvgAggregateInput'] = None,
        sum: Optional['types.IcleLogSumAggregateInput'] = None,
        min: Optional['types.IcleLogMinAggregateInput'] = None,
        max: Optional['types.IcleLogMaxAggregateInput'] = None,
        having: Optional['types.IcleLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IcleLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IcleLogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IcleLogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IcleLogGroupByOutput']:
        """Group IcleLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IcleLog fields to group records by
        where
            IcleLog filter to select records
        take
            Limit the maximum number of IcleLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IcleLogGroupByOutput]
            A list of dictionaries representing the IcleLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IcleLog records by deviceId values
        # and count how many records are in each group
        results = IcleLog.prisma().group_by(
            ['deviceId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DeploymentActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Deployment]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Deployment.prisma().query_raw(
            'SELECT * FROM Deployment WHERE id = $1',
            'fjjbegge',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Deployment
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Deployment.prisma().query_first(
            'SELECT * FROM Deployment WHERE name = $1',
            'bdiifhbieb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.DeploymentCreateInput,
        include: Optional[types.DeploymentInclude] = None
    ) -> _PrismaModelT:
        """Create a new Deployment record.

        Parameters
        ----------
        data
            Deployment record data
        include
            Specifies which relations should be loaded on the returned Deployment model

        Returns
        -------
        prisma.models.Deployment
            The created Deployment record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Deployment record from just the required fields
        deployment = Deployment.prisma().create(
            data={
                # data to create a Deployment record
                'name': 'cdcaejhgg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.DeploymentCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Deployment records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Deployment record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Deployment.prisma().create_many(
            data=[
                {
                    # data to create a Deployment record
                    'name': 'jbijgfbfj',
                },
                {
                    # data to create a Deployment record
                    'name': 'ggfbeddia',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.DeploymentWhereUniqueInput,
        include: Optional[types.DeploymentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Deployment record.

        Parameters
        ----------
        where
            Deployment filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Deployment model

        Returns
        -------
        prisma.models.Deployment
            The deleted Deployment record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployment = Deployment.prisma().delete(
            where={
                'id': 'djjejdaj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.DeploymentWhereUniqueInput,
        include: Optional[types.DeploymentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Deployment record.

        Parameters
        ----------
        where
            Deployment filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Deployment model

        Returns
        -------
        prisma.models.Deployment
            The found Deployment record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployment = Deployment.prisma().find_unique(
            where={
                'id': 'bjabbfceji',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.DeploymentWhereUniqueInput,
        include: Optional[types.DeploymentInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Deployment record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Deployment filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Deployment model

        Returns
        -------
        prisma.models.Deployment
            The found Deployment record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployment = Deployment.prisma().find_unique_or_raise(
            where={
                'id': 'bgchfbjibb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeploymentWhereInput] = None,
        cursor: Optional[types.DeploymentWhereUniqueInput] = None,
        include: Optional[types.DeploymentInclude] = None,
        order: Optional[Union[types.DeploymentOrderByInput, List[types.DeploymentOrderByInput]]] = None,
        distinct: Optional[List[types.DeploymentScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Deployment records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Deployment records returned
        skip
            Ignore the first N results
        where
            Deployment filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Deployment model
        order
            Order the returned Deployment records by any field
        distinct
            Filter Deployment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Deployment]
            The list of all Deployment records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Deployment records
        deployments = Deployment.prisma().find_many(take=10)

        # find the first 5 Deployment records ordered by the productId field
        deployments = Deployment.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeploymentWhereInput] = None,
        cursor: Optional[types.DeploymentWhereUniqueInput] = None,
        include: Optional[types.DeploymentInclude] = None,
        order: Optional[Union[types.DeploymentOrderByInput, List[types.DeploymentOrderByInput]]] = None,
        distinct: Optional[List[types.DeploymentScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Deployment record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Deployment filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Deployment model
        order
            Order the returned Deployment records by any field
        distinct
            Filter Deployment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Deployment
            The first Deployment record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Deployment record ordered by the status field
        deployment = Deployment.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeploymentWhereInput] = None,
        cursor: Optional[types.DeploymentWhereUniqueInput] = None,
        include: Optional[types.DeploymentInclude] = None,
        order: Optional[Union[types.DeploymentOrderByInput, List[types.DeploymentOrderByInput]]] = None,
        distinct: Optional[List[types.DeploymentScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Deployment record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Deployment filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Deployment model
        order
            Order the returned Deployment records by any field
        distinct
            Filter Deployment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Deployment
            The first Deployment record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Deployment record ordered by the config field
        deployment = Deployment.prisma().find_first_or_raise(
            skip=1,
            order={
                'config': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.DeploymentUpdateInput,
        where: types.DeploymentWhereUniqueInput,
        include: Optional[types.DeploymentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Deployment record.

        Parameters
        ----------
        data
            Deployment record data specifying what to update
        where
            Deployment filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Deployment model

        Returns
        -------
        prisma.models.Deployment
            The updated Deployment record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        deployment = Deployment.prisma().update(
            where={
                'id': 'bajecchdjc',
            },
            data={
                # data to update the Deployment record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.DeploymentWhereUniqueInput,
        data: types.DeploymentUpsertInput,
        include: Optional[types.DeploymentInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Deployment filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Deployment model

        Returns
        -------
        prisma.models.Deployment
            The created or updated Deployment record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployment = Deployment.prisma().upsert(
            where={
                'id': 'dfgacajif',
            },
            data={
                'create': {
                    'id': 'dfgacajif',
                    'name': 'ggfbeddia',
                },
                'update': {
                    'name': 'ggfbeddia',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.DeploymentUpdateManyMutationInput,
        where: types.DeploymentWhereInput,
    ) -> int:
        """Update multiple Deployment records

        Parameters
        ----------
        data
            Deployment data to update the selected Deployment records to
        where
            Filter to select the Deployment records to update

        Returns
        -------
        int
            The total number of Deployment records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Deployment records
        total = Deployment.prisma().update_many(
            data={
                'version': 'bgdiddfadi'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeploymentWhereInput] = None,
        cursor: Optional[types.DeploymentWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Deployment records present in the database

        Parameters
        ----------
        select
            Select the Deployment fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Deployment filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeploymentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Deployment.prisma().count()

        # results: prisma.types.DeploymentCountAggregateOutput
        results = Deployment.prisma().count(
            select={
                '_all': True,
                'createdById': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.DeploymentCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeploymentWhereInput] = None,
        cursor: Optional[types.DeploymentWhereUniqueInput] = None,
    ) -> types.DeploymentCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.DeploymentCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeploymentWhereInput] = None,
        cursor: Optional[types.DeploymentWhereUniqueInput] = None,
    ) -> Union[int, types.DeploymentCountAggregateOutput]:
        """Count the number of Deployment records present in the database

        Parameters
        ----------
        select
            Select the Deployment fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Deployment filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeploymentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Deployment.prisma().count()

        # results: prisma.types.DeploymentCountAggregateOutput
        results = Deployment.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DeploymentCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.DeploymentWhereInput] = None
    ) -> int:
        """Delete multiple Deployment records.

        Parameters
        ----------
        where
            Optional Deployment filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Deployment records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Deployment records
        total = Deployment.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.DeploymentScalarFieldKeys'],
        *,
        where: Optional['types.DeploymentWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DeploymentAvgAggregateInput'] = None,
        sum: Optional['types.DeploymentSumAggregateInput'] = None,
        min: Optional['types.DeploymentMinAggregateInput'] = None,
        max: Optional['types.DeploymentMaxAggregateInput'] = None,
        having: Optional['types.DeploymentScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DeploymentCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DeploymentScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DeploymentScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DeploymentGroupByOutput']:
        """Group Deployment records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Deployment fields to group records by
        where
            Deployment filter to select records
        take
            Limit the maximum number of Deployment records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DeploymentGroupByOutput]
            A list of dictionaries representing the Deployment record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Deployment records by updatedAt values
        # and count how many records are in each group
        results = Deployment.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Test]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Test.prisma().query_raw(
            'SELECT * FROM Test WHERE id = $1',
            'bijbfghhhf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Test
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Test.prisma().query_first(
            'SELECT * FROM Test WHERE name = $1',
            'bahchhihdc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestCreateInput,
        include: Optional[types.TestInclude] = None
    ) -> _PrismaModelT:
        """Create a new Test record.

        Parameters
        ----------
        data
            Test record data
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The created Test record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Test record from just the required fields
        test = Test.prisma().create(
            data={
                # data to create a Test record
                'name': 'bihjdcibib',
                'productId': 'bfhhjbbdha',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Test records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Test record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Test.prisma().create_many(
            data=[
                {
                    # data to create a Test record
                    'name': 'faehcjfdb',
                    'productId': 'bbaiefbee',
                },
                {
                    # data to create a Test record
                    'name': 'bdaacgjbaf',
                    'productId': 'biibaighec',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Test record.

        Parameters
        ----------
        where
            Test filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The deleted Test record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().delete(
            where={
                'id': 'baicdfeidj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Test record.

        Parameters
        ----------
        where
            Test filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The found Test record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().find_unique(
            where={
                'id': 'befgiciadg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Test record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Test filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The found Test record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().find_unique_or_raise(
            where={
                'id': 'cbcehahedh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
        include: Optional[types.TestInclude] = None,
        order: Optional[Union[types.TestOrderByInput, List[types.TestOrderByInput]]] = None,
        distinct: Optional[List[types.TestScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Test records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Test records returned
        skip
            Ignore the first N results
        where
            Test filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Test model
        order
            Order the returned Test records by any field
        distinct
            Filter Test records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Test]
            The list of all Test records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Test records
        tests = Test.prisma().find_many(take=10)

        # find the first 5 Test records ordered by the productId field
        tests = Test.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
        include: Optional[types.TestInclude] = None,
        order: Optional[Union[types.TestOrderByInput, List[types.TestOrderByInput]]] = None,
        distinct: Optional[List[types.TestScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Test record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Test filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Test model
        order
            Order the returned Test records by any field
        distinct
            Filter Test records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Test
            The first Test record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Test record ordered by the description field
        test = Test.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
        include: Optional[types.TestInclude] = None,
        order: Optional[Union[types.TestOrderByInput, List[types.TestOrderByInput]]] = None,
        distinct: Optional[List[types.TestScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Test record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Test filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Test model
        order
            Order the returned Test records by any field
        distinct
            Filter Test records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Test
            The first Test record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Test record ordered by the category field
        test = Test.prisma().find_first_or_raise(
            skip=1,
            order={
                'category': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestUpdateInput,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Test record.

        Parameters
        ----------
        data
            Test record data specifying what to update
        where
            Test filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The updated Test record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        test = Test.prisma().update(
            where={
                'id': 'bcjihiaide',
            },
            data={
                # data to update the Test record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestWhereUniqueInput,
        data: types.TestUpsertInput,
        include: Optional[types.TestInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Test filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The created or updated Test record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().upsert(
            where={
                'id': 'bagfijcgfj',
            },
            data={
                'create': {
                    'id': 'bagfijcgfj',
                    'name': 'bdaacgjbaf',
                    'productId': 'biibaighec',
                },
                'update': {
                    'name': 'bdaacgjbaf',
                    'productId': 'biibaighec',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestUpdateManyMutationInput,
        where: types.TestWhereInput,
    ) -> int:
        """Update multiple Test records

        Parameters
        ----------
        data
            Test data to update the selected Test records to
        where
            Filter to select the Test records to update

        Returns
        -------
        int
            The total number of Test records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Test records
        total = Test.prisma().update_many(
            data={
                'sortOrder': 1266478832
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Test records present in the database

        Parameters
        ----------
        select
            Select the Test fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Test filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Test.prisma().count()

        # results: prisma.types.TestCountAggregateOutput
        results = Test.prisma().count(
            select={
                '_all': True,
                'config': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
    ) -> types.TestCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
    ) -> Union[int, types.TestCountAggregateOutput]:
        """Count the number of Test records present in the database

        Parameters
        ----------
        select
            Select the Test fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Test filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Test.prisma().count()

        # results: prisma.types.TestCountAggregateOutput
        results = Test.prisma().count(
            select={
                '_all': True,
                'enabled': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestWhereInput] = None
    ) -> int:
        """Delete multiple Test records.

        Parameters
        ----------
        where
            Optional Test filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Test records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Test records
        total = Test.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestScalarFieldKeys'],
        *,
        where: Optional['types.TestWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestAvgAggregateInput'] = None,
        sum: Optional['types.TestSumAggregateInput'] = None,
        min: Optional['types.TestMinAggregateInput'] = None,
        max: Optional['types.TestMaxAggregateInput'] = None,
        having: Optional['types.TestScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestGroupByOutput']:
        """Group Test records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Test fields to group records by
        where
            Test filter to select records
        take
            Limit the maximum number of Test records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestGroupByOutput]
            A list of dictionaries representing the Test record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Test records by createdAt values
        # and count how many records are in each group
        results = Test.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestExecutionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TestExecution]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TestExecution.prisma().query_raw(
            'SELECT * FROM TestExecution WHERE id = $1',
            'bjcdacgacf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TestExecution
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TestExecution.prisma().query_first(
            'SELECT * FROM TestExecution WHERE testId = $1',
            'jfieeahi',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestExecutionCreateInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> _PrismaModelT:
        """Create a new TestExecution record.

        Parameters
        ----------
        data
            TestExecution record data
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The created TestExecution record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TestExecution record from just the required fields
        testexecution = TestExecution.prisma().create(
            data={
                # data to create a TestExecution record
                'testId': 'bijfjbddfj',
                'nodeId': 'cdcdjdcee',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestExecutionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TestExecution records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TestExecution record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TestExecution.prisma().create_many(
            data=[
                {
                    # data to create a TestExecution record
                    'testId': 'bbbgjdbgcb',
                    'nodeId': 'bcedacgecg',
                },
                {
                    # data to create a TestExecution record
                    'testId': 'cbdffjeh',
                    'nodeId': 'idbcdhbci',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TestExecution record.

        Parameters
        ----------
        where
            TestExecution filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The deleted TestExecution record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().delete(
            where={
                'id': 'bacegehahd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TestExecution record.

        Parameters
        ----------
        where
            TestExecution filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The found TestExecution record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().find_unique(
            where={
                'id': 'ebedeihec',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TestExecution record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TestExecution filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The found TestExecution record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().find_unique_or_raise(
            where={
                'id': 'bajagjdfbb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
        include: Optional[types.TestExecutionInclude] = None,
        order: Optional[Union[types.TestExecutionOrderByInput, List[types.TestExecutionOrderByInput]]] = None,
        distinct: Optional[List[types.TestExecutionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TestExecution records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TestExecution records returned
        skip
            Ignore the first N results
        where
            TestExecution filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestExecution model
        order
            Order the returned TestExecution records by any field
        distinct
            Filter TestExecution records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TestExecution]
            The list of all TestExecution records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TestExecution records
        testexecutions = TestExecution.prisma().find_many(take=10)

        # find the first 5 TestExecution records ordered by the nodeId field
        testexecutions = TestExecution.prisma().find_many(
            take=5,
            order={
                'nodeId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
        include: Optional[types.TestExecutionInclude] = None,
        order: Optional[Union[types.TestExecutionOrderByInput, List[types.TestExecutionOrderByInput]]] = None,
        distinct: Optional[List[types.TestExecutionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TestExecution record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestExecution filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestExecution model
        order
            Order the returned TestExecution records by any field
        distinct
            Filter TestExecution records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestExecution
            The first TestExecution record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestExecution record ordered by the deviceId field
        testexecution = TestExecution.prisma().find_first(
            skip=1,
            order={
                'deviceId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
        include: Optional[types.TestExecutionInclude] = None,
        order: Optional[Union[types.TestExecutionOrderByInput, List[types.TestExecutionOrderByInput]]] = None,
        distinct: Optional[List[types.TestExecutionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TestExecution record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestExecution filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestExecution model
        order
            Order the returned TestExecution records by any field
        distinct
            Filter TestExecution records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestExecution
            The first TestExecution record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestExecution record ordered by the slotId field
        testexecution = TestExecution.prisma().find_first_or_raise(
            skip=1,
            order={
                'slotId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestExecutionUpdateInput,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TestExecution record.

        Parameters
        ----------
        data
            TestExecution record data specifying what to update
        where
            TestExecution filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The updated TestExecution record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().update(
            where={
                'id': 'bggedbjggi',
            },
            data={
                # data to update the TestExecution record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestExecutionWhereUniqueInput,
        data: types.TestExecutionUpsertInput,
        include: Optional[types.TestExecutionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TestExecution filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The created or updated TestExecution record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().upsert(
            where={
                'id': 'hgbafifcf',
            },
            data={
                'create': {
                    'id': 'hgbafifcf',
                    'testId': 'cbdffjeh',
                    'nodeId': 'idbcdhbci',
                },
                'update': {
                    'testId': 'cbdffjeh',
                    'nodeId': 'idbcdhbci',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestExecutionUpdateManyMutationInput,
        where: types.TestExecutionWhereInput,
    ) -> int:
        """Update multiple TestExecution records

        Parameters
        ----------
        data
            TestExecution data to update the selected TestExecution records to
        where
            Filter to select the TestExecution records to update

        Returns
        -------
        int
            The total number of TestExecution records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TestExecution records
        total = TestExecution.prisma().update_many(
            data={
                'status': enums.TestExecutionStatus.QUEUED
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TestExecution records present in the database

        Parameters
        ----------
        select
            Select the TestExecution fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestExecution filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestExecutionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestExecution.prisma().count()

        # results: prisma.types.TestExecutionCountAggregateOutput
        results = TestExecution.prisma().count(
            select={
                '_all': True,
                'config': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestExecutionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
    ) -> types.TestExecutionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestExecutionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
    ) -> Union[int, types.TestExecutionCountAggregateOutput]:
        """Count the number of TestExecution records present in the database

        Parameters
        ----------
        select
            Select the TestExecution fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestExecution filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestExecutionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestExecution.prisma().count()

        # results: prisma.types.TestExecutionCountAggregateOutput
        results = TestExecution.prisma().count(
            select={
                '_all': True,
                'triggeredById': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestExecutionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestExecutionWhereInput] = None
    ) -> int:
        """Delete multiple TestExecution records.

        Parameters
        ----------
        where
            Optional TestExecution filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TestExecution records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TestExecution records
        total = TestExecution.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestExecutionScalarFieldKeys'],
        *,
        where: Optional['types.TestExecutionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestExecutionAvgAggregateInput'] = None,
        sum: Optional['types.TestExecutionSumAggregateInput'] = None,
        min: Optional['types.TestExecutionMinAggregateInput'] = None,
        max: Optional['types.TestExecutionMaxAggregateInput'] = None,
        having: Optional['types.TestExecutionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestExecutionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestExecutionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestExecutionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestExecutionGroupByOutput']:
        """Group TestExecution records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TestExecution fields to group records by
        where
            TestExecution filter to select records
        take
            Limit the maximum number of TestExecution records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestExecutionGroupByOutput]
            A list of dictionaries representing the TestExecution record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TestExecution records by startedAt values
        # and count how many records are in each group
        results = TestExecution.prisma().group_by(
            ['startedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestStepActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TestStep]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TestStep.prisma().query_raw(
            'SELECT * FROM TestStep WHERE id = $1',
            'bejiecfecg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TestStep
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TestStep.prisma().query_first(
            'SELECT * FROM TestStep WHERE executionId = $1',
            'bjgacaeagh',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestStepCreateInput,
        include: Optional[types.TestStepInclude] = None
    ) -> _PrismaModelT:
        """Create a new TestStep record.

        Parameters
        ----------
        data
            TestStep record data
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The created TestStep record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TestStep record from just the required fields
        teststep = TestStep.prisma().create(
            data={
                # data to create a TestStep record
                'executionId': 'beeaihbefg',
                'stepIndex': 1515643353,
                'name': 'jbgheibja',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestStepCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TestStep records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TestStep record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TestStep.prisma().create_many(
            data=[
                {
                    # data to create a TestStep record
                    'executionId': 'eejajbid',
                    'stepIndex': 457323084,
                    'name': 'cadejecgbd',
                },
                {
                    # data to create a TestStep record
                    'executionId': 'bahjhjjhcc',
                    'stepIndex': 417171355,
                    'name': 'bdiefcdfhg',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TestStep record.

        Parameters
        ----------
        where
            TestStep filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The deleted TestStep record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().delete(
            where={
                'id': 'cheifeghd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TestStep record.

        Parameters
        ----------
        where
            TestStep filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The found TestStep record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().find_unique(
            where={
                'id': 'fgijheefe',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TestStep record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TestStep filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The found TestStep record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().find_unique_or_raise(
            where={
                'id': 'hcbgbhfch',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
        include: Optional[types.TestStepInclude] = None,
        order: Optional[Union[types.TestStepOrderByInput, List[types.TestStepOrderByInput]]] = None,
        distinct: Optional[List[types.TestStepScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TestStep records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TestStep records returned
        skip
            Ignore the first N results
        where
            TestStep filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestStep model
        order
            Order the returned TestStep records by any field
        distinct
            Filter TestStep records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TestStep]
            The list of all TestStep records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TestStep records
        teststeps = TestStep.prisma().find_many(take=10)

        # find the first 5 TestStep records ordered by the stepIndex field
        teststeps = TestStep.prisma().find_many(
            take=5,
            order={
                'stepIndex': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
        include: Optional[types.TestStepInclude] = None,
        order: Optional[Union[types.TestStepOrderByInput, List[types.TestStepOrderByInput]]] = None,
        distinct: Optional[List[types.TestStepScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TestStep record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestStep filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestStep model
        order
            Order the returned TestStep records by any field
        distinct
            Filter TestStep records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestStep
            The first TestStep record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestStep record ordered by the name field
        teststep = TestStep.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
        include: Optional[types.TestStepInclude] = None,
        order: Optional[Union[types.TestStepOrderByInput, List[types.TestStepOrderByInput]]] = None,
        distinct: Optional[List[types.TestStepScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TestStep record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestStep filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestStep model
        order
            Order the returned TestStep records by any field
        distinct
            Filter TestStep records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestStep
            The first TestStep record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestStep record ordered by the status field
        teststep = TestStep.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestStepUpdateInput,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TestStep record.

        Parameters
        ----------
        data
            TestStep record data specifying what to update
        where
            TestStep filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The updated TestStep record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        teststep = TestStep.prisma().update(
            where={
                'id': 'bfbbbgbfhc',
            },
            data={
                # data to update the TestStep record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestStepWhereUniqueInput,
        data: types.TestStepUpsertInput,
        include: Optional[types.TestStepInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TestStep filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The created or updated TestStep record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().upsert(
            where={
                'id': 'ibijjdeb',
            },
            data={
                'create': {
                    'id': 'ibijjdeb',
                    'executionId': 'bahjhjjhcc',
                    'stepIndex': 417171355,
                    'name': 'bdiefcdfhg',
                },
                'update': {
                    'executionId': 'bahjhjjhcc',
                    'stepIndex': 417171355,
                    'name': 'bdiefcdfhg',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestStepUpdateManyMutationInput,
        where: types.TestStepWhereInput,
    ) -> int:
        """Update multiple TestStep records

        Parameters
        ----------
        data
            TestStep data to update the selected TestStep records to
        where
            Filter to select the TestStep records to update

        Returns
        -------
        int
            The total number of TestStep records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TestStep records
        total = TestStep.prisma().update_many(
            data={
                'passed': True
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TestStep records present in the database

        Parameters
        ----------
        select
            Select the TestStep fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestStep filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestStepCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestStep.prisma().count()

        # results: prisma.types.TestStepCountAggregateOutput
        results = TestStep.prisma().count(
            select={
                '_all': True,
                'errorMessage': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestStepCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
    ) -> types.TestStepCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestStepCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
    ) -> Union[int, types.TestStepCountAggregateOutput]:
        """Count the number of TestStep records present in the database

        Parameters
        ----------
        select
            Select the TestStep fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestStep filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestStepCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestStep.prisma().count()

        # results: prisma.types.TestStepCountAggregateOutput
        results = TestStep.prisma().count(
            select={
                '_all': True,
                'measurements': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestStepCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestStepWhereInput] = None
    ) -> int:
        """Delete multiple TestStep records.

        Parameters
        ----------
        where
            Optional TestStep filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TestStep records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TestStep records
        total = TestStep.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestStepScalarFieldKeys'],
        *,
        where: Optional['types.TestStepWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestStepAvgAggregateInput'] = None,
        sum: Optional['types.TestStepSumAggregateInput'] = None,
        min: Optional['types.TestStepMinAggregateInput'] = None,
        max: Optional['types.TestStepMaxAggregateInput'] = None,
        having: Optional['types.TestStepScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestStepCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestStepScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestStepScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestStepGroupByOutput']:
        """Group TestStep records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TestStep fields to group records by
        where
            TestStep filter to select records
        take
            Limit the maximum number of TestStep records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestStepGroupByOutput]
            A list of dictionaries representing the TestStep record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TestStep records by logOutput values
        # and count how many records are in each group
        results = TestStep.prisma().group_by(
            ['logOutput'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.User]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = User.prisma().query_raw(
            'SELECT * FROM User WHERE id = $1',
            'bgcahjbafj',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.User
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().query_first(
            'SELECT * FROM User WHERE email = $1',
            'bihhgeihca',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UserCreateInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Create a new User record.

        Parameters
        ----------
        data
            User record data
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created User record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a User record from just the required fields
        user = User.prisma().create(
            data={
                # data to create a User record
                'email': 'bdgbfahbef',
                'name': 'fccjhidic',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple User records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of User record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = User.prisma().create_many(
            data=[
                {
                    # data to create a User record
                    'email': 'bdgfdgdaff',
                    'name': 'bicgeaiaga',
                },
                {
                    # data to create a User record
                    'email': 'fajhhafab',
                    'name': 'bfeiccieec',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single User record.

        Parameters
        ----------
        where
            User filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The deleted User record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().delete(
            where={
                'id': 'hciegiihf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique User record.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique(
            where={
                'id': 'bahifjfga',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique User record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique_or_raise(
            where={
                'id': 'baebfehjaf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple User records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N results
        where
            User filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.User]
            The list of all User records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 User records
        users = User.prisma().find_many(take=10)

        # find the first 5 User records ordered by the name field
        users = User.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single User record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the externalId field
        user = User.prisma().find_first(
            skip=1,
            order={
                'externalId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single User record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the permissionSetId field
        user = User.prisma().find_first_or_raise(
            skip=1,
            order={
                'permissionSetId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UserUpdateInput,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single User record.

        Parameters
        ----------
        data
            User record data specifying what to update
        where
            User filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The updated User record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().update(
            where={
                'id': 'bjchdacjfa',
            },
            data={
                # data to update the User record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UserWhereUniqueInput,
        data: types.UserUpsertInput,
        include: Optional[types.UserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            User filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created or updated User record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().upsert(
            where={
                'id': 'fcadcaihb',
            },
            data={
                'create': {
                    'id': 'fcadcaihb',
                    'email': 'fajhhafab',
                    'name': 'bfeiccieec',
                },
                'update': {
                    'email': 'fajhhafab',
                    'name': 'bfeiccieec',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UserUpdateManyMutationInput,
        where: types.UserWhereInput,
    ) -> int:
        """Update multiple User records

        Parameters
        ----------
        data
            User data to update the selected User records to
        where
            Filter to select the User records to update

        Returns
        -------
        int
            The total number of User records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all User records
        total = User.prisma().update_many(
            data={
                'active': True
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'lastSeenAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> types.UserCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> Union[int, types.UserCountAggregateOutput]:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UserWhereInput] = None
    ) -> int:
        """Delete multiple User records.

        Parameters
        ----------
        where
            Optional User filter to find the records to be deleted

        Returns
        -------
        int
            The total number of User records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all User records
        total = User.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UserScalarFieldKeys'],
        *,
        where: Optional['types.UserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserAvgAggregateInput'] = None,
        sum: Optional['types.UserSumAggregateInput'] = None,
        min: Optional['types.UserMinAggregateInput'] = None,
        max: Optional['types.UserMaxAggregateInput'] = None,
        having: Optional['types.UserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserGroupByOutput']:
        """Group User records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar User fields to group records by
        where
            User filter to select records
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserGroupByOutput]
            A list of dictionaries representing the User record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group User records by updatedAt values
        # and count how many records are in each group
        results = User.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PermissionSetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PermissionSet]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PermissionSet.prisma().query_raw(
            'SELECT * FROM PermissionSet WHERE id = $1',
            'bagbibfheb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PermissionSet
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PermissionSet.prisma().query_first(
            'SELECT * FROM PermissionSet WHERE name = $1',
            'bidgcabjag',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PermissionSetCreateInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> _PrismaModelT:
        """Create a new PermissionSet record.

        Parameters
        ----------
        data
            PermissionSet record data
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The created PermissionSet record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PermissionSet record from just the required fields
        permissionset = PermissionSet.prisma().create(
            data={
                # data to create a PermissionSet record
                'name': 'hechjdjih',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PermissionSetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PermissionSet records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PermissionSet record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PermissionSet.prisma().create_many(
            data=[
                {
                    # data to create a PermissionSet record
                    'name': 'cgfjefhci',
                },
                {
                    # data to create a PermissionSet record
                    'name': 'egcidbiab',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PermissionSet record.

        Parameters
        ----------
        where
            PermissionSet filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The deleted PermissionSet record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().delete(
            where={
                'id': 'cehjjfgeg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PermissionSet record.

        Parameters
        ----------
        where
            PermissionSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The found PermissionSet record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().find_unique(
            where={
                'id': 'hiaaaeeeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PermissionSet record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PermissionSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The found PermissionSet record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().find_unique_or_raise(
            where={
                'id': 'ediajdeie',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
        include: Optional[types.PermissionSetInclude] = None,
        order: Optional[Union[types.PermissionSetOrderByInput, List[types.PermissionSetOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionSetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PermissionSet records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PermissionSet records returned
        skip
            Ignore the first N results
        where
            PermissionSet filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PermissionSet model
        order
            Order the returned PermissionSet records by any field
        distinct
            Filter PermissionSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PermissionSet]
            The list of all PermissionSet records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PermissionSet records
        permissionsets = PermissionSet.prisma().find_many(take=10)

        # find the first 5 PermissionSet records ordered by the description field
        permissionsets = PermissionSet.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
        include: Optional[types.PermissionSetInclude] = None,
        order: Optional[Union[types.PermissionSetOrderByInput, List[types.PermissionSetOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionSetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PermissionSet record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PermissionSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PermissionSet model
        order
            Order the returned PermissionSet records by any field
        distinct
            Filter PermissionSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PermissionSet
            The first PermissionSet record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PermissionSet record ordered by the permissions field
        permissionset = PermissionSet.prisma().find_first(
            skip=1,
            order={
                'permissions': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
        include: Optional[types.PermissionSetInclude] = None,
        order: Optional[Union[types.PermissionSetOrderByInput, List[types.PermissionSetOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionSetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PermissionSet record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PermissionSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PermissionSet model
        order
            Order the returned PermissionSet records by any field
        distinct
            Filter PermissionSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PermissionSet
            The first PermissionSet record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PermissionSet record ordered by the createdAt field
        permissionset = PermissionSet.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PermissionSetUpdateInput,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PermissionSet record.

        Parameters
        ----------
        data
            PermissionSet record data specifying what to update
        where
            PermissionSet filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The updated PermissionSet record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().update(
            where={
                'id': 'jdgefgafd',
            },
            data={
                # data to update the PermissionSet record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PermissionSetWhereUniqueInput,
        data: types.PermissionSetUpsertInput,
        include: Optional[types.PermissionSetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PermissionSet filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The created or updated PermissionSet record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().upsert(
            where={
                'id': 'bhhihbfeac',
            },
            data={
                'create': {
                    'id': 'bhhihbfeac',
                    'name': 'egcidbiab',
                },
                'update': {
                    'name': 'egcidbiab',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PermissionSetUpdateManyMutationInput,
        where: types.PermissionSetWhereInput,
    ) -> int:
        """Update multiple PermissionSet records

        Parameters
        ----------
        data
            PermissionSet data to update the selected PermissionSet records to
        where
            Filter to select the PermissionSet records to update

        Returns
        -------
        int
            The total number of PermissionSet records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PermissionSet records
        total = PermissionSet.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PermissionSet records present in the database

        Parameters
        ----------
        select
            Select the PermissionSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PermissionSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PermissionSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PermissionSet.prisma().count()

        # results: prisma.types.PermissionSetCountAggregateOutput
        results = PermissionSet.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PermissionSetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
    ) -> types.PermissionSetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PermissionSetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
    ) -> Union[int, types.PermissionSetCountAggregateOutput]:
        """Count the number of PermissionSet records present in the database

        Parameters
        ----------
        select
            Select the PermissionSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PermissionSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PermissionSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PermissionSet.prisma().count()

        # results: prisma.types.PermissionSetCountAggregateOutput
        results = PermissionSet.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PermissionSetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PermissionSetWhereInput] = None
    ) -> int:
        """Delete multiple PermissionSet records.

        Parameters
        ----------
        where
            Optional PermissionSet filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PermissionSet records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PermissionSet records
        total = PermissionSet.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PermissionSetScalarFieldKeys'],
        *,
        where: Optional['types.PermissionSetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PermissionSetAvgAggregateInput'] = None,
        sum: Optional['types.PermissionSetSumAggregateInput'] = None,
        min: Optional['types.PermissionSetMinAggregateInput'] = None,
        max: Optional['types.PermissionSetMaxAggregateInput'] = None,
        having: Optional['types.PermissionSetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PermissionSetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PermissionSetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PermissionSetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PermissionSetGroupByOutput']:
        """Group PermissionSet records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PermissionSet fields to group records by
        where
            PermissionSet filter to select records
        take
            Limit the maximum number of PermissionSet records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PermissionSetGroupByOutput]
            A list of dictionaries representing the PermissionSet record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PermissionSet records by description values
        # and count how many records are in each group
        results = PermissionSet.prisma().group_by(
            ['description'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ApiKeyActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ApiKey]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ApiKey.prisma().query_raw(
            'SELECT * FROM ApiKey WHERE id = $1',
            'bajegedfhj',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ApiKey
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ApiKey.prisma().query_first(
            'SELECT * FROM ApiKey WHERE name = $1',
            'bhfjihjfhg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ApiKeyCreateInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> _PrismaModelT:
        """Create a new ApiKey record.

        Parameters
        ----------
        data
            ApiKey record data
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The created ApiKey record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ApiKey record from just the required fields
        apikey = ApiKey.prisma().create(
            data={
                # data to create a ApiKey record
                'name': 'iicaadeeb',
                'keyHash': 'bigjggdhac',
                'keyPrefix': 'bfdbbbjhad',
                'userId': 'bdibjhdici',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ApiKeyCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ApiKey records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ApiKey record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ApiKey.prisma().create_many(
            data=[
                {
                    # data to create a ApiKey record
                    'name': 'bceigehcbd',
                    'keyHash': 'caehahihfe',
                    'keyPrefix': 'jjacdhech',
                    'userId': 'bbbjadabjc',
                },
                {
                    # data to create a ApiKey record
                    'name': 'hhaechedd',
                    'keyHash': 'gjjgegdic',
                    'keyPrefix': 'bijbiccd',
                    'userId': 'cbaagdieci',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ApiKey record.

        Parameters
        ----------
        where
            ApiKey filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The deleted ApiKey record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().delete(
            where={
                'id': 'jchifaegj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ApiKey record.

        Parameters
        ----------
        where
            ApiKey filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The found ApiKey record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().find_unique(
            where={
                'id': 'dhhdhfebi',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ApiKey record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ApiKey filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The found ApiKey record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().find_unique_or_raise(
            where={
                'id': 'decchddih',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
        include: Optional[types.ApiKeyInclude] = None,
        order: Optional[Union[types.ApiKeyOrderByInput, List[types.ApiKeyOrderByInput]]] = None,
        distinct: Optional[List[types.ApiKeyScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ApiKey records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ApiKey records returned
        skip
            Ignore the first N results
        where
            ApiKey filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ApiKey model
        order
            Order the returned ApiKey records by any field
        distinct
            Filter ApiKey records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ApiKey]
            The list of all ApiKey records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ApiKey records
        apikeys = ApiKey.prisma().find_many(take=10)

        # find the first 5 ApiKey records ordered by the keyHash field
        apikeys = ApiKey.prisma().find_many(
            take=5,
            order={
                'keyHash': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
        include: Optional[types.ApiKeyInclude] = None,
        order: Optional[Union[types.ApiKeyOrderByInput, List[types.ApiKeyOrderByInput]]] = None,
        distinct: Optional[List[types.ApiKeyScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ApiKey record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ApiKey filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ApiKey model
        order
            Order the returned ApiKey records by any field
        distinct
            Filter ApiKey records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ApiKey
            The first ApiKey record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ApiKey record ordered by the keyPrefix field
        apikey = ApiKey.prisma().find_first(
            skip=1,
            order={
                'keyPrefix': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
        include: Optional[types.ApiKeyInclude] = None,
        order: Optional[Union[types.ApiKeyOrderByInput, List[types.ApiKeyOrderByInput]]] = None,
        distinct: Optional[List[types.ApiKeyScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ApiKey record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ApiKey filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ApiKey model
        order
            Order the returned ApiKey records by any field
        distinct
            Filter ApiKey records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ApiKey
            The first ApiKey record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ApiKey record ordered by the userId field
        apikey = ApiKey.prisma().find_first_or_raise(
            skip=1,
            order={
                'userId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ApiKeyUpdateInput,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ApiKey record.

        Parameters
        ----------
        data
            ApiKey record data specifying what to update
        where
            ApiKey filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The updated ApiKey record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        apikey = ApiKey.prisma().update(
            where={
                'id': 'bbegbdehci',
            },
            data={
                # data to update the ApiKey record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ApiKeyWhereUniqueInput,
        data: types.ApiKeyUpsertInput,
        include: Optional[types.ApiKeyInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ApiKey filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The created or updated ApiKey record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().upsert(
            where={
                'id': 'bcebgbhhgb',
            },
            data={
                'create': {
                    'id': 'bcebgbhhgb',
                    'name': 'hhaechedd',
                    'keyHash': 'gjjgegdic',
                    'keyPrefix': 'bijbiccd',
                    'userId': 'cbaagdieci',
                },
                'update': {
                    'name': 'hhaechedd',
                    'keyHash': 'gjjgegdic',
                    'keyPrefix': 'bijbiccd',
                    'userId': 'cbaagdieci',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ApiKeyUpdateManyMutationInput,
        where: types.ApiKeyWhereInput,
    ) -> int:
        """Update multiple ApiKey records

        Parameters
        ----------
        data
            ApiKey data to update the selected ApiKey records to
        where
            Filter to select the ApiKey records to update

        Returns
        -------
        int
            The total number of ApiKey records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ApiKey records
        total = ApiKey.prisma().update_many(
            data={
                'expiresAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ApiKey records present in the database

        Parameters
        ----------
        select
            Select the ApiKey fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ApiKey filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ApiKeyCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ApiKey.prisma().count()

        # results: prisma.types.ApiKeyCountAggregateOutput
        results = ApiKey.prisma().count(
            select={
                '_all': True,
                'lastUsedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ApiKeyCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
    ) -> types.ApiKeyCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ApiKeyCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
    ) -> Union[int, types.ApiKeyCountAggregateOutput]:
        """Count the number of ApiKey records present in the database

        Parameters
        ----------
        select
            Select the ApiKey fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ApiKey filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ApiKeyCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ApiKey.prisma().count()

        # results: prisma.types.ApiKeyCountAggregateOutput
        results = ApiKey.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ApiKeyCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ApiKeyWhereInput] = None
    ) -> int:
        """Delete multiple ApiKey records.

        Parameters
        ----------
        where
            Optional ApiKey filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ApiKey records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ApiKey records
        total = ApiKey.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ApiKeyScalarFieldKeys'],
        *,
        where: Optional['types.ApiKeyWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ApiKeyAvgAggregateInput'] = None,
        sum: Optional['types.ApiKeySumAggregateInput'] = None,
        min: Optional['types.ApiKeyMinAggregateInput'] = None,
        max: Optional['types.ApiKeyMaxAggregateInput'] = None,
        having: Optional['types.ApiKeyScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ApiKeyCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ApiKeyScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ApiKeyScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ApiKeyGroupByOutput']:
        """Group ApiKey records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ApiKey fields to group records by
        where
            ApiKey filter to select records
        take
            Limit the maximum number of ApiKey records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ApiKeyGroupByOutput]
            A list of dictionaries representing the ApiKey record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ApiKey records by id values
        # and count how many records are in each group
        results = ApiKey.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AuditLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AuditLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = AuditLog.prisma().query_raw(
            'SELECT * FROM AuditLog WHERE id = $1',
            'ghegfaceg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AuditLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = AuditLog.prisma().query_first(
            'SELECT * FROM AuditLog WHERE userId = $1',
            'hdeaejgah',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.AuditLogCreateInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new AuditLog record.

        Parameters
        ----------
        data
            AuditLog record data
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The created AuditLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AuditLog record from just the required fields
        auditlog = AuditLog.prisma().create(
            data={
                # data to create a AuditLog record
                'action': 'ddiiihaci',
                'entityType': 'jdhfacdgf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.AuditLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AuditLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AuditLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = AuditLog.prisma().create_many(
            data=[
                {
                    # data to create a AuditLog record
                    'action': 'fcbbahcbi',
                    'entityType': 'edcgchech',
                },
                {
                    # data to create a AuditLog record
                    'action': 'bfehccdcge',
                    'entityType': 'ehcibdegf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AuditLog record.

        Parameters
        ----------
        where
            AuditLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The deleted AuditLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().delete(
            where={
                'id': 'ebhjifbbi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AuditLog record.

        Parameters
        ----------
        where
            AuditLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The found AuditLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().find_unique(
            where={
                'id': 'jaigficdj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AuditLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AuditLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The found AuditLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().find_unique_or_raise(
            where={
                'id': 'bggcfifgbc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
        include: Optional[types.AuditLogInclude] = None,
        order: Optional[Union[types.AuditLogOrderByInput, List[types.AuditLogOrderByInput]]] = None,
        distinct: Optional[List[types.AuditLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AuditLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AuditLog records returned
        skip
            Ignore the first N results
        where
            AuditLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AuditLog model
        order
            Order the returned AuditLog records by any field
        distinct
            Filter AuditLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AuditLog]
            The list of all AuditLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AuditLog records
        auditlogs = AuditLog.prisma().find_many(take=10)

        # find the first 5 AuditLog records ordered by the action field
        auditlogs = AuditLog.prisma().find_many(
            take=5,
            order={
                'action': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
        include: Optional[types.AuditLogInclude] = None,
        order: Optional[Union[types.AuditLogOrderByInput, List[types.AuditLogOrderByInput]]] = None,
        distinct: Optional[List[types.AuditLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AuditLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AuditLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AuditLog model
        order
            Order the returned AuditLog records by any field
        distinct
            Filter AuditLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AuditLog
            The first AuditLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AuditLog record ordered by the entityType field
        auditlog = AuditLog.prisma().find_first(
            skip=1,
            order={
                'entityType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
        include: Optional[types.AuditLogInclude] = None,
        order: Optional[Union[types.AuditLogOrderByInput, List[types.AuditLogOrderByInput]]] = None,
        distinct: Optional[List[types.AuditLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AuditLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AuditLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AuditLog model
        order
            Order the returned AuditLog records by any field
        distinct
            Filter AuditLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AuditLog
            The first AuditLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AuditLog record ordered by the entityId field
        auditlog = AuditLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'entityId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.AuditLogUpdateInput,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AuditLog record.

        Parameters
        ----------
        data
            AuditLog record data specifying what to update
        where
            AuditLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The updated AuditLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().update(
            where={
                'id': 'bhidfjibgf',
            },
            data={
                # data to update the AuditLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.AuditLogWhereUniqueInput,
        data: types.AuditLogUpsertInput,
        include: Optional[types.AuditLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AuditLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The created or updated AuditLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().upsert(
            where={
                'id': 'fcgdaijha',
            },
            data={
                'create': {
                    'id': 'fcgdaijha',
                    'action': 'bfehccdcge',
                    'entityType': 'ehcibdegf',
                },
                'update': {
                    'action': 'bfehccdcge',
                    'entityType': 'ehcibdegf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.AuditLogUpdateManyMutationInput,
        where: types.AuditLogWhereInput,
    ) -> int:
        """Update multiple AuditLog records

        Parameters
        ----------
        data
            AuditLog data to update the selected AuditLog records to
        where
            Filter to select the AuditLog records to update

        Returns
        -------
        int
            The total number of AuditLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AuditLog records
        total = AuditLog.prisma().update_many(
            data={
                'details': Json({'bbgfajah': True})
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AuditLog records present in the database

        Parameters
        ----------
        select
            Select the AuditLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AuditLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AuditLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = AuditLog.prisma().count()

        # results: prisma.types.AuditLogCountAggregateOutput
        results = AuditLog.prisma().count(
            select={
                '_all': True,
                'ipAddress': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.AuditLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
    ) -> types.AuditLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.AuditLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
    ) -> Union[int, types.AuditLogCountAggregateOutput]:
        """Count the number of AuditLog records present in the database

        Parameters
        ----------
        select
            Select the AuditLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AuditLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AuditLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = AuditLog.prisma().count()

        # results: prisma.types.AuditLogCountAggregateOutput
        results = AuditLog.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AuditLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.AuditLogWhereInput] = None
    ) -> int:
        """Delete multiple AuditLog records.

        Parameters
        ----------
        where
            Optional AuditLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AuditLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AuditLog records
        total = AuditLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.AuditLogScalarFieldKeys'],
        *,
        where: Optional['types.AuditLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AuditLogAvgAggregateInput'] = None,
        sum: Optional['types.AuditLogSumAggregateInput'] = None,
        min: Optional['types.AuditLogMinAggregateInput'] = None,
        max: Optional['types.AuditLogMaxAggregateInput'] = None,
        having: Optional['types.AuditLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AuditLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AuditLogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AuditLogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AuditLogGroupByOutput']:
        """Group AuditLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AuditLog fields to group records by
        where
            AuditLog filter to select records
        take
            Limit the maximum number of AuditLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AuditLogGroupByOutput]
            A list of dictionaries representing the AuditLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AuditLog records by id values
        # and count how many records are in each group
        results = AuditLog.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SettingActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Setting]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Setting.prisma().query_raw(
            'SELECT * FROM Setting WHERE key = $1',
            'bbaecgicia',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Setting
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Setting.prisma().query_first(
            'SELECT * FROM Setting WHERE value = $1',
            'hedacahfd',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.SettingCreateInput,
        include: Optional[types.SettingInclude] = None
    ) -> _PrismaModelT:
        """Create a new Setting record.

        Parameters
        ----------
        data
            Setting record data
        include
            Specifies which relations should be loaded on the returned Setting model

        Returns
        -------
        prisma.models.Setting
            The created Setting record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Setting record from just the required fields
        setting = Setting.prisma().create(
            data={
                # data to create a Setting record
                'key': 'bhgjecjac',
                'value': 'bfbdieidhf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.SettingCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Setting records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Setting record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Setting.prisma().create_many(
            data=[
                {
                    # data to create a Setting record
                    'key': 'ffjbdcfia',
                    'value': 'hfeidjicb',
                },
                {
                    # data to create a Setting record
                    'key': 'bbfbdccdag',
                    'value': 'biggefaged',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.SettingWhereUniqueInput,
        include: Optional[types.SettingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Setting record.

        Parameters
        ----------
        where
            Setting filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Setting model

        Returns
        -------
        prisma.models.Setting
            The deleted Setting record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        setting = Setting.prisma().delete(
            where={
                'key': 'gfdbeaejg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.SettingWhereUniqueInput,
        include: Optional[types.SettingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Setting record.

        Parameters
        ----------
        where
            Setting filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Setting model

        Returns
        -------
        prisma.models.Setting
            The found Setting record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        setting = Setting.prisma().find_unique(
            where={
                'key': 'bhacdebcb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.SettingWhereUniqueInput,
        include: Optional[types.SettingInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Setting record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Setting filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Setting model

        Returns
        -------
        prisma.models.Setting
            The found Setting record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        setting = Setting.prisma().find_unique_or_raise(
            where={
                'key': 'jhffeibhe',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SettingWhereInput] = None,
        cursor: Optional[types.SettingWhereUniqueInput] = None,
        include: Optional[types.SettingInclude] = None,
        order: Optional[Union[types.SettingOrderByInput, List[types.SettingOrderByInput]]] = None,
        distinct: Optional[List[types.SettingScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Setting records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Setting records returned
        skip
            Ignore the first N results
        where
            Setting filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Setting model
        order
            Order the returned Setting records by any field
        distinct
            Filter Setting records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Setting]
            The list of all Setting records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Setting records
        settings = Setting.prisma().find_many(take=10)

        # find the first 5 Setting records ordered by the description field
        settings = Setting.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SettingWhereInput] = None,
        cursor: Optional[types.SettingWhereUniqueInput] = None,
        include: Optional[types.SettingInclude] = None,
        order: Optional[Union[types.SettingOrderByInput, List[types.SettingOrderByInput]]] = None,
        distinct: Optional[List[types.SettingScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Setting record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Setting filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Setting model
        order
            Order the returned Setting records by any field
        distinct
            Filter Setting records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Setting
            The first Setting record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Setting record ordered by the createdAt field
        setting = Setting.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SettingWhereInput] = None,
        cursor: Optional[types.SettingWhereUniqueInput] = None,
        include: Optional[types.SettingInclude] = None,
        order: Optional[Union[types.SettingOrderByInput, List[types.SettingOrderByInput]]] = None,
        distinct: Optional[List[types.SettingScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Setting record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Setting filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Setting model
        order
            Order the returned Setting records by any field
        distinct
            Filter Setting records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Setting
            The first Setting record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Setting record ordered by the updatedAt field
        setting = Setting.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.SettingUpdateInput,
        where: types.SettingWhereUniqueInput,
        include: Optional[types.SettingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Setting record.

        Parameters
        ----------
        data
            Setting record data specifying what to update
        where
            Setting filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Setting model

        Returns
        -------
        prisma.models.Setting
            The updated Setting record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        setting = Setting.prisma().update(
            where={
                'key': 'iaiieefjb',
            },
            data={
                # data to update the Setting record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.SettingWhereUniqueInput,
        data: types.SettingUpsertInput,
        include: Optional[types.SettingInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Setting filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Setting model

        Returns
        -------
        prisma.models.Setting
            The created or updated Setting record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        setting = Setting.prisma().upsert(
            where={
                'key': 'jjhjdgjfg',
            },
            data={
                'create': {
                    'key': 'jjhjdgjfg',
                    'value': 'biggefaged',
                },
                'update': {
                    'value': 'biggefaged',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.SettingUpdateManyMutationInput,
        where: types.SettingWhereInput,
    ) -> int:
        """Update multiple Setting records

        Parameters
        ----------
        data
            Setting data to update the selected Setting records to
        where
            Filter to select the Setting records to update

        Returns
        -------
        int
            The total number of Setting records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Setting records
        total = Setting.prisma().update_many(
            data={
                'key': 'bhhjcfadhd'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SettingWhereInput] = None,
        cursor: Optional[types.SettingWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Setting records present in the database

        Parameters
        ----------
        select
            Select the Setting fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Setting filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SettingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Setting.prisma().count()

        # results: prisma.types.SettingCountAggregateOutput
        results = Setting.prisma().count(
            select={
                '_all': True,
                'value': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.SettingCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SettingWhereInput] = None,
        cursor: Optional[types.SettingWhereUniqueInput] = None,
    ) -> types.SettingCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.SettingCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SettingWhereInput] = None,
        cursor: Optional[types.SettingWhereUniqueInput] = None,
    ) -> Union[int, types.SettingCountAggregateOutput]:
        """Count the number of Setting records present in the database

        Parameters
        ----------
        select
            Select the Setting fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Setting filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SettingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Setting.prisma().count()

        # results: prisma.types.SettingCountAggregateOutput
        results = Setting.prisma().count(
            select={
                '_all': True,
                'description': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SettingCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.SettingWhereInput] = None
    ) -> int:
        """Delete multiple Setting records.

        Parameters
        ----------
        where
            Optional Setting filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Setting records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Setting records
        total = Setting.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.SettingScalarFieldKeys'],
        *,
        where: Optional['types.SettingWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SettingAvgAggregateInput'] = None,
        sum: Optional['types.SettingSumAggregateInput'] = None,
        min: Optional['types.SettingMinAggregateInput'] = None,
        max: Optional['types.SettingMaxAggregateInput'] = None,
        having: Optional['types.SettingScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SettingCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SettingScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SettingScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SettingGroupByOutput']:
        """Group Setting records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Setting fields to group records by
        where
            Setting filter to select records
        take
            Limit the maximum number of Setting records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SettingGroupByOutput]
            A list of dictionaries representing the Setting record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Setting records by createdAt values
        # and count how many records are in each group
        results = Setting.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Log]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Log.prisma().query_raw(
            'SELECT * FROM Log WHERE id = $1',
            'fbejadda',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Log
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Log.prisma().query_first(
            'SELECT * FROM Log WHERE level = $1',
            enums.LogLevel.DEBUG,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.LogCreateInput,
        include: Optional[types.LogInclude] = None
    ) -> _PrismaModelT:
        """Create a new Log record.

        Parameters
        ----------
        data
            Log record data
        include
            Specifies which relations should be loaded on the returned Log model

        Returns
        -------
        prisma.models.Log
            The created Log record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Log record from just the required fields
        log = Log.prisma().create(
            data={
                # data to create a Log record
                'message': 'bjeaigeddb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.LogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Log records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Log record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Log.prisma().create_many(
            data=[
                {
                    # data to create a Log record
                    'message': 'eceecabdg',
                },
                {
                    # data to create a Log record
                    'message': 'bdgaajbcab',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.LogWhereUniqueInput,
        include: Optional[types.LogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Log record.

        Parameters
        ----------
        where
            Log filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Log model

        Returns
        -------
        prisma.models.Log
            The deleted Log record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        log = Log.prisma().delete(
            where={
                'id': 'chhgehieg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.LogWhereUniqueInput,
        include: Optional[types.LogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Log record.

        Parameters
        ----------
        where
            Log filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Log model

        Returns
        -------
        prisma.models.Log
            The found Log record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        log = Log.prisma().find_unique(
            where={
                'id': 'faggbiggd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.LogWhereUniqueInput,
        include: Optional[types.LogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Log record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Log filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Log model

        Returns
        -------
        prisma.models.Log
            The found Log record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        log = Log.prisma().find_unique_or_raise(
            where={
                'id': 'bciijiajei',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogWhereInput] = None,
        cursor: Optional[types.LogWhereUniqueInput] = None,
        include: Optional[types.LogInclude] = None,
        order: Optional[Union[types.LogOrderByInput, List[types.LogOrderByInput]]] = None,
        distinct: Optional[List[types.LogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Log records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Log records returned
        skip
            Ignore the first N results
        where
            Log filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Log model
        order
            Order the returned Log records by any field
        distinct
            Filter Log records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Log]
            The list of all Log records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Log records
        logs = Log.prisma().find_many(take=10)

        # find the first 5 Log records ordered by the message field
        logs = Log.prisma().find_many(
            take=5,
            order={
                'message': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LogWhereInput] = None,
        cursor: Optional[types.LogWhereUniqueInput] = None,
        include: Optional[types.LogInclude] = None,
        order: Optional[Union[types.LogOrderByInput, List[types.LogOrderByInput]]] = None,
        distinct: Optional[List[types.LogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Log record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Log filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Log model
        order
            Order the returned Log records by any field
        distinct
            Filter Log records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Log
            The first Log record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Log record ordered by the source field
        log = Log.prisma().find_first(
            skip=1,
            order={
                'source': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LogWhereInput] = None,
        cursor: Optional[types.LogWhereUniqueInput] = None,
        include: Optional[types.LogInclude] = None,
        order: Optional[Union[types.LogOrderByInput, List[types.LogOrderByInput]]] = None,
        distinct: Optional[List[types.LogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Log record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Log filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Log model
        order
            Order the returned Log records by any field
        distinct
            Filter Log records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Log
            The first Log record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Log record ordered by the executionId field
        log = Log.prisma().find_first_or_raise(
            skip=1,
            order={
                'executionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.LogUpdateInput,
        where: types.LogWhereUniqueInput,
        include: Optional[types.LogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Log record.

        Parameters
        ----------
        data
            Log record data specifying what to update
        where
            Log filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Log model

        Returns
        -------
        prisma.models.Log
            The updated Log record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        log = Log.prisma().update(
            where={
                'id': 'bdjaadchgf',
            },
            data={
                # data to update the Log record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.LogWhereUniqueInput,
        data: types.LogUpsertInput,
        include: Optional[types.LogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Log filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Log model

        Returns
        -------
        prisma.models.Log
            The created or updated Log record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        log = Log.prisma().upsert(
            where={
                'id': 'biadfeebhi',
            },
            data={
                'create': {
                    'id': 'biadfeebhi',
                    'message': 'bdgaajbcab',
                },
                'update': {
                    'message': 'bdgaajbcab',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.LogUpdateManyMutationInput,
        where: types.LogWhereInput,
    ) -> int:
        """Update multiple Log records

        Parameters
        ----------
        data
            Log data to update the selected Log records to
        where
            Filter to select the Log records to update

        Returns
        -------
        int
            The total number of Log records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Log records
        total = Log.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogWhereInput] = None,
        cursor: Optional[types.LogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Log records present in the database

        Parameters
        ----------
        select
            Select the Log fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Log filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Log.prisma().count()

        # results: prisma.types.LogCountAggregateOutput
        results = Log.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.LogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogWhereInput] = None,
        cursor: Optional[types.LogWhereUniqueInput] = None,
    ) -> types.LogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.LogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LogWhereInput] = None,
        cursor: Optional[types.LogWhereUniqueInput] = None,
    ) -> Union[int, types.LogCountAggregateOutput]:
        """Count the number of Log records present in the database

        Parameters
        ----------
        select
            Select the Log fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Log filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Log.prisma().count()

        # results: prisma.types.LogCountAggregateOutput
        results = Log.prisma().count(
            select={
                '_all': True,
                'level': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.LogWhereInput] = None
    ) -> int:
        """Delete multiple Log records.

        Parameters
        ----------
        where
            Optional Log filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Log records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Log records
        total = Log.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.LogScalarFieldKeys'],
        *,
        where: Optional['types.LogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LogAvgAggregateInput'] = None,
        sum: Optional['types.LogSumAggregateInput'] = None,
        min: Optional['types.LogMinAggregateInput'] = None,
        max: Optional['types.LogMaxAggregateInput'] = None,
        having: Optional['types.LogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LogGroupByOutput']:
        """Group Log records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Log fields to group records by
        where
            Log filter to select records
        take
            Limit the maximum number of Log records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LogGroupByOutput]
            A list of dictionaries representing the Log record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Log records by message values
        # and count how many records are in each group
        results = Log.prisma().group_by(
            ['message'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models