# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ProductActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Product]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Product.prisma().query_raw(
            'SELECT * FROM Product WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Product
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Product.prisma().query_first(
            'SELECT * FROM Product WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductCreateInput,
        include: Optional[types.ProductInclude] = None
    ) -> _PrismaModelT:
        """Create a new Product record.

        Parameters
        ----------
        data
            Product record data
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The created Product record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Product record from just the required fields
        product = Product.prisma().create(
            data={
                # data to create a Product record
                'name': 'ggciceaie',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Product records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Product record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Product.prisma().create_many(
            data=[
                {
                    # data to create a Product record
                    'name': 'bbehjachib',
                },
                {
                    # data to create a Product record
                    'name': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Product record.

        Parameters
        ----------
        where
            Product filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The deleted Product record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().delete(
            where={
                'id': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Product record.

        Parameters
        ----------
        where
            Product filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The found Product record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().find_unique(
            where={
                'id': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Product record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Product filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The found Product record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().find_unique_or_raise(
            where={
                'id': 'eigcfgbif',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
        include: Optional[types.ProductInclude] = None,
        order: Optional[Union[types.ProductOrderByInput, List[types.ProductOrderByInput]]] = None,
        distinct: Optional[List[types.ProductScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Product records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Product records returned
        skip
            Ignore the first N results
        where
            Product filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Product model
        order
            Order the returned Product records by any field
        distinct
            Filter Product records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Product]
            The list of all Product records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Product records
        products = Product.prisma().find_many(take=10)

        # find the first 5 Product records ordered by the slug field
        products = Product.prisma().find_many(
            take=5,
            order={
                'slug': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
        include: Optional[types.ProductInclude] = None,
        order: Optional[Union[types.ProductOrderByInput, List[types.ProductOrderByInput]]] = None,
        distinct: Optional[List[types.ProductScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Product record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Product filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Product model
        order
            Order the returned Product records by any field
        distinct
            Filter Product records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Product
            The first Product record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Product record ordered by the description field
        product = Product.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
        include: Optional[types.ProductInclude] = None,
        order: Optional[Union[types.ProductOrderByInput, List[types.ProductOrderByInput]]] = None,
        distinct: Optional[List[types.ProductScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Product record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Product filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Product model
        order
            Order the returned Product records by any field
        distinct
            Filter Product records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Product
            The first Product record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Product record ordered by the active field
        product = Product.prisma().find_first_or_raise(
            skip=1,
            order={
                'active': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductUpdateInput,
        where: types.ProductWhereUniqueInput,
        include: Optional[types.ProductInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Product record.

        Parameters
        ----------
        data
            Product record data specifying what to update
        where
            Product filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The updated Product record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        product = Product.prisma().update(
            where={
                'id': 'bagcfbhiig',
            },
            data={
                # data to update the Product record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductWhereUniqueInput,
        data: types.ProductUpsertInput,
        include: Optional[types.ProductInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Product filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Product model

        Returns
        -------
        prisma.models.Product
            The created or updated Product record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        product = Product.prisma().upsert(
            where={
                'id': 'cghideieh',
            },
            data={
                'create': {
                    'id': 'cghideieh',
                    'name': 'cadfabfehe',
                },
                'update': {
                    'name': 'cadfabfehe',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductUpdateManyMutationInput,
        where: types.ProductWhereInput,
    ) -> int:
        """Update multiple Product records

        Parameters
        ----------
        data
            Product data to update the selected Product records to
        where
            Filter to select the Product records to update

        Returns
        -------
        int
            The total number of Product records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Product records
        total = Product.prisma().update_many(
            data={
                'fwRepoSlug': 'biabhbdai'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Product records present in the database

        Parameters
        ----------
        select
            Select the Product fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Product filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Product.prisma().count()

        # results: prisma.types.ProductCountAggregateOutput
        results = Product.prisma().count(
            select={
                '_all': True,
                'mfgFwRepoSlug': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
    ) -> types.ProductCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductWhereInput] = None,
        cursor: Optional[types.ProductWhereUniqueInput] = None,
    ) -> Union[int, types.ProductCountAggregateOutput]:
        """Count the number of Product records present in the database

        Parameters
        ----------
        select
            Select the Product fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Product filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Product.prisma().count()

        # results: prisma.types.ProductCountAggregateOutput
        results = Product.prisma().count(
            select={
                '_all': True,
                'builderImage': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductWhereInput] = None
    ) -> int:
        """Delete multiple Product records.

        Parameters
        ----------
        where
            Optional Product filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Product records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Product records
        total = Product.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductScalarFieldKeys'],
        *,
        where: Optional['types.ProductWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductAvgAggregateInput'] = None,
        sum: Optional['types.ProductSumAggregateInput'] = None,
        min: Optional['types.ProductMinAggregateInput'] = None,
        max: Optional['types.ProductMaxAggregateInput'] = None,
        having: Optional['types.ProductScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductGroupByOutput']:
        """Group Product records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Product fields to group records by
        where
            Product filter to select records
        take
            Limit the maximum number of Product records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductGroupByOutput]
            A list of dictionaries representing the Product record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Product records by buildConfig values
        # and count how many records are in each group
        results = Product.prisma().group_by(
            ['buildConfig'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestPackageActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TestPackage]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TestPackage.prisma().query_raw(
            'SELECT * FROM TestPackage WHERE id = $1',
            'idghgaicb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TestPackage
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TestPackage.prisma().query_first(
            'SELECT * FROM TestPackage WHERE productId = $1',
            'fjfddhigg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestPackageCreateInput,
        include: Optional[types.TestPackageInclude] = None
    ) -> _PrismaModelT:
        """Create a new TestPackage record.

        Parameters
        ----------
        data
            TestPackage record data
        include
            Specifies which relations should be loaded on the returned TestPackage model

        Returns
        -------
        prisma.models.TestPackage
            The created TestPackage record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TestPackage record from just the required fields
        testpackage = TestPackage.prisma().create(
            data={
                # data to create a TestPackage record
                'productId': 'hjaecfifb',
                'version': 'cbbbjbfcii',
                'storageKey': 'bbejhfidcb',
                'frameworkVersion': 'bgeecijdgg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestPackageCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TestPackage records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TestPackage record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TestPackage.prisma().create_many(
            data=[
                {
                    # data to create a TestPackage record
                    'productId': 'bdiicjafbj',
                    'version': 'bgehebiafc',
                    'storageKey': 'bghffegacj',
                    'frameworkVersion': 'bhghchehcc',
                },
                {
                    # data to create a TestPackage record
                    'productId': 'dcgchcbbf',
                    'version': 'bdedcabahc',
                    'storageKey': 'ghfhiafcb',
                    'frameworkVersion': 'heejgedji',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestPackageWhereUniqueInput,
        include: Optional[types.TestPackageInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TestPackage record.

        Parameters
        ----------
        where
            TestPackage filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TestPackage model

        Returns
        -------
        prisma.models.TestPackage
            The deleted TestPackage record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testpackage = TestPackage.prisma().delete(
            where={
                'id': 'bjgjgibgbf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestPackageWhereUniqueInput,
        include: Optional[types.TestPackageInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TestPackage record.

        Parameters
        ----------
        where
            TestPackage filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestPackage model

        Returns
        -------
        prisma.models.TestPackage
            The found TestPackage record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testpackage = TestPackage.prisma().find_unique(
            where={
                'id': 'bbbgbhfjge',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestPackageWhereUniqueInput,
        include: Optional[types.TestPackageInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TestPackage record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TestPackage filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestPackage model

        Returns
        -------
        prisma.models.TestPackage
            The found TestPackage record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testpackage = TestPackage.prisma().find_unique_or_raise(
            where={
                'id': 'igbehcbab',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestPackageWhereInput] = None,
        cursor: Optional[types.TestPackageWhereUniqueInput] = None,
        include: Optional[types.TestPackageInclude] = None,
        order: Optional[Union[types.TestPackageOrderByInput, List[types.TestPackageOrderByInput]]] = None,
        distinct: Optional[List[types.TestPackageScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TestPackage records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TestPackage records returned
        skip
            Ignore the first N results
        where
            TestPackage filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestPackage model
        order
            Order the returned TestPackage records by any field
        distinct
            Filter TestPackage records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TestPackage]
            The list of all TestPackage records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TestPackage records
        testpackages = TestPackage.prisma().find_many(take=10)

        # find the first 5 TestPackage records ordered by the version field
        testpackages = TestPackage.prisma().find_many(
            take=5,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestPackageWhereInput] = None,
        cursor: Optional[types.TestPackageWhereUniqueInput] = None,
        include: Optional[types.TestPackageInclude] = None,
        order: Optional[Union[types.TestPackageOrderByInput, List[types.TestPackageOrderByInput]]] = None,
        distinct: Optional[List[types.TestPackageScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TestPackage record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestPackage filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestPackage model
        order
            Order the returned TestPackage records by any field
        distinct
            Filter TestPackage records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestPackage
            The first TestPackage record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestPackage record ordered by the status field
        testpackage = TestPackage.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestPackageWhereInput] = None,
        cursor: Optional[types.TestPackageWhereUniqueInput] = None,
        include: Optional[types.TestPackageInclude] = None,
        order: Optional[Union[types.TestPackageOrderByInput, List[types.TestPackageOrderByInput]]] = None,
        distinct: Optional[List[types.TestPackageScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TestPackage record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestPackage filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestPackage model
        order
            Order the returned TestPackage records by any field
        distinct
            Filter TestPackage records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestPackage
            The first TestPackage record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestPackage record ordered by the storageKey field
        testpackage = TestPackage.prisma().find_first_or_raise(
            skip=1,
            order={
                'storageKey': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestPackageUpdateInput,
        where: types.TestPackageWhereUniqueInput,
        include: Optional[types.TestPackageInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TestPackage record.

        Parameters
        ----------
        data
            TestPackage record data specifying what to update
        where
            TestPackage filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TestPackage model

        Returns
        -------
        prisma.models.TestPackage
            The updated TestPackage record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        testpackage = TestPackage.prisma().update(
            where={
                'id': 'bdadaadhag',
            },
            data={
                # data to update the TestPackage record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestPackageWhereUniqueInput,
        data: types.TestPackageUpsertInput,
        include: Optional[types.TestPackageInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TestPackage filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TestPackage model

        Returns
        -------
        prisma.models.TestPackage
            The created or updated TestPackage record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testpackage = TestPackage.prisma().upsert(
            where={
                'id': 'bgiggdidbf',
            },
            data={
                'create': {
                    'id': 'bgiggdidbf',
                    'productId': 'dcgchcbbf',
                    'version': 'bdedcabahc',
                    'storageKey': 'ghfhiafcb',
                    'frameworkVersion': 'heejgedji',
                },
                'update': {
                    'productId': 'dcgchcbbf',
                    'version': 'bdedcabahc',
                    'storageKey': 'ghfhiafcb',
                    'frameworkVersion': 'heejgedji',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestPackageUpdateManyMutationInput,
        where: types.TestPackageWhereInput,
    ) -> int:
        """Update multiple TestPackage records

        Parameters
        ----------
        data
            TestPackage data to update the selected TestPackage records to
        where
            Filter to select the TestPackage records to update

        Returns
        -------
        int
            The total number of TestPackage records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TestPackage records
        total = TestPackage.prisma().update_many(
            data={
                'frameworkVersion': 'caaaedabfc'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestPackageWhereInput] = None,
        cursor: Optional[types.TestPackageWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TestPackage records present in the database

        Parameters
        ----------
        select
            Select the TestPackage fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestPackage filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestPackageCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestPackage.prisma().count()

        # results: prisma.types.TestPackageCountAggregateOutput
        results = TestPackage.prisma().count(
            select={
                '_all': True,
                'manifestHash': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestPackageCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestPackageWhereInput] = None,
        cursor: Optional[types.TestPackageWhereUniqueInput] = None,
    ) -> types.TestPackageCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestPackageCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestPackageWhereInput] = None,
        cursor: Optional[types.TestPackageWhereUniqueInput] = None,
    ) -> Union[int, types.TestPackageCountAggregateOutput]:
        """Count the number of TestPackage records present in the database

        Parameters
        ----------
        select
            Select the TestPackage fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestPackage filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestPackageCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestPackage.prisma().count()

        # results: prisma.types.TestPackageCountAggregateOutput
        results = TestPackage.prisma().count(
            select={
                '_all': True,
                'testCount': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestPackageCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestPackageWhereInput] = None
    ) -> int:
        """Delete multiple TestPackage records.

        Parameters
        ----------
        where
            Optional TestPackage filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TestPackage records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TestPackage records
        total = TestPackage.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestPackageScalarFieldKeys'],
        *,
        where: Optional['types.TestPackageWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestPackageAvgAggregateInput'] = None,
        sum: Optional['types.TestPackageSumAggregateInput'] = None,
        min: Optional['types.TestPackageMinAggregateInput'] = None,
        max: Optional['types.TestPackageMaxAggregateInput'] = None,
        having: Optional['types.TestPackageScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestPackageCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestPackageScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestPackageScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestPackageGroupByOutput']:
        """Group TestPackage records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TestPackage fields to group records by
        where
            TestPackage filter to select records
        take
            Limit the maximum number of TestPackage records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestPackageGroupByOutput]
            A list of dictionaries representing the TestPackage record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TestPackage records by stagesEnabled values
        # and count how many records are in each group
        results = TestPackage.prisma().group_by(
            ['stagesEnabled'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ProductTargetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ProductTarget]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ProductTarget.prisma().query_raw(
            'SELECT * FROM ProductTarget WHERE id = $1',
            'bigibebcib',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ProductTarget
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ProductTarget.prisma().query_first(
            'SELECT * FROM ProductTarget WHERE boardRevisionId = $1',
            'bigaiehgcc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductTargetCreateInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> _PrismaModelT:
        """Create a new ProductTarget record.

        Parameters
        ----------
        data
            ProductTarget record data
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The created ProductTarget record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ProductTarget record from just the required fields
        producttarget = ProductTarget.prisma().create(
            data={
                # data to create a ProductTarget record
                'boardRevisionId': 'beeifcbebf',
                'role': 'bgcigfahea',
                'soc': 'bcejgaggif',
                'appId': 835903122,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductTargetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ProductTarget records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ProductTarget record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ProductTarget.prisma().create_many(
            data=[
                {
                    # data to create a ProductTarget record
                    'boardRevisionId': 'hgdhbjhhj',
                    'role': 'ecjjjfbae',
                    'soc': 'bhhfibbigf',
                    'appId': 893145566,
                },
                {
                    # data to create a ProductTarget record
                    'boardRevisionId': 'jjfeafhfj',
                    'role': 'cbachdgfce',
                    'soc': 'chbfcacbd',
                    'appId': 456633834,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ProductTarget record.

        Parameters
        ----------
        where
            ProductTarget filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The deleted ProductTarget record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().delete(
            where={
                'id': 'caficfigfb',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ProductTarget record.

        Parameters
        ----------
        where
            ProductTarget filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The found ProductTarget record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().find_unique(
            where={
                'id': 'bfidgijfjc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ProductTarget record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ProductTarget filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The found ProductTarget record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().find_unique_or_raise(
            where={
                'id': 'ihieecagf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
        include: Optional[types.ProductTargetInclude] = None,
        order: Optional[Union[types.ProductTargetOrderByInput, List[types.ProductTargetOrderByInput]]] = None,
        distinct: Optional[List[types.ProductTargetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ProductTarget records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ProductTarget records returned
        skip
            Ignore the first N results
        where
            ProductTarget filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductTarget model
        order
            Order the returned ProductTarget records by any field
        distinct
            Filter ProductTarget records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ProductTarget]
            The list of all ProductTarget records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ProductTarget records
        producttargets = ProductTarget.prisma().find_many(take=10)

        # find the first 5 ProductTarget records ordered by the role field
        producttargets = ProductTarget.prisma().find_many(
            take=5,
            order={
                'role': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
        include: Optional[types.ProductTargetInclude] = None,
        order: Optional[Union[types.ProductTargetOrderByInput, List[types.ProductTargetOrderByInput]]] = None,
        distinct: Optional[List[types.ProductTargetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ProductTarget record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductTarget filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductTarget model
        order
            Order the returned ProductTarget records by any field
        distinct
            Filter ProductTarget records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductTarget
            The first ProductTarget record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductTarget record ordered by the soc field
        producttarget = ProductTarget.prisma().find_first(
            skip=1,
            order={
                'soc': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
        include: Optional[types.ProductTargetInclude] = None,
        order: Optional[Union[types.ProductTargetOrderByInput, List[types.ProductTargetOrderByInput]]] = None,
        distinct: Optional[List[types.ProductTargetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ProductTarget record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductTarget filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductTarget model
        order
            Order the returned ProductTarget records by any field
        distinct
            Filter ProductTarget records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductTarget
            The first ProductTarget record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductTarget record ordered by the appId field
        producttarget = ProductTarget.prisma().find_first_or_raise(
            skip=1,
            order={
                'appId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductTargetUpdateInput,
        where: types.ProductTargetWhereUniqueInput,
        include: Optional[types.ProductTargetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ProductTarget record.

        Parameters
        ----------
        data
            ProductTarget record data specifying what to update
        where
            ProductTarget filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The updated ProductTarget record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().update(
            where={
                'id': 'bghfciaafe',
            },
            data={
                # data to update the ProductTarget record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductTargetWhereUniqueInput,
        data: types.ProductTargetUpsertInput,
        include: Optional[types.ProductTargetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ProductTarget filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ProductTarget model

        Returns
        -------
        prisma.models.ProductTarget
            The created or updated ProductTarget record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        producttarget = ProductTarget.prisma().upsert(
            where={
                'id': 'bgchfhgceh',
            },
            data={
                'create': {
                    'id': 'bgchfhgceh',
                    'boardRevisionId': 'jjfeafhfj',
                    'role': 'cbachdgfce',
                    'soc': 'chbfcacbd',
                    'appId': 456633834,
                },
                'update': {
                    'boardRevisionId': 'jjfeafhfj',
                    'role': 'cbachdgfce',
                    'soc': 'chbfcacbd',
                    'appId': 456633834,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductTargetUpdateManyMutationInput,
        where: types.ProductTargetWhereInput,
    ) -> int:
        """Update multiple ProductTarget records

        Parameters
        ----------
        data
            ProductTarget data to update the selected ProductTarget records to
        where
            Filter to select the ProductTarget records to update

        Returns
        -------
        int
            The total number of ProductTarget records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ProductTarget records
        total = ProductTarget.prisma().update_many(
            data={
                'id': 'cafeiaccbc'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ProductTarget records present in the database

        Parameters
        ----------
        select
            Select the ProductTarget fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductTarget filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductTargetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductTarget.prisma().count()

        # results: prisma.types.ProductTargetCountAggregateOutput
        results = ProductTarget.prisma().count(
            select={
                '_all': True,
                'boardRevisionId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductTargetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
    ) -> types.ProductTargetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductTargetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductTargetWhereInput] = None,
        cursor: Optional[types.ProductTargetWhereUniqueInput] = None,
    ) -> Union[int, types.ProductTargetCountAggregateOutput]:
        """Count the number of ProductTarget records present in the database

        Parameters
        ----------
        select
            Select the ProductTarget fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductTarget filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductTargetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductTarget.prisma().count()

        # results: prisma.types.ProductTargetCountAggregateOutput
        results = ProductTarget.prisma().count(
            select={
                '_all': True,
                'role': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductTargetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductTargetWhereInput] = None
    ) -> int:
        """Delete multiple ProductTarget records.

        Parameters
        ----------
        where
            Optional ProductTarget filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ProductTarget records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ProductTarget records
        total = ProductTarget.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductTargetScalarFieldKeys'],
        *,
        where: Optional['types.ProductTargetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductTargetAvgAggregateInput'] = None,
        sum: Optional['types.ProductTargetSumAggregateInput'] = None,
        min: Optional['types.ProductTargetMinAggregateInput'] = None,
        max: Optional['types.ProductTargetMaxAggregateInput'] = None,
        having: Optional['types.ProductTargetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductTargetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductTargetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductTargetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductTargetGroupByOutput']:
        """Group ProductTarget records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ProductTarget fields to group records by
        where
            ProductTarget filter to select records
        take
            Limit the maximum number of ProductTarget records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductTargetGroupByOutput]
            A list of dictionaries representing the ProductTarget record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ProductTarget records by soc values
        # and count how many records are in each group
        results = ProductTarget.prisma().group_by(
            ['soc'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BoardActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Board]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Board.prisma().query_raw(
            'SELECT * FROM Board WHERE id = $1',
            'gaddfhfh',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Board
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Board.prisma().query_first(
            'SELECT * FROM Board WHERE productId = $1',
            'gieegcbeg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BoardCreateInput,
        include: Optional[types.BoardInclude] = None
    ) -> _PrismaModelT:
        """Create a new Board record.

        Parameters
        ----------
        data
            Board record data
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The created Board record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Board record from just the required fields
        board = Board.prisma().create(
            data={
                # data to create a Board record
                'productId': 'bgcffadich',
                'name': 'fcbichhci',
                'ckBoardsFamily': 'bcggadccgf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BoardCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Board records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Board record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Board.prisma().create_many(
            data=[
                {
                    # data to create a Board record
                    'productId': 'jdcfdcgc',
                    'name': 'cafdaehjid',
                    'ckBoardsFamily': 'gifdddbia',
                },
                {
                    # data to create a Board record
                    'productId': 'bchehecef',
                    'name': 'jeijcbhfe',
                    'ckBoardsFamily': 'bjgejjabff',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Board record.

        Parameters
        ----------
        where
            Board filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The deleted Board record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().delete(
            where={
                'id': 'bcciijbibg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Board record.

        Parameters
        ----------
        where
            Board filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The found Board record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().find_unique(
            where={
                'id': 'cffcachfd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Board record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Board filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The found Board record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().find_unique_or_raise(
            where={
                'id': 'bccdfhdigc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
        include: Optional[types.BoardInclude] = None,
        order: Optional[Union[types.BoardOrderByInput, List[types.BoardOrderByInput]]] = None,
        distinct: Optional[List[types.BoardScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Board records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Board records returned
        skip
            Ignore the first N results
        where
            Board filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Board model
        order
            Order the returned Board records by any field
        distinct
            Filter Board records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Board]
            The list of all Board records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Board records
        boards = Board.prisma().find_many(take=10)

        # find the first 5 Board records ordered by the name field
        boards = Board.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
        include: Optional[types.BoardInclude] = None,
        order: Optional[Union[types.BoardOrderByInput, List[types.BoardOrderByInput]]] = None,
        distinct: Optional[List[types.BoardScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Board record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Board filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Board model
        order
            Order the returned Board records by any field
        distinct
            Filter Board records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Board
            The first Board record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Board record ordered by the ckBoardsFamily field
        board = Board.prisma().find_first(
            skip=1,
            order={
                'ckBoardsFamily': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
        include: Optional[types.BoardInclude] = None,
        order: Optional[Union[types.BoardOrderByInput, List[types.BoardOrderByInput]]] = None,
        distinct: Optional[List[types.BoardScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Board record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Board filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Board model
        order
            Order the returned Board records by any field
        distinct
            Filter Board records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Board
            The first Board record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Board record ordered by the vendor field
        board = Board.prisma().find_first_or_raise(
            skip=1,
            order={
                'vendor': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BoardUpdateInput,
        where: types.BoardWhereUniqueInput,
        include: Optional[types.BoardInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Board record.

        Parameters
        ----------
        data
            Board record data specifying what to update
        where
            Board filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The updated Board record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        board = Board.prisma().update(
            where={
                'id': 'febcgjbfj',
            },
            data={
                # data to update the Board record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BoardWhereUniqueInput,
        data: types.BoardUpsertInput,
        include: Optional[types.BoardInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Board filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Board model

        Returns
        -------
        prisma.models.Board
            The created or updated Board record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        board = Board.prisma().upsert(
            where={
                'id': 'bageiegghg',
            },
            data={
                'create': {
                    'id': 'bageiegghg',
                    'productId': 'bchehecef',
                    'name': 'jeijcbhfe',
                    'ckBoardsFamily': 'bjgejjabff',
                },
                'update': {
                    'productId': 'bchehecef',
                    'name': 'jeijcbhfe',
                    'ckBoardsFamily': 'bjgejjabff',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BoardUpdateManyMutationInput,
        where: types.BoardWhereInput,
    ) -> int:
        """Update multiple Board records

        Parameters
        ----------
        data
            Board data to update the selected Board records to
        where
            Filter to select the Board records to update

        Returns
        -------
        int
            The total number of Board records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Board records
        total = Board.prisma().update_many(
            data={
                'description': 'faidicegb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Board records present in the database

        Parameters
        ----------
        select
            Select the Board fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Board filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Board.prisma().count()

        # results: prisma.types.BoardCountAggregateOutput
        results = Board.prisma().count(
            select={
                '_all': True,
                'active': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BoardCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
    ) -> types.BoardCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BoardCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardWhereInput] = None,
        cursor: Optional[types.BoardWhereUniqueInput] = None,
    ) -> Union[int, types.BoardCountAggregateOutput]:
        """Count the number of Board records present in the database

        Parameters
        ----------
        select
            Select the Board fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Board filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Board.prisma().count()

        # results: prisma.types.BoardCountAggregateOutput
        results = Board.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BoardCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BoardWhereInput] = None
    ) -> int:
        """Delete multiple Board records.

        Parameters
        ----------
        where
            Optional Board filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Board records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Board records
        total = Board.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BoardScalarFieldKeys'],
        *,
        where: Optional['types.BoardWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BoardAvgAggregateInput'] = None,
        sum: Optional['types.BoardSumAggregateInput'] = None,
        min: Optional['types.BoardMinAggregateInput'] = None,
        max: Optional['types.BoardMaxAggregateInput'] = None,
        having: Optional['types.BoardScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BoardCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BoardScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BoardScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BoardGroupByOutput']:
        """Group Board records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Board fields to group records by
        where
            Board filter to select records
        take
            Limit the maximum number of Board records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BoardGroupByOutput]
            A list of dictionaries representing the Board record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Board records by updatedAt values
        # and count how many records are in each group
        results = Board.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BoardRevisionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BoardRevision]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BoardRevision.prisma().query_raw(
            'SELECT * FROM BoardRevision WHERE id = $1',
            'bacecgfhbe',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BoardRevision
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BoardRevision.prisma().query_first(
            'SELECT * FROM BoardRevision WHERE boardId = $1',
            'ihcahiead',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BoardRevisionCreateInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> _PrismaModelT:
        """Create a new BoardRevision record.

        Parameters
        ----------
        data
            BoardRevision record data
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The created BoardRevision record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BoardRevision record from just the required fields
        boardrevision = BoardRevision.prisma().create(
            data={
                # data to create a BoardRevision record
                'boardId': 'biheheiajg',
                'version': 'jbgijghgb',
                'ckBoardsName': 'hgjcghfbi',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BoardRevisionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BoardRevision records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BoardRevision record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BoardRevision.prisma().create_many(
            data=[
                {
                    # data to create a BoardRevision record
                    'boardId': 'icadbcehj',
                    'version': 'jchciaee',
                    'ckBoardsName': 'deeificjd',
                },
                {
                    # data to create a BoardRevision record
                    'boardId': 'bbcbhebbda',
                    'version': 'bejfijgcfb',
                    'ckBoardsName': 'caifcbgii',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BoardRevision record.

        Parameters
        ----------
        where
            BoardRevision filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The deleted BoardRevision record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().delete(
            where={
                'id': 'igaibbfgj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BoardRevision record.

        Parameters
        ----------
        where
            BoardRevision filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The found BoardRevision record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().find_unique(
            where={
                'id': 'bggajdcbbi',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BoardRevision record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BoardRevision filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The found BoardRevision record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().find_unique_or_raise(
            where={
                'id': 'fcfhgbjed',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
        include: Optional[types.BoardRevisionInclude] = None,
        order: Optional[Union[types.BoardRevisionOrderByInput, List[types.BoardRevisionOrderByInput]]] = None,
        distinct: Optional[List[types.BoardRevisionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BoardRevision records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BoardRevision records returned
        skip
            Ignore the first N results
        where
            BoardRevision filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BoardRevision model
        order
            Order the returned BoardRevision records by any field
        distinct
            Filter BoardRevision records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BoardRevision]
            The list of all BoardRevision records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BoardRevision records
        boardrevisions = BoardRevision.prisma().find_many(take=10)

        # find the first 5 BoardRevision records ordered by the version field
        boardrevisions = BoardRevision.prisma().find_many(
            take=5,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
        include: Optional[types.BoardRevisionInclude] = None,
        order: Optional[Union[types.BoardRevisionOrderByInput, List[types.BoardRevisionOrderByInput]]] = None,
        distinct: Optional[List[types.BoardRevisionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BoardRevision record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BoardRevision filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BoardRevision model
        order
            Order the returned BoardRevision records by any field
        distinct
            Filter BoardRevision records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BoardRevision
            The first BoardRevision record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BoardRevision record ordered by the ckBoardsName field
        boardrevision = BoardRevision.prisma().find_first(
            skip=1,
            order={
                'ckBoardsName': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
        include: Optional[types.BoardRevisionInclude] = None,
        order: Optional[Union[types.BoardRevisionOrderByInput, List[types.BoardRevisionOrderByInput]]] = None,
        distinct: Optional[List[types.BoardRevisionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BoardRevision record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BoardRevision filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BoardRevision model
        order
            Order the returned BoardRevision records by any field
        distinct
            Filter BoardRevision records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BoardRevision
            The first BoardRevision record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BoardRevision record ordered by the socs field
        boardrevision = BoardRevision.prisma().find_first_or_raise(
            skip=1,
            order={
                'socs': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BoardRevisionUpdateInput,
        where: types.BoardRevisionWhereUniqueInput,
        include: Optional[types.BoardRevisionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BoardRevision record.

        Parameters
        ----------
        data
            BoardRevision record data specifying what to update
        where
            BoardRevision filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The updated BoardRevision record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().update(
            where={
                'id': 'hdgcajhjg',
            },
            data={
                # data to update the BoardRevision record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BoardRevisionWhereUniqueInput,
        data: types.BoardRevisionUpsertInput,
        include: Optional[types.BoardRevisionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BoardRevision filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BoardRevision model

        Returns
        -------
        prisma.models.BoardRevision
            The created or updated BoardRevision record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        boardrevision = BoardRevision.prisma().upsert(
            where={
                'id': 'ejdjahicb',
            },
            data={
                'create': {
                    'id': 'ejdjahicb',
                    'boardId': 'bbcbhebbda',
                    'version': 'bejfijgcfb',
                    'ckBoardsName': 'caifcbgii',
                },
                'update': {
                    'boardId': 'bbcbhebbda',
                    'version': 'bejfijgcfb',
                    'ckBoardsName': 'caifcbgii',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BoardRevisionUpdateManyMutationInput,
        where: types.BoardRevisionWhereInput,
    ) -> int:
        """Update multiple BoardRevision records

        Parameters
        ----------
        data
            BoardRevision data to update the selected BoardRevision records to
        where
            Filter to select the BoardRevision records to update

        Returns
        -------
        int
            The total number of BoardRevision records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BoardRevision records
        total = BoardRevision.prisma().update_many(
            data={
                'deviceType': 639686562
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BoardRevision records present in the database

        Parameters
        ----------
        select
            Select the BoardRevision fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BoardRevision filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardRevisionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BoardRevision.prisma().count()

        # results: prisma.types.BoardRevisionCountAggregateOutput
        results = BoardRevision.prisma().count(
            select={
                '_all': True,
                'deviceVariant': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BoardRevisionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
    ) -> types.BoardRevisionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BoardRevisionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BoardRevisionWhereInput] = None,
        cursor: Optional[types.BoardRevisionWhereUniqueInput] = None,
    ) -> Union[int, types.BoardRevisionCountAggregateOutput]:
        """Count the number of BoardRevision records present in the database

        Parameters
        ----------
        select
            Select the BoardRevision fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BoardRevision filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BoardRevisionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BoardRevision.prisma().count()

        # results: prisma.types.BoardRevisionCountAggregateOutput
        results = BoardRevision.prisma().count(
            select={
                '_all': True,
                'modemVersion': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BoardRevisionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BoardRevisionWhereInput] = None
    ) -> int:
        """Delete multiple BoardRevision records.

        Parameters
        ----------
        where
            Optional BoardRevision filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BoardRevision records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BoardRevision records
        total = BoardRevision.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BoardRevisionScalarFieldKeys'],
        *,
        where: Optional['types.BoardRevisionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BoardRevisionAvgAggregateInput'] = None,
        sum: Optional['types.BoardRevisionSumAggregateInput'] = None,
        min: Optional['types.BoardRevisionMinAggregateInput'] = None,
        max: Optional['types.BoardRevisionMaxAggregateInput'] = None,
        having: Optional['types.BoardRevisionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BoardRevisionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BoardRevisionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BoardRevisionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BoardRevisionGroupByOutput']:
        """Group BoardRevision records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BoardRevision fields to group records by
        where
            BoardRevision filter to select records
        take
            Limit the maximum number of BoardRevision records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BoardRevisionGroupByOutput]
            A list of dictionaries representing the BoardRevision record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BoardRevision records by modemStorageKey values
        # and count how many records are in each group
        results = BoardRevision.prisma().group_by(
            ['modemStorageKey'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FirmwareSetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FirmwareSet]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FirmwareSet.prisma().query_raw(
            'SELECT * FROM FirmwareSet WHERE id = $1',
            'gfeaahdeh',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FirmwareSet
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FirmwareSet.prisma().query_first(
            'SELECT * FROM FirmwareSet WHERE productId = $1',
            'bjafcgbffc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FirmwareSetCreateInput,
        include: Optional[types.FirmwareSetInclude] = None
    ) -> _PrismaModelT:
        """Create a new FirmwareSet record.

        Parameters
        ----------
        data
            FirmwareSet record data
        include
            Specifies which relations should be loaded on the returned FirmwareSet model

        Returns
        -------
        prisma.models.FirmwareSet
            The created FirmwareSet record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FirmwareSet record from just the required fields
        firmwareset = FirmwareSet.prisma().create(
            data={
                # data to create a FirmwareSet record
                'productId': 'hihegjif',
                'version': 'bdjidcidac',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FirmwareSetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FirmwareSet records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FirmwareSet record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FirmwareSet.prisma().create_many(
            data=[
                {
                    # data to create a FirmwareSet record
                    'productId': 'ifgaaagff',
                    'version': 'befcddgjce',
                },
                {
                    # data to create a FirmwareSet record
                    'productId': 'bfhdbjjgfd',
                    'version': 'cabdjadaji',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FirmwareSetWhereUniqueInput,
        include: Optional[types.FirmwareSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FirmwareSet record.

        Parameters
        ----------
        where
            FirmwareSet filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareSet model

        Returns
        -------
        prisma.models.FirmwareSet
            The deleted FirmwareSet record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwareset = FirmwareSet.prisma().delete(
            where={
                'id': 'faajgfadf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FirmwareSetWhereUniqueInput,
        include: Optional[types.FirmwareSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FirmwareSet record.

        Parameters
        ----------
        where
            FirmwareSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareSet model

        Returns
        -------
        prisma.models.FirmwareSet
            The found FirmwareSet record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwareset = FirmwareSet.prisma().find_unique(
            where={
                'id': 'biaagcedjc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FirmwareSetWhereUniqueInput,
        include: Optional[types.FirmwareSetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FirmwareSet record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FirmwareSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareSet model

        Returns
        -------
        prisma.models.FirmwareSet
            The found FirmwareSet record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwareset = FirmwareSet.prisma().find_unique_or_raise(
            where={
                'id': 'cahhaghecf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareSetWhereInput] = None,
        cursor: Optional[types.FirmwareSetWhereUniqueInput] = None,
        include: Optional[types.FirmwareSetInclude] = None,
        order: Optional[Union[types.FirmwareSetOrderByInput, List[types.FirmwareSetOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareSetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FirmwareSet records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FirmwareSet records returned
        skip
            Ignore the first N results
        where
            FirmwareSet filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareSet model
        order
            Order the returned FirmwareSet records by any field
        distinct
            Filter FirmwareSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FirmwareSet]
            The list of all FirmwareSet records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FirmwareSet records
        firmwaresets = FirmwareSet.prisma().find_many(take=10)

        # find the first 5 FirmwareSet records ordered by the boardRevisionId field
        firmwaresets = FirmwareSet.prisma().find_many(
            take=5,
            order={
                'boardRevisionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareSetWhereInput] = None,
        cursor: Optional[types.FirmwareSetWhereUniqueInput] = None,
        include: Optional[types.FirmwareSetInclude] = None,
        order: Optional[Union[types.FirmwareSetOrderByInput, List[types.FirmwareSetOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareSetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FirmwareSet record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FirmwareSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareSet model
        order
            Order the returned FirmwareSet records by any field
        distinct
            Filter FirmwareSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FirmwareSet
            The first FirmwareSet record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FirmwareSet record ordered by the version field
        firmwareset = FirmwareSet.prisma().find_first(
            skip=1,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareSetWhereInput] = None,
        cursor: Optional[types.FirmwareSetWhereUniqueInput] = None,
        include: Optional[types.FirmwareSetInclude] = None,
        order: Optional[Union[types.FirmwareSetOrderByInput, List[types.FirmwareSetOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareSetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FirmwareSet record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FirmwareSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareSet model
        order
            Order the returned FirmwareSet records by any field
        distinct
            Filter FirmwareSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FirmwareSet
            The first FirmwareSet record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FirmwareSet record ordered by the variant field
        firmwareset = FirmwareSet.prisma().find_first_or_raise(
            skip=1,
            order={
                'variant': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FirmwareSetUpdateInput,
        where: types.FirmwareSetWhereUniqueInput,
        include: Optional[types.FirmwareSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FirmwareSet record.

        Parameters
        ----------
        data
            FirmwareSet record data specifying what to update
        where
            FirmwareSet filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FirmwareSet model

        Returns
        -------
        prisma.models.FirmwareSet
            The updated FirmwareSet record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        firmwareset = FirmwareSet.prisma().update(
            where={
                'id': 'bghcbbcidi',
            },
            data={
                # data to update the FirmwareSet record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FirmwareSetWhereUniqueInput,
        data: types.FirmwareSetUpsertInput,
        include: Optional[types.FirmwareSetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FirmwareSet filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FirmwareSet model

        Returns
        -------
        prisma.models.FirmwareSet
            The created or updated FirmwareSet record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwareset = FirmwareSet.prisma().upsert(
            where={
                'id': 'jcgghhgdj',
            },
            data={
                'create': {
                    'id': 'jcgghhgdj',
                    'productId': 'bfhdbjjgfd',
                    'version': 'cabdjadaji',
                },
                'update': {
                    'productId': 'bfhdbjjgfd',
                    'version': 'cabdjadaji',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FirmwareSetUpdateManyMutationInput,
        where: types.FirmwareSetWhereInput,
    ) -> int:
        """Update multiple FirmwareSet records

        Parameters
        ----------
        data
            FirmwareSet data to update the selected FirmwareSet records to
        where
            Filter to select the FirmwareSet records to update

        Returns
        -------
        int
            The total number of FirmwareSet records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FirmwareSet records
        total = FirmwareSet.prisma().update_many(
            data={
                'releaseTrack': enums.ReleaseTrack.bench
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareSetWhereInput] = None,
        cursor: Optional[types.FirmwareSetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FirmwareSet records present in the database

        Parameters
        ----------
        select
            Select the FirmwareSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FirmwareSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FirmwareSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FirmwareSet.prisma().count()

        # results: prisma.types.FirmwareSetCountAggregateOutput
        results = FirmwareSet.prisma().count(
            select={
                '_all': True,
                'isManufacturing': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FirmwareSetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareSetWhereInput] = None,
        cursor: Optional[types.FirmwareSetWhereUniqueInput] = None,
    ) -> types.FirmwareSetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FirmwareSetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareSetWhereInput] = None,
        cursor: Optional[types.FirmwareSetWhereUniqueInput] = None,
    ) -> Union[int, types.FirmwareSetCountAggregateOutput]:
        """Count the number of FirmwareSet records present in the database

        Parameters
        ----------
        select
            Select the FirmwareSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FirmwareSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FirmwareSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FirmwareSet.prisma().count()

        # results: prisma.types.FirmwareSetCountAggregateOutput
        results = FirmwareSet.prisma().count(
            select={
                '_all': True,
                'isDebug': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FirmwareSetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FirmwareSetWhereInput] = None
    ) -> int:
        """Delete multiple FirmwareSet records.

        Parameters
        ----------
        where
            Optional FirmwareSet filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FirmwareSet records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FirmwareSet records
        total = FirmwareSet.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FirmwareSetScalarFieldKeys'],
        *,
        where: Optional['types.FirmwareSetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FirmwareSetAvgAggregateInput'] = None,
        sum: Optional['types.FirmwareSetSumAggregateInput'] = None,
        min: Optional['types.FirmwareSetMinAggregateInput'] = None,
        max: Optional['types.FirmwareSetMaxAggregateInput'] = None,
        having: Optional['types.FirmwareSetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FirmwareSetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FirmwareSetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FirmwareSetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FirmwareSetGroupByOutput']:
        """Group FirmwareSet records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FirmwareSet fields to group records by
        where
            FirmwareSet filter to select records
        take
            Limit the maximum number of FirmwareSet records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FirmwareSetGroupByOutput]
            A list of dictionaries representing the FirmwareSet record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FirmwareSet records by source values
        # and count how many records are in each group
        results = FirmwareSet.prisma().group_by(
            ['source'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FirmwareBuildActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FirmwareBuild]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FirmwareBuild.prisma().query_raw(
            'SELECT * FROM FirmwareBuild WHERE id = $1',
            'beehgcebbg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FirmwareBuild
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FirmwareBuild.prisma().query_first(
            'SELECT * FROM FirmwareBuild WHERE firmwareSetId = $1',
            'bhdiaidiaf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FirmwareBuildCreateInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> _PrismaModelT:
        """Create a new FirmwareBuild record.

        Parameters
        ----------
        data
            FirmwareBuild record data
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The created FirmwareBuild record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FirmwareBuild record from just the required fields
        firmwarebuild = FirmwareBuild.prisma().create(
            data={
                # data to create a FirmwareBuild record
                'firmwareSetId': 'deajegcfi',
                'filename': 'gabahhhjf',
                'sizeBytes': 3487239552,
                'checksum': 'bifficggej',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FirmwareBuildCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FirmwareBuild records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FirmwareBuild record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FirmwareBuild.prisma().create_many(
            data=[
                {
                    # data to create a FirmwareBuild record
                    'firmwareSetId': 'bgbbaajbic',
                    'filename': 'eegghdhjb',
                    'sizeBytes': 3606820752,
                    'checksum': 'gdcgcgagj',
                },
                {
                    # data to create a FirmwareBuild record
                    'firmwareSetId': 'bhceabbgja',
                    'filename': 'ehabfhegh',
                    'sizeBytes': 14510518944,
                    'checksum': 'bfdgheeegf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FirmwareBuild record.

        Parameters
        ----------
        where
            FirmwareBuild filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The deleted FirmwareBuild record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().delete(
            where={
                'id': 'ececbijji',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FirmwareBuild record.

        Parameters
        ----------
        where
            FirmwareBuild filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The found FirmwareBuild record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().find_unique(
            where={
                'id': 'cbcfgdcdhf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FirmwareBuild record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FirmwareBuild filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The found FirmwareBuild record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().find_unique_or_raise(
            where={
                'id': 'fdgjfbhia',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
        include: Optional[types.FirmwareBuildInclude] = None,
        order: Optional[Union[types.FirmwareBuildOrderByInput, List[types.FirmwareBuildOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareBuildScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FirmwareBuild records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FirmwareBuild records returned
        skip
            Ignore the first N results
        where
            FirmwareBuild filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model
        order
            Order the returned FirmwareBuild records by any field
        distinct
            Filter FirmwareBuild records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FirmwareBuild]
            The list of all FirmwareBuild records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FirmwareBuild records
        firmwarebuilds = FirmwareBuild.prisma().find_many(take=10)

        # find the first 5 FirmwareBuild records ordered by the targetId field
        firmwarebuilds = FirmwareBuild.prisma().find_many(
            take=5,
            order={
                'targetId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
        include: Optional[types.FirmwareBuildInclude] = None,
        order: Optional[Union[types.FirmwareBuildOrderByInput, List[types.FirmwareBuildOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareBuildScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FirmwareBuild record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model
        order
            Order the returned FirmwareBuild records by any field
        distinct
            Filter FirmwareBuild records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FirmwareBuild
            The first FirmwareBuild record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FirmwareBuild record ordered by the versionString field
        firmwarebuild = FirmwareBuild.prisma().find_first(
            skip=1,
            order={
                'versionString': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
        include: Optional[types.FirmwareBuildInclude] = None,
        order: Optional[Union[types.FirmwareBuildOrderByInput, List[types.FirmwareBuildOrderByInput]]] = None,
        distinct: Optional[List[types.FirmwareBuildScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FirmwareBuild record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model
        order
            Order the returned FirmwareBuild records by any field
        distinct
            Filter FirmwareBuild records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FirmwareBuild
            The first FirmwareBuild record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FirmwareBuild record ordered by the hexStorageKey field
        firmwarebuild = FirmwareBuild.prisma().find_first_or_raise(
            skip=1,
            order={
                'hexStorageKey': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FirmwareBuildUpdateInput,
        where: types.FirmwareBuildWhereUniqueInput,
        include: Optional[types.FirmwareBuildInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FirmwareBuild record.

        Parameters
        ----------
        data
            FirmwareBuild record data specifying what to update
        where
            FirmwareBuild filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The updated FirmwareBuild record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().update(
            where={
                'id': 'jcehcdchh',
            },
            data={
                # data to update the FirmwareBuild record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FirmwareBuildWhereUniqueInput,
        data: types.FirmwareBuildUpsertInput,
        include: Optional[types.FirmwareBuildInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FirmwareBuild filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FirmwareBuild model

        Returns
        -------
        prisma.models.FirmwareBuild
            The created or updated FirmwareBuild record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        firmwarebuild = FirmwareBuild.prisma().upsert(
            where={
                'id': 'bgcbjdhjcc',
            },
            data={
                'create': {
                    'id': 'bgcbjdhjcc',
                    'firmwareSetId': 'bhceabbgja',
                    'filename': 'ehabfhegh',
                    'sizeBytes': 14510518944,
                    'checksum': 'bfdgheeegf',
                },
                'update': {
                    'firmwareSetId': 'bhceabbgja',
                    'filename': 'ehabfhegh',
                    'sizeBytes': 14510518944,
                    'checksum': 'bfdgheeegf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FirmwareBuildUpdateManyMutationInput,
        where: types.FirmwareBuildWhereInput,
    ) -> int:
        """Update multiple FirmwareBuild records

        Parameters
        ----------
        data
            FirmwareBuild data to update the selected FirmwareBuild records to
        where
            Filter to select the FirmwareBuild records to update

        Returns
        -------
        int
            The total number of FirmwareBuild records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FirmwareBuild records
        total = FirmwareBuild.prisma().update_many(
            data={
                'hexEncStorageKey': 'bieiidcabj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FirmwareBuild records present in the database

        Parameters
        ----------
        select
            Select the FirmwareBuild fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FirmwareBuildCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FirmwareBuild.prisma().count()

        # results: prisma.types.FirmwareBuildCountAggregateOutput
        results = FirmwareBuild.prisma().count(
            select={
                '_all': True,
                'cfwStorageKey': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FirmwareBuildCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
    ) -> types.FirmwareBuildCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FirmwareBuildCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FirmwareBuildWhereInput] = None,
        cursor: Optional[types.FirmwareBuildWhereUniqueInput] = None,
    ) -> Union[int, types.FirmwareBuildCountAggregateOutput]:
        """Count the number of FirmwareBuild records present in the database

        Parameters
        ----------
        select
            Select the FirmwareBuild fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FirmwareBuild filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FirmwareBuildCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FirmwareBuild.prisma().count()

        # results: prisma.types.FirmwareBuildCountAggregateOutput
        results = FirmwareBuild.prisma().count(
            select={
                '_all': True,
                'manifestKey': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FirmwareBuildCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FirmwareBuildWhereInput] = None
    ) -> int:
        """Delete multiple FirmwareBuild records.

        Parameters
        ----------
        where
            Optional FirmwareBuild filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FirmwareBuild records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FirmwareBuild records
        total = FirmwareBuild.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FirmwareBuildScalarFieldKeys'],
        *,
        where: Optional['types.FirmwareBuildWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FirmwareBuildAvgAggregateInput'] = None,
        sum: Optional['types.FirmwareBuildSumAggregateInput'] = None,
        min: Optional['types.FirmwareBuildMinAggregateInput'] = None,
        max: Optional['types.FirmwareBuildMaxAggregateInput'] = None,
        having: Optional['types.FirmwareBuildScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FirmwareBuildCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FirmwareBuildScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FirmwareBuildScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FirmwareBuildGroupByOutput']:
        """Group FirmwareBuild records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FirmwareBuild fields to group records by
        where
            FirmwareBuild filter to select records
        take
            Limit the maximum number of FirmwareBuild records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FirmwareBuildGroupByOutput]
            A list of dictionaries representing the FirmwareBuild record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FirmwareBuild records by filename values
        # and count how many records are in each group
        results = FirmwareBuild.prisma().group_by(
            ['filename'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ProductStageConfigActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ProductStageConfig]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ProductStageConfig.prisma().query_raw(
            'SELECT * FROM ProductStageConfig WHERE id = $1',
            'bjcbfcieaa',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ProductStageConfig
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ProductStageConfig.prisma().query_first(
            'SELECT * FROM ProductStageConfig WHERE productId = $1',
            'cbaaechiej',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductStageConfigCreateInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> _PrismaModelT:
        """Create a new ProductStageConfig record.

        Parameters
        ----------
        data
            ProductStageConfig record data
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The created ProductStageConfig record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ProductStageConfig record from just the required fields
        productstageconfig = ProductStageConfig.prisma().create(
            data={
                # data to create a ProductStageConfig record
                'productId': 'iejbeaaeg',
                'stage': 928152175,
                'name': 'chdadcaga',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductStageConfigCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ProductStageConfig records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ProductStageConfig record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ProductStageConfig.prisma().create_many(
            data=[
                {
                    # data to create a ProductStageConfig record
                    'productId': 'jicieifbh',
                    'stage': 510737498,
                    'name': 'cbbheiicgh',
                },
                {
                    # data to create a ProductStageConfig record
                    'productId': 'beabjeejdg',
                    'stage': 1297607553,
                    'name': 'fbjeiiffa',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ProductStageConfig record.

        Parameters
        ----------
        where
            ProductStageConfig filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The deleted ProductStageConfig record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().delete(
            where={
                'id': 'jhgidcgbf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ProductStageConfig record.

        Parameters
        ----------
        where
            ProductStageConfig filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The found ProductStageConfig record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().find_unique(
            where={
                'id': 'bgjgecfejc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ProductStageConfig record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ProductStageConfig filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The found ProductStageConfig record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().find_unique_or_raise(
            where={
                'id': 'bgjcgchib',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
        include: Optional[types.ProductStageConfigInclude] = None,
        order: Optional[Union[types.ProductStageConfigOrderByInput, List[types.ProductStageConfigOrderByInput]]] = None,
        distinct: Optional[List[types.ProductStageConfigScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ProductStageConfig records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ProductStageConfig records returned
        skip
            Ignore the first N results
        where
            ProductStageConfig filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model
        order
            Order the returned ProductStageConfig records by any field
        distinct
            Filter ProductStageConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ProductStageConfig]
            The list of all ProductStageConfig records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ProductStageConfig records
        productstageconfigs = ProductStageConfig.prisma().find_many(take=10)

        # find the first 5 ProductStageConfig records ordered by the stage field
        productstageconfigs = ProductStageConfig.prisma().find_many(
            take=5,
            order={
                'stage': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
        include: Optional[types.ProductStageConfigInclude] = None,
        order: Optional[Union[types.ProductStageConfigOrderByInput, List[types.ProductStageConfigOrderByInput]]] = None,
        distinct: Optional[List[types.ProductStageConfigScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ProductStageConfig record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model
        order
            Order the returned ProductStageConfig records by any field
        distinct
            Filter ProductStageConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductStageConfig
            The first ProductStageConfig record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductStageConfig record ordered by the name field
        productstageconfig = ProductStageConfig.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
        include: Optional[types.ProductStageConfigInclude] = None,
        order: Optional[Union[types.ProductStageConfigOrderByInput, List[types.ProductStageConfigOrderByInput]]] = None,
        distinct: Optional[List[types.ProductStageConfigScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ProductStageConfig record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model
        order
            Order the returned ProductStageConfig records by any field
        distinct
            Filter ProductStageConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductStageConfig
            The first ProductStageConfig record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductStageConfig record ordered by the enabled field
        productstageconfig = ProductStageConfig.prisma().find_first_or_raise(
            skip=1,
            order={
                'enabled': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductStageConfigUpdateInput,
        where: types.ProductStageConfigWhereUniqueInput,
        include: Optional[types.ProductStageConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ProductStageConfig record.

        Parameters
        ----------
        data
            ProductStageConfig record data specifying what to update
        where
            ProductStageConfig filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The updated ProductStageConfig record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().update(
            where={
                'id': 'bacdaibgfa',
            },
            data={
                # data to update the ProductStageConfig record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductStageConfigWhereUniqueInput,
        data: types.ProductStageConfigUpsertInput,
        include: Optional[types.ProductStageConfigInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ProductStageConfig filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ProductStageConfig model

        Returns
        -------
        prisma.models.ProductStageConfig
            The created or updated ProductStageConfig record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productstageconfig = ProductStageConfig.prisma().upsert(
            where={
                'id': 'dchgibach',
            },
            data={
                'create': {
                    'id': 'dchgibach',
                    'productId': 'beabjeejdg',
                    'stage': 1297607553,
                    'name': 'fbjeiiffa',
                },
                'update': {
                    'productId': 'beabjeejdg',
                    'stage': 1297607553,
                    'name': 'fbjeiiffa',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductStageConfigUpdateManyMutationInput,
        where: types.ProductStageConfigWhereInput,
    ) -> int:
        """Update multiple ProductStageConfig records

        Parameters
        ----------
        data
            ProductStageConfig data to update the selected ProductStageConfig records to
        where
            Filter to select the ProductStageConfig records to update

        Returns
        -------
        int
            The total number of ProductStageConfig records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ProductStageConfig records
        total = ProductStageConfig.prisma().update_many(
            data={
                'boardRevisionId': 'fchheijjc'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ProductStageConfig records present in the database

        Parameters
        ----------
        select
            Select the ProductStageConfig fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductStageConfigCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductStageConfig.prisma().count()

        # results: prisma.types.ProductStageConfigCountAggregateOutput
        results = ProductStageConfig.prisma().count(
            select={
                '_all': True,
                'watchBranch': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductStageConfigCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
    ) -> types.ProductStageConfigCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductStageConfigCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductStageConfigWhereInput] = None,
        cursor: Optional[types.ProductStageConfigWhereUniqueInput] = None,
    ) -> Union[int, types.ProductStageConfigCountAggregateOutput]:
        """Count the number of ProductStageConfig records present in the database

        Parameters
        ----------
        select
            Select the ProductStageConfig fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductStageConfig filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductStageConfigCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductStageConfig.prisma().count()

        # results: prisma.types.ProductStageConfigCountAggregateOutput
        results = ProductStageConfig.prisma().count(
            select={
                '_all': True,
                'triggerTypes': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductStageConfigCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductStageConfigWhereInput] = None
    ) -> int:
        """Delete multiple ProductStageConfig records.

        Parameters
        ----------
        where
            Optional ProductStageConfig filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ProductStageConfig records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ProductStageConfig records
        total = ProductStageConfig.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductStageConfigScalarFieldKeys'],
        *,
        where: Optional['types.ProductStageConfigWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductStageConfigAvgAggregateInput'] = None,
        sum: Optional['types.ProductStageConfigSumAggregateInput'] = None,
        min: Optional['types.ProductStageConfigMinAggregateInput'] = None,
        max: Optional['types.ProductStageConfigMaxAggregateInput'] = None,
        having: Optional['types.ProductStageConfigScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductStageConfigCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductStageConfigScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductStageConfigScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductStageConfigGroupByOutput']:
        """Group ProductStageConfig records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ProductStageConfig fields to group records by
        where
            ProductStageConfig filter to select records
        take
            Limit the maximum number of ProductStageConfig records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductStageConfigGroupByOutput]
            A list of dictionaries representing the ProductStageConfig record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ProductStageConfig records by signingKeyId values
        # and count how many records are in each group
        results = ProductStageConfig.prisma().group_by(
            ['signingKeyId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ValidationQueueEntryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ValidationQueueEntry]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ValidationQueueEntry.prisma().query_raw(
            'SELECT * FROM ValidationQueueEntry WHERE id = $1',
            'cacjdfhejh',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ValidationQueueEntry.prisma().query_first(
            'SELECT * FROM ValidationQueueEntry WHERE buildRunId = $1',
            'bdbifjhbbi',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ValidationQueueEntryCreateInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> _PrismaModelT:
        """Create a new ValidationQueueEntry record.

        Parameters
        ----------
        data
            ValidationQueueEntry record data
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The created ValidationQueueEntry record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ValidationQueueEntry record from just the required fields
        validationqueueentry = ValidationQueueEntry.prisma().create(
            data={
                # data to create a ValidationQueueEntry record
                'buildRunId': 'cbccbbcdfb',
                'stage': 1024943020,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ValidationQueueEntryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ValidationQueueEntry records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ValidationQueueEntry record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ValidationQueueEntry.prisma().create_many(
            data=[
                {
                    # data to create a ValidationQueueEntry record
                    'buildRunId': 'bhbhdahfaj',
                    'stage': 1598124042,
                },
                {
                    # data to create a ValidationQueueEntry record
                    'buildRunId': 'ibhgcdbgd',
                    'stage': 1030557336,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ValidationQueueEntry record.

        Parameters
        ----------
        where
            ValidationQueueEntry filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The deleted ValidationQueueEntry record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().delete(
            where={
                'id': 'bbdbfcfihd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ValidationQueueEntry record.

        Parameters
        ----------
        where
            ValidationQueueEntry filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The found ValidationQueueEntry record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().find_unique(
            where={
                'id': 'cbagggbji',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ValidationQueueEntry record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ValidationQueueEntry filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The found ValidationQueueEntry record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().find_unique_or_raise(
            where={
                'id': 'bchgafhjed',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
        include: Optional[types.ValidationQueueEntryInclude] = None,
        order: Optional[Union[types.ValidationQueueEntryOrderByInput, List[types.ValidationQueueEntryOrderByInput]]] = None,
        distinct: Optional[List[types.ValidationQueueEntryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ValidationQueueEntry records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ValidationQueueEntry records returned
        skip
            Ignore the first N results
        where
            ValidationQueueEntry filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model
        order
            Order the returned ValidationQueueEntry records by any field
        distinct
            Filter ValidationQueueEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ValidationQueueEntry]
            The list of all ValidationQueueEntry records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ValidationQueueEntry records
        validationqueueentrys = ValidationQueueEntry.prisma().find_many(take=10)

        # find the first 5 ValidationQueueEntry records ordered by the stageConfigId field
        validationqueueentrys = ValidationQueueEntry.prisma().find_many(
            take=5,
            order={
                'stageConfigId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
        include: Optional[types.ValidationQueueEntryInclude] = None,
        order: Optional[Union[types.ValidationQueueEntryOrderByInput, List[types.ValidationQueueEntryOrderByInput]]] = None,
        distinct: Optional[List[types.ValidationQueueEntryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ValidationQueueEntry record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model
        order
            Order the returned ValidationQueueEntry records by any field
        distinct
            Filter ValidationQueueEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The first ValidationQueueEntry record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ValidationQueueEntry record ordered by the stage field
        validationqueueentry = ValidationQueueEntry.prisma().find_first(
            skip=1,
            order={
                'stage': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
        include: Optional[types.ValidationQueueEntryInclude] = None,
        order: Optional[Union[types.ValidationQueueEntryOrderByInput, List[types.ValidationQueueEntryOrderByInput]]] = None,
        distinct: Optional[List[types.ValidationQueueEntryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ValidationQueueEntry record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model
        order
            Order the returned ValidationQueueEntry records by any field
        distinct
            Filter ValidationQueueEntry records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The first ValidationQueueEntry record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ValidationQueueEntry record ordered by the priority field
        validationqueueentry = ValidationQueueEntry.prisma().find_first_or_raise(
            skip=1,
            order={
                'priority': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ValidationQueueEntryUpdateInput,
        where: types.ValidationQueueEntryWhereUniqueInput,
        include: Optional[types.ValidationQueueEntryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ValidationQueueEntry record.

        Parameters
        ----------
        data
            ValidationQueueEntry record data specifying what to update
        where
            ValidationQueueEntry filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The updated ValidationQueueEntry record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().update(
            where={
                'id': 'heffgjdei',
            },
            data={
                # data to update the ValidationQueueEntry record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ValidationQueueEntryWhereUniqueInput,
        data: types.ValidationQueueEntryUpsertInput,
        include: Optional[types.ValidationQueueEntryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ValidationQueueEntry filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ValidationQueueEntry model

        Returns
        -------
        prisma.models.ValidationQueueEntry
            The created or updated ValidationQueueEntry record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        validationqueueentry = ValidationQueueEntry.prisma().upsert(
            where={
                'id': 'dahihgbeb',
            },
            data={
                'create': {
                    'id': 'dahihgbeb',
                    'buildRunId': 'ibhgcdbgd',
                    'stage': 1030557336,
                },
                'update': {
                    'buildRunId': 'ibhgcdbgd',
                    'stage': 1030557336,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ValidationQueueEntryUpdateManyMutationInput,
        where: types.ValidationQueueEntryWhereInput,
    ) -> int:
        """Update multiple ValidationQueueEntry records

        Parameters
        ----------
        data
            ValidationQueueEntry data to update the selected ValidationQueueEntry records to
        where
            Filter to select the ValidationQueueEntry records to update

        Returns
        -------
        int
            The total number of ValidationQueueEntry records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ValidationQueueEntry records
        total = ValidationQueueEntry.prisma().update_many(
            data={
                'status': enums.QueueEntryStatus.QUEUED
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ValidationQueueEntry records present in the database

        Parameters
        ----------
        select
            Select the ValidationQueueEntry fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ValidationQueueEntryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ValidationQueueEntry.prisma().count()

        # results: prisma.types.ValidationQueueEntryCountAggregateOutput
        results = ValidationQueueEntry.prisma().count(
            select={
                '_all': True,
                'fixtureId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ValidationQueueEntryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
    ) -> types.ValidationQueueEntryCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ValidationQueueEntryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ValidationQueueEntryWhereInput] = None,
        cursor: Optional[types.ValidationQueueEntryWhereUniqueInput] = None,
    ) -> Union[int, types.ValidationQueueEntryCountAggregateOutput]:
        """Count the number of ValidationQueueEntry records present in the database

        Parameters
        ----------
        select
            Select the ValidationQueueEntry fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ValidationQueueEntry filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ValidationQueueEntryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ValidationQueueEntry.prisma().count()

        # results: prisma.types.ValidationQueueEntryCountAggregateOutput
        results = ValidationQueueEntry.prisma().count(
            select={
                '_all': True,
                'sessionId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ValidationQueueEntryCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ValidationQueueEntryWhereInput] = None
    ) -> int:
        """Delete multiple ValidationQueueEntry records.

        Parameters
        ----------
        where
            Optional ValidationQueueEntry filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ValidationQueueEntry records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ValidationQueueEntry records
        total = ValidationQueueEntry.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ValidationQueueEntryScalarFieldKeys'],
        *,
        where: Optional['types.ValidationQueueEntryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ValidationQueueEntryAvgAggregateInput'] = None,
        sum: Optional['types.ValidationQueueEntrySumAggregateInput'] = None,
        min: Optional['types.ValidationQueueEntryMinAggregateInput'] = None,
        max: Optional['types.ValidationQueueEntryMaxAggregateInput'] = None,
        having: Optional['types.ValidationQueueEntryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ValidationQueueEntryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ValidationQueueEntryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ValidationQueueEntryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ValidationQueueEntryGroupByOutput']:
        """Group ValidationQueueEntry records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ValidationQueueEntry fields to group records by
        where
            ValidationQueueEntry filter to select records
        take
            Limit the maximum number of ValidationQueueEntry records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ValidationQueueEntryGroupByOutput]
            A list of dictionaries representing the ValidationQueueEntry record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ValidationQueueEntry records by reason values
        # and count how many records are in each group
        results = ValidationQueueEntry.prisma().group_by(
            ['reason'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BuildRunActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BuildRun]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BuildRun.prisma().query_raw(
            'SELECT * FROM BuildRun WHERE id = $1',
            'bgheaejbcc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BuildRun
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BuildRun.prisma().query_first(
            'SELECT * FROM BuildRun WHERE name = $1',
            'bfcgifeged',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BuildRunCreateInput,
        include: Optional[types.BuildRunInclude] = None
    ) -> _PrismaModelT:
        """Create a new BuildRun record.

        Parameters
        ----------
        data
            BuildRun record data
        include
            Specifies which relations should be loaded on the returned BuildRun model

        Returns
        -------
        prisma.models.BuildRun
            The created BuildRun record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BuildRun record from just the required fields
        buildrun = BuildRun.prisma().create(
            data={
                # data to create a BuildRun record
                'productId': 'jfiahhbae',
                'board': 'bfbdafajcb',
                'branch': 'caeghehde',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BuildRunCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BuildRun records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BuildRun record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BuildRun.prisma().create_many(
            data=[
                {
                    # data to create a BuildRun record
                    'productId': 'caghgfbggd',
                    'board': 'bbidjbbjaa',
                    'branch': 'bfijhaejdd',
                },
                {
                    # data to create a BuildRun record
                    'productId': 'bcedehfiji',
                    'board': 'bdgjicijhb',
                    'branch': 'bghifjdeia',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BuildRunWhereUniqueInput,
        include: Optional[types.BuildRunInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BuildRun record.

        Parameters
        ----------
        where
            BuildRun filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BuildRun model

        Returns
        -------
        prisma.models.BuildRun
            The deleted BuildRun record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildrun = BuildRun.prisma().delete(
            where={
                'id': 'eadfcbbcb',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BuildRunWhereUniqueInput,
        include: Optional[types.BuildRunInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BuildRun record.

        Parameters
        ----------
        where
            BuildRun filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildRun model

        Returns
        -------
        prisma.models.BuildRun
            The found BuildRun record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildrun = BuildRun.prisma().find_unique(
            where={
                'id': 'geihgahba',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BuildRunWhereUniqueInput,
        include: Optional[types.BuildRunInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BuildRun record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BuildRun filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildRun model

        Returns
        -------
        prisma.models.BuildRun
            The found BuildRun record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildrun = BuildRun.prisma().find_unique_or_raise(
            where={
                'id': 'gahdcdhbj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildRunWhereInput] = None,
        cursor: Optional[types.BuildRunWhereUniqueInput] = None,
        include: Optional[types.BuildRunInclude] = None,
        order: Optional[Union[types.BuildRunOrderByInput, List[types.BuildRunOrderByInput]]] = None,
        distinct: Optional[List[types.BuildRunScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BuildRun records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BuildRun records returned
        skip
            Ignore the first N results
        where
            BuildRun filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildRun model
        order
            Order the returned BuildRun records by any field
        distinct
            Filter BuildRun records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BuildRun]
            The list of all BuildRun records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BuildRun records
        buildruns = BuildRun.prisma().find_many(take=10)

        # find the first 5 BuildRun records ordered by the productId field
        buildruns = BuildRun.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildRunWhereInput] = None,
        cursor: Optional[types.BuildRunWhereUniqueInput] = None,
        include: Optional[types.BuildRunInclude] = None,
        order: Optional[Union[types.BuildRunOrderByInput, List[types.BuildRunOrderByInput]]] = None,
        distinct: Optional[List[types.BuildRunScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BuildRun record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildRun filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildRun model
        order
            Order the returned BuildRun records by any field
        distinct
            Filter BuildRun records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildRun
            The first BuildRun record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildRun record ordered by the board field
        buildrun = BuildRun.prisma().find_first(
            skip=1,
            order={
                'board': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildRunWhereInput] = None,
        cursor: Optional[types.BuildRunWhereUniqueInput] = None,
        include: Optional[types.BuildRunInclude] = None,
        order: Optional[Union[types.BuildRunOrderByInput, List[types.BuildRunOrderByInput]]] = None,
        distinct: Optional[List[types.BuildRunScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BuildRun record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildRun filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildRun model
        order
            Order the returned BuildRun records by any field
        distinct
            Filter BuildRun records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildRun
            The first BuildRun record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildRun record ordered by the branch field
        buildrun = BuildRun.prisma().find_first_or_raise(
            skip=1,
            order={
                'branch': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BuildRunUpdateInput,
        where: types.BuildRunWhereUniqueInput,
        include: Optional[types.BuildRunInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BuildRun record.

        Parameters
        ----------
        data
            BuildRun record data specifying what to update
        where
            BuildRun filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BuildRun model

        Returns
        -------
        prisma.models.BuildRun
            The updated BuildRun record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        buildrun = BuildRun.prisma().update(
            where={
                'id': 'begiijahea',
            },
            data={
                # data to update the BuildRun record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BuildRunWhereUniqueInput,
        data: types.BuildRunUpsertInput,
        include: Optional[types.BuildRunInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BuildRun filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BuildRun model

        Returns
        -------
        prisma.models.BuildRun
            The created or updated BuildRun record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildrun = BuildRun.prisma().upsert(
            where={
                'id': 'gcjadjaaf',
            },
            data={
                'create': {
                    'id': 'gcjadjaaf',
                    'productId': 'bcedehfiji',
                    'board': 'bdgjicijhb',
                    'branch': 'bghifjdeia',
                },
                'update': {
                    'productId': 'bcedehfiji',
                    'board': 'bdgjicijhb',
                    'branch': 'bghifjdeia',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BuildRunUpdateManyMutationInput,
        where: types.BuildRunWhereInput,
    ) -> int:
        """Update multiple BuildRun records

        Parameters
        ----------
        data
            BuildRun data to update the selected BuildRun records to
        where
            Filter to select the BuildRun records to update

        Returns
        -------
        int
            The total number of BuildRun records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BuildRun records
        total = BuildRun.prisma().update_many(
            data={
                'commitSha': 'bcbebgiaic'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildRunWhereInput] = None,
        cursor: Optional[types.BuildRunWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BuildRun records present in the database

        Parameters
        ----------
        select
            Select the BuildRun fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildRun filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildRunCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildRun.prisma().count()

        # results: prisma.types.BuildRunCountAggregateOutput
        results = BuildRun.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BuildRunCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildRunWhereInput] = None,
        cursor: Optional[types.BuildRunWhereUniqueInput] = None,
    ) -> types.BuildRunCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BuildRunCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildRunWhereInput] = None,
        cursor: Optional[types.BuildRunWhereUniqueInput] = None,
    ) -> Union[int, types.BuildRunCountAggregateOutput]:
        """Count the number of BuildRun records present in the database

        Parameters
        ----------
        select
            Select the BuildRun fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildRun filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildRunCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildRun.prisma().count()

        # results: prisma.types.BuildRunCountAggregateOutput
        results = BuildRun.prisma().count(
            select={
                '_all': True,
                'triggerType': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BuildRunCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BuildRunWhereInput] = None
    ) -> int:
        """Delete multiple BuildRun records.

        Parameters
        ----------
        where
            Optional BuildRun filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BuildRun records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BuildRun records
        total = BuildRun.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BuildRunScalarFieldKeys'],
        *,
        where: Optional['types.BuildRunWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BuildRunAvgAggregateInput'] = None,
        sum: Optional['types.BuildRunSumAggregateInput'] = None,
        min: Optional['types.BuildRunMinAggregateInput'] = None,
        max: Optional['types.BuildRunMaxAggregateInput'] = None,
        having: Optional['types.BuildRunScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BuildRunCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BuildRunScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BuildRunScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BuildRunGroupByOutput']:
        """Group BuildRun records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BuildRun fields to group records by
        where
            BuildRun filter to select records
        take
            Limit the maximum number of BuildRun records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BuildRunGroupByOutput]
            A list of dictionaries representing the BuildRun record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BuildRun records by triggerData values
        # and count how many records are in each group
        results = BuildRun.prisma().group_by(
            ['triggerData'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BuildJobActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BuildJob]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BuildJob.prisma().query_raw(
            'SELECT * FROM BuildJob WHERE id = $1',
            'ijigbdcbj',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BuildJob
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BuildJob.prisma().query_first(
            'SELECT * FROM BuildJob WHERE productId = $1',
            'gfidhicai',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BuildJobCreateInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> _PrismaModelT:
        """Create a new BuildJob record.

        Parameters
        ----------
        data
            BuildJob record data
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The created BuildJob record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BuildJob record from just the required fields
        buildjob = BuildJob.prisma().create(
            data={
                # data to create a BuildJob record
                'productId': 'jfegcaafh',
                'board': 'bcbeiajjfa',
                'target': 'baehicaajf',
                'branch': 'bdachdeiga',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BuildJobCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BuildJob records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BuildJob record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BuildJob.prisma().create_many(
            data=[
                {
                    # data to create a BuildJob record
                    'productId': 'ijdafccef',
                    'board': 'ciaaiddag',
                    'target': 'fejggijff',
                    'branch': 'hghjaaai',
                },
                {
                    # data to create a BuildJob record
                    'productId': 'cajicjjdef',
                    'board': 'cefjaadec',
                    'target': 'ibbigdigd',
                    'branch': 'bdiiiabbii',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BuildJob record.

        Parameters
        ----------
        where
            BuildJob filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The deleted BuildJob record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().delete(
            where={
                'id': 'hfcfhhadh',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BuildJob record.

        Parameters
        ----------
        where
            BuildJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The found BuildJob record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().find_unique(
            where={
                'id': 'bbihggdcji',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BuildJob record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BuildJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The found BuildJob record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().find_unique_or_raise(
            where={
                'id': 'hgjgibdgd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
        include: Optional[types.BuildJobInclude] = None,
        order: Optional[Union[types.BuildJobOrderByInput, List[types.BuildJobOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BuildJob records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BuildJob records returned
        skip
            Ignore the first N results
        where
            BuildJob filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJob model
        order
            Order the returned BuildJob records by any field
        distinct
            Filter BuildJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BuildJob]
            The list of all BuildJob records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BuildJob records
        buildjobs = BuildJob.prisma().find_many(take=10)

        # find the first 5 BuildJob records ordered by the board field
        buildjobs = BuildJob.prisma().find_many(
            take=5,
            order={
                'board': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
        include: Optional[types.BuildJobInclude] = None,
        order: Optional[Union[types.BuildJobOrderByInput, List[types.BuildJobOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BuildJob record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJob model
        order
            Order the returned BuildJob records by any field
        distinct
            Filter BuildJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildJob
            The first BuildJob record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildJob record ordered by the target field
        buildjob = BuildJob.prisma().find_first(
            skip=1,
            order={
                'target': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
        include: Optional[types.BuildJobInclude] = None,
        order: Optional[Union[types.BuildJobOrderByInput, List[types.BuildJobOrderByInput]]] = None,
        distinct: Optional[List[types.BuildJobScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BuildJob record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildJob model
        order
            Order the returned BuildJob records by any field
        distinct
            Filter BuildJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildJob
            The first BuildJob record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildJob record ordered by the variant field
        buildjob = BuildJob.prisma().find_first_or_raise(
            skip=1,
            order={
                'variant': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BuildJobUpdateInput,
        where: types.BuildJobWhereUniqueInput,
        include: Optional[types.BuildJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BuildJob record.

        Parameters
        ----------
        data
            BuildJob record data specifying what to update
        where
            BuildJob filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The updated BuildJob record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().update(
            where={
                'id': 'bcbecjfice',
            },
            data={
                # data to update the BuildJob record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BuildJobWhereUniqueInput,
        data: types.BuildJobUpsertInput,
        include: Optional[types.BuildJobInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BuildJob filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BuildJob model

        Returns
        -------
        prisma.models.BuildJob
            The created or updated BuildJob record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildjob = BuildJob.prisma().upsert(
            where={
                'id': 'bacbebhjjd',
            },
            data={
                'create': {
                    'id': 'bacbebhjjd',
                    'productId': 'cajicjjdef',
                    'board': 'cefjaadec',
                    'target': 'ibbigdigd',
                    'branch': 'bdiiiabbii',
                },
                'update': {
                    'productId': 'cajicjjdef',
                    'board': 'cefjaadec',
                    'target': 'ibbigdigd',
                    'branch': 'bdiiiabbii',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BuildJobUpdateManyMutationInput,
        where: types.BuildJobWhereInput,
    ) -> int:
        """Update multiple BuildJob records

        Parameters
        ----------
        data
            BuildJob data to update the selected BuildJob records to
        where
            Filter to select the BuildJob records to update

        Returns
        -------
        int
            The total number of BuildJob records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BuildJob records
        total = BuildJob.prisma().update_many(
            data={
                'mtibRev': 'dfbfaddhe'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BuildJob records present in the database

        Parameters
        ----------
        select
            Select the BuildJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildJob.prisma().count()

        # results: prisma.types.BuildJobCountAggregateOutput
        results = BuildJob.prisma().count(
            select={
                '_all': True,
                'branch': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BuildJobCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
    ) -> types.BuildJobCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BuildJobCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildJobWhereInput] = None,
        cursor: Optional[types.BuildJobWhereUniqueInput] = None,
    ) -> Union[int, types.BuildJobCountAggregateOutput]:
        """Count the number of BuildJob records present in the database

        Parameters
        ----------
        select
            Select the BuildJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildJob.prisma().count()

        # results: prisma.types.BuildJobCountAggregateOutput
        results = BuildJob.prisma().count(
            select={
                '_all': True,
                'commitSha': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BuildJobCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BuildJobWhereInput] = None
    ) -> int:
        """Delete multiple BuildJob records.

        Parameters
        ----------
        where
            Optional BuildJob filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BuildJob records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BuildJob records
        total = BuildJob.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BuildJobScalarFieldKeys'],
        *,
        where: Optional['types.BuildJobWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BuildJobAvgAggregateInput'] = None,
        sum: Optional['types.BuildJobSumAggregateInput'] = None,
        min: Optional['types.BuildJobMinAggregateInput'] = None,
        max: Optional['types.BuildJobMaxAggregateInput'] = None,
        having: Optional['types.BuildJobScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BuildJobCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BuildJobScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BuildJobScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BuildJobGroupByOutput']:
        """Group BuildJob records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BuildJob fields to group records by
        where
            BuildJob filter to select records
        take
            Limit the maximum number of BuildJob records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BuildJobGroupByOutput]
            A list of dictionaries representing the BuildJob record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BuildJob records by status values
        # and count how many records are in each group
        results = BuildJob.prisma().group_by(
            ['status'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BuildArtifactActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BuildArtifact]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = BuildArtifact.prisma().query_raw(
            'SELECT * FROM BuildArtifact WHERE id = $1',
            'bdcbbieibf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BuildArtifact
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = BuildArtifact.prisma().query_first(
            'SELECT * FROM BuildArtifact WHERE buildJobId = $1',
            'dgjhdcggi',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.BuildArtifactCreateInput,
        include: Optional[types.BuildArtifactInclude] = None
    ) -> _PrismaModelT:
        """Create a new BuildArtifact record.

        Parameters
        ----------
        data
            BuildArtifact record data
        include
            Specifies which relations should be loaded on the returned BuildArtifact model

        Returns
        -------
        prisma.models.BuildArtifact
            The created BuildArtifact record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BuildArtifact record from just the required fields
        buildartifact = BuildArtifact.prisma().create(
            data={
                # data to create a BuildArtifact record
                'buildJobId': 'bbjbcdfabd',
                'name': 'gchfgbcec',
                'storageKey': 'bihcjfcjah',
                'sizeBytes': 21519385056,
                'checksum': 'bibedjhcej',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.BuildArtifactCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BuildArtifact records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BuildArtifact record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = BuildArtifact.prisma().create_many(
            data=[
                {
                    # data to create a BuildArtifact record
                    'buildJobId': 'bjcdajabfa',
                    'name': 'bchhceeeff',
                    'storageKey': 'bbgaifhdaa',
                    'sizeBytes': 4334765532,
                    'checksum': 'beagfbbjig',
                },
                {
                    # data to create a BuildArtifact record
                    'buildJobId': 'beicihhijb',
                    'name': 'fgggcdcjg',
                    'storageKey': 'ccjbbjigf',
                    'sizeBytes': 21000132840,
                    'checksum': 'ebajedhhf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.BuildArtifactWhereUniqueInput,
        include: Optional[types.BuildArtifactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BuildArtifact record.

        Parameters
        ----------
        where
            BuildArtifact filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BuildArtifact model

        Returns
        -------
        prisma.models.BuildArtifact
            The deleted BuildArtifact record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildartifact = BuildArtifact.prisma().delete(
            where={
                'id': 'jajacedge',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.BuildArtifactWhereUniqueInput,
        include: Optional[types.BuildArtifactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BuildArtifact record.

        Parameters
        ----------
        where
            BuildArtifact filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildArtifact model

        Returns
        -------
        prisma.models.BuildArtifact
            The found BuildArtifact record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildartifact = BuildArtifact.prisma().find_unique(
            where={
                'id': 'hffgbabgf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.BuildArtifactWhereUniqueInput,
        include: Optional[types.BuildArtifactInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BuildArtifact record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BuildArtifact filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BuildArtifact model

        Returns
        -------
        prisma.models.BuildArtifact
            The found BuildArtifact record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildartifact = BuildArtifact.prisma().find_unique_or_raise(
            where={
                'id': 'biacbiieja',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildArtifactWhereInput] = None,
        cursor: Optional[types.BuildArtifactWhereUniqueInput] = None,
        include: Optional[types.BuildArtifactInclude] = None,
        order: Optional[Union[types.BuildArtifactOrderByInput, List[types.BuildArtifactOrderByInput]]] = None,
        distinct: Optional[List[types.BuildArtifactScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BuildArtifact records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BuildArtifact records returned
        skip
            Ignore the first N results
        where
            BuildArtifact filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildArtifact model
        order
            Order the returned BuildArtifact records by any field
        distinct
            Filter BuildArtifact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BuildArtifact]
            The list of all BuildArtifact records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BuildArtifact records
        buildartifacts = BuildArtifact.prisma().find_many(take=10)

        # find the first 5 BuildArtifact records ordered by the name field
        buildartifacts = BuildArtifact.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildArtifactWhereInput] = None,
        cursor: Optional[types.BuildArtifactWhereUniqueInput] = None,
        include: Optional[types.BuildArtifactInclude] = None,
        order: Optional[Union[types.BuildArtifactOrderByInput, List[types.BuildArtifactOrderByInput]]] = None,
        distinct: Optional[List[types.BuildArtifactScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BuildArtifact record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildArtifact filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildArtifact model
        order
            Order the returned BuildArtifact records by any field
        distinct
            Filter BuildArtifact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildArtifact
            The first BuildArtifact record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildArtifact record ordered by the storageKey field
        buildartifact = BuildArtifact.prisma().find_first(
            skip=1,
            order={
                'storageKey': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BuildArtifactWhereInput] = None,
        cursor: Optional[types.BuildArtifactWhereUniqueInput] = None,
        include: Optional[types.BuildArtifactInclude] = None,
        order: Optional[Union[types.BuildArtifactOrderByInput, List[types.BuildArtifactOrderByInput]]] = None,
        distinct: Optional[List[types.BuildArtifactScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BuildArtifact record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BuildArtifact filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BuildArtifact model
        order
            Order the returned BuildArtifact records by any field
        distinct
            Filter BuildArtifact records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BuildArtifact
            The first BuildArtifact record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BuildArtifact record ordered by the sizeBytes field
        buildartifact = BuildArtifact.prisma().find_first_or_raise(
            skip=1,
            order={
                'sizeBytes': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.BuildArtifactUpdateInput,
        where: types.BuildArtifactWhereUniqueInput,
        include: Optional[types.BuildArtifactInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BuildArtifact record.

        Parameters
        ----------
        data
            BuildArtifact record data specifying what to update
        where
            BuildArtifact filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BuildArtifact model

        Returns
        -------
        prisma.models.BuildArtifact
            The updated BuildArtifact record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        buildartifact = BuildArtifact.prisma().update(
            where={
                'id': 'cjejbgbff',
            },
            data={
                # data to update the BuildArtifact record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.BuildArtifactWhereUniqueInput,
        data: types.BuildArtifactUpsertInput,
        include: Optional[types.BuildArtifactInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BuildArtifact filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BuildArtifact model

        Returns
        -------
        prisma.models.BuildArtifact
            The created or updated BuildArtifact record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        buildartifact = BuildArtifact.prisma().upsert(
            where={
                'id': 'fgeahddae',
            },
            data={
                'create': {
                    'id': 'fgeahddae',
                    'buildJobId': 'beicihhijb',
                    'name': 'fgggcdcjg',
                    'storageKey': 'ccjbbjigf',
                    'sizeBytes': 21000132840,
                    'checksum': 'ebajedhhf',
                },
                'update': {
                    'buildJobId': 'beicihhijb',
                    'name': 'fgggcdcjg',
                    'storageKey': 'ccjbbjigf',
                    'sizeBytes': 21000132840,
                    'checksum': 'ebajedhhf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.BuildArtifactUpdateManyMutationInput,
        where: types.BuildArtifactWhereInput,
    ) -> int:
        """Update multiple BuildArtifact records

        Parameters
        ----------
        data
            BuildArtifact data to update the selected BuildArtifact records to
        where
            Filter to select the BuildArtifact records to update

        Returns
        -------
        int
            The total number of BuildArtifact records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BuildArtifact records
        total = BuildArtifact.prisma().update_many(
            data={
                'checksum': 'diageigcf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildArtifactWhereInput] = None,
        cursor: Optional[types.BuildArtifactWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BuildArtifact records present in the database

        Parameters
        ----------
        select
            Select the BuildArtifact fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildArtifact filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildArtifactCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildArtifact.prisma().count()

        # results: prisma.types.BuildArtifactCountAggregateOutput
        results = BuildArtifact.prisma().count(
            select={
                '_all': True,
                'role': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.BuildArtifactCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildArtifactWhereInput] = None,
        cursor: Optional[types.BuildArtifactWhereUniqueInput] = None,
    ) -> types.BuildArtifactCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.BuildArtifactCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BuildArtifactWhereInput] = None,
        cursor: Optional[types.BuildArtifactWhereUniqueInput] = None,
    ) -> Union[int, types.BuildArtifactCountAggregateOutput]:
        """Count the number of BuildArtifact records present in the database

        Parameters
        ----------
        select
            Select the BuildArtifact fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BuildArtifact filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BuildArtifactCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = BuildArtifact.prisma().count()

        # results: prisma.types.BuildArtifactCountAggregateOutput
        results = BuildArtifact.prisma().count(
            select={
                '_all': True,
                'processor': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BuildArtifactCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.BuildArtifactWhereInput] = None
    ) -> int:
        """Delete multiple BuildArtifact records.

        Parameters
        ----------
        where
            Optional BuildArtifact filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BuildArtifact records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BuildArtifact records
        total = BuildArtifact.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.BuildArtifactScalarFieldKeys'],
        *,
        where: Optional['types.BuildArtifactWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BuildArtifactAvgAggregateInput'] = None,
        sum: Optional['types.BuildArtifactSumAggregateInput'] = None,
        min: Optional['types.BuildArtifactMinAggregateInput'] = None,
        max: Optional['types.BuildArtifactMaxAggregateInput'] = None,
        having: Optional['types.BuildArtifactScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BuildArtifactCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BuildArtifactScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BuildArtifactScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BuildArtifactGroupByOutput']:
        """Group BuildArtifact records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BuildArtifact fields to group records by
        where
            BuildArtifact filter to select records
        take
            Limit the maximum number of BuildArtifact records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BuildArtifactGroupByOutput]
            A list of dictionaries representing the BuildArtifact record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BuildArtifact records by artifactType values
        # and count how many records are in each group
        results = BuildArtifact.prisma().group_by(
            ['artifactType'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SessionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Session]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Session.prisma().query_raw(
            'SELECT * FROM Session WHERE id = $1',
            'badagbgeha',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Session
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Session.prisma().query_first(
            'SELECT * FROM Session WHERE name = $1',
            'ibgebbjch',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.SessionCreateInput,
        include: Optional[types.SessionInclude] = None
    ) -> _PrismaModelT:
        """Create a new Session record.

        Parameters
        ----------
        data
            Session record data
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The created Session record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Session record from just the required fields
        session = Session.prisma().create(
            data={
                # data to create a Session record
                'name': 'baieajjiee',
                'type': enums.SessionType.MANUFACTURING,
                'productId': 'bahjhaccfd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.SessionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Session records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Session record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Session.prisma().create_many(
            data=[
                {
                    # data to create a Session record
                    'name': 'hffhfabhi',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'bbcigiadhb',
                },
                {
                    # data to create a Session record
                    'name': 'cfjagbbae',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'bbbfhdidef',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Session record.

        Parameters
        ----------
        where
            Session filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The deleted Session record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().delete(
            where={
                'id': 'bdadhibhec',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Session record.

        Parameters
        ----------
        where
            Session filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The found Session record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().find_unique(
            where={
                'id': 'bfhdjaiejf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Session record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Session filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The found Session record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().find_unique_or_raise(
            where={
                'id': 'bbjfijjadg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Session records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Session records returned
        skip
            Ignore the first N results
        where
            Session filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Session]
            The list of all Session records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Session records
        sessions = Session.prisma().find_many(take=10)

        # find the first 5 Session records ordered by the type field
        sessions = Session.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Session record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Session filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Session
            The first Session record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Session record ordered by the productId field
        session = Session.prisma().find_first(
            skip=1,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
        include: Optional[types.SessionInclude] = None,
        order: Optional[Union[types.SessionOrderByInput, List[types.SessionOrderByInput]]] = None,
        distinct: Optional[List[types.SessionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Session record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Session filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Session model
        order
            Order the returned Session records by any field
        distinct
            Filter Session records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Session
            The first Session record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Session record ordered by the status field
        session = Session.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.SessionUpdateInput,
        where: types.SessionWhereUniqueInput,
        include: Optional[types.SessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Session record.

        Parameters
        ----------
        data
            Session record data specifying what to update
        where
            Session filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The updated Session record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        session = Session.prisma().update(
            where={
                'id': 'hdjacbehh',
            },
            data={
                # data to update the Session record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.SessionWhereUniqueInput,
        data: types.SessionUpsertInput,
        include: Optional[types.SessionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Session filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Session model

        Returns
        -------
        prisma.models.Session
            The created or updated Session record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        session = Session.prisma().upsert(
            where={
                'id': 'bhcccbeaba',
            },
            data={
                'create': {
                    'id': 'bhcccbeaba',
                    'name': 'cfjagbbae',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'bbbfhdidef',
                },
                'update': {
                    'name': 'cfjagbbae',
                    'type': enums.SessionType.MANUFACTURING,
                    'productId': 'bbbfhdidef',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.SessionUpdateManyMutationInput,
        where: types.SessionWhereInput,
    ) -> int:
        """Update multiple Session records

        Parameters
        ----------
        data
            Session data to update the selected Session records to
        where
            Filter to select the Session records to update

        Returns
        -------
        int
            The total number of Session records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Session records
        total = Session.prisma().update_many(
            data={
                'fixtureId': 'bcgjbdgjdj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Session records present in the database

        Parameters
        ----------
        select
            Select the Session fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Session filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Session.prisma().count()

        # results: prisma.types.SessionCountAggregateOutput
        results = Session.prisma().count(
            select={
                '_all': True,
                'buildRunId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.SessionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> types.SessionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.SessionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessionWhereInput] = None,
        cursor: Optional[types.SessionWhereUniqueInput] = None,
    ) -> Union[int, types.SessionCountAggregateOutput]:
        """Count the number of Session records present in the database

        Parameters
        ----------
        select
            Select the Session fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Session filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Session.prisma().count()

        # results: prisma.types.SessionCountAggregateOutput
        results = Session.prisma().count(
            select={
                '_all': True,
                'testPackageId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SessionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.SessionWhereInput] = None
    ) -> int:
        """Delete multiple Session records.

        Parameters
        ----------
        where
            Optional Session filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Session records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Session records
        total = Session.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.SessionScalarFieldKeys'],
        *,
        where: Optional['types.SessionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SessionAvgAggregateInput'] = None,
        sum: Optional['types.SessionSumAggregateInput'] = None,
        min: Optional['types.SessionMinAggregateInput'] = None,
        max: Optional['types.SessionMaxAggregateInput'] = None,
        having: Optional['types.SessionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SessionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SessionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SessionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SessionGroupByOutput']:
        """Group Session records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Session fields to group records by
        where
            Session filter to select records
        take
            Limit the maximum number of Session records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SessionGroupByOutput]
            A list of dictionaries representing the Session record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Session records by targetCount values
        # and count how many records are in each group
        results = Session.prisma().group_by(
            ['targetCount'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DeviceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Device]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Device.prisma().query_raw(
            'SELECT * FROM Device WHERE id = $1',
            'fhdbhifae',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Device
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Device.prisma().query_first(
            'SELECT * FROM Device WHERE serialNumber = $1',
            'beeacgfcej',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.DeviceCreateInput,
        include: Optional[types.DeviceInclude] = None
    ) -> _PrismaModelT:
        """Create a new Device record.

        Parameters
        ----------
        data
            Device record data
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The created Device record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Device record from just the required fields
        device = Device.prisma().create(
            data={
                # data to create a Device record
                'serialNumber': 'bbifhdiicc',
                'sessionId': 'bgjeccejad',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.DeviceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Device records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Device record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Device.prisma().create_many(
            data=[
                {
                    # data to create a Device record
                    'serialNumber': 'bjagdgabbg',
                    'sessionId': 'bjbbcffdij',
                },
                {
                    # data to create a Device record
                    'serialNumber': 'begcgchdi',
                    'sessionId': 'bhbjceagbb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Device record.

        Parameters
        ----------
        where
            Device filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The deleted Device record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().delete(
            where={
                'id': 'bjeifffjdg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Device record.

        Parameters
        ----------
        where
            Device filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The found Device record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().find_unique(
            where={
                'id': 'bdidcfdfjd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Device record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Device filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The found Device record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().find_unique_or_raise(
            where={
                'id': 'dfeggejja',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
        include: Optional[types.DeviceInclude] = None,
        order: Optional[Union[types.DeviceOrderByInput, List[types.DeviceOrderByInput]]] = None,
        distinct: Optional[List[types.DeviceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Device records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Device records returned
        skip
            Ignore the first N results
        where
            Device filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Device model
        order
            Order the returned Device records by any field
        distinct
            Filter Device records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Device]
            The list of all Device records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Device records
        devices = Device.prisma().find_many(take=10)

        # find the first 5 Device records ordered by the sessionId field
        devices = Device.prisma().find_many(
            take=5,
            order={
                'sessionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
        include: Optional[types.DeviceInclude] = None,
        order: Optional[Union[types.DeviceOrderByInput, List[types.DeviceOrderByInput]]] = None,
        distinct: Optional[List[types.DeviceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Device record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Device filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Device model
        order
            Order the returned Device records by any field
        distinct
            Filter Device records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Device
            The first Device record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Device record ordered by the status field
        device = Device.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
        include: Optional[types.DeviceInclude] = None,
        order: Optional[Union[types.DeviceOrderByInput, List[types.DeviceOrderByInput]]] = None,
        distinct: Optional[List[types.DeviceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Device record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Device filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Device model
        order
            Order the returned Device records by any field
        distinct
            Filter Device records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Device
            The first Device record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Device record ordered by the metadata field
        device = Device.prisma().find_first_or_raise(
            skip=1,
            order={
                'metadata': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.DeviceUpdateInput,
        where: types.DeviceWhereUniqueInput,
        include: Optional[types.DeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Device record.

        Parameters
        ----------
        data
            Device record data specifying what to update
        where
            Device filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The updated Device record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        device = Device.prisma().update(
            where={
                'id': 'gehbgghbj',
            },
            data={
                # data to update the Device record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.DeviceWhereUniqueInput,
        data: types.DeviceUpsertInput,
        include: Optional[types.DeviceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Device filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Device model

        Returns
        -------
        prisma.models.Device
            The created or updated Device record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        device = Device.prisma().upsert(
            where={
                'id': 'dfhaijeie',
            },
            data={
                'create': {
                    'id': 'dfhaijeie',
                    'serialNumber': 'begcgchdi',
                    'sessionId': 'bhbjceagbb',
                },
                'update': {
                    'serialNumber': 'begcgchdi',
                    'sessionId': 'bhbjceagbb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.DeviceUpdateManyMutationInput,
        where: types.DeviceWhereInput,
    ) -> int:
        """Update multiple Device records

        Parameters
        ----------
        data
            Device data to update the selected Device records to
        where
            Filter to select the Device records to update

        Returns
        -------
        int
            The total number of Device records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Device records
        total = Device.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Device records present in the database

        Parameters
        ----------
        select
            Select the Device fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Device filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Device.prisma().count()

        # results: prisma.types.DeviceCountAggregateOutput
        results = Device.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.DeviceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
    ) -> types.DeviceCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.DeviceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeviceWhereInput] = None,
        cursor: Optional[types.DeviceWhereUniqueInput] = None,
    ) -> Union[int, types.DeviceCountAggregateOutput]:
        """Count the number of Device records present in the database

        Parameters
        ----------
        select
            Select the Device fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Device filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Device.prisma().count()

        # results: prisma.types.DeviceCountAggregateOutput
        results = Device.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DeviceCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.DeviceWhereInput] = None
    ) -> int:
        """Delete multiple Device records.

        Parameters
        ----------
        where
            Optional Device filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Device records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Device records
        total = Device.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.DeviceScalarFieldKeys'],
        *,
        where: Optional['types.DeviceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DeviceAvgAggregateInput'] = None,
        sum: Optional['types.DeviceSumAggregateInput'] = None,
        min: Optional['types.DeviceMinAggregateInput'] = None,
        max: Optional['types.DeviceMaxAggregateInput'] = None,
        having: Optional['types.DeviceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DeviceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DeviceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DeviceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DeviceGroupByOutput']:
        """Group Device records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Device fields to group records by
        where
            Device filter to select records
        take
            Limit the maximum number of Device records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DeviceGroupByOutput]
            A list of dictionaries representing the Device record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Device records by serialNumber values
        # and count how many records are in each group
        results = Device.prisma().group_by(
            ['serialNumber'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FixtureDesignActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FixtureDesign]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FixtureDesign.prisma().query_raw(
            'SELECT * FROM FixtureDesign WHERE id = $1',
            'gbcdjgicb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FixtureDesign
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FixtureDesign.prisma().query_first(
            'SELECT * FROM FixtureDesign WHERE name = $1',
            'biaibdagac',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FixtureDesignCreateInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> _PrismaModelT:
        """Create a new FixtureDesign record.

        Parameters
        ----------
        data
            FixtureDesign record data
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The created FixtureDesign record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FixtureDesign record from just the required fields
        fixturedesign = FixtureDesign.prisma().create(
            data={
                # data to create a FixtureDesign record
                'name': 'bbfbheibcd',
                'boardRevisionId': 'hiagajie',
                'revision': 'eeejidbif',
                'profileTemplate': Json({'efgbahec': True}),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FixtureDesignCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FixtureDesign records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FixtureDesign record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FixtureDesign.prisma().create_many(
            data=[
                {
                    # data to create a FixtureDesign record
                    'name': 'hgjaiebfb',
                    'boardRevisionId': 'bddefjjabc',
                    'revision': 'bbbghgbadh',
                    'profileTemplate': Json({'bbhcgagaic': True}),
                },
                {
                    # data to create a FixtureDesign record
                    'name': 'ddaabegbb',
                    'boardRevisionId': 'bhgibfgbbc',
                    'revision': 'hbgcihef',
                    'profileTemplate': Json({'ffhgghde': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FixtureDesign record.

        Parameters
        ----------
        where
            FixtureDesign filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The deleted FixtureDesign record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().delete(
            where={
                'id': 'ibcadcejf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FixtureDesign record.

        Parameters
        ----------
        where
            FixtureDesign filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The found FixtureDesign record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().find_unique(
            where={
                'id': 'bdcdfgccdg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FixtureDesign record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FixtureDesign filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The found FixtureDesign record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().find_unique_or_raise(
            where={
                'id': 'edhjgdfh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
        include: Optional[types.FixtureDesignInclude] = None,
        order: Optional[Union[types.FixtureDesignOrderByInput, List[types.FixtureDesignOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureDesignScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FixtureDesign records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FixtureDesign records returned
        skip
            Ignore the first N results
        where
            FixtureDesign filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureDesign model
        order
            Order the returned FixtureDesign records by any field
        distinct
            Filter FixtureDesign records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FixtureDesign]
            The list of all FixtureDesign records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FixtureDesign records
        fixturedesigns = FixtureDesign.prisma().find_many(take=10)

        # find the first 5 FixtureDesign records ordered by the boardRevisionId field
        fixturedesigns = FixtureDesign.prisma().find_many(
            take=5,
            order={
                'boardRevisionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
        include: Optional[types.FixtureDesignInclude] = None,
        order: Optional[Union[types.FixtureDesignOrderByInput, List[types.FixtureDesignOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureDesignScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FixtureDesign record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureDesign filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureDesign model
        order
            Order the returned FixtureDesign records by any field
        distinct
            Filter FixtureDesign records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureDesign
            The first FixtureDesign record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureDesign record ordered by the revision field
        fixturedesign = FixtureDesign.prisma().find_first(
            skip=1,
            order={
                'revision': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
        include: Optional[types.FixtureDesignInclude] = None,
        order: Optional[Union[types.FixtureDesignOrderByInput, List[types.FixtureDesignOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureDesignScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FixtureDesign record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureDesign filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureDesign model
        order
            Order the returned FixtureDesign records by any field
        distinct
            Filter FixtureDesign records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureDesign
            The first FixtureDesign record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureDesign record ordered by the capabilities field
        fixturedesign = FixtureDesign.prisma().find_first_or_raise(
            skip=1,
            order={
                'capabilities': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FixtureDesignUpdateInput,
        where: types.FixtureDesignWhereUniqueInput,
        include: Optional[types.FixtureDesignInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FixtureDesign record.

        Parameters
        ----------
        data
            FixtureDesign record data specifying what to update
        where
            FixtureDesign filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The updated FixtureDesign record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().update(
            where={
                'id': 'bdeffdadda',
            },
            data={
                # data to update the FixtureDesign record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FixtureDesignWhereUniqueInput,
        data: types.FixtureDesignUpsertInput,
        include: Optional[types.FixtureDesignInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FixtureDesign filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FixtureDesign model

        Returns
        -------
        prisma.models.FixtureDesign
            The created or updated FixtureDesign record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixturedesign = FixtureDesign.prisma().upsert(
            where={
                'id': 'bjgfdihchf',
            },
            data={
                'create': {
                    'id': 'bjgfdihchf',
                    'name': 'ddaabegbb',
                    'boardRevisionId': 'bhgibfgbbc',
                    'revision': 'hbgcihef',
                    'profileTemplate': Json({'ffhgghde': True}),
                },
                'update': {
                    'name': 'ddaabegbb',
                    'boardRevisionId': 'bhgibfgbbc',
                    'revision': 'hbgcihef',
                    'profileTemplate': Json({'ffhgghde': True}),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FixtureDesignUpdateManyMutationInput,
        where: types.FixtureDesignWhereInput,
    ) -> int:
        """Update multiple FixtureDesign records

        Parameters
        ----------
        data
            FixtureDesign data to update the selected FixtureDesign records to
        where
            Filter to select the FixtureDesign records to update

        Returns
        -------
        int
            The total number of FixtureDesign records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FixtureDesign records
        total = FixtureDesign.prisma().update_many(
            data={
                'profileTemplate': Json({'iaeihdeei': True})
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FixtureDesign records present in the database

        Parameters
        ----------
        select
            Select the FixtureDesign fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureDesign filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureDesignCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureDesign.prisma().count()

        # results: prisma.types.FixtureDesignCountAggregateOutput
        results = FixtureDesign.prisma().count(
            select={
                '_all': True,
                'schematicUrl': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FixtureDesignCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
    ) -> types.FixtureDesignCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FixtureDesignCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureDesignWhereInput] = None,
        cursor: Optional[types.FixtureDesignWhereUniqueInput] = None,
    ) -> Union[int, types.FixtureDesignCountAggregateOutput]:
        """Count the number of FixtureDesign records present in the database

        Parameters
        ----------
        select
            Select the FixtureDesign fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureDesign filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureDesignCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureDesign.prisma().count()

        # results: prisma.types.FixtureDesignCountAggregateOutput
        results = FixtureDesign.prisma().count(
            select={
                '_all': True,
                'bomUrl': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FixtureDesignCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FixtureDesignWhereInput] = None
    ) -> int:
        """Delete multiple FixtureDesign records.

        Parameters
        ----------
        where
            Optional FixtureDesign filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FixtureDesign records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FixtureDesign records
        total = FixtureDesign.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FixtureDesignScalarFieldKeys'],
        *,
        where: Optional['types.FixtureDesignWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FixtureDesignAvgAggregateInput'] = None,
        sum: Optional['types.FixtureDesignSumAggregateInput'] = None,
        min: Optional['types.FixtureDesignMinAggregateInput'] = None,
        max: Optional['types.FixtureDesignMaxAggregateInput'] = None,
        having: Optional['types.FixtureDesignScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FixtureDesignCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FixtureDesignScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FixtureDesignScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FixtureDesignGroupByOutput']:
        """Group FixtureDesign records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FixtureDesign fields to group records by
        where
            FixtureDesign filter to select records
        take
            Limit the maximum number of FixtureDesign records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FixtureDesignGroupByOutput]
            A list of dictionaries representing the FixtureDesign record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FixtureDesign records by assemblyGuide values
        # and count how many records are in each group
        results = FixtureDesign.prisma().group_by(
            ['assemblyGuide'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FixtureActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Fixture]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Fixture.prisma().query_raw(
            'SELECT * FROM Fixture WHERE id = $1',
            'bfggejgfbd',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Fixture
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Fixture.prisma().query_first(
            'SELECT * FROM Fixture WHERE name = $1',
            'ifaaaedja',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FixtureCreateInput,
        include: Optional[types.FixtureInclude] = None
    ) -> _PrismaModelT:
        """Create a new Fixture record.

        Parameters
        ----------
        data
            Fixture record data
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The created Fixture record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Fixture record from just the required fields
        fixture = Fixture.prisma().create(
            data={
                # data to create a Fixture record
                'name': 'cbajdjjabf',
                'productId': 'bcicggedea',
                'type': enums.NodeType.MANUFACTURING,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FixtureCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Fixture records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Fixture record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Fixture.prisma().create_many(
            data=[
                {
                    # data to create a Fixture record
                    'name': 'cebcdadjh',
                    'productId': 'ehfigdgac',
                    'type': enums.NodeType.MANUFACTURING,
                },
                {
                    # data to create a Fixture record
                    'name': 'bhbgccijjf',
                    'productId': 'bigjhdgbjc',
                    'type': enums.NodeType.MANUFACTURING,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Fixture record.

        Parameters
        ----------
        where
            Fixture filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The deleted Fixture record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().delete(
            where={
                'id': 'bfifdebhfd',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Fixture record.

        Parameters
        ----------
        where
            Fixture filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The found Fixture record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().find_unique(
            where={
                'id': 'cjchbjde',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Fixture record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Fixture filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The found Fixture record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().find_unique_or_raise(
            where={
                'id': 'bfiibjcehj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
        include: Optional[types.FixtureInclude] = None,
        order: Optional[Union[types.FixtureOrderByInput, List[types.FixtureOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Fixture records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Fixture records returned
        skip
            Ignore the first N results
        where
            Fixture filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fixture model
        order
            Order the returned Fixture records by any field
        distinct
            Filter Fixture records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Fixture]
            The list of all Fixture records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Fixture records
        fixtures = Fixture.prisma().find_many(take=10)

        # find the first 5 Fixture records ordered by the stationId field
        fixtures = Fixture.prisma().find_many(
            take=5,
            order={
                'stationId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
        include: Optional[types.FixtureInclude] = None,
        order: Optional[Union[types.FixtureOrderByInput, List[types.FixtureOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Fixture record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Fixture filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fixture model
        order
            Order the returned Fixture records by any field
        distinct
            Filter Fixture records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Fixture
            The first Fixture record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Fixture record ordered by the productId field
        fixture = Fixture.prisma().find_first(
            skip=1,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
        include: Optional[types.FixtureInclude] = None,
        order: Optional[Union[types.FixtureOrderByInput, List[types.FixtureOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Fixture record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Fixture filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Fixture model
        order
            Order the returned Fixture records by any field
        distinct
            Filter Fixture records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Fixture
            The first Fixture record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Fixture record ordered by the boardRevisionId field
        fixture = Fixture.prisma().find_first_or_raise(
            skip=1,
            order={
                'boardRevisionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FixtureUpdateInput,
        where: types.FixtureWhereUniqueInput,
        include: Optional[types.FixtureInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Fixture record.

        Parameters
        ----------
        data
            Fixture record data specifying what to update
        where
            Fixture filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The updated Fixture record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fixture = Fixture.prisma().update(
            where={
                'id': 'ijieafghg',
            },
            data={
                # data to update the Fixture record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FixtureWhereUniqueInput,
        data: types.FixtureUpsertInput,
        include: Optional[types.FixtureInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Fixture filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Fixture model

        Returns
        -------
        prisma.models.Fixture
            The created or updated Fixture record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixture = Fixture.prisma().upsert(
            where={
                'id': 'hhhegahcf',
            },
            data={
                'create': {
                    'id': 'hhhegahcf',
                    'name': 'bhbgccijjf',
                    'productId': 'bigjhdgbjc',
                    'type': enums.NodeType.MANUFACTURING,
                },
                'update': {
                    'name': 'bhbgccijjf',
                    'productId': 'bigjhdgbjc',
                    'type': enums.NodeType.MANUFACTURING,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FixtureUpdateManyMutationInput,
        where: types.FixtureWhereInput,
    ) -> int:
        """Update multiple Fixture records

        Parameters
        ----------
        data
            Fixture data to update the selected Fixture records to
        where
            Filter to select the Fixture records to update

        Returns
        -------
        int
            The total number of Fixture records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Fixture records
        total = Fixture.prisma().update_many(
            data={
                'type': enums.NodeType.MANUFACTURING
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Fixture records present in the database

        Parameters
        ----------
        select
            Select the Fixture fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Fixture filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Fixture.prisma().count()

        # results: prisma.types.FixtureCountAggregateOutput
        results = Fixture.prisma().count(
            select={
                '_all': True,
                'designId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FixtureCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
    ) -> types.FixtureCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FixtureCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureWhereInput] = None,
        cursor: Optional[types.FixtureWhereUniqueInput] = None,
    ) -> Union[int, types.FixtureCountAggregateOutput]:
        """Count the number of Fixture records present in the database

        Parameters
        ----------
        select
            Select the Fixture fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Fixture filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Fixture.prisma().count()

        # results: prisma.types.FixtureCountAggregateOutput
        results = Fixture.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FixtureCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FixtureWhereInput] = None
    ) -> int:
        """Delete multiple Fixture records.

        Parameters
        ----------
        where
            Optional Fixture filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Fixture records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Fixture records
        total = Fixture.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FixtureScalarFieldKeys'],
        *,
        where: Optional['types.FixtureWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FixtureAvgAggregateInput'] = None,
        sum: Optional['types.FixtureSumAggregateInput'] = None,
        min: Optional['types.FixtureMinAggregateInput'] = None,
        max: Optional['types.FixtureMaxAggregateInput'] = None,
        having: Optional['types.FixtureScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FixtureCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FixtureScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FixtureScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FixtureGroupByOutput']:
        """Group Fixture records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Fixture fields to group records by
        where
            Fixture filter to select records
        take
            Limit the maximum number of Fixture records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FixtureGroupByOutput]
            A list of dictionaries representing the Fixture record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Fixture records by lockedBy values
        # and count how many records are in each group
        results = Fixture.prisma().group_by(
            ['lockedBy'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FixtureSlotActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FixtureSlot]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = FixtureSlot.prisma().query_raw(
            'SELECT * FROM FixtureSlot WHERE id = $1',
            'edhijefdi',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FixtureSlot
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = FixtureSlot.prisma().query_first(
            'SELECT * FROM FixtureSlot WHERE fixtureId = $1',
            'djddecjhb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.FixtureSlotCreateInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> _PrismaModelT:
        """Create a new FixtureSlot record.

        Parameters
        ----------
        data
            FixtureSlot record data
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The created FixtureSlot record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FixtureSlot record from just the required fields
        fixtureslot = FixtureSlot.prisma().create(
            data={
                # data to create a FixtureSlot record
                'fixtureId': 'bgdicjhie',
                'slotIndex': 248152689,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.FixtureSlotCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FixtureSlot records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FixtureSlot record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = FixtureSlot.prisma().create_many(
            data=[
                {
                    # data to create a FixtureSlot record
                    'fixtureId': 'debhbfada',
                    'slotIndex': 1692766423,
                },
                {
                    # data to create a FixtureSlot record
                    'fixtureId': 'igggcfjg',
                    'slotIndex': 1697895597,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FixtureSlot record.

        Parameters
        ----------
        where
            FixtureSlot filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The deleted FixtureSlot record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().delete(
            where={
                'id': 'bcigdhache',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FixtureSlot record.

        Parameters
        ----------
        where
            FixtureSlot filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The found FixtureSlot record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().find_unique(
            where={
                'id': 'igefhgdhb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FixtureSlot record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FixtureSlot filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The found FixtureSlot record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().find_unique_or_raise(
            where={
                'id': 'ejbiifbae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
        include: Optional[types.FixtureSlotInclude] = None,
        order: Optional[Union[types.FixtureSlotOrderByInput, List[types.FixtureSlotOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureSlotScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FixtureSlot records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FixtureSlot records returned
        skip
            Ignore the first N results
        where
            FixtureSlot filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureSlot model
        order
            Order the returned FixtureSlot records by any field
        distinct
            Filter FixtureSlot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FixtureSlot]
            The list of all FixtureSlot records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FixtureSlot records
        fixtureslots = FixtureSlot.prisma().find_many(take=10)

        # find the first 5 FixtureSlot records ordered by the slotIndex field
        fixtureslots = FixtureSlot.prisma().find_many(
            take=5,
            order={
                'slotIndex': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
        include: Optional[types.FixtureSlotInclude] = None,
        order: Optional[Union[types.FixtureSlotOrderByInput, List[types.FixtureSlotOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureSlotScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FixtureSlot record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureSlot filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureSlot model
        order
            Order the returned FixtureSlot records by any field
        distinct
            Filter FixtureSlot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureSlot
            The first FixtureSlot record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureSlot record ordered by the label field
        fixtureslot = FixtureSlot.prisma().find_first(
            skip=1,
            order={
                'label': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
        include: Optional[types.FixtureSlotInclude] = None,
        order: Optional[Union[types.FixtureSlotOrderByInput, List[types.FixtureSlotOrderByInput]]] = None,
        distinct: Optional[List[types.FixtureSlotScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FixtureSlot record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FixtureSlot filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FixtureSlot model
        order
            Order the returned FixtureSlot records by any field
        distinct
            Filter FixtureSlot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FixtureSlot
            The first FixtureSlot record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FixtureSlot record ordered by the nodeId field
        fixtureslot = FixtureSlot.prisma().find_first_or_raise(
            skip=1,
            order={
                'nodeId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.FixtureSlotUpdateInput,
        where: types.FixtureSlotWhereUniqueInput,
        include: Optional[types.FixtureSlotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FixtureSlot record.

        Parameters
        ----------
        data
            FixtureSlot record data specifying what to update
        where
            FixtureSlot filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The updated FixtureSlot record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().update(
            where={
                'id': 'djcfgedjd',
            },
            data={
                # data to update the FixtureSlot record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.FixtureSlotWhereUniqueInput,
        data: types.FixtureSlotUpsertInput,
        include: Optional[types.FixtureSlotInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FixtureSlot filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FixtureSlot model

        Returns
        -------
        prisma.models.FixtureSlot
            The created or updated FixtureSlot record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        fixtureslot = FixtureSlot.prisma().upsert(
            where={
                'id': 'bdbjcdegag',
            },
            data={
                'create': {
                    'id': 'bdbjcdegag',
                    'fixtureId': 'igggcfjg',
                    'slotIndex': 1697895597,
                },
                'update': {
                    'fixtureId': 'igggcfjg',
                    'slotIndex': 1697895597,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.FixtureSlotUpdateManyMutationInput,
        where: types.FixtureSlotWhereInput,
    ) -> int:
        """Update multiple FixtureSlot records

        Parameters
        ----------
        data
            FixtureSlot data to update the selected FixtureSlot records to
        where
            Filter to select the FixtureSlot records to update

        Returns
        -------
        int
            The total number of FixtureSlot records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FixtureSlot records
        total = FixtureSlot.prisma().update_many(
            data={
                'active': False
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FixtureSlot records present in the database

        Parameters
        ----------
        select
            Select the FixtureSlot fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureSlot filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureSlotCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureSlot.prisma().count()

        # results: prisma.types.FixtureSlotCountAggregateOutput
        results = FixtureSlot.prisma().count(
            select={
                '_all': True,
                'jlinkAppSerial': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.FixtureSlotCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
    ) -> types.FixtureSlotCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.FixtureSlotCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FixtureSlotWhereInput] = None,
        cursor: Optional[types.FixtureSlotWhereUniqueInput] = None,
    ) -> Union[int, types.FixtureSlotCountAggregateOutput]:
        """Count the number of FixtureSlot records present in the database

        Parameters
        ----------
        select
            Select the FixtureSlot fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FixtureSlot filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FixtureSlotCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = FixtureSlot.prisma().count()

        # results: prisma.types.FixtureSlotCountAggregateOutput
        results = FixtureSlot.prisma().count(
            select={
                '_all': True,
                'jlinkCommsSerial': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FixtureSlotCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.FixtureSlotWhereInput] = None
    ) -> int:
        """Delete multiple FixtureSlot records.

        Parameters
        ----------
        where
            Optional FixtureSlot filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FixtureSlot records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FixtureSlot records
        total = FixtureSlot.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.FixtureSlotScalarFieldKeys'],
        *,
        where: Optional['types.FixtureSlotWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FixtureSlotAvgAggregateInput'] = None,
        sum: Optional['types.FixtureSlotSumAggregateInput'] = None,
        min: Optional['types.FixtureSlotMinAggregateInput'] = None,
        max: Optional['types.FixtureSlotMaxAggregateInput'] = None,
        having: Optional['types.FixtureSlotScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FixtureSlotCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FixtureSlotScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FixtureSlotScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FixtureSlotGroupByOutput']:
        """Group FixtureSlot records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FixtureSlot fields to group records by
        where
            FixtureSlot filter to select records
        take
            Limit the maximum number of FixtureSlot records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FixtureSlotGroupByOutput]
            A list of dictionaries representing the FixtureSlot record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FixtureSlot records by uartAppPath values
        # and count how many records are in each group
        results = FixtureSlot.prisma().group_by(
            ['uartAppPath'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class NodeActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Node]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Node.prisma().query_raw(
            'SELECT * FROM Node WHERE id = $1',
            'bcjjffegfc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Node
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Node.prisma().query_first(
            'SELECT * FROM Node WHERE name = $1',
            'cahaeaicjd',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.NodeCreateInput,
        include: Optional[types.NodeInclude] = None
    ) -> _PrismaModelT:
        """Create a new Node record.

        Parameters
        ----------
        data
            Node record data
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The created Node record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Node record from just the required fields
        node = Node.prisma().create(
            data={
                # data to create a Node record
                'name': 'ibbjaacbi',
                'hostname': 'djgacbcch',
                'type': enums.NodeType.MANUFACTURING,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.NodeCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Node records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Node record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Node.prisma().create_many(
            data=[
                {
                    # data to create a Node record
                    'name': 'geeeegace',
                    'hostname': 'bbgdigchd',
                    'type': enums.NodeType.MANUFACTURING,
                },
                {
                    # data to create a Node record
                    'name': 'dajcifgdi',
                    'hostname': 'ccedhdbj',
                    'type': enums.NodeType.MANUFACTURING,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Node record.

        Parameters
        ----------
        where
            Node filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The deleted Node record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().delete(
            where={
                'id': 'bjaabjjjce',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Node record.

        Parameters
        ----------
        where
            Node filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The found Node record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().find_unique(
            where={
                'id': 'cafhdcdcjd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Node record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Node filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The found Node record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().find_unique_or_raise(
            where={
                'id': 'bdeebbhbdi',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
        include: Optional[types.NodeInclude] = None,
        order: Optional[Union[types.NodeOrderByInput, List[types.NodeOrderByInput]]] = None,
        distinct: Optional[List[types.NodeScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Node records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Node records returned
        skip
            Ignore the first N results
        where
            Node filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Node model
        order
            Order the returned Node records by any field
        distinct
            Filter Node records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Node]
            The list of all Node records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Node records
        nodes = Node.prisma().find_many(take=10)

        # find the first 5 Node records ordered by the hostname field
        nodes = Node.prisma().find_many(
            take=5,
            order={
                'hostname': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
        include: Optional[types.NodeInclude] = None,
        order: Optional[Union[types.NodeOrderByInput, List[types.NodeOrderByInput]]] = None,
        distinct: Optional[List[types.NodeScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Node record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Node filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Node model
        order
            Order the returned Node records by any field
        distinct
            Filter Node records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Node
            The first Node record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Node record ordered by the type field
        node = Node.prisma().find_first(
            skip=1,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
        include: Optional[types.NodeInclude] = None,
        order: Optional[Union[types.NodeOrderByInput, List[types.NodeOrderByInput]]] = None,
        distinct: Optional[List[types.NodeScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Node record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Node filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Node model
        order
            Order the returned Node records by any field
        distinct
            Filter Node records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Node
            The first Node record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Node record ordered by the status field
        node = Node.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.NodeUpdateInput,
        where: types.NodeWhereUniqueInput,
        include: Optional[types.NodeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Node record.

        Parameters
        ----------
        data
            Node record data specifying what to update
        where
            Node filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The updated Node record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        node = Node.prisma().update(
            where={
                'id': 'cafcbdchah',
            },
            data={
                # data to update the Node record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.NodeWhereUniqueInput,
        data: types.NodeUpsertInput,
        include: Optional[types.NodeInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Node filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Node model

        Returns
        -------
        prisma.models.Node
            The created or updated Node record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        node = Node.prisma().upsert(
            where={
                'id': 'bdffbehbae',
            },
            data={
                'create': {
                    'id': 'bdffbehbae',
                    'name': 'dajcifgdi',
                    'hostname': 'ccedhdbj',
                    'type': enums.NodeType.MANUFACTURING,
                },
                'update': {
                    'name': 'dajcifgdi',
                    'hostname': 'ccedhdbj',
                    'type': enums.NodeType.MANUFACTURING,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.NodeUpdateManyMutationInput,
        where: types.NodeWhereInput,
    ) -> int:
        """Update multiple Node records

        Parameters
        ----------
        data
            Node data to update the selected Node records to
        where
            Filter to select the Node records to update

        Returns
        -------
        int
            The total number of Node records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Node records
        total = Node.prisma().update_many(
            data={
                'ipAddress': 'ieahjgeb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Node records present in the database

        Parameters
        ----------
        select
            Select the Node fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Node filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NodeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Node.prisma().count()

        # results: prisma.types.NodeCountAggregateOutput
        results = Node.prisma().count(
            select={
                '_all': True,
                'hardwareRevision': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.NodeCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
    ) -> types.NodeCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.NodeCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NodeWhereInput] = None,
        cursor: Optional[types.NodeWhereUniqueInput] = None,
    ) -> Union[int, types.NodeCountAggregateOutput]:
        """Count the number of Node records present in the database

        Parameters
        ----------
        select
            Select the Node fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Node filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NodeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Node.prisma().count()

        # results: prisma.types.NodeCountAggregateOutput
        results = Node.prisma().count(
            select={
                '_all': True,
                'metadata': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.NodeCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.NodeWhereInput] = None
    ) -> int:
        """Delete multiple Node records.

        Parameters
        ----------
        where
            Optional Node filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Node records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Node records
        total = Node.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.NodeScalarFieldKeys'],
        *,
        where: Optional['types.NodeWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.NodeAvgAggregateInput'] = None,
        sum: Optional['types.NodeSumAggregateInput'] = None,
        min: Optional['types.NodeMinAggregateInput'] = None,
        max: Optional['types.NodeMaxAggregateInput'] = None,
        having: Optional['types.NodeScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.NodeCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.NodeScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.NodeScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.NodeGroupByOutput']:
        """Group Node records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Node fields to group records by
        where
            Node filter to select records
        take
            Limit the maximum number of Node records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.NodeGroupByOutput]
            A list of dictionaries representing the Node record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Node records by createdAt values
        # and count how many records are in each group
        results = Node.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IcleDeviceActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IcleDevice]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = IcleDevice.prisma().query_raw(
            'SELECT * FROM IcleDevice WHERE id = $1',
            'hfeeddceg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IcleDevice
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = IcleDevice.prisma().query_first(
            'SELECT * FROM IcleDevice WHERE deviceId = $1',
            'dbecgbbid',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.IcleDeviceCreateInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> _PrismaModelT:
        """Create a new IcleDevice record.

        Parameters
        ----------
        data
            IcleDevice record data
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The created IcleDevice record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IcleDevice record from just the required fields
        icledevice = IcleDevice.prisma().create(
            data={
                # data to create a IcleDevice record
                'deviceId': 'cchghigae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.IcleDeviceCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IcleDevice records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IcleDevice record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = IcleDevice.prisma().create_many(
            data=[
                {
                    # data to create a IcleDevice record
                    'deviceId': 'ecdjjjhab',
                },
                {
                    # data to create a IcleDevice record
                    'deviceId': 'biachfede',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IcleDevice record.

        Parameters
        ----------
        where
            IcleDevice filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The deleted IcleDevice record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().delete(
            where={
                'id': 'fhgaibff',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IcleDevice record.

        Parameters
        ----------
        where
            IcleDevice filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The found IcleDevice record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().find_unique(
            where={
                'id': 'cadajbcbca',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IcleDevice record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IcleDevice filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The found IcleDevice record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().find_unique_or_raise(
            where={
                'id': 'bjheigfcdd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
        include: Optional[types.IcleDeviceInclude] = None,
        order: Optional[Union[types.IcleDeviceOrderByInput, List[types.IcleDeviceOrderByInput]]] = None,
        distinct: Optional[List[types.IcleDeviceScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IcleDevice records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IcleDevice records returned
        skip
            Ignore the first N results
        where
            IcleDevice filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleDevice model
        order
            Order the returned IcleDevice records by any field
        distinct
            Filter IcleDevice records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IcleDevice]
            The list of all IcleDevice records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IcleDevice records
        icledevices = IcleDevice.prisma().find_many(take=10)

        # find the first 5 IcleDevice records ordered by the name field
        icledevices = IcleDevice.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
        include: Optional[types.IcleDeviceInclude] = None,
        order: Optional[Union[types.IcleDeviceOrderByInput, List[types.IcleDeviceOrderByInput]]] = None,
        distinct: Optional[List[types.IcleDeviceScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IcleDevice record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleDevice filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleDevice model
        order
            Order the returned IcleDevice records by any field
        distinct
            Filter IcleDevice records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleDevice
            The first IcleDevice record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleDevice record ordered by the ipAddress field
        icledevice = IcleDevice.prisma().find_first(
            skip=1,
            order={
                'ipAddress': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
        include: Optional[types.IcleDeviceInclude] = None,
        order: Optional[Union[types.IcleDeviceOrderByInput, List[types.IcleDeviceOrderByInput]]] = None,
        distinct: Optional[List[types.IcleDeviceScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IcleDevice record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleDevice filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleDevice model
        order
            Order the returned IcleDevice records by any field
        distinct
            Filter IcleDevice records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleDevice
            The first IcleDevice record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleDevice record ordered by the macAddress field
        icledevice = IcleDevice.prisma().find_first_or_raise(
            skip=1,
            order={
                'macAddress': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.IcleDeviceUpdateInput,
        where: types.IcleDeviceWhereUniqueInput,
        include: Optional[types.IcleDeviceInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IcleDevice record.

        Parameters
        ----------
        data
            IcleDevice record data specifying what to update
        where
            IcleDevice filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The updated IcleDevice record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().update(
            where={
                'id': 'bjejigcdcg',
            },
            data={
                # data to update the IcleDevice record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.IcleDeviceWhereUniqueInput,
        data: types.IcleDeviceUpsertInput,
        include: Optional[types.IcleDeviceInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IcleDevice filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IcleDevice model

        Returns
        -------
        prisma.models.IcleDevice
            The created or updated IcleDevice record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        icledevice = IcleDevice.prisma().upsert(
            where={
                'id': 'bifiiibcah',
            },
            data={
                'create': {
                    'id': 'bifiiibcah',
                    'deviceId': 'biachfede',
                },
                'update': {
                    'deviceId': 'biachfede',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.IcleDeviceUpdateManyMutationInput,
        where: types.IcleDeviceWhereInput,
    ) -> int:
        """Update multiple IcleDevice records

        Parameters
        ----------
        data
            IcleDevice data to update the selected IcleDevice records to
        where
            Filter to select the IcleDevice records to update

        Returns
        -------
        int
            The total number of IcleDevice records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IcleDevice records
        total = IcleDevice.prisma().update_many(
            data={
                'firmwareVersion': 'dbjibjdaa'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IcleDevice records present in the database

        Parameters
        ----------
        select
            Select the IcleDevice fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleDevice filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleDeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleDevice.prisma().count()

        # results: prisma.types.IcleDeviceCountAggregateOutput
        results = IcleDevice.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.IcleDeviceCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
    ) -> types.IcleDeviceCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.IcleDeviceCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleDeviceWhereInput] = None,
        cursor: Optional[types.IcleDeviceWhereUniqueInput] = None,
    ) -> Union[int, types.IcleDeviceCountAggregateOutput]:
        """Count the number of IcleDevice records present in the database

        Parameters
        ----------
        select
            Select the IcleDevice fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleDevice filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleDeviceCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleDevice.prisma().count()

        # results: prisma.types.IcleDeviceCountAggregateOutput
        results = IcleDevice.prisma().count(
            select={
                '_all': True,
                'registered': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IcleDeviceCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.IcleDeviceWhereInput] = None
    ) -> int:
        """Delete multiple IcleDevice records.

        Parameters
        ----------
        where
            Optional IcleDevice filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IcleDevice records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IcleDevice records
        total = IcleDevice.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.IcleDeviceScalarFieldKeys'],
        *,
        where: Optional['types.IcleDeviceWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IcleDeviceAvgAggregateInput'] = None,
        sum: Optional['types.IcleDeviceSumAggregateInput'] = None,
        min: Optional['types.IcleDeviceMinAggregateInput'] = None,
        max: Optional['types.IcleDeviceMaxAggregateInput'] = None,
        having: Optional['types.IcleDeviceScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IcleDeviceCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IcleDeviceScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IcleDeviceScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IcleDeviceGroupByOutput']:
        """Group IcleDevice records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IcleDevice fields to group records by
        where
            IcleDevice filter to select records
        take
            Limit the maximum number of IcleDevice records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IcleDeviceGroupByOutput]
            A list of dictionaries representing the IcleDevice record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IcleDevice records by lastHeartbeat values
        # and count how many records are in each group
        results = IcleDevice.prisma().group_by(
            ['lastHeartbeat'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IclePendingCommandActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IclePendingCommand]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = IclePendingCommand.prisma().query_raw(
            'SELECT * FROM IclePendingCommand WHERE id = $1',
            'dgijbdiaf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IclePendingCommand
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = IclePendingCommand.prisma().query_first(
            'SELECT * FROM IclePendingCommand WHERE deviceId = $1',
            'begfaigba',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.IclePendingCommandCreateInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> _PrismaModelT:
        """Create a new IclePendingCommand record.

        Parameters
        ----------
        data
            IclePendingCommand record data
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The created IclePendingCommand record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IclePendingCommand record from just the required fields
        iclependingcommand = IclePendingCommand.prisma().create(
            data={
                # data to create a IclePendingCommand record
                'deviceId': 'bdjiafcgjb',
                'commandType': 'bficecgcfg',
                'payload': Json({'cbjjeedcj': True}),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.IclePendingCommandCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IclePendingCommand records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IclePendingCommand record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = IclePendingCommand.prisma().create_many(
            data=[
                {
                    # data to create a IclePendingCommand record
                    'deviceId': 'dedgbbhja',
                    'commandType': 'cabiahchj',
                    'payload': Json({'cgbeccfce': True}),
                },
                {
                    # data to create a IclePendingCommand record
                    'deviceId': 'fcjcagef',
                    'commandType': 'bgdhaeacic',
                    'payload': Json({'caffafcheh': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IclePendingCommand record.

        Parameters
        ----------
        where
            IclePendingCommand filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The deleted IclePendingCommand record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().delete(
            where={
                'id': 'fjjbegge',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IclePendingCommand record.

        Parameters
        ----------
        where
            IclePendingCommand filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The found IclePendingCommand record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().find_unique(
            where={
                'id': 'bdiifhbieb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IclePendingCommand record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IclePendingCommand filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The found IclePendingCommand record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().find_unique_or_raise(
            where={
                'id': 'cdcaejhgg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
        include: Optional[types.IclePendingCommandInclude] = None,
        order: Optional[Union[types.IclePendingCommandOrderByInput, List[types.IclePendingCommandOrderByInput]]] = None,
        distinct: Optional[List[types.IclePendingCommandScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IclePendingCommand records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IclePendingCommand records returned
        skip
            Ignore the first N results
        where
            IclePendingCommand filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model
        order
            Order the returned IclePendingCommand records by any field
        distinct
            Filter IclePendingCommand records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IclePendingCommand]
            The list of all IclePendingCommand records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IclePendingCommand records
        iclependingcommands = IclePendingCommand.prisma().find_many(take=10)

        # find the first 5 IclePendingCommand records ordered by the commandType field
        iclependingcommands = IclePendingCommand.prisma().find_many(
            take=5,
            order={
                'commandType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
        include: Optional[types.IclePendingCommandInclude] = None,
        order: Optional[Union[types.IclePendingCommandOrderByInput, List[types.IclePendingCommandOrderByInput]]] = None,
        distinct: Optional[List[types.IclePendingCommandScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IclePendingCommand record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model
        order
            Order the returned IclePendingCommand records by any field
        distinct
            Filter IclePendingCommand records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IclePendingCommand
            The first IclePendingCommand record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IclePendingCommand record ordered by the payload field
        iclependingcommand = IclePendingCommand.prisma().find_first(
            skip=1,
            order={
                'payload': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
        include: Optional[types.IclePendingCommandInclude] = None,
        order: Optional[Union[types.IclePendingCommandOrderByInput, List[types.IclePendingCommandOrderByInput]]] = None,
        distinct: Optional[List[types.IclePendingCommandScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IclePendingCommand record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model
        order
            Order the returned IclePendingCommand records by any field
        distinct
            Filter IclePendingCommand records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IclePendingCommand
            The first IclePendingCommand record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IclePendingCommand record ordered by the priority field
        iclependingcommand = IclePendingCommand.prisma().find_first_or_raise(
            skip=1,
            order={
                'priority': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.IclePendingCommandUpdateInput,
        where: types.IclePendingCommandWhereUniqueInput,
        include: Optional[types.IclePendingCommandInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IclePendingCommand record.

        Parameters
        ----------
        data
            IclePendingCommand record data specifying what to update
        where
            IclePendingCommand filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The updated IclePendingCommand record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().update(
            where={
                'id': 'jbijgfbfj',
            },
            data={
                # data to update the IclePendingCommand record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.IclePendingCommandWhereUniqueInput,
        data: types.IclePendingCommandUpsertInput,
        include: Optional[types.IclePendingCommandInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IclePendingCommand filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IclePendingCommand model

        Returns
        -------
        prisma.models.IclePendingCommand
            The created or updated IclePendingCommand record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclependingcommand = IclePendingCommand.prisma().upsert(
            where={
                'id': 'ggfbeddia',
            },
            data={
                'create': {
                    'id': 'ggfbeddia',
                    'deviceId': 'fcjcagef',
                    'commandType': 'bgdhaeacic',
                    'payload': Json({'caffafcheh': True}),
                },
                'update': {
                    'deviceId': 'fcjcagef',
                    'commandType': 'bgdhaeacic',
                    'payload': Json({'caffafcheh': True}),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.IclePendingCommandUpdateManyMutationInput,
        where: types.IclePendingCommandWhereInput,
    ) -> int:
        """Update multiple IclePendingCommand records

        Parameters
        ----------
        data
            IclePendingCommand data to update the selected IclePendingCommand records to
        where
            Filter to select the IclePendingCommand records to update

        Returns
        -------
        int
            The total number of IclePendingCommand records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IclePendingCommand records
        total = IclePendingCommand.prisma().update_many(
            data={
                'expiresAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IclePendingCommand records present in the database

        Parameters
        ----------
        select
            Select the IclePendingCommand fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IclePendingCommandCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IclePendingCommand.prisma().count()

        # results: prisma.types.IclePendingCommandCountAggregateOutput
        results = IclePendingCommand.prisma().count(
            select={
                '_all': True,
                'acknowledged': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.IclePendingCommandCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
    ) -> types.IclePendingCommandCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.IclePendingCommandCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IclePendingCommandWhereInput] = None,
        cursor: Optional[types.IclePendingCommandWhereUniqueInput] = None,
    ) -> Union[int, types.IclePendingCommandCountAggregateOutput]:
        """Count the number of IclePendingCommand records present in the database

        Parameters
        ----------
        select
            Select the IclePendingCommand fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IclePendingCommand filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IclePendingCommandCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IclePendingCommand.prisma().count()

        # results: prisma.types.IclePendingCommandCountAggregateOutput
        results = IclePendingCommand.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IclePendingCommandCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.IclePendingCommandWhereInput] = None
    ) -> int:
        """Delete multiple IclePendingCommand records.

        Parameters
        ----------
        where
            Optional IclePendingCommand filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IclePendingCommand records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IclePendingCommand records
        total = IclePendingCommand.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.IclePendingCommandScalarFieldKeys'],
        *,
        where: Optional['types.IclePendingCommandWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IclePendingCommandAvgAggregateInput'] = None,
        sum: Optional['types.IclePendingCommandSumAggregateInput'] = None,
        min: Optional['types.IclePendingCommandMinAggregateInput'] = None,
        max: Optional['types.IclePendingCommandMaxAggregateInput'] = None,
        having: Optional['types.IclePendingCommandScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IclePendingCommandCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IclePendingCommandScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IclePendingCommandScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IclePendingCommandGroupByOutput']:
        """Group IclePendingCommand records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IclePendingCommand fields to group records by
        where
            IclePendingCommand filter to select records
        take
            Limit the maximum number of IclePendingCommand records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IclePendingCommandGroupByOutput]
            A list of dictionaries representing the IclePendingCommand record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IclePendingCommand records by updatedAt values
        # and count how many records are in each group
        results = IclePendingCommand.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class IcleLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.IcleLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = IcleLog.prisma().query_raw(
            'SELECT * FROM IcleLog WHERE id = $1',
            'djjejdaj',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.IcleLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = IcleLog.prisma().query_first(
            'SELECT * FROM IcleLog WHERE deviceId = $1',
            'bjabbfceji',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.IcleLogCreateInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new IcleLog record.

        Parameters
        ----------
        data
            IcleLog record data
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The created IcleLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a IcleLog record from just the required fields
        iclelog = IcleLog.prisma().create(
            data={
                # data to create a IcleLog record
                'deviceId': 'bgchfbjibb',
                'filename': 'bajecchdjc',
                'storageKey': 'dfgacajif',
                'sizeBytes': 19660020456,
                'format': 'bijbfghhhf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.IcleLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple IcleLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of IcleLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = IcleLog.prisma().create_many(
            data=[
                {
                    # data to create a IcleLog record
                    'deviceId': 'bahchhihdc',
                    'filename': 'bihjdcibib',
                    'storageKey': 'bfhhjbbdha',
                    'sizeBytes': 6056754372,
                    'format': 'bbaiefbee',
                },
                {
                    # data to create a IcleLog record
                    'deviceId': 'bdaacgjbaf',
                    'filename': 'biibaighec',
                    'storageKey': 'baicdfeidj',
                    'sizeBytes': 17481936432,
                    'format': 'cbcehahedh',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single IcleLog record.

        Parameters
        ----------
        where
            IcleLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The deleted IcleLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().delete(
            where={
                'id': 'bcjihiaide',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique IcleLog record.

        Parameters
        ----------
        where
            IcleLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The found IcleLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().find_unique(
            where={
                'id': 'bagfijcgfj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique IcleLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            IcleLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The found IcleLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().find_unique_or_raise(
            where={
                'id': 'bcggehiidc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
        include: Optional[types.IcleLogInclude] = None,
        order: Optional[Union[types.IcleLogOrderByInput, List[types.IcleLogOrderByInput]]] = None,
        distinct: Optional[List[types.IcleLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple IcleLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of IcleLog records returned
        skip
            Ignore the first N results
        where
            IcleLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleLog model
        order
            Order the returned IcleLog records by any field
        distinct
            Filter IcleLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.IcleLog]
            The list of all IcleLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 IcleLog records
        iclelogs = IcleLog.prisma().find_many(take=10)

        # find the first 5 IcleLog records ordered by the filename field
        iclelogs = IcleLog.prisma().find_many(
            take=5,
            order={
                'filename': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
        include: Optional[types.IcleLogInclude] = None,
        order: Optional[Union[types.IcleLogOrderByInput, List[types.IcleLogOrderByInput]]] = None,
        distinct: Optional[List[types.IcleLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single IcleLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleLog model
        order
            Order the returned IcleLog records by any field
        distinct
            Filter IcleLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleLog
            The first IcleLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleLog record ordered by the storageKey field
        iclelog = IcleLog.prisma().find_first(
            skip=1,
            order={
                'storageKey': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
        include: Optional[types.IcleLogInclude] = None,
        order: Optional[Union[types.IcleLogOrderByInput, List[types.IcleLogOrderByInput]]] = None,
        distinct: Optional[List[types.IcleLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single IcleLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            IcleLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned IcleLog model
        order
            Order the returned IcleLog records by any field
        distinct
            Filter IcleLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.IcleLog
            The first IcleLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second IcleLog record ordered by the sizeBytes field
        iclelog = IcleLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'sizeBytes': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.IcleLogUpdateInput,
        where: types.IcleLogWhereUniqueInput,
        include: Optional[types.IcleLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single IcleLog record.

        Parameters
        ----------
        data
            IcleLog record data specifying what to update
        where
            IcleLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The updated IcleLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().update(
            where={
                'id': 'bjcdacgacf',
            },
            data={
                # data to update the IcleLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.IcleLogWhereUniqueInput,
        data: types.IcleLogUpsertInput,
        include: Optional[types.IcleLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            IcleLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned IcleLog model

        Returns
        -------
        prisma.models.IcleLog
            The created or updated IcleLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        iclelog = IcleLog.prisma().upsert(
            where={
                'id': 'jfieeahi',
            },
            data={
                'create': {
                    'id': 'jfieeahi',
                    'deviceId': 'bdaacgjbaf',
                    'filename': 'biibaighec',
                    'storageKey': 'baicdfeidj',
                    'sizeBytes': 17481936432,
                    'format': 'cbcehahedh',
                },
                'update': {
                    'deviceId': 'bdaacgjbaf',
                    'filename': 'biibaighec',
                    'storageKey': 'baicdfeidj',
                    'sizeBytes': 17481936432,
                    'format': 'cbcehahedh',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.IcleLogUpdateManyMutationInput,
        where: types.IcleLogWhereInput,
    ) -> int:
        """Update multiple IcleLog records

        Parameters
        ----------
        data
            IcleLog data to update the selected IcleLog records to
        where
            Filter to select the IcleLog records to update

        Returns
        -------
        int
            The total number of IcleLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all IcleLog records
        total = IcleLog.prisma().update_many(
            data={
                'format': 'bijfjbddfj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of IcleLog records present in the database

        Parameters
        ----------
        select
            Select the IcleLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleLog.prisma().count()

        # results: prisma.types.IcleLogCountAggregateOutput
        results = IcleLog.prisma().count(
            select={
                '_all': True,
                'uploadedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.IcleLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
    ) -> types.IcleLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.IcleLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.IcleLogWhereInput] = None,
        cursor: Optional[types.IcleLogWhereUniqueInput] = None,
    ) -> Union[int, types.IcleLogCountAggregateOutput]:
        """Count the number of IcleLog records present in the database

        Parameters
        ----------
        select
            Select the IcleLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            IcleLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.IcleLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = IcleLog.prisma().count()

        # results: prisma.types.IcleLogCountAggregateOutput
        results = IcleLog.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.IcleLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.IcleLogWhereInput] = None
    ) -> int:
        """Delete multiple IcleLog records.

        Parameters
        ----------
        where
            Optional IcleLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of IcleLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all IcleLog records
        total = IcleLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.IcleLogScalarFieldKeys'],
        *,
        where: Optional['types.IcleLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.IcleLogAvgAggregateInput'] = None,
        sum: Optional['types.IcleLogSumAggregateInput'] = None,
        min: Optional['types.IcleLogMinAggregateInput'] = None,
        max: Optional['types.IcleLogMaxAggregateInput'] = None,
        having: Optional['types.IcleLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.IcleLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.IcleLogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.IcleLogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.IcleLogGroupByOutput']:
        """Group IcleLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar IcleLog fields to group records by
        where
            IcleLog filter to select records
        take
            Limit the maximum number of IcleLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.IcleLogGroupByOutput]
            A list of dictionaries representing the IcleLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group IcleLog records by deviceId values
        # and count how many records are in each group
        results = IcleLog.prisma().group_by(
            ['deviceId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Test]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Test.prisma().query_raw(
            'SELECT * FROM Test WHERE id = $1',
            'cdcdjdcee',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Test
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Test.prisma().query_first(
            'SELECT * FROM Test WHERE name = $1',
            'bbbgjdbgcb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestCreateInput,
        include: Optional[types.TestInclude] = None
    ) -> _PrismaModelT:
        """Create a new Test record.

        Parameters
        ----------
        data
            Test record data
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The created Test record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Test record from just the required fields
        test = Test.prisma().create(
            data={
                # data to create a Test record
                'name': 'bcedacgecg',
                'productId': 'cbdffjeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Test records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Test record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Test.prisma().create_many(
            data=[
                {
                    # data to create a Test record
                    'name': 'idbcdhbci',
                    'productId': 'bacegehahd',
                },
                {
                    # data to create a Test record
                    'name': 'ebedeihec',
                    'productId': 'bajagjdfbb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Test record.

        Parameters
        ----------
        where
            Test filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The deleted Test record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().delete(
            where={
                'id': 'bggedbjggi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Test record.

        Parameters
        ----------
        where
            Test filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The found Test record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().find_unique(
            where={
                'id': 'hgbafifcf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Test record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Test filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The found Test record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().find_unique_or_raise(
            where={
                'id': 'bejiecfecg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
        include: Optional[types.TestInclude] = None,
        order: Optional[Union[types.TestOrderByInput, List[types.TestOrderByInput]]] = None,
        distinct: Optional[List[types.TestScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Test records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Test records returned
        skip
            Ignore the first N results
        where
            Test filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Test model
        order
            Order the returned Test records by any field
        distinct
            Filter Test records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Test]
            The list of all Test records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Test records
        tests = Test.prisma().find_many(take=10)

        # find the first 5 Test records ordered by the productId field
        tests = Test.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
        include: Optional[types.TestInclude] = None,
        order: Optional[Union[types.TestOrderByInput, List[types.TestOrderByInput]]] = None,
        distinct: Optional[List[types.TestScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Test record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Test filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Test model
        order
            Order the returned Test records by any field
        distinct
            Filter Test records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Test
            The first Test record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Test record ordered by the description field
        test = Test.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
        include: Optional[types.TestInclude] = None,
        order: Optional[Union[types.TestOrderByInput, List[types.TestOrderByInput]]] = None,
        distinct: Optional[List[types.TestScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Test record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Test filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Test model
        order
            Order the returned Test records by any field
        distinct
            Filter Test records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Test
            The first Test record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Test record ordered by the category field
        test = Test.prisma().find_first_or_raise(
            skip=1,
            order={
                'category': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestUpdateInput,
        where: types.TestWhereUniqueInput,
        include: Optional[types.TestInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Test record.

        Parameters
        ----------
        data
            Test record data specifying what to update
        where
            Test filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The updated Test record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        test = Test.prisma().update(
            where={
                'id': 'bjgacaeagh',
            },
            data={
                # data to update the Test record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestWhereUniqueInput,
        data: types.TestUpsertInput,
        include: Optional[types.TestInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Test filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Test model

        Returns
        -------
        prisma.models.Test
            The created or updated Test record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        test = Test.prisma().upsert(
            where={
                'id': 'beeaihbefg',
            },
            data={
                'create': {
                    'id': 'beeaihbefg',
                    'name': 'ebedeihec',
                    'productId': 'bajagjdfbb',
                },
                'update': {
                    'name': 'ebedeihec',
                    'productId': 'bajagjdfbb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestUpdateManyMutationInput,
        where: types.TestWhereInput,
    ) -> int:
        """Update multiple Test records

        Parameters
        ----------
        data
            Test data to update the selected Test records to
        where
            Filter to select the Test records to update

        Returns
        -------
        int
            The total number of Test records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Test records
        total = Test.prisma().update_many(
            data={
                'sortOrder': 1515643353
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Test records present in the database

        Parameters
        ----------
        select
            Select the Test fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Test filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Test.prisma().count()

        # results: prisma.types.TestCountAggregateOutput
        results = Test.prisma().count(
            select={
                '_all': True,
                'config': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
    ) -> types.TestCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestWhereInput] = None,
        cursor: Optional[types.TestWhereUniqueInput] = None,
    ) -> Union[int, types.TestCountAggregateOutput]:
        """Count the number of Test records present in the database

        Parameters
        ----------
        select
            Select the Test fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Test filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Test.prisma().count()

        # results: prisma.types.TestCountAggregateOutput
        results = Test.prisma().count(
            select={
                '_all': True,
                'enabled': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestWhereInput] = None
    ) -> int:
        """Delete multiple Test records.

        Parameters
        ----------
        where
            Optional Test filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Test records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Test records
        total = Test.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestScalarFieldKeys'],
        *,
        where: Optional['types.TestWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestAvgAggregateInput'] = None,
        sum: Optional['types.TestSumAggregateInput'] = None,
        min: Optional['types.TestMinAggregateInput'] = None,
        max: Optional['types.TestMaxAggregateInput'] = None,
        having: Optional['types.TestScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestGroupByOutput']:
        """Group Test records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Test fields to group records by
        where
            Test filter to select records
        take
            Limit the maximum number of Test records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestGroupByOutput]
            A list of dictionaries representing the Test record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Test records by createdAt values
        # and count how many records are in each group
        results = Test.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestExecutionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TestExecution]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TestExecution.prisma().query_raw(
            'SELECT * FROM TestExecution WHERE id = $1',
            'jbgheibja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TestExecution
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TestExecution.prisma().query_first(
            'SELECT * FROM TestExecution WHERE testId = $1',
            'eejajbid',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestExecutionCreateInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> _PrismaModelT:
        """Create a new TestExecution record.

        Parameters
        ----------
        data
            TestExecution record data
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The created TestExecution record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TestExecution record from just the required fields
        testexecution = TestExecution.prisma().create(
            data={
                # data to create a TestExecution record
                'testId': 'efhdcdaie',
                'nodeId': 'cadejecgbd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestExecutionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TestExecution records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TestExecution record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TestExecution.prisma().create_many(
            data=[
                {
                    # data to create a TestExecution record
                    'testId': 'bahjhjjhcc',
                    'nodeId': 'ebhbhbdff',
                },
                {
                    # data to create a TestExecution record
                    'testId': 'bdiefcdfhg',
                    'nodeId': 'cheifeghd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TestExecution record.

        Parameters
        ----------
        where
            TestExecution filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The deleted TestExecution record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().delete(
            where={
                'id': 'fgijheefe',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TestExecution record.

        Parameters
        ----------
        where
            TestExecution filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The found TestExecution record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().find_unique(
            where={
                'id': 'hcbgbhfch',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TestExecution record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TestExecution filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The found TestExecution record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().find_unique_or_raise(
            where={
                'id': 'bfbbbgbfhc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
        include: Optional[types.TestExecutionInclude] = None,
        order: Optional[Union[types.TestExecutionOrderByInput, List[types.TestExecutionOrderByInput]]] = None,
        distinct: Optional[List[types.TestExecutionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TestExecution records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TestExecution records returned
        skip
            Ignore the first N results
        where
            TestExecution filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestExecution model
        order
            Order the returned TestExecution records by any field
        distinct
            Filter TestExecution records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TestExecution]
            The list of all TestExecution records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TestExecution records
        testexecutions = TestExecution.prisma().find_many(take=10)

        # find the first 5 TestExecution records ordered by the nodeId field
        testexecutions = TestExecution.prisma().find_many(
            take=5,
            order={
                'nodeId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
        include: Optional[types.TestExecutionInclude] = None,
        order: Optional[Union[types.TestExecutionOrderByInput, List[types.TestExecutionOrderByInput]]] = None,
        distinct: Optional[List[types.TestExecutionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TestExecution record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestExecution filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestExecution model
        order
            Order the returned TestExecution records by any field
        distinct
            Filter TestExecution records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestExecution
            The first TestExecution record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestExecution record ordered by the deviceId field
        testexecution = TestExecution.prisma().find_first(
            skip=1,
            order={
                'deviceId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
        include: Optional[types.TestExecutionInclude] = None,
        order: Optional[Union[types.TestExecutionOrderByInput, List[types.TestExecutionOrderByInput]]] = None,
        distinct: Optional[List[types.TestExecutionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TestExecution record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestExecution filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestExecution model
        order
            Order the returned TestExecution records by any field
        distinct
            Filter TestExecution records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestExecution
            The first TestExecution record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestExecution record ordered by the slotId field
        testexecution = TestExecution.prisma().find_first_or_raise(
            skip=1,
            order={
                'slotId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestExecutionUpdateInput,
        where: types.TestExecutionWhereUniqueInput,
        include: Optional[types.TestExecutionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TestExecution record.

        Parameters
        ----------
        data
            TestExecution record data specifying what to update
        where
            TestExecution filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The updated TestExecution record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().update(
            where={
                'id': 'ibijjdeb',
            },
            data={
                # data to update the TestExecution record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestExecutionWhereUniqueInput,
        data: types.TestExecutionUpsertInput,
        include: Optional[types.TestExecutionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TestExecution filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TestExecution model

        Returns
        -------
        prisma.models.TestExecution
            The created or updated TestExecution record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        testexecution = TestExecution.prisma().upsert(
            where={
                'id': 'caehiccddi',
            },
            data={
                'create': {
                    'id': 'caehiccddi',
                    'testId': 'bdiefcdfhg',
                    'nodeId': 'cheifeghd',
                },
                'update': {
                    'testId': 'bdiefcdfhg',
                    'nodeId': 'cheifeghd',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestExecutionUpdateManyMutationInput,
        where: types.TestExecutionWhereInput,
    ) -> int:
        """Update multiple TestExecution records

        Parameters
        ----------
        data
            TestExecution data to update the selected TestExecution records to
        where
            Filter to select the TestExecution records to update

        Returns
        -------
        int
            The total number of TestExecution records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TestExecution records
        total = TestExecution.prisma().update_many(
            data={
                'status': enums.TestExecutionStatus.QUEUED
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TestExecution records present in the database

        Parameters
        ----------
        select
            Select the TestExecution fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestExecution filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestExecutionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestExecution.prisma().count()

        # results: prisma.types.TestExecutionCountAggregateOutput
        results = TestExecution.prisma().count(
            select={
                '_all': True,
                'config': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestExecutionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
    ) -> types.TestExecutionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestExecutionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestExecutionWhereInput] = None,
        cursor: Optional[types.TestExecutionWhereUniqueInput] = None,
    ) -> Union[int, types.TestExecutionCountAggregateOutput]:
        """Count the number of TestExecution records present in the database

        Parameters
        ----------
        select
            Select the TestExecution fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestExecution filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestExecutionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestExecution.prisma().count()

        # results: prisma.types.TestExecutionCountAggregateOutput
        results = TestExecution.prisma().count(
            select={
                '_all': True,
                'triggeredById': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestExecutionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestExecutionWhereInput] = None
    ) -> int:
        """Delete multiple TestExecution records.

        Parameters
        ----------
        where
            Optional TestExecution filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TestExecution records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TestExecution records
        total = TestExecution.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestExecutionScalarFieldKeys'],
        *,
        where: Optional['types.TestExecutionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestExecutionAvgAggregateInput'] = None,
        sum: Optional['types.TestExecutionSumAggregateInput'] = None,
        min: Optional['types.TestExecutionMinAggregateInput'] = None,
        max: Optional['types.TestExecutionMaxAggregateInput'] = None,
        having: Optional['types.TestExecutionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestExecutionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestExecutionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestExecutionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestExecutionGroupByOutput']:
        """Group TestExecution records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TestExecution fields to group records by
        where
            TestExecution filter to select records
        take
            Limit the maximum number of TestExecution records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestExecutionGroupByOutput]
            A list of dictionaries representing the TestExecution record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TestExecution records by startedAt values
        # and count how many records are in each group
        results = TestExecution.prisma().group_by(
            ['startedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TestStepActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TestStep]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TestStep.prisma().query_raw(
            'SELECT * FROM TestStep WHERE id = $1',
            'bgcahjbafj',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TestStep
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TestStep.prisma().query_first(
            'SELECT * FROM TestStep WHERE executionId = $1',
            'bihhgeihca',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TestStepCreateInput,
        include: Optional[types.TestStepInclude] = None
    ) -> _PrismaModelT:
        """Create a new TestStep record.

        Parameters
        ----------
        data
            TestStep record data
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The created TestStep record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TestStep record from just the required fields
        teststep = TestStep.prisma().create(
            data={
                # data to create a TestStep record
                'executionId': 'bdgbfahbef',
                'stepIndex': 522978382,
                'name': 'bdgfdgdaff',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TestStepCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TestStep records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TestStep record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TestStep.prisma().create_many(
            data=[
                {
                    # data to create a TestStep record
                    'executionId': 'bicgeaiaga',
                    'stepIndex': 509770501,
                    'name': 'bfeiccieec',
                },
                {
                    # data to create a TestStep record
                    'executionId': 'hciegiihf',
                    'stepIndex': 107859560,
                    'name': 'baebfehjaf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TestStep record.

        Parameters
        ----------
        where
            TestStep filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The deleted TestStep record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().delete(
            where={
                'id': 'bjchdacjfa',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TestStep record.

        Parameters
        ----------
        where
            TestStep filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The found TestStep record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().find_unique(
            where={
                'id': 'fcadcaihb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TestStep record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TestStep filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The found TestStep record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().find_unique_or_raise(
            where={
                'id': 'bbdcibggga',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
        include: Optional[types.TestStepInclude] = None,
        order: Optional[Union[types.TestStepOrderByInput, List[types.TestStepOrderByInput]]] = None,
        distinct: Optional[List[types.TestStepScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TestStep records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TestStep records returned
        skip
            Ignore the first N results
        where
            TestStep filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestStep model
        order
            Order the returned TestStep records by any field
        distinct
            Filter TestStep records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TestStep]
            The list of all TestStep records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TestStep records
        teststeps = TestStep.prisma().find_many(take=10)

        # find the first 5 TestStep records ordered by the stepIndex field
        teststeps = TestStep.prisma().find_many(
            take=5,
            order={
                'stepIndex': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
        include: Optional[types.TestStepInclude] = None,
        order: Optional[Union[types.TestStepOrderByInput, List[types.TestStepOrderByInput]]] = None,
        distinct: Optional[List[types.TestStepScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TestStep record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestStep filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestStep model
        order
            Order the returned TestStep records by any field
        distinct
            Filter TestStep records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestStep
            The first TestStep record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestStep record ordered by the name field
        teststep = TestStep.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
        include: Optional[types.TestStepInclude] = None,
        order: Optional[Union[types.TestStepOrderByInput, List[types.TestStepOrderByInput]]] = None,
        distinct: Optional[List[types.TestStepScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TestStep record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TestStep filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TestStep model
        order
            Order the returned TestStep records by any field
        distinct
            Filter TestStep records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TestStep
            The first TestStep record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TestStep record ordered by the status field
        teststep = TestStep.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TestStepUpdateInput,
        where: types.TestStepWhereUniqueInput,
        include: Optional[types.TestStepInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TestStep record.

        Parameters
        ----------
        data
            TestStep record data specifying what to update
        where
            TestStep filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The updated TestStep record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        teststep = TestStep.prisma().update(
            where={
                'id': 'bagbibfheb',
            },
            data={
                # data to update the TestStep record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TestStepWhereUniqueInput,
        data: types.TestStepUpsertInput,
        include: Optional[types.TestStepInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TestStep filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TestStep model

        Returns
        -------
        prisma.models.TestStep
            The created or updated TestStep record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teststep = TestStep.prisma().upsert(
            where={
                'id': 'bidgcabjag',
            },
            data={
                'create': {
                    'id': 'bidgcabjag',
                    'executionId': 'hciegiihf',
                    'stepIndex': 107859560,
                    'name': 'baebfehjaf',
                },
                'update': {
                    'executionId': 'hciegiihf',
                    'stepIndex': 107859560,
                    'name': 'baebfehjaf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TestStepUpdateManyMutationInput,
        where: types.TestStepWhereInput,
    ) -> int:
        """Update multiple TestStep records

        Parameters
        ----------
        data
            TestStep data to update the selected TestStep records to
        where
            Filter to select the TestStep records to update

        Returns
        -------
        int
            The total number of TestStep records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TestStep records
        total = TestStep.prisma().update_many(
            data={
                'passed': False
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TestStep records present in the database

        Parameters
        ----------
        select
            Select the TestStep fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestStep filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestStepCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestStep.prisma().count()

        # results: prisma.types.TestStepCountAggregateOutput
        results = TestStep.prisma().count(
            select={
                '_all': True,
                'errorMessage': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TestStepCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
    ) -> types.TestStepCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TestStepCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TestStepWhereInput] = None,
        cursor: Optional[types.TestStepWhereUniqueInput] = None,
    ) -> Union[int, types.TestStepCountAggregateOutput]:
        """Count the number of TestStep records present in the database

        Parameters
        ----------
        select
            Select the TestStep fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TestStep filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TestStepCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TestStep.prisma().count()

        # results: prisma.types.TestStepCountAggregateOutput
        results = TestStep.prisma().count(
            select={
                '_all': True,
                'measurements': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TestStepCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TestStepWhereInput] = None
    ) -> int:
        """Delete multiple TestStep records.

        Parameters
        ----------
        where
            Optional TestStep filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TestStep records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TestStep records
        total = TestStep.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TestStepScalarFieldKeys'],
        *,
        where: Optional['types.TestStepWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TestStepAvgAggregateInput'] = None,
        sum: Optional['types.TestStepSumAggregateInput'] = None,
        min: Optional['types.TestStepMinAggregateInput'] = None,
        max: Optional['types.TestStepMaxAggregateInput'] = None,
        having: Optional['types.TestStepScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TestStepCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TestStepScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TestStepScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TestStepGroupByOutput']:
        """Group TestStep records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TestStep fields to group records by
        where
            TestStep filter to select records
        take
            Limit the maximum number of TestStep records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TestStepGroupByOutput]
            A list of dictionaries representing the TestStep record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TestStep records by logOutput values
        # and count how many records are in each group
        results = TestStep.prisma().group_by(
            ['logOutput'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.User]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = User.prisma().query_raw(
            'SELECT * FROM User WHERE id = $1',
            'cgfjefhci',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.User
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().query_first(
            'SELECT * FROM User WHERE email = $1',
            'egcidbiab',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UserCreateInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Create a new User record.

        Parameters
        ----------
        data
            User record data
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created User record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a User record from just the required fields
        user = User.prisma().create(
            data={
                # data to create a User record
                'email': 'cehjjfgeg',
                'name': 'hiaaaeeeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple User records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of User record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = User.prisma().create_many(
            data=[
                {
                    # data to create a User record
                    'email': 'ediajdeie',
                    'name': 'jdgefgafd',
                },
                {
                    # data to create a User record
                    'email': 'bhhihbfeac',
                    'name': 'bajegedfhj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single User record.

        Parameters
        ----------
        where
            User filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The deleted User record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().delete(
            where={
                'id': 'bhfjihjfhg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique User record.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique(
            where={
                'id': 'iicaadeeb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique User record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique_or_raise(
            where={
                'id': 'bigjggdhac',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple User records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N results
        where
            User filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.User]
            The list of all User records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 User records
        users = User.prisma().find_many(take=10)

        # find the first 5 User records ordered by the name field
        users = User.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single User record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the externalId field
        user = User.prisma().find_first(
            skip=1,
            order={
                'externalId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single User record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the role field
        user = User.prisma().find_first_or_raise(
            skip=1,
            order={
                'role': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UserUpdateInput,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single User record.

        Parameters
        ----------
        data
            User record data specifying what to update
        where
            User filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The updated User record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().update(
            where={
                'id': 'bfdbbbjhad',
            },
            data={
                # data to update the User record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UserWhereUniqueInput,
        data: types.UserUpsertInput,
        include: Optional[types.UserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            User filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created or updated User record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().upsert(
            where={
                'id': 'bdibjhdici',
            },
            data={
                'create': {
                    'id': 'bdibjhdici',
                    'email': 'bhhihbfeac',
                    'name': 'bajegedfhj',
                },
                'update': {
                    'email': 'bhhihbfeac',
                    'name': 'bajegedfhj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UserUpdateManyMutationInput,
        where: types.UserWhereInput,
    ) -> int:
        """Update multiple User records

        Parameters
        ----------
        data
            User data to update the selected User records to
        where
            Filter to select the User records to update

        Returns
        -------
        int
            The total number of User records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all User records
        total = User.prisma().update_many(
            data={
                'permissionSetId': 'bceigehcbd'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'active': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> types.UserCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> Union[int, types.UserCountAggregateOutput]:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'lastSeenAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UserWhereInput] = None
    ) -> int:
        """Delete multiple User records.

        Parameters
        ----------
        where
            Optional User filter to find the records to be deleted

        Returns
        -------
        int
            The total number of User records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all User records
        total = User.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UserScalarFieldKeys'],
        *,
        where: Optional['types.UserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserAvgAggregateInput'] = None,
        sum: Optional['types.UserSumAggregateInput'] = None,
        min: Optional['types.UserMinAggregateInput'] = None,
        max: Optional['types.UserMaxAggregateInput'] = None,
        having: Optional['types.UserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserGroupByOutput']:
        """Group User records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar User fields to group records by
        where
            User filter to select records
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserGroupByOutput]
            A list of dictionaries representing the User record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group User records by createdAt values
        # and count how many records are in each group
        results = User.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ProductAccessActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ProductAccess]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ProductAccess.prisma().query_raw(
            'SELECT * FROM ProductAccess WHERE id = $1',
            'caehahihfe',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ProductAccess
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ProductAccess.prisma().query_first(
            'SELECT * FROM ProductAccess WHERE userId = $1',
            'jjacdhech',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ProductAccessCreateInput,
        include: Optional[types.ProductAccessInclude] = None
    ) -> _PrismaModelT:
        """Create a new ProductAccess record.

        Parameters
        ----------
        data
            ProductAccess record data
        include
            Specifies which relations should be loaded on the returned ProductAccess model

        Returns
        -------
        prisma.models.ProductAccess
            The created ProductAccess record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ProductAccess record from just the required fields
        productaccess = ProductAccess.prisma().create(
            data={
                # data to create a ProductAccess record
                'userId': 'bbbjadabjc',
                'productId': 'hhaechedd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ProductAccessCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ProductAccess records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ProductAccess record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ProductAccess.prisma().create_many(
            data=[
                {
                    # data to create a ProductAccess record
                    'userId': 'gjjgegdic',
                    'productId': 'bijbiccd',
                },
                {
                    # data to create a ProductAccess record
                    'userId': 'cbaagdieci',
                    'productId': 'jchifaegj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ProductAccessWhereUniqueInput,
        include: Optional[types.ProductAccessInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ProductAccess record.

        Parameters
        ----------
        where
            ProductAccess filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ProductAccess model

        Returns
        -------
        prisma.models.ProductAccess
            The deleted ProductAccess record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productaccess = ProductAccess.prisma().delete(
            where={
                'id': 'dhhdhfebi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ProductAccessWhereUniqueInput,
        include: Optional[types.ProductAccessInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ProductAccess record.

        Parameters
        ----------
        where
            ProductAccess filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductAccess model

        Returns
        -------
        prisma.models.ProductAccess
            The found ProductAccess record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productaccess = ProductAccess.prisma().find_unique(
            where={
                'id': 'decchddih',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ProductAccessWhereUniqueInput,
        include: Optional[types.ProductAccessInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ProductAccess record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ProductAccess filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ProductAccess model

        Returns
        -------
        prisma.models.ProductAccess
            The found ProductAccess record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productaccess = ProductAccess.prisma().find_unique_or_raise(
            where={
                'id': 'bbegbdehci',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductAccessWhereInput] = None,
        cursor: Optional[types.ProductAccessWhereUniqueInput] = None,
        include: Optional[types.ProductAccessInclude] = None,
        order: Optional[Union[types.ProductAccessOrderByInput, List[types.ProductAccessOrderByInput]]] = None,
        distinct: Optional[List[types.ProductAccessScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ProductAccess records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ProductAccess records returned
        skip
            Ignore the first N results
        where
            ProductAccess filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductAccess model
        order
            Order the returned ProductAccess records by any field
        distinct
            Filter ProductAccess records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ProductAccess]
            The list of all ProductAccess records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ProductAccess records
        productaccess = ProductAccess.prisma().find_many(take=10)

        # find the first 5 ProductAccess records ordered by the productId field
        productaccess = ProductAccess.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductAccessWhereInput] = None,
        cursor: Optional[types.ProductAccessWhereUniqueInput] = None,
        include: Optional[types.ProductAccessInclude] = None,
        order: Optional[Union[types.ProductAccessOrderByInput, List[types.ProductAccessOrderByInput]]] = None,
        distinct: Optional[List[types.ProductAccessScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ProductAccess record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductAccess filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductAccess model
        order
            Order the returned ProductAccess records by any field
        distinct
            Filter ProductAccess records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductAccess
            The first ProductAccess record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductAccess record ordered by the level field
        productaccess = ProductAccess.prisma().find_first(
            skip=1,
            order={
                'level': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProductAccessWhereInput] = None,
        cursor: Optional[types.ProductAccessWhereUniqueInput] = None,
        include: Optional[types.ProductAccessInclude] = None,
        order: Optional[Union[types.ProductAccessOrderByInput, List[types.ProductAccessOrderByInput]]] = None,
        distinct: Optional[List[types.ProductAccessScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ProductAccess record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ProductAccess filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ProductAccess model
        order
            Order the returned ProductAccess records by any field
        distinct
            Filter ProductAccess records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ProductAccess
            The first ProductAccess record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ProductAccess record ordered by the createdAt field
        productaccess = ProductAccess.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ProductAccessUpdateInput,
        where: types.ProductAccessWhereUniqueInput,
        include: Optional[types.ProductAccessInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ProductAccess record.

        Parameters
        ----------
        data
            ProductAccess record data specifying what to update
        where
            ProductAccess filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ProductAccess model

        Returns
        -------
        prisma.models.ProductAccess
            The updated ProductAccess record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        productaccess = ProductAccess.prisma().update(
            where={
                'id': 'bcebgbhhgb',
            },
            data={
                # data to update the ProductAccess record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ProductAccessWhereUniqueInput,
        data: types.ProductAccessUpsertInput,
        include: Optional[types.ProductAccessInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ProductAccess filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ProductAccess model

        Returns
        -------
        prisma.models.ProductAccess
            The created or updated ProductAccess record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        productaccess = ProductAccess.prisma().upsert(
            where={
                'id': 'ghegfaceg',
            },
            data={
                'create': {
                    'id': 'ghegfaceg',
                    'userId': 'cbaagdieci',
                    'productId': 'jchifaegj',
                },
                'update': {
                    'userId': 'cbaagdieci',
                    'productId': 'jchifaegj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ProductAccessUpdateManyMutationInput,
        where: types.ProductAccessWhereInput,
    ) -> int:
        """Update multiple ProductAccess records

        Parameters
        ----------
        data
            ProductAccess data to update the selected ProductAccess records to
        where
            Filter to select the ProductAccess records to update

        Returns
        -------
        int
            The total number of ProductAccess records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ProductAccess records
        total = ProductAccess.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductAccessWhereInput] = None,
        cursor: Optional[types.ProductAccessWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ProductAccess records present in the database

        Parameters
        ----------
        select
            Select the ProductAccess fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductAccess filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductAccessCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductAccess.prisma().count()

        # results: prisma.types.ProductAccessCountAggregateOutput
        results = ProductAccess.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ProductAccessCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductAccessWhereInput] = None,
        cursor: Optional[types.ProductAccessWhereUniqueInput] = None,
    ) -> types.ProductAccessCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ProductAccessCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProductAccessWhereInput] = None,
        cursor: Optional[types.ProductAccessWhereUniqueInput] = None,
    ) -> Union[int, types.ProductAccessCountAggregateOutput]:
        """Count the number of ProductAccess records present in the database

        Parameters
        ----------
        select
            Select the ProductAccess fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ProductAccess filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProductAccessCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ProductAccess.prisma().count()

        # results: prisma.types.ProductAccessCountAggregateOutput
        results = ProductAccess.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProductAccessCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ProductAccessWhereInput] = None
    ) -> int:
        """Delete multiple ProductAccess records.

        Parameters
        ----------
        where
            Optional ProductAccess filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ProductAccess records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ProductAccess records
        total = ProductAccess.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ProductAccessScalarFieldKeys'],
        *,
        where: Optional['types.ProductAccessWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProductAccessAvgAggregateInput'] = None,
        sum: Optional['types.ProductAccessSumAggregateInput'] = None,
        min: Optional['types.ProductAccessMinAggregateInput'] = None,
        max: Optional['types.ProductAccessMaxAggregateInput'] = None,
        having: Optional['types.ProductAccessScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProductAccessCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProductAccessScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProductAccessScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProductAccessGroupByOutput']:
        """Group ProductAccess records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ProductAccess fields to group records by
        where
            ProductAccess filter to select records
        take
            Limit the maximum number of ProductAccess records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProductAccessGroupByOutput]
            A list of dictionaries representing the ProductAccess record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ProductAccess records by productId values
        # and count how many records are in each group
        results = ProductAccess.prisma().group_by(
            ['productId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PermissionSetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PermissionSet]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PermissionSet.prisma().query_raw(
            'SELECT * FROM PermissionSet WHERE id = $1',
            'hdeaejgah',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PermissionSet
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PermissionSet.prisma().query_first(
            'SELECT * FROM PermissionSet WHERE name = $1',
            'ddiiihaci',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PermissionSetCreateInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> _PrismaModelT:
        """Create a new PermissionSet record.

        Parameters
        ----------
        data
            PermissionSet record data
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The created PermissionSet record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PermissionSet record from just the required fields
        permissionset = PermissionSet.prisma().create(
            data={
                # data to create a PermissionSet record
                'name': 'jdhfacdgf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PermissionSetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PermissionSet records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PermissionSet record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PermissionSet.prisma().create_many(
            data=[
                {
                    # data to create a PermissionSet record
                    'name': 'fcbbahcbi',
                },
                {
                    # data to create a PermissionSet record
                    'name': 'edcgchech',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PermissionSet record.

        Parameters
        ----------
        where
            PermissionSet filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The deleted PermissionSet record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().delete(
            where={
                'id': 'bfehccdcge',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PermissionSet record.

        Parameters
        ----------
        where
            PermissionSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The found PermissionSet record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().find_unique(
            where={
                'id': 'ehcibdegf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PermissionSet record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PermissionSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The found PermissionSet record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().find_unique_or_raise(
            where={
                'id': 'ebhjifbbi',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
        include: Optional[types.PermissionSetInclude] = None,
        order: Optional[Union[types.PermissionSetOrderByInput, List[types.PermissionSetOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionSetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PermissionSet records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PermissionSet records returned
        skip
            Ignore the first N results
        where
            PermissionSet filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PermissionSet model
        order
            Order the returned PermissionSet records by any field
        distinct
            Filter PermissionSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PermissionSet]
            The list of all PermissionSet records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PermissionSet records
        permissionsets = PermissionSet.prisma().find_many(take=10)

        # find the first 5 PermissionSet records ordered by the description field
        permissionsets = PermissionSet.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
        include: Optional[types.PermissionSetInclude] = None,
        order: Optional[Union[types.PermissionSetOrderByInput, List[types.PermissionSetOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionSetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PermissionSet record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PermissionSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PermissionSet model
        order
            Order the returned PermissionSet records by any field
        distinct
            Filter PermissionSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PermissionSet
            The first PermissionSet record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PermissionSet record ordered by the permissions field
        permissionset = PermissionSet.prisma().find_first(
            skip=1,
            order={
                'permissions': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
        include: Optional[types.PermissionSetInclude] = None,
        order: Optional[Union[types.PermissionSetOrderByInput, List[types.PermissionSetOrderByInput]]] = None,
        distinct: Optional[List[types.PermissionSetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PermissionSet record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PermissionSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PermissionSet model
        order
            Order the returned PermissionSet records by any field
        distinct
            Filter PermissionSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PermissionSet
            The first PermissionSet record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PermissionSet record ordered by the createdAt field
        permissionset = PermissionSet.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PermissionSetUpdateInput,
        where: types.PermissionSetWhereUniqueInput,
        include: Optional[types.PermissionSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PermissionSet record.

        Parameters
        ----------
        data
            PermissionSet record data specifying what to update
        where
            PermissionSet filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The updated PermissionSet record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().update(
            where={
                'id': 'jaigficdj',
            },
            data={
                # data to update the PermissionSet record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PermissionSetWhereUniqueInput,
        data: types.PermissionSetUpsertInput,
        include: Optional[types.PermissionSetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PermissionSet filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PermissionSet model

        Returns
        -------
        prisma.models.PermissionSet
            The created or updated PermissionSet record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        permissionset = PermissionSet.prisma().upsert(
            where={
                'id': 'bggcfifgbc',
            },
            data={
                'create': {
                    'id': 'bggcfifgbc',
                    'name': 'edcgchech',
                },
                'update': {
                    'name': 'edcgchech',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PermissionSetUpdateManyMutationInput,
        where: types.PermissionSetWhereInput,
    ) -> int:
        """Update multiple PermissionSet records

        Parameters
        ----------
        data
            PermissionSet data to update the selected PermissionSet records to
        where
            Filter to select the PermissionSet records to update

        Returns
        -------
        int
            The total number of PermissionSet records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PermissionSet records
        total = PermissionSet.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PermissionSet records present in the database

        Parameters
        ----------
        select
            Select the PermissionSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PermissionSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PermissionSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PermissionSet.prisma().count()

        # results: prisma.types.PermissionSetCountAggregateOutput
        results = PermissionSet.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PermissionSetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
    ) -> types.PermissionSetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PermissionSetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PermissionSetWhereInput] = None,
        cursor: Optional[types.PermissionSetWhereUniqueInput] = None,
    ) -> Union[int, types.PermissionSetCountAggregateOutput]:
        """Count the number of PermissionSet records present in the database

        Parameters
        ----------
        select
            Select the PermissionSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PermissionSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PermissionSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PermissionSet.prisma().count()

        # results: prisma.types.PermissionSetCountAggregateOutput
        results = PermissionSet.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PermissionSetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PermissionSetWhereInput] = None
    ) -> int:
        """Delete multiple PermissionSet records.

        Parameters
        ----------
        where
            Optional PermissionSet filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PermissionSet records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PermissionSet records
        total = PermissionSet.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PermissionSetScalarFieldKeys'],
        *,
        where: Optional['types.PermissionSetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PermissionSetAvgAggregateInput'] = None,
        sum: Optional['types.PermissionSetSumAggregateInput'] = None,
        min: Optional['types.PermissionSetMinAggregateInput'] = None,
        max: Optional['types.PermissionSetMaxAggregateInput'] = None,
        having: Optional['types.PermissionSetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PermissionSetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PermissionSetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PermissionSetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PermissionSetGroupByOutput']:
        """Group PermissionSet records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PermissionSet fields to group records by
        where
            PermissionSet filter to select records
        take
            Limit the maximum number of PermissionSet records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PermissionSetGroupByOutput]
            A list of dictionaries representing the PermissionSet record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PermissionSet records by description values
        # and count how many records are in each group
        results = PermissionSet.prisma().group_by(
            ['description'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ApiKeyActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ApiKey]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ApiKey.prisma().query_raw(
            'SELECT * FROM ApiKey WHERE id = $1',
            'bhidfjibgf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ApiKey
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ApiKey.prisma().query_first(
            'SELECT * FROM ApiKey WHERE name = $1',
            'fcgdaijha',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ApiKeyCreateInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> _PrismaModelT:
        """Create a new ApiKey record.

        Parameters
        ----------
        data
            ApiKey record data
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The created ApiKey record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ApiKey record from just the required fields
        apikey = ApiKey.prisma().create(
            data={
                # data to create a ApiKey record
                'name': 'bbgfajah',
                'keyHash': 'bbaecgicia',
                'keyPrefix': 'hedacahfd',
                'userId': 'bhgjecjac',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ApiKeyCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ApiKey records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ApiKey record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ApiKey.prisma().create_many(
            data=[
                {
                    # data to create a ApiKey record
                    'name': 'bfbdieidhf',
                    'keyHash': 'ffjbdcfia',
                    'keyPrefix': 'hfeidjicb',
                    'userId': 'bbfbdccdag',
                },
                {
                    # data to create a ApiKey record
                    'name': 'biggefaged',
                    'keyHash': 'gfdbeaejg',
                    'keyPrefix': 'bhacdebcb',
                    'userId': 'jhffeibhe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ApiKey record.

        Parameters
        ----------
        where
            ApiKey filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The deleted ApiKey record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().delete(
            where={
                'id': 'iaiieefjb',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ApiKey record.

        Parameters
        ----------
        where
            ApiKey filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The found ApiKey record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().find_unique(
            where={
                'id': 'jjhjdgjfg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ApiKey record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ApiKey filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The found ApiKey record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().find_unique_or_raise(
            where={
                'id': 'bhhjcfadhd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
        include: Optional[types.ApiKeyInclude] = None,
        order: Optional[Union[types.ApiKeyOrderByInput, List[types.ApiKeyOrderByInput]]] = None,
        distinct: Optional[List[types.ApiKeyScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ApiKey records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ApiKey records returned
        skip
            Ignore the first N results
        where
            ApiKey filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ApiKey model
        order
            Order the returned ApiKey records by any field
        distinct
            Filter ApiKey records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ApiKey]
            The list of all ApiKey records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ApiKey records
        apikeys = ApiKey.prisma().find_many(take=10)

        # find the first 5 ApiKey records ordered by the keyHash field
        apikeys = ApiKey.prisma().find_many(
            take=5,
            order={
                'keyHash': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
        include: Optional[types.ApiKeyInclude] = None,
        order: Optional[Union[types.ApiKeyOrderByInput, List[types.ApiKeyOrderByInput]]] = None,
        distinct: Optional[List[types.ApiKeyScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ApiKey record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ApiKey filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ApiKey model
        order
            Order the returned ApiKey records by any field
        distinct
            Filter ApiKey records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ApiKey
            The first ApiKey record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ApiKey record ordered by the keyPrefix field
        apikey = ApiKey.prisma().find_first(
            skip=1,
            order={
                'keyPrefix': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
        include: Optional[types.ApiKeyInclude] = None,
        order: Optional[Union[types.ApiKeyOrderByInput, List[types.ApiKeyOrderByInput]]] = None,
        distinct: Optional[List[types.ApiKeyScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ApiKey record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ApiKey filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ApiKey model
        order
            Order the returned ApiKey records by any field
        distinct
            Filter ApiKey records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ApiKey
            The first ApiKey record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ApiKey record ordered by the userId field
        apikey = ApiKey.prisma().find_first_or_raise(
            skip=1,
            order={
                'userId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ApiKeyUpdateInput,
        where: types.ApiKeyWhereUniqueInput,
        include: Optional[types.ApiKeyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ApiKey record.

        Parameters
        ----------
        data
            ApiKey record data specifying what to update
        where
            ApiKey filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The updated ApiKey record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        apikey = ApiKey.prisma().update(
            where={
                'id': 'fbejadda',
            },
            data={
                # data to update the ApiKey record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ApiKeyWhereUniqueInput,
        data: types.ApiKeyUpsertInput,
        include: Optional[types.ApiKeyInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ApiKey filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ApiKey model

        Returns
        -------
        prisma.models.ApiKey
            The created or updated ApiKey record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        apikey = ApiKey.prisma().upsert(
            where={
                'id': 'bjeaigeddb',
            },
            data={
                'create': {
                    'id': 'bjeaigeddb',
                    'name': 'biggefaged',
                    'keyHash': 'gfdbeaejg',
                    'keyPrefix': 'bhacdebcb',
                    'userId': 'jhffeibhe',
                },
                'update': {
                    'name': 'biggefaged',
                    'keyHash': 'gfdbeaejg',
                    'keyPrefix': 'bhacdebcb',
                    'userId': 'jhffeibhe',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ApiKeyUpdateManyMutationInput,
        where: types.ApiKeyWhereInput,
    ) -> int:
        """Update multiple ApiKey records

        Parameters
        ----------
        data
            ApiKey data to update the selected ApiKey records to
        where
            Filter to select the ApiKey records to update

        Returns
        -------
        int
            The total number of ApiKey records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ApiKey records
        total = ApiKey.prisma().update_many(
            data={
                'expiresAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ApiKey records present in the database

        Parameters
        ----------
        select
            Select the ApiKey fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ApiKey filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ApiKeyCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ApiKey.prisma().count()

        # results: prisma.types.ApiKeyCountAggregateOutput
        results = ApiKey.prisma().count(
            select={
                '_all': True,
                'lastUsedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ApiKeyCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
    ) -> types.ApiKeyCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ApiKeyCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ApiKeyWhereInput] = None,
        cursor: Optional[types.ApiKeyWhereUniqueInput] = None,
    ) -> Union[int, types.ApiKeyCountAggregateOutput]:
        """Count the number of ApiKey records present in the database

        Parameters
        ----------
        select
            Select the ApiKey fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ApiKey filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ApiKeyCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ApiKey.prisma().count()

        # results: prisma.types.ApiKeyCountAggregateOutput
        results = ApiKey.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ApiKeyCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ApiKeyWhereInput] = None
    ) -> int:
        """Delete multiple ApiKey records.

        Parameters
        ----------
        where
            Optional ApiKey filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ApiKey records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ApiKey records
        total = ApiKey.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ApiKeyScalarFieldKeys'],
        *,
        where: Optional['types.ApiKeyWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ApiKeyAvgAggregateInput'] = None,
        sum: Optional['types.ApiKeySumAggregateInput'] = None,
        min: Optional['types.ApiKeyMinAggregateInput'] = None,
        max: Optional['types.ApiKeyMaxAggregateInput'] = None,
        having: Optional['types.ApiKeyScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ApiKeyCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ApiKeyScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ApiKeyScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ApiKeyGroupByOutput']:
        """Group ApiKey records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ApiKey fields to group records by
        where
            ApiKey filter to select records
        take
            Limit the maximum number of ApiKey records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ApiKeyGroupByOutput]
            A list of dictionaries representing the ApiKey record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ApiKey records by id values
        # and count how many records are in each group
        results = ApiKey.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AuditLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AuditLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = AuditLog.prisma().query_raw(
            'SELECT * FROM AuditLog WHERE id = $1',
            'eceecabdg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AuditLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = AuditLog.prisma().query_first(
            'SELECT * FROM AuditLog WHERE userId = $1',
            'bdgaajbcab',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.AuditLogCreateInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new AuditLog record.

        Parameters
        ----------
        data
            AuditLog record data
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The created AuditLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AuditLog record from just the required fields
        auditlog = AuditLog.prisma().create(
            data={
                # data to create a AuditLog record
                'action': 'chhgehieg',
                'entityType': 'faggbiggd',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.AuditLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AuditLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AuditLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = AuditLog.prisma().create_many(
            data=[
                {
                    # data to create a AuditLog record
                    'action': 'bciijiajei',
                    'entityType': 'bdjaadchgf',
                },
                {
                    # data to create a AuditLog record
                    'action': 'biadfeebhi',
                    'entityType': 'biajdcaged',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AuditLog record.

        Parameters
        ----------
        where
            AuditLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The deleted AuditLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().delete(
            where={
                'id': 'ddgjjicai',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AuditLog record.

        Parameters
        ----------
        where
            AuditLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The found AuditLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().find_unique(
            where={
                'id': 'efihdjdcb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AuditLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AuditLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The found AuditLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().find_unique_or_raise(
            where={
                'id': 'bdeacajaig',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
        include: Optional[types.AuditLogInclude] = None,
        order: Optional[Union[types.AuditLogOrderByInput, List[types.AuditLogOrderByInput]]] = None,
        distinct: Optional[List[types.AuditLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AuditLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AuditLog records returned
        skip
            Ignore the first N results
        where
            AuditLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AuditLog model
        order
            Order the returned AuditLog records by any field
        distinct
            Filter AuditLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AuditLog]
            The list of all AuditLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AuditLog records
        auditlogs = AuditLog.prisma().find_many(take=10)

        # find the first 5 AuditLog records ordered by the action field
        auditlogs = AuditLog.prisma().find_many(
            take=5,
            order={
                'action': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
        include: Optional[types.AuditLogInclude] = None,
        order: Optional[Union[types.AuditLogOrderByInput, List[types.AuditLogOrderByInput]]] = None,
        distinct: Optional[List[types.AuditLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AuditLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AuditLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AuditLog model
        order
            Order the returned AuditLog records by any field
        distinct
            Filter AuditLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AuditLog
            The first AuditLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AuditLog record ordered by the entityType field
        auditlog = AuditLog.prisma().find_first(
            skip=1,
            order={
                'entityType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
        include: Optional[types.AuditLogInclude] = None,
        order: Optional[Union[types.AuditLogOrderByInput, List[types.AuditLogOrderByInput]]] = None,
        distinct: Optional[List[types.AuditLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AuditLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AuditLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AuditLog model
        order
            Order the returned AuditLog records by any field
        distinct
            Filter AuditLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AuditLog
            The first AuditLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AuditLog record ordered by the entityId field
        auditlog = AuditLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'entityId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.AuditLogUpdateInput,
        where: types.AuditLogWhereUniqueInput,
        include: Optional[types.AuditLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AuditLog record.

        Parameters
        ----------
        data
            AuditLog record data specifying what to update
        where
            AuditLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The updated AuditLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().update(
            where={
                'id': 'jddggchfb',
            },
            data={
                # data to update the AuditLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.AuditLogWhereUniqueInput,
        data: types.AuditLogUpsertInput,
        include: Optional[types.AuditLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AuditLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AuditLog model

        Returns
        -------
        prisma.models.AuditLog
            The created or updated AuditLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        auditlog = AuditLog.prisma().upsert(
            where={
                'id': 'bccbfcfdfg',
            },
            data={
                'create': {
                    'id': 'bccbfcfdfg',
                    'action': 'biadfeebhi',
                    'entityType': 'biajdcaged',
                },
                'update': {
                    'action': 'biadfeebhi',
                    'entityType': 'biajdcaged',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.AuditLogUpdateManyMutationInput,
        where: types.AuditLogWhereInput,
    ) -> int:
        """Update multiple AuditLog records

        Parameters
        ----------
        data
            AuditLog data to update the selected AuditLog records to
        where
            Filter to select the AuditLog records to update

        Returns
        -------
        int
            The total number of AuditLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AuditLog records
        total = AuditLog.prisma().update_many(
            data={
                'details': Json({'bjchjgjib': True})
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AuditLog records present in the database

        Parameters
        ----------
        select
            Select the AuditLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AuditLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AuditLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = AuditLog.prisma().count()

        # results: prisma.types.AuditLogCountAggregateOutput
        results = AuditLog.prisma().count(
            select={
                '_all': True,
                'ipAddress': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.AuditLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
    ) -> types.AuditLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.AuditLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AuditLogWhereInput] = None,
        cursor: Optional[types.AuditLogWhereUniqueInput] = None,
    ) -> Union[int, types.AuditLogCountAggregateOutput]:
        """Count the number of AuditLog records present in the database

        Parameters
        ----------
        select
            Select the AuditLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AuditLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AuditLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = AuditLog.prisma().count()

        # results: prisma.types.AuditLogCountAggregateOutput
        results = AuditLog.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AuditLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.AuditLogWhereInput] = None
    ) -> int:
        """Delete multiple AuditLog records.

        Parameters
        ----------
        where
            Optional AuditLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AuditLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AuditLog records
        total = AuditLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.AuditLogScalarFieldKeys'],
        *,
        where: Optional['types.AuditLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AuditLogAvgAggregateInput'] = None,
        sum: Optional['types.AuditLogSumAggregateInput'] = None,
        min: Optional['types.AuditLogMinAggregateInput'] = None,
        max: Optional['types.AuditLogMaxAggregateInput'] = None,
        having: Optional['types.AuditLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AuditLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AuditLogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AuditLogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AuditLogGroupByOutput']:
        """Group AuditLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AuditLog fields to group records by
        where
            AuditLog filter to select records
        take
            Limit the maximum number of AuditLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AuditLogGroupByOutput]
            A list of dictionaries representing the AuditLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AuditLog records by id values
        # and count how many records are in each group
        results = AuditLog.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SecretActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Secret]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Secret.prisma().query_raw(
            'SELECT * FROM Secret WHERE id = $1',
            'ibiaaifcc',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Secret
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Secret.prisma().query_first(
            'SELECT * FROM Secret WHERE name = $1',
            'bffejbbadf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.SecretCreateInput,
        include: Optional[types.SecretInclude] = None
    ) -> _PrismaModelT:
        """Create a new Secret record.

        Parameters
        ----------
        data
            Secret record data
        include
            Specifies which relations should be loaded on the returned Secret model

        Returns
        -------
        prisma.models.Secret
            The created Secret record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Secret record from just the required fields
        secret = Secret.prisma().create(
            data={
                # data to create a Secret record
                'name': 'bajgjiebfc',
                'type': 'bhahahadif',
                'value': 'bijhaihbcg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.SecretCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Secret records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Secret record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Secret.prisma().create_many(
            data=[
                {
                    # data to create a Secret record
                    'name': 'cbgcjeecd',
                    'type': 'bebibighfg',
                    'value': 'bbhjeejidh',
                },
                {
                    # data to create a Secret record
                    'name': 'bfgebdfefi',
                    'type': 'jgijgagjb',
                    'value': 'biagjfcfga',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.SecretWhereUniqueInput,
        include: Optional[types.SecretInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Secret record.

        Parameters
        ----------
        where
            Secret filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Secret model

        Returns
        -------
        prisma.models.Secret
            The deleted Secret record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        secret = Secret.prisma().delete(
            where={
                'id': 'bggbhejccf',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.SecretWhereUniqueInput,
        include: Optional[types.SecretInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Secret record.

        Parameters
        ----------
        where
            Secret filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Secret model

        Returns
        -------
        prisma.models.Secret
            The found Secret record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        secret = Secret.prisma().find_unique(
            where={
                'id': 'ichjgigcc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.SecretWhereUniqueInput,
        include: Optional[types.SecretInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Secret record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Secret filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Secret model

        Returns
        -------
        prisma.models.Secret
            The found Secret record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        secret = Secret.prisma().find_unique_or_raise(
            where={
                'id': 'bhiiegaibf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecretWhereInput] = None,
        cursor: Optional[types.SecretWhereUniqueInput] = None,
        include: Optional[types.SecretInclude] = None,
        order: Optional[Union[types.SecretOrderByInput, List[types.SecretOrderByInput]]] = None,
        distinct: Optional[List[types.SecretScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Secret records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Secret records returned
        skip
            Ignore the first N results
        where
            Secret filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Secret model
        order
            Order the returned Secret records by any field
        distinct
            Filter Secret records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Secret]
            The list of all Secret records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Secret records
        secrets = Secret.prisma().find_many(take=10)

        # find the first 5 Secret records ordered by the type field
        secrets = Secret.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SecretWhereInput] = None,
        cursor: Optional[types.SecretWhereUniqueInput] = None,
        include: Optional[types.SecretInclude] = None,
        order: Optional[Union[types.SecretOrderByInput, List[types.SecretOrderByInput]]] = None,
        distinct: Optional[List[types.SecretScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Secret record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Secret filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Secret model
        order
            Order the returned Secret records by any field
        distinct
            Filter Secret records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Secret
            The first Secret record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Secret record ordered by the value field
        secret = Secret.prisma().find_first(
            skip=1,
            order={
                'value': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SecretWhereInput] = None,
        cursor: Optional[types.SecretWhereUniqueInput] = None,
        include: Optional[types.SecretInclude] = None,
        order: Optional[Union[types.SecretOrderByInput, List[types.SecretOrderByInput]]] = None,
        distinct: Optional[List[types.SecretScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Secret record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Secret filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Secret model
        order
            Order the returned Secret records by any field
        distinct
            Filter Secret records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Secret
            The first Secret record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Secret record ordered by the description field
        secret = Secret.prisma().find_first_or_raise(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.SecretUpdateInput,
        where: types.SecretWhereUniqueInput,
        include: Optional[types.SecretInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Secret record.

        Parameters
        ----------
        data
            Secret record data specifying what to update
        where
            Secret filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Secret model

        Returns
        -------
        prisma.models.Secret
            The updated Secret record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        secret = Secret.prisma().update(
            where={
                'id': 'bdegidheae',
            },
            data={
                # data to update the Secret record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.SecretWhereUniqueInput,
        data: types.SecretUpsertInput,
        include: Optional[types.SecretInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Secret filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Secret model

        Returns
        -------
        prisma.models.Secret
            The created or updated Secret record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        secret = Secret.prisma().upsert(
            where={
                'id': 'bddbbjidhd',
            },
            data={
                'create': {
                    'id': 'bddbbjidhd',
                    'name': 'bfgebdfefi',
                    'type': 'jgijgagjb',
                    'value': 'biagjfcfga',
                },
                'update': {
                    'name': 'bfgebdfefi',
                    'type': 'jgijgagjb',
                    'value': 'biagjfcfga',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.SecretUpdateManyMutationInput,
        where: types.SecretWhereInput,
    ) -> int:
        """Update multiple Secret records

        Parameters
        ----------
        data
            Secret data to update the selected Secret records to
        where
            Filter to select the Secret records to update

        Returns
        -------
        int
            The total number of Secret records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Secret records
        total = Secret.prisma().update_many(
            data={
                'createdById': 'jefedjaji'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecretWhereInput] = None,
        cursor: Optional[types.SecretWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Secret records present in the database

        Parameters
        ----------
        select
            Select the Secret fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Secret filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SecretCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Secret.prisma().count()

        # results: prisma.types.SecretCountAggregateOutput
        results = Secret.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.SecretCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecretWhereInput] = None,
        cursor: Optional[types.SecretWhereUniqueInput] = None,
    ) -> types.SecretCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.SecretCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecretWhereInput] = None,
        cursor: Optional[types.SecretWhereUniqueInput] = None,
    ) -> Union[int, types.SecretCountAggregateOutput]:
        """Count the number of Secret records present in the database

        Parameters
        ----------
        select
            Select the Secret fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Secret filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SecretCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Secret.prisma().count()

        # results: prisma.types.SecretCountAggregateOutput
        results = Secret.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SecretCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.SecretWhereInput] = None
    ) -> int:
        """Delete multiple Secret records.

        Parameters
        ----------
        where
            Optional Secret filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Secret records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Secret records
        total = Secret.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.SecretScalarFieldKeys'],
        *,
        where: Optional['types.SecretWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SecretAvgAggregateInput'] = None,
        sum: Optional['types.SecretSumAggregateInput'] = None,
        min: Optional['types.SecretMinAggregateInput'] = None,
        max: Optional['types.SecretMaxAggregateInput'] = None,
        having: Optional['types.SecretScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SecretCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SecretScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SecretScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SecretGroupByOutput']:
        """Group Secret records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Secret fields to group records by
        where
            Secret filter to select records
        take
            Limit the maximum number of Secret records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SecretGroupByOutput]
            A list of dictionaries representing the Secret record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Secret records by id values
        # and count how many records are in each group
        results = Secret.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PollCacheActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PollCache]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = PollCache.prisma().query_raw(
            'SELECT * FROM PollCache WHERE id = $1',
            'eicbgeabb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PollCache
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = PollCache.prisma().query_first(
            'SELECT * FROM PollCache WHERE repoSlug = $1',
            'fchjhjjbc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PollCacheCreateInput,
        include: Optional[types.PollCacheInclude] = None
    ) -> _PrismaModelT:
        """Create a new PollCache record.

        Parameters
        ----------
        data
            PollCache record data
        include
            Specifies which relations should be loaded on the returned PollCache model

        Returns
        -------
        prisma.models.PollCache
            The created PollCache record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PollCache record from just the required fields
        pollcache = PollCache.prisma().create(
            data={
                # data to create a PollCache record
                'repoSlug': 'bagfajdjcb',
                'prId': 1337654086,
                'commitSha': 'bfgchaiff',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PollCacheCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PollCache records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PollCache record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = PollCache.prisma().create_many(
            data=[
                {
                    # data to create a PollCache record
                    'repoSlug': 'cifcjfbfg',
                    'prId': 132986973,
                    'commitSha': 'bfefdahgac',
                },
                {
                    # data to create a PollCache record
                    'repoSlug': 'bichcieged',
                    'prId': 2089438624,
                    'commitSha': 'bebebdfbcj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PollCacheWhereUniqueInput,
        include: Optional[types.PollCacheInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PollCache record.

        Parameters
        ----------
        where
            PollCache filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PollCache model

        Returns
        -------
        prisma.models.PollCache
            The deleted PollCache record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pollcache = PollCache.prisma().delete(
            where={
                'id': 'caffheehji',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PollCacheWhereUniqueInput,
        include: Optional[types.PollCacheInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PollCache record.

        Parameters
        ----------
        where
            PollCache filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PollCache model

        Returns
        -------
        prisma.models.PollCache
            The found PollCache record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pollcache = PollCache.prisma().find_unique(
            where={
                'id': 'hfdidadjj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PollCacheWhereUniqueInput,
        include: Optional[types.PollCacheInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PollCache record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PollCache filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PollCache model

        Returns
        -------
        prisma.models.PollCache
            The found PollCache record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pollcache = PollCache.prisma().find_unique_or_raise(
            where={
                'id': 'bcdiiadeae',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PollCacheWhereInput] = None,
        cursor: Optional[types.PollCacheWhereUniqueInput] = None,
        include: Optional[types.PollCacheInclude] = None,
        order: Optional[Union[types.PollCacheOrderByInput, List[types.PollCacheOrderByInput]]] = None,
        distinct: Optional[List[types.PollCacheScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PollCache records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PollCache records returned
        skip
            Ignore the first N results
        where
            PollCache filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PollCache model
        order
            Order the returned PollCache records by any field
        distinct
            Filter PollCache records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PollCache]
            The list of all PollCache records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PollCache records
        pollcaches = PollCache.prisma().find_many(take=10)

        # find the first 5 PollCache records ordered by the prId field
        pollcaches = PollCache.prisma().find_many(
            take=5,
            order={
                'prId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PollCacheWhereInput] = None,
        cursor: Optional[types.PollCacheWhereUniqueInput] = None,
        include: Optional[types.PollCacheInclude] = None,
        order: Optional[Union[types.PollCacheOrderByInput, List[types.PollCacheOrderByInput]]] = None,
        distinct: Optional[List[types.PollCacheScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PollCache record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PollCache filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PollCache model
        order
            Order the returned PollCache records by any field
        distinct
            Filter PollCache records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PollCache
            The first PollCache record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PollCache record ordered by the commitSha field
        pollcache = PollCache.prisma().find_first(
            skip=1,
            order={
                'commitSha': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PollCacheWhereInput] = None,
        cursor: Optional[types.PollCacheWhereUniqueInput] = None,
        include: Optional[types.PollCacheInclude] = None,
        order: Optional[Union[types.PollCacheOrderByInput, List[types.PollCacheOrderByInput]]] = None,
        distinct: Optional[List[types.PollCacheScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PollCache record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PollCache filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PollCache model
        order
            Order the returned PollCache records by any field
        distinct
            Filter PollCache records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PollCache
            The first PollCache record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PollCache record ordered by the createdAt field
        pollcache = PollCache.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PollCacheUpdateInput,
        where: types.PollCacheWhereUniqueInput,
        include: Optional[types.PollCacheInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PollCache record.

        Parameters
        ----------
        data
            PollCache record data specifying what to update
        where
            PollCache filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PollCache model

        Returns
        -------
        prisma.models.PollCache
            The updated PollCache record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        pollcache = PollCache.prisma().update(
            where={
                'id': 'bdhhaeiaif',
            },
            data={
                # data to update the PollCache record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PollCacheWhereUniqueInput,
        data: types.PollCacheUpsertInput,
        include: Optional[types.PollCacheInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PollCache filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PollCache model

        Returns
        -------
        prisma.models.PollCache
            The created or updated PollCache record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        pollcache = PollCache.prisma().upsert(
            where={
                'id': 'caffgabefa',
            },
            data={
                'create': {
                    'id': 'caffgabefa',
                    'repoSlug': 'bichcieged',
                    'prId': 2089438624,
                    'commitSha': 'bebebdfbcj',
                },
                'update': {
                    'repoSlug': 'bichcieged',
                    'prId': 2089438624,
                    'commitSha': 'bebebdfbcj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PollCacheUpdateManyMutationInput,
        where: types.PollCacheWhereInput,
    ) -> int:
        """Update multiple PollCache records

        Parameters
        ----------
        data
            PollCache data to update the selected PollCache records to
        where
            Filter to select the PollCache records to update

        Returns
        -------
        int
            The total number of PollCache records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PollCache records
        total = PollCache.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PollCacheWhereInput] = None,
        cursor: Optional[types.PollCacheWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PollCache records present in the database

        Parameters
        ----------
        select
            Select the PollCache fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PollCache filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PollCacheCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PollCache.prisma().count()

        # results: prisma.types.PollCacheCountAggregateOutput
        results = PollCache.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PollCacheCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PollCacheWhereInput] = None,
        cursor: Optional[types.PollCacheWhereUniqueInput] = None,
    ) -> types.PollCacheCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PollCacheCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PollCacheWhereInput] = None,
        cursor: Optional[types.PollCacheWhereUniqueInput] = None,
    ) -> Union[int, types.PollCacheCountAggregateOutput]:
        """Count the number of PollCache records present in the database

        Parameters
        ----------
        select
            Select the PollCache fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PollCache filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PollCacheCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = PollCache.prisma().count()

        # results: prisma.types.PollCacheCountAggregateOutput
        results = PollCache.prisma().count(
            select={
                '_all': True,
                'repoSlug': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PollCacheCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PollCacheWhereInput] = None
    ) -> int:
        """Delete multiple PollCache records.

        Parameters
        ----------
        where
            Optional PollCache filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PollCache records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PollCache records
        total = PollCache.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PollCacheScalarFieldKeys'],
        *,
        where: Optional['types.PollCacheWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PollCacheAvgAggregateInput'] = None,
        sum: Optional['types.PollCacheSumAggregateInput'] = None,
        min: Optional['types.PollCacheMinAggregateInput'] = None,
        max: Optional['types.PollCacheMaxAggregateInput'] = None,
        having: Optional['types.PollCacheScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PollCacheCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PollCacheScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PollCacheScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PollCacheGroupByOutput']:
        """Group PollCache records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PollCache fields to group records by
        where
            PollCache filter to select records
        take
            Limit the maximum number of PollCache records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PollCacheGroupByOutput]
            A list of dictionaries representing the PollCache record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PollCache records by prId values
        # and count how many records are in each group
        results = PollCache.prisma().group_by(
            ['prId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class RecipeVersionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.RecipeVersion]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = RecipeVersion.prisma().query_raw(
            'SELECT * FROM RecipeVersion WHERE id = $1',
            'bjjajgjbbf',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.RecipeVersion
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = RecipeVersion.prisma().query_first(
            'SELECT * FROM RecipeVersion WHERE productId = $1',
            'jdecjdgii',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.RecipeVersionCreateInput,
        include: Optional[types.RecipeVersionInclude] = None
    ) -> _PrismaModelT:
        """Create a new RecipeVersion record.

        Parameters
        ----------
        data
            RecipeVersion record data
        include
            Specifies which relations should be loaded on the returned RecipeVersion model

        Returns
        -------
        prisma.models.RecipeVersion
            The created RecipeVersion record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a RecipeVersion record from just the required fields
        recipeversion = RecipeVersion.prisma().create(
            data={
                # data to create a RecipeVersion record
                'productId': 'bcgbcdjjfd',
                'version': 1826537974,
                'content': 'ihfjdgceh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.RecipeVersionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple RecipeVersion records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of RecipeVersion record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = RecipeVersion.prisma().create_many(
            data=[
                {
                    # data to create a RecipeVersion record
                    'productId': 'gdaedbiee',
                    'version': 1813115597,
                    'content': 'bdjfefjjha',
                },
                {
                    # data to create a RecipeVersion record
                    'productId': 'bjcjfbafeh',
                    'version': 1276927696,
                    'content': 'idgcdejfd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.RecipeVersionWhereUniqueInput,
        include: Optional[types.RecipeVersionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single RecipeVersion record.

        Parameters
        ----------
        where
            RecipeVersion filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned RecipeVersion model

        Returns
        -------
        prisma.models.RecipeVersion
            The deleted RecipeVersion record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipeversion = RecipeVersion.prisma().delete(
            where={
                'id': 'baajcgbaag',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.RecipeVersionWhereUniqueInput,
        include: Optional[types.RecipeVersionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique RecipeVersion record.

        Parameters
        ----------
        where
            RecipeVersion filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned RecipeVersion model

        Returns
        -------
        prisma.models.RecipeVersion
            The found RecipeVersion record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipeversion = RecipeVersion.prisma().find_unique(
            where={
                'id': 'eahbafhhf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.RecipeVersionWhereUniqueInput,
        include: Optional[types.RecipeVersionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique RecipeVersion record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            RecipeVersion filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned RecipeVersion model

        Returns
        -------
        prisma.models.RecipeVersion
            The found RecipeVersion record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipeversion = RecipeVersion.prisma().find_unique_or_raise(
            where={
                'id': 'chbgcaajc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeVersionWhereInput] = None,
        cursor: Optional[types.RecipeVersionWhereUniqueInput] = None,
        include: Optional[types.RecipeVersionInclude] = None,
        order: Optional[Union[types.RecipeVersionOrderByInput, List[types.RecipeVersionOrderByInput]]] = None,
        distinct: Optional[List[types.RecipeVersionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple RecipeVersion records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of RecipeVersion records returned
        skip
            Ignore the first N results
        where
            RecipeVersion filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RecipeVersion model
        order
            Order the returned RecipeVersion records by any field
        distinct
            Filter RecipeVersion records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.RecipeVersion]
            The list of all RecipeVersion records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 RecipeVersion records
        recipeversions = RecipeVersion.prisma().find_many(take=10)

        # find the first 5 RecipeVersion records ordered by the version field
        recipeversions = RecipeVersion.prisma().find_many(
            take=5,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RecipeVersionWhereInput] = None,
        cursor: Optional[types.RecipeVersionWhereUniqueInput] = None,
        include: Optional[types.RecipeVersionInclude] = None,
        order: Optional[Union[types.RecipeVersionOrderByInput, List[types.RecipeVersionOrderByInput]]] = None,
        distinct: Optional[List[types.RecipeVersionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single RecipeVersion record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            RecipeVersion filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RecipeVersion model
        order
            Order the returned RecipeVersion records by any field
        distinct
            Filter RecipeVersion records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.RecipeVersion
            The first RecipeVersion record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second RecipeVersion record ordered by the content field
        recipeversion = RecipeVersion.prisma().find_first(
            skip=1,
            order={
                'content': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RecipeVersionWhereInput] = None,
        cursor: Optional[types.RecipeVersionWhereUniqueInput] = None,
        include: Optional[types.RecipeVersionInclude] = None,
        order: Optional[Union[types.RecipeVersionOrderByInput, List[types.RecipeVersionOrderByInput]]] = None,
        distinct: Optional[List[types.RecipeVersionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single RecipeVersion record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            RecipeVersion filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RecipeVersion model
        order
            Order the returned RecipeVersion records by any field
        distinct
            Filter RecipeVersion records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.RecipeVersion
            The first RecipeVersion record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second RecipeVersion record ordered by the status field
        recipeversion = RecipeVersion.prisma().find_first_or_raise(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.RecipeVersionUpdateInput,
        where: types.RecipeVersionWhereUniqueInput,
        include: Optional[types.RecipeVersionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single RecipeVersion record.

        Parameters
        ----------
        data
            RecipeVersion record data specifying what to update
        where
            RecipeVersion filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned RecipeVersion model

        Returns
        -------
        prisma.models.RecipeVersion
            The updated RecipeVersion record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        recipeversion = RecipeVersion.prisma().update(
            where={
                'id': 'baafadjcdh',
            },
            data={
                # data to update the RecipeVersion record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.RecipeVersionWhereUniqueInput,
        data: types.RecipeVersionUpsertInput,
        include: Optional[types.RecipeVersionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            RecipeVersion filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned RecipeVersion model

        Returns
        -------
        prisma.models.RecipeVersion
            The created or updated RecipeVersion record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipeversion = RecipeVersion.prisma().upsert(
            where={
                'id': 'beihjdafig',
            },
            data={
                'create': {
                    'id': 'beihjdafig',
                    'productId': 'bjcjfbafeh',
                    'version': 1276927696,
                    'content': 'idgcdejfd',
                },
                'update': {
                    'productId': 'bjcjfbafeh',
                    'version': 1276927696,
                    'content': 'idgcdejfd',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.RecipeVersionUpdateManyMutationInput,
        where: types.RecipeVersionWhereInput,
    ) -> int:
        """Update multiple RecipeVersion records

        Parameters
        ----------
        data
            RecipeVersion data to update the selected RecipeVersion records to
        where
            Filter to select the RecipeVersion records to update

        Returns
        -------
        int
            The total number of RecipeVersion records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all RecipeVersion records
        total = RecipeVersion.prisma().update_many(
            data={
                'changeNote': 'cbdbchfabj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeVersionWhereInput] = None,
        cursor: Optional[types.RecipeVersionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of RecipeVersion records present in the database

        Parameters
        ----------
        select
            Select the RecipeVersion fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            RecipeVersion filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RecipeVersionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = RecipeVersion.prisma().count()

        # results: prisma.types.RecipeVersionCountAggregateOutput
        results = RecipeVersion.prisma().count(
            select={
                '_all': True,
                'createdById': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.RecipeVersionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeVersionWhereInput] = None,
        cursor: Optional[types.RecipeVersionWhereUniqueInput] = None,
    ) -> types.RecipeVersionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.RecipeVersionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeVersionWhereInput] = None,
        cursor: Optional[types.RecipeVersionWhereUniqueInput] = None,
    ) -> Union[int, types.RecipeVersionCountAggregateOutput]:
        """Count the number of RecipeVersion records present in the database

        Parameters
        ----------
        select
            Select the RecipeVersion fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            RecipeVersion filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RecipeVersionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = RecipeVersion.prisma().count()

        # results: prisma.types.RecipeVersionCountAggregateOutput
        results = RecipeVersion.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RecipeVersionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.RecipeVersionWhereInput] = None
    ) -> int:
        """Delete multiple RecipeVersion records.

        Parameters
        ----------
        where
            Optional RecipeVersion filter to find the records to be deleted

        Returns
        -------
        int
            The total number of RecipeVersion records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all RecipeVersion records
        total = RecipeVersion.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.RecipeVersionScalarFieldKeys'],
        *,
        where: Optional['types.RecipeVersionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RecipeVersionAvgAggregateInput'] = None,
        sum: Optional['types.RecipeVersionSumAggregateInput'] = None,
        min: Optional['types.RecipeVersionMinAggregateInput'] = None,
        max: Optional['types.RecipeVersionMaxAggregateInput'] = None,
        having: Optional['types.RecipeVersionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RecipeVersionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RecipeVersionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RecipeVersionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RecipeVersionGroupByOutput']:
        """Group RecipeVersion records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar RecipeVersion fields to group records by
        where
            RecipeVersion filter to select records
        take
            Limit the maximum number of RecipeVersion records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RecipeVersionGroupByOutput]
            A list of dictionaries representing the RecipeVersion record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group RecipeVersion records by id values
        # and count how many records are in each group
        results = RecipeVersion.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class RecipeTemplateActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.RecipeTemplate]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = RecipeTemplate.prisma().query_raw(
            'SELECT * FROM RecipeTemplate WHERE id = $1',
            'bffjafacbg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.RecipeTemplate
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = RecipeTemplate.prisma().query_first(
            'SELECT * FROM RecipeTemplate WHERE name = $1',
            'cbafbcgeab',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.RecipeTemplateCreateInput,
        include: Optional[types.RecipeTemplateInclude] = None
    ) -> _PrismaModelT:
        """Create a new RecipeTemplate record.

        Parameters
        ----------
        data
            RecipeTemplate record data
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model

        Returns
        -------
        prisma.models.RecipeTemplate
            The created RecipeTemplate record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a RecipeTemplate record from just the required fields
        recipetemplate = RecipeTemplate.prisma().create(
            data={
                # data to create a RecipeTemplate record
                'name': 'bedgbciecc',
                'content': 'bfeehfcihb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.RecipeTemplateCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple RecipeTemplate records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of RecipeTemplate record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = RecipeTemplate.prisma().create_many(
            data=[
                {
                    # data to create a RecipeTemplate record
                    'name': 'ibagheidg',
                    'content': 'bibfefchh',
                },
                {
                    # data to create a RecipeTemplate record
                    'name': 'baeeigbje',
                    'content': 'fibhgbdb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.RecipeTemplateWhereUniqueInput,
        include: Optional[types.RecipeTemplateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single RecipeTemplate record.

        Parameters
        ----------
        where
            RecipeTemplate filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model

        Returns
        -------
        prisma.models.RecipeTemplate
            The deleted RecipeTemplate record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipetemplate = RecipeTemplate.prisma().delete(
            where={
                'id': 'bbifhfaabg',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.RecipeTemplateWhereUniqueInput,
        include: Optional[types.RecipeTemplateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique RecipeTemplate record.

        Parameters
        ----------
        where
            RecipeTemplate filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model

        Returns
        -------
        prisma.models.RecipeTemplate
            The found RecipeTemplate record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipetemplate = RecipeTemplate.prisma().find_unique(
            where={
                'id': 'cbbhjdebdh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.RecipeTemplateWhereUniqueInput,
        include: Optional[types.RecipeTemplateInclude] = None
    ) -> _PrismaModelT:
        """Find a unique RecipeTemplate record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            RecipeTemplate filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model

        Returns
        -------
        prisma.models.RecipeTemplate
            The found RecipeTemplate record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipetemplate = RecipeTemplate.prisma().find_unique_or_raise(
            where={
                'id': 'hddcfecha',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeTemplateWhereInput] = None,
        cursor: Optional[types.RecipeTemplateWhereUniqueInput] = None,
        include: Optional[types.RecipeTemplateInclude] = None,
        order: Optional[Union[types.RecipeTemplateOrderByInput, List[types.RecipeTemplateOrderByInput]]] = None,
        distinct: Optional[List[types.RecipeTemplateScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple RecipeTemplate records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of RecipeTemplate records returned
        skip
            Ignore the first N results
        where
            RecipeTemplate filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model
        order
            Order the returned RecipeTemplate records by any field
        distinct
            Filter RecipeTemplate records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.RecipeTemplate]
            The list of all RecipeTemplate records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 RecipeTemplate records
        recipetemplates = RecipeTemplate.prisma().find_many(take=10)

        # find the first 5 RecipeTemplate records ordered by the description field
        recipetemplates = RecipeTemplate.prisma().find_many(
            take=5,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RecipeTemplateWhereInput] = None,
        cursor: Optional[types.RecipeTemplateWhereUniqueInput] = None,
        include: Optional[types.RecipeTemplateInclude] = None,
        order: Optional[Union[types.RecipeTemplateOrderByInput, List[types.RecipeTemplateOrderByInput]]] = None,
        distinct: Optional[List[types.RecipeTemplateScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single RecipeTemplate record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            RecipeTemplate filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model
        order
            Order the returned RecipeTemplate records by any field
        distinct
            Filter RecipeTemplate records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.RecipeTemplate
            The first RecipeTemplate record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second RecipeTemplate record ordered by the content field
        recipetemplate = RecipeTemplate.prisma().find_first(
            skip=1,
            order={
                'content': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RecipeTemplateWhereInput] = None,
        cursor: Optional[types.RecipeTemplateWhereUniqueInput] = None,
        include: Optional[types.RecipeTemplateInclude] = None,
        order: Optional[Union[types.RecipeTemplateOrderByInput, List[types.RecipeTemplateOrderByInput]]] = None,
        distinct: Optional[List[types.RecipeTemplateScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single RecipeTemplate record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            RecipeTemplate filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model
        order
            Order the returned RecipeTemplate records by any field
        distinct
            Filter RecipeTemplate records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.RecipeTemplate
            The first RecipeTemplate record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second RecipeTemplate record ordered by the category field
        recipetemplate = RecipeTemplate.prisma().find_first_or_raise(
            skip=1,
            order={
                'category': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.RecipeTemplateUpdateInput,
        where: types.RecipeTemplateWhereUniqueInput,
        include: Optional[types.RecipeTemplateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single RecipeTemplate record.

        Parameters
        ----------
        data
            RecipeTemplate record data specifying what to update
        where
            RecipeTemplate filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model

        Returns
        -------
        prisma.models.RecipeTemplate
            The updated RecipeTemplate record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        recipetemplate = RecipeTemplate.prisma().update(
            where={
                'id': 'bibbehhehb',
            },
            data={
                # data to update the RecipeTemplate record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.RecipeTemplateWhereUniqueInput,
        data: types.RecipeTemplateUpsertInput,
        include: Optional[types.RecipeTemplateInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            RecipeTemplate filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned RecipeTemplate model

        Returns
        -------
        prisma.models.RecipeTemplate
            The created or updated RecipeTemplate record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        recipetemplate = RecipeTemplate.prisma().upsert(
            where={
                'id': 'cibfeagfc',
            },
            data={
                'create': {
                    'id': 'cibfeagfc',
                    'name': 'baeeigbje',
                    'content': 'fibhgbdb',
                },
                'update': {
                    'name': 'baeeigbje',
                    'content': 'fibhgbdb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.RecipeTemplateUpdateManyMutationInput,
        where: types.RecipeTemplateWhereInput,
    ) -> int:
        """Update multiple RecipeTemplate records

        Parameters
        ----------
        data
            RecipeTemplate data to update the selected RecipeTemplate records to
        where
            Filter to select the RecipeTemplate records to update

        Returns
        -------
        int
            The total number of RecipeTemplate records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all RecipeTemplate records
        total = RecipeTemplate.prisma().update_many(
            data={
                'sortOrder': 818561781
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeTemplateWhereInput] = None,
        cursor: Optional[types.RecipeTemplateWhereUniqueInput] = None,
    ) -> int:
        """Count the number of RecipeTemplate records present in the database

        Parameters
        ----------
        select
            Select the RecipeTemplate fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            RecipeTemplate filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RecipeTemplateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = RecipeTemplate.prisma().count()

        # results: prisma.types.RecipeTemplateCountAggregateOutput
        results = RecipeTemplate.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.RecipeTemplateCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeTemplateWhereInput] = None,
        cursor: Optional[types.RecipeTemplateWhereUniqueInput] = None,
    ) -> types.RecipeTemplateCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.RecipeTemplateCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RecipeTemplateWhereInput] = None,
        cursor: Optional[types.RecipeTemplateWhereUniqueInput] = None,
    ) -> Union[int, types.RecipeTemplateCountAggregateOutput]:
        """Count the number of RecipeTemplate records present in the database

        Parameters
        ----------
        select
            Select the RecipeTemplate fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            RecipeTemplate filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RecipeTemplateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = RecipeTemplate.prisma().count()

        # results: prisma.types.RecipeTemplateCountAggregateOutput
        results = RecipeTemplate.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RecipeTemplateCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.RecipeTemplateWhereInput] = None
    ) -> int:
        """Delete multiple RecipeTemplate records.

        Parameters
        ----------
        where
            Optional RecipeTemplate filter to find the records to be deleted

        Returns
        -------
        int
            The total number of RecipeTemplate records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all RecipeTemplate records
        total = RecipeTemplate.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.RecipeTemplateScalarFieldKeys'],
        *,
        where: Optional['types.RecipeTemplateWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RecipeTemplateAvgAggregateInput'] = None,
        sum: Optional['types.RecipeTemplateSumAggregateInput'] = None,
        min: Optional['types.RecipeTemplateMinAggregateInput'] = None,
        max: Optional['types.RecipeTemplateMaxAggregateInput'] = None,
        having: Optional['types.RecipeTemplateScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RecipeTemplateCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RecipeTemplateScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RecipeTemplateScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RecipeTemplateGroupByOutput']:
        """Group RecipeTemplate records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar RecipeTemplate fields to group records by
        where
            RecipeTemplate filter to select records
        take
            Limit the maximum number of RecipeTemplate records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RecipeTemplateGroupByOutput]
            A list of dictionaries representing the RecipeTemplate record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group RecipeTemplate records by id values
        # and count how many records are in each group
        results = RecipeTemplate.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StageBuildMatrixActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StageBuildMatrix]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = StageBuildMatrix.prisma().query_raw(
            'SELECT * FROM StageBuildMatrix WHERE id = $1',
            'bgjehbiaja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StageBuildMatrix
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = StageBuildMatrix.prisma().query_first(
            'SELECT * FROM StageBuildMatrix WHERE stageConfigId = $1',
            'cadgbfjjf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.StageBuildMatrixCreateInput,
        include: Optional[types.StageBuildMatrixInclude] = None
    ) -> _PrismaModelT:
        """Create a new StageBuildMatrix record.

        Parameters
        ----------
        data
            StageBuildMatrix record data
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model

        Returns
        -------
        prisma.models.StageBuildMatrix
            The created StageBuildMatrix record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StageBuildMatrix record from just the required fields
        stagebuildmatrix = StageBuildMatrix.prisma().create(
            data={
                # data to create a StageBuildMatrix record
                'stageConfigId': 'bgceeachbc',
                'label': 'bbcaehffaf',
                'fwType': 'ddbhhchfi',
                'variant': 'bidbdafbhf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.StageBuildMatrixCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StageBuildMatrix records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StageBuildMatrix record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = StageBuildMatrix.prisma().create_many(
            data=[
                {
                    # data to create a StageBuildMatrix record
                    'stageConfigId': 'caehibbhia',
                    'label': 'iafgcehej',
                    'fwType': 'bbaeaehiec',
                    'variant': 'cfjghaged',
                },
                {
                    # data to create a StageBuildMatrix record
                    'stageConfigId': 'biggeddiea',
                    'label': 'biahhhjceb',
                    'fwType': 'bdjjdgddhe',
                    'variant': 'cbaaahdbgh',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.StageBuildMatrixWhereUniqueInput,
        include: Optional[types.StageBuildMatrixInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StageBuildMatrix record.

        Parameters
        ----------
        where
            StageBuildMatrix filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model

        Returns
        -------
        prisma.models.StageBuildMatrix
            The deleted StageBuildMatrix record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stagebuildmatrix = StageBuildMatrix.prisma().delete(
            where={
                'id': 'cahhcbdhii',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.StageBuildMatrixWhereUniqueInput,
        include: Optional[types.StageBuildMatrixInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StageBuildMatrix record.

        Parameters
        ----------
        where
            StageBuildMatrix filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model

        Returns
        -------
        prisma.models.StageBuildMatrix
            The found StageBuildMatrix record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stagebuildmatrix = StageBuildMatrix.prisma().find_unique(
            where={
                'id': 'baibcghaef',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.StageBuildMatrixWhereUniqueInput,
        include: Optional[types.StageBuildMatrixInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StageBuildMatrix record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StageBuildMatrix filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model

        Returns
        -------
        prisma.models.StageBuildMatrix
            The found StageBuildMatrix record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stagebuildmatrix = StageBuildMatrix.prisma().find_unique_or_raise(
            where={
                'id': 'dajaaacfa',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StageBuildMatrixWhereInput] = None,
        cursor: Optional[types.StageBuildMatrixWhereUniqueInput] = None,
        include: Optional[types.StageBuildMatrixInclude] = None,
        order: Optional[Union[types.StageBuildMatrixOrderByInput, List[types.StageBuildMatrixOrderByInput]]] = None,
        distinct: Optional[List[types.StageBuildMatrixScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StageBuildMatrix records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StageBuildMatrix records returned
        skip
            Ignore the first N results
        where
            StageBuildMatrix filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model
        order
            Order the returned StageBuildMatrix records by any field
        distinct
            Filter StageBuildMatrix records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StageBuildMatrix]
            The list of all StageBuildMatrix records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StageBuildMatrix records
        stagebuildmatrixs = StageBuildMatrix.prisma().find_many(take=10)

        # find the first 5 StageBuildMatrix records ordered by the sortOrder field
        stagebuildmatrixs = StageBuildMatrix.prisma().find_many(
            take=5,
            order={
                'sortOrder': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StageBuildMatrixWhereInput] = None,
        cursor: Optional[types.StageBuildMatrixWhereUniqueInput] = None,
        include: Optional[types.StageBuildMatrixInclude] = None,
        order: Optional[Union[types.StageBuildMatrixOrderByInput, List[types.StageBuildMatrixOrderByInput]]] = None,
        distinct: Optional[List[types.StageBuildMatrixScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StageBuildMatrix record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StageBuildMatrix filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model
        order
            Order the returned StageBuildMatrix records by any field
        distinct
            Filter StageBuildMatrix records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StageBuildMatrix
            The first StageBuildMatrix record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StageBuildMatrix record ordered by the label field
        stagebuildmatrix = StageBuildMatrix.prisma().find_first(
            skip=1,
            order={
                'label': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StageBuildMatrixWhereInput] = None,
        cursor: Optional[types.StageBuildMatrixWhereUniqueInput] = None,
        include: Optional[types.StageBuildMatrixInclude] = None,
        order: Optional[Union[types.StageBuildMatrixOrderByInput, List[types.StageBuildMatrixOrderByInput]]] = None,
        distinct: Optional[List[types.StageBuildMatrixScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StageBuildMatrix record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StageBuildMatrix filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model
        order
            Order the returned StageBuildMatrix records by any field
        distinct
            Filter StageBuildMatrix records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StageBuildMatrix
            The first StageBuildMatrix record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StageBuildMatrix record ordered by the fwType field
        stagebuildmatrix = StageBuildMatrix.prisma().find_first_or_raise(
            skip=1,
            order={
                'fwType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.StageBuildMatrixUpdateInput,
        where: types.StageBuildMatrixWhereUniqueInput,
        include: Optional[types.StageBuildMatrixInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StageBuildMatrix record.

        Parameters
        ----------
        data
            StageBuildMatrix record data specifying what to update
        where
            StageBuildMatrix filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model

        Returns
        -------
        prisma.models.StageBuildMatrix
            The updated StageBuildMatrix record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        stagebuildmatrix = StageBuildMatrix.prisma().update(
            where={
                'id': 'gdibgabah',
            },
            data={
                # data to update the StageBuildMatrix record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.StageBuildMatrixWhereUniqueInput,
        data: types.StageBuildMatrixUpsertInput,
        include: Optional[types.StageBuildMatrixInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StageBuildMatrix filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StageBuildMatrix model

        Returns
        -------
        prisma.models.StageBuildMatrix
            The created or updated StageBuildMatrix record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stagebuildmatrix = StageBuildMatrix.prisma().upsert(
            where={
                'id': 'bbdgidjcea',
            },
            data={
                'create': {
                    'id': 'bbdgidjcea',
                    'stageConfigId': 'biggeddiea',
                    'label': 'biahhhjceb',
                    'fwType': 'bdjjdgddhe',
                    'variant': 'cbaaahdbgh',
                },
                'update': {
                    'stageConfigId': 'biggeddiea',
                    'label': 'biahhhjceb',
                    'fwType': 'bdjjdgddhe',
                    'variant': 'cbaaahdbgh',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.StageBuildMatrixUpdateManyMutationInput,
        where: types.StageBuildMatrixWhereInput,
    ) -> int:
        """Update multiple StageBuildMatrix records

        Parameters
        ----------
        data
            StageBuildMatrix data to update the selected StageBuildMatrix records to
        where
            Filter to select the StageBuildMatrix records to update

        Returns
        -------
        int
            The total number of StageBuildMatrix records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StageBuildMatrix records
        total = StageBuildMatrix.prisma().update_many(
            data={
                'variant': 'bigggbcjgb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StageBuildMatrixWhereInput] = None,
        cursor: Optional[types.StageBuildMatrixWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StageBuildMatrix records present in the database

        Parameters
        ----------
        select
            Select the StageBuildMatrix fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StageBuildMatrix filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StageBuildMatrixCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StageBuildMatrix.prisma().count()

        # results: prisma.types.StageBuildMatrixCountAggregateOutput
        results = StageBuildMatrix.prisma().count(
            select={
                '_all': True,
                'configLog': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.StageBuildMatrixCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StageBuildMatrixWhereInput] = None,
        cursor: Optional[types.StageBuildMatrixWhereUniqueInput] = None,
    ) -> types.StageBuildMatrixCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.StageBuildMatrixCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StageBuildMatrixWhereInput] = None,
        cursor: Optional[types.StageBuildMatrixWhereUniqueInput] = None,
    ) -> Union[int, types.StageBuildMatrixCountAggregateOutput]:
        """Count the number of StageBuildMatrix records present in the database

        Parameters
        ----------
        select
            Select the StageBuildMatrix fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StageBuildMatrix filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StageBuildMatrixCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StageBuildMatrix.prisma().count()

        # results: prisma.types.StageBuildMatrixCountAggregateOutput
        results = StageBuildMatrix.prisma().count(
            select={
                '_all': True,
                'producesHex': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StageBuildMatrixCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.StageBuildMatrixWhereInput] = None
    ) -> int:
        """Delete multiple StageBuildMatrix records.

        Parameters
        ----------
        where
            Optional StageBuildMatrix filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StageBuildMatrix records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StageBuildMatrix records
        total = StageBuildMatrix.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.StageBuildMatrixScalarFieldKeys'],
        *,
        where: Optional['types.StageBuildMatrixWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StageBuildMatrixAvgAggregateInput'] = None,
        sum: Optional['types.StageBuildMatrixSumAggregateInput'] = None,
        min: Optional['types.StageBuildMatrixMinAggregateInput'] = None,
        max: Optional['types.StageBuildMatrixMaxAggregateInput'] = None,
        having: Optional['types.StageBuildMatrixScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StageBuildMatrixCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StageBuildMatrixScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StageBuildMatrixScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StageBuildMatrixGroupByOutput']:
        """Group StageBuildMatrix records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StageBuildMatrix fields to group records by
        where
            StageBuildMatrix filter to select records
        take
            Limit the maximum number of StageBuildMatrix records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StageBuildMatrixGroupByOutput]
            A list of dictionaries representing the StageBuildMatrix record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StageBuildMatrix records by producesCfw values
        # and count how many records are in each group
        results = StageBuildMatrix.prisma().group_by(
            ['producesCfw'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AssetSetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AssetSet]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = AssetSet.prisma().query_raw(
            'SELECT * FROM AssetSet WHERE id = $1',
            'bfhhaicdhe',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AssetSet
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = AssetSet.prisma().query_first(
            'SELECT * FROM AssetSet WHERE productId = $1',
            'bgdeigedch',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.AssetSetCreateInput,
        include: Optional[types.AssetSetInclude] = None
    ) -> _PrismaModelT:
        """Create a new AssetSet record.

        Parameters
        ----------
        data
            AssetSet record data
        include
            Specifies which relations should be loaded on the returned AssetSet model

        Returns
        -------
        prisma.models.AssetSet
            The created AssetSet record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AssetSet record from just the required fields
        assetset = AssetSet.prisma().create(
            data={
                # data to create a AssetSet record
                'productId': 'gedhbbhdc',
                'version': 'bcgcffdgcj',
                'source': enums.AssetSetSource.BUILD_SERVICE,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.AssetSetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AssetSet records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AssetSet record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = AssetSet.prisma().create_many(
            data=[
                {
                    # data to create a AssetSet record
                    'productId': 'bihjhiiji',
                    'version': 'ididibbff',
                    'source': enums.AssetSetSource.BUILD_SERVICE,
                },
                {
                    # data to create a AssetSet record
                    'productId': 'cgbfcibga',
                    'version': 'bcibjgahcj',
                    'source': enums.AssetSetSource.BUILD_SERVICE,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.AssetSetWhereUniqueInput,
        include: Optional[types.AssetSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AssetSet record.

        Parameters
        ----------
        where
            AssetSet filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AssetSet model

        Returns
        -------
        prisma.models.AssetSet
            The deleted AssetSet record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetset = AssetSet.prisma().delete(
            where={
                'id': 'bbbbedfcda',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.AssetSetWhereUniqueInput,
        include: Optional[types.AssetSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AssetSet record.

        Parameters
        ----------
        where
            AssetSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AssetSet model

        Returns
        -------
        prisma.models.AssetSet
            The found AssetSet record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetset = AssetSet.prisma().find_unique(
            where={
                'id': 'gejjhdbid',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.AssetSetWhereUniqueInput,
        include: Optional[types.AssetSetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AssetSet record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AssetSet filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AssetSet model

        Returns
        -------
        prisma.models.AssetSet
            The found AssetSet record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetset = AssetSet.prisma().find_unique_or_raise(
            where={
                'id': 'bjfaacchjg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetSetWhereInput] = None,
        cursor: Optional[types.AssetSetWhereUniqueInput] = None,
        include: Optional[types.AssetSetInclude] = None,
        order: Optional[Union[types.AssetSetOrderByInput, List[types.AssetSetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetSetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AssetSet records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AssetSet records returned
        skip
            Ignore the first N results
        where
            AssetSet filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AssetSet model
        order
            Order the returned AssetSet records by any field
        distinct
            Filter AssetSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AssetSet]
            The list of all AssetSet records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AssetSet records
        assetsets = AssetSet.prisma().find_many(take=10)

        # find the first 5 AssetSet records ordered by the boardRevisionId field
        assetsets = AssetSet.prisma().find_many(
            take=5,
            order={
                'boardRevisionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetSetWhereInput] = None,
        cursor: Optional[types.AssetSetWhereUniqueInput] = None,
        include: Optional[types.AssetSetInclude] = None,
        order: Optional[Union[types.AssetSetOrderByInput, List[types.AssetSetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetSetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AssetSet record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AssetSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AssetSet model
        order
            Order the returned AssetSet records by any field
        distinct
            Filter AssetSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AssetSet
            The first AssetSet record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AssetSet record ordered by the version field
        assetset = AssetSet.prisma().find_first(
            skip=1,
            order={
                'version': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetSetWhereInput] = None,
        cursor: Optional[types.AssetSetWhereUniqueInput] = None,
        include: Optional[types.AssetSetInclude] = None,
        order: Optional[Union[types.AssetSetOrderByInput, List[types.AssetSetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetSetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AssetSet record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AssetSet filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AssetSet model
        order
            Order the returned AssetSet records by any field
        distinct
            Filter AssetSet records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AssetSet
            The first AssetSet record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AssetSet record ordered by the variant field
        assetset = AssetSet.prisma().find_first_or_raise(
            skip=1,
            order={
                'variant': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.AssetSetUpdateInput,
        where: types.AssetSetWhereUniqueInput,
        include: Optional[types.AssetSetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AssetSet record.

        Parameters
        ----------
        data
            AssetSet record data specifying what to update
        where
            AssetSet filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AssetSet model

        Returns
        -------
        prisma.models.AssetSet
            The updated AssetSet record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        assetset = AssetSet.prisma().update(
            where={
                'id': 'beeigdcchh',
            },
            data={
                # data to update the AssetSet record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.AssetSetWhereUniqueInput,
        data: types.AssetSetUpsertInput,
        include: Optional[types.AssetSetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AssetSet filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AssetSet model

        Returns
        -------
        prisma.models.AssetSet
            The created or updated AssetSet record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetset = AssetSet.prisma().upsert(
            where={
                'id': 'bgihcicggg',
            },
            data={
                'create': {
                    'id': 'bgihcicggg',
                    'productId': 'cgbfcibga',
                    'version': 'bcibjgahcj',
                    'source': enums.AssetSetSource.BUILD_SERVICE,
                },
                'update': {
                    'productId': 'cgbfcibga',
                    'version': 'bcibjgahcj',
                    'source': enums.AssetSetSource.BUILD_SERVICE,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.AssetSetUpdateManyMutationInput,
        where: types.AssetSetWhereInput,
    ) -> int:
        """Update multiple AssetSet records

        Parameters
        ----------
        data
            AssetSet data to update the selected AssetSet records to
        where
            Filter to select the AssetSet records to update

        Returns
        -------
        int
            The total number of AssetSet records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AssetSet records
        total = AssetSet.prisma().update_many(
            data={
                'stage': 1988855515
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetSetWhereInput] = None,
        cursor: Optional[types.AssetSetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AssetSet records present in the database

        Parameters
        ----------
        select
            Select the AssetSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AssetSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = AssetSet.prisma().count()

        # results: prisma.types.AssetSetCountAggregateOutput
        results = AssetSet.prisma().count(
            select={
                '_all': True,
                'source': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.AssetSetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetSetWhereInput] = None,
        cursor: Optional[types.AssetSetWhereUniqueInput] = None,
    ) -> types.AssetSetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.AssetSetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetSetWhereInput] = None,
        cursor: Optional[types.AssetSetWhereUniqueInput] = None,
    ) -> Union[int, types.AssetSetCountAggregateOutput]:
        """Count the number of AssetSet records present in the database

        Parameters
        ----------
        select
            Select the AssetSet fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AssetSet filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetSetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = AssetSet.prisma().count()

        # results: prisma.types.AssetSetCountAggregateOutput
        results = AssetSet.prisma().count(
            select={
                '_all': True,
                'buildRunId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AssetSetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.AssetSetWhereInput] = None
    ) -> int:
        """Delete multiple AssetSet records.

        Parameters
        ----------
        where
            Optional AssetSet filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AssetSet records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AssetSet records
        total = AssetSet.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.AssetSetScalarFieldKeys'],
        *,
        where: Optional['types.AssetSetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AssetSetAvgAggregateInput'] = None,
        sum: Optional['types.AssetSetSumAggregateInput'] = None,
        min: Optional['types.AssetSetMinAggregateInput'] = None,
        max: Optional['types.AssetSetMaxAggregateInput'] = None,
        having: Optional['types.AssetSetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AssetSetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AssetSetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AssetSetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AssetSetGroupByOutput']:
        """Group AssetSet records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AssetSet fields to group records by
        where
            AssetSet filter to select records
        take
            Limit the maximum number of AssetSet records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AssetSetGroupByOutput]
            A list of dictionaries representing the AssetSet record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AssetSet records by externalBuildId values
        # and count how many records are in each group
        results = AssetSet.prisma().group_by(
            ['externalBuildId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AssetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Asset]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Asset.prisma().query_raw(
            'SELECT * FROM Asset WHERE id = $1',
            'bgjceijaia',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Asset
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Asset.prisma().query_first(
            'SELECT * FROM Asset WHERE assetSetId = $1',
            'hifbdafhh',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.AssetCreateInput,
        include: Optional[types.AssetInclude] = None
    ) -> _PrismaModelT:
        """Create a new Asset record.

        Parameters
        ----------
        data
            Asset record data
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The created Asset record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Asset record from just the required fields
        asset = Asset.prisma().create(
            data={
                # data to create a Asset record
                'assetSetId': 'bcabicaife',
                'label': 'ibgjfchfj',
                'role': 'bghafddebc',
                'artifactType': 'bgaidgfbjh',
                'storageKey': 'bdejjhhggg',
                'filename': 'fdahaiafb',
                'sizeBytes': 20979927744,
                'checksum': 'ddecgfgj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.AssetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Asset records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Asset record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Asset.prisma().create_many(
            data=[
                {
                    # data to create a Asset record
                    'assetSetId': 'baafhjifec',
                    'label': 'jgbcfighb',
                    'role': 'bagjdcgfce',
                    'artifactType': 'gjjfcfbij',
                    'storageKey': 'fdejhhede',
                    'filename': 'jfjjgcdbf',
                    'sizeBytes': 10079737824,
                    'checksum': 'bjgdidgdfd',
                },
                {
                    # data to create a Asset record
                    'assetSetId': 'bijcbbccda',
                    'label': 'baiadjcdfj',
                    'role': 'eeighihea',
                    'artifactType': 'bjgcaahcjd',
                    'storageKey': 'eajaedjfe',
                    'filename': 'diebbdcbj',
                    'sizeBytes': 19401894144,
                    'checksum': 'biefijhhcb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Asset record.

        Parameters
        ----------
        where
            Asset filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The deleted Asset record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = Asset.prisma().delete(
            where={
                'id': 'bhgbgacihi',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Asset record.

        Parameters
        ----------
        where
            Asset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The found Asset record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = Asset.prisma().find_unique(
            where={
                'id': 'bhjigebfgh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Asset record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Asset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The found Asset record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = Asset.prisma().find_unique_or_raise(
            where={
                'id': 'bjjchjieee',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Asset records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Asset records returned
        skip
            Ignore the first N results
        where
            Asset filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Asset]
            The list of all Asset records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Asset records
        assets = Asset.prisma().find_many(take=10)

        # find the first 5 Asset records ordered by the label field
        assets = Asset.prisma().find_many(
            take=5,
            order={
                'label': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Asset record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Asset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Asset
            The first Asset record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Asset record ordered by the role field
        asset = Asset.prisma().find_first(
            skip=1,
            order={
                'role': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Asset record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Asset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Asset
            The first Asset record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Asset record ordered by the processor field
        asset = Asset.prisma().find_first_or_raise(
            skip=1,
            order={
                'processor': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.AssetUpdateInput,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Asset record.

        Parameters
        ----------
        data
            Asset record data specifying what to update
        where
            Asset filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The updated Asset record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        asset = Asset.prisma().update(
            where={
                'id': 'hgchdfhjh',
            },
            data={
                # data to update the Asset record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.AssetWhereUniqueInput,
        data: types.AssetUpsertInput,
        include: Optional[types.AssetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Asset filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The created or updated Asset record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = Asset.prisma().upsert(
            where={
                'id': 'cadjagcdeg',
            },
            data={
                'create': {
                    'id': 'cadjagcdeg',
                    'assetSetId': 'bijcbbccda',
                    'label': 'baiadjcdfj',
                    'role': 'eeighihea',
                    'artifactType': 'bjgcaahcjd',
                    'storageKey': 'eajaedjfe',
                    'filename': 'diebbdcbj',
                    'sizeBytes': 19401894144,
                    'checksum': 'biefijhhcb',
                },
                'update': {
                    'assetSetId': 'bijcbbccda',
                    'label': 'baiadjcdfj',
                    'role': 'eeighihea',
                    'artifactType': 'bjgcaahcjd',
                    'storageKey': 'eajaedjfe',
                    'filename': 'diebbdcbj',
                    'sizeBytes': 19401894144,
                    'checksum': 'biefijhhcb',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.AssetUpdateManyMutationInput,
        where: types.AssetWhereInput,
    ) -> int:
        """Update multiple Asset records

        Parameters
        ----------
        data
            Asset data to update the selected Asset records to
        where
            Filter to select the Asset records to update

        Returns
        -------
        int
            The total number of Asset records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Asset records
        total = Asset.prisma().update_many(
            data={
                'artifactType': 'hchffbghf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Asset records present in the database

        Parameters
        ----------
        select
            Select the Asset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Asset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Asset.prisma().count()

        # results: prisma.types.AssetCountAggregateOutput
        results = Asset.prisma().count(
            select={
                '_all': True,
                'storageKey': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.AssetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> types.AssetCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.AssetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> Union[int, types.AssetCountAggregateOutput]:
        """Count the number of Asset records present in the database

        Parameters
        ----------
        select
            Select the Asset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Asset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Asset.prisma().count()

        # results: prisma.types.AssetCountAggregateOutput
        results = Asset.prisma().count(
            select={
                '_all': True,
                'filename': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AssetCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.AssetWhereInput] = None
    ) -> int:
        """Delete multiple Asset records.

        Parameters
        ----------
        where
            Optional Asset filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Asset records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Asset records
        total = Asset.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.AssetScalarFieldKeys'],
        *,
        where: Optional['types.AssetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AssetAvgAggregateInput'] = None,
        sum: Optional['types.AssetSumAggregateInput'] = None,
        min: Optional['types.AssetMinAggregateInput'] = None,
        max: Optional['types.AssetMaxAggregateInput'] = None,
        having: Optional['types.AssetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AssetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AssetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AssetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AssetGroupByOutput']:
        """Group Asset records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Asset fields to group records by
        where
            Asset filter to select records
        take
            Limit the maximum number of Asset records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AssetGroupByOutput]
            A list of dictionaries representing the Asset record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Asset records by sizeBytes values
        # and count how many records are in each group
        results = Asset.prisma().group_by(
            ['sizeBytes'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models