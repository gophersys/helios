meta {
  name: Revoke API Key
  type: http
  seq: 4
}

post {
  url: {{restServerHostname}}/authentication/apikeys/revoke?apiKeyId=xlRkaPqk
  body: json
  auth: inherit
}

query {
  apiKeyId: xlRkaPqk
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "RevocationReason": "Test revoke"
  }
}
