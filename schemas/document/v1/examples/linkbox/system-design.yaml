meta:
  id: system-design
  type: system-design
  schema_version: 1.0.0
  project: linkbox
  status: approved
  version: 1
  created: 2026-06-12
  updated: 2026-06-12
  authors:
    - run: run-arch-1c40
    - human: mateo
  links:
    realizes: []
    supersedes: null
    informs: []

data:
  components:
    - id: CMP-0001
      name: linkbox-api
      responsibility: |
        Owns the server-side business logic: saving and tagging links, managing collections,
        publishing and revoking share tokens, and enforcing per-user access on every request.
        Persists the domain entities and serves the Connect RPC surface the web app calls.
      cell: go-backend
      depends_on: []
      connectors:
        - F1
        - F5
      links:
        realizes:
          - REQ-0001
          - REQ-0005
          - ENT-0001
    - id: CMP-0002
      name: linkbox-web
      responsibility: |
        The Svelte web client: the single save field and library list, the tag editor, and the
        collection publish/share UI. Calls linkbox-api over Connect and renders the read-only
        public collection view for share-link visitors.
      cell: svelte-ui
      depends_on:
        - CMP-0001
      connectors:
        - F6
      links:
        realizes:
          - WF-0001
          - WF-0002
  environments_plan:
    environments:
      - development
      - staging
      - production
    substrate_map:
      development: docker-compose
      staging: kubernetes
      production: kubernetes
