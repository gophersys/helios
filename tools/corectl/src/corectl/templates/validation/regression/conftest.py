"""Regression stage fixtures — full functional regression.

Tests here demonstrate the asset-set firmware download pattern in their
scaffold form — the first regression test ships a real ``stage_assets.hex(...)``
call so new authors see how to pin firmware by label.
"""
