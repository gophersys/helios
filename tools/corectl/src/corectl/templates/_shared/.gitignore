__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
logs/
dist/
*.egg-info/
.venv/
.env

# DEV_HOLD fixture-claim state — written by `corectl test claim`, never
# committed. The state file pins the dev's hardware lease + heartbeat
# daemon PID; the log is the daemon's rotating file output.
.concord-claim.json
.concord-claim.log
