package configuration

// Configuration is a full-spelled spine TYPE outlier — banned as a type name; the
// idiomatic spine type is Config (10 §5 founder ruling). It must be flagged.
type Configuration struct{}

// Dependencies is the other full-spelled spine TYPE outlier — use Deps. Flagged.
type Dependencies struct{}
