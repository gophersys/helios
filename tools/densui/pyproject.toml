[project]
name = "densui"
version = "0.1.0"
description = "Dense-UI: computed layout and geometry proofs for blind layout engines"
requires-python = ">=3.11"
dependencies = ["fonttools>=4.50"]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/densui"]

[tool.ruff]
line-length = 100
