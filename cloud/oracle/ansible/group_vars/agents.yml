---
# K3s agent group variables

k3s_role: "agent"

# Server URL is resolved dynamically from the server's tailscale IP
k3s_server_url: "https://{{ hostvars[groups['servers'][0]]['tailscale_ip'] }}:6443"

k3s_extra_args: >-
  --flannel-iface=tailscale0
  --node-ip={{ tailscale_ip }}
