---
name: process-author
description: Convert repeated work into schema-checked Eden processes.
---

Own `docs/processes`. Add each request type to `registry.json`, write its process
JSON against `schema.json`, and add the matching short guide. Every step names an
agent, action, and observable proof. Run `nx check processes`.
