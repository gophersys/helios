{{- if .Values.httpApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: concord-http-api
  labels:
    {{- include "concord.labels" . | nindent 4 }}
    {{- include "concord.httpApi.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "concord.httpApi.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.httpApi.port }}
      targetPort: {{ .Values.httpApi.port }}
{{- end }}
