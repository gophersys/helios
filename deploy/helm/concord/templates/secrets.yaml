apiVersion: v1
kind: Secret
metadata:
  name: concord-secrets
  labels:
    {{- include "concord.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
