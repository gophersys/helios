# GENERATED by deploy/servicespec. DO NOT EDIT BY HAND.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platformgateway
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: platformgateway
    app.kubernetes.io/part-of: eden
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: platformgateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: platformgateway
    spec:
      serviceAccountName: platformgateway
      containers:
        - name: platformgateway
          image: "{{ .Values.registry }}/platformgateway:{{ .Values.tag }}"
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: EDEN_STAGE
              value: "production"
            - name: EDEN_GATEWAY_ADDRESS
              value: ":8080"
            - name: EDEN_VAULT_MODE
              value: "token-file"
            - name: EDEN_VAULT_TOKEN_FILE
              value: "/vault/secrets/token"
            - name: VAULT_ADDR
              value: "http://vault.eden.svc.cluster.local:8200"
            - name: EDEN_GATEWAY_JWT_SECRET_REF
              value: "vault://eden/{{ .Values.stage }}#platformgateway-jwt-signing-key"
            - name: EDEN_GATEWAY_DATABASE_DSN_REF
              value: "vault://eden/{{ .Values.stage }}#platformgateway-database-dsn"
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 512Mi
              ephemeral-storage: 1024Mi
          livenessProbe:
            httpGet:
              path: /healthz/live
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz/ready
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          volumeMounts:
            - name: eden-vault-token
              mountPath: /vault/secrets
              readOnly: true
      volumes:
        - name: eden-vault-token
          secret:
            secretName: eden-vault-token
            items:
              - key: token
                path: token
