# GENERATED by deploy/servicespec. DO NOT EDIT BY HAND.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentgateway
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: agentgateway
    app.kubernetes.io/part-of: eden
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: agentgateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agentgateway
    spec:
      serviceAccountName: eden-agent
      containers:
        - name: agentgateway
          image: "{{ .Values.registry }}/agentgateway:{{ .Values.tag }}"
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: EDEN_GATEWAY_ADDRESS
              value: ":8080"
            - name: EDEN_NATS_URL
              value: "nats://nats:4222"
            - name: EDEN_VAULT_MODE
              value: "token-file"
            - name: EDEN_VAULT_TOKEN_FILE
              value: "/vault/secrets/token"
            - name: VAULT_ADDR
              value: "http://vault:8200"
            - name: EDEN_GATEWAY_JWT_SECRET_REF
              value: "vault://eden/{{ .Values.stage }}#agentgateway-jwt-signing-key"
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 512Mi
              ephemeral-storage: 1024Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
