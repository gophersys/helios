# Underdetermination list — instance iG (run3)

Verbatim section (b) of the instance's return.

1. `pir.targets.unit_cost.sell` — ABSENT vs UNKNOWN. The single verdict-flipping fork. `conditional_fields` says "explicit not-for-sale is ABSENT; silence is UNKNOWN". The founder never says "not for sale" verbatim but says "its just for me" and "2, and maybe 10 for my freinds", and answers only the build half of `q.scale.cost`. `declared-none-general` says declared-none covers ANY explicit statement that no value binds — but does a statement about the *user set* count as an explicit statement about *sale*? Chose ABSENT → `metric.viability.economics` amber → verdict go. UNKNOWN → core metric unknown → precedence rule 2 → verdict blocked.
2. `q.power.duty` typo "akwas on schel". "wakes on schedule" vs "always ... on schedule". Chose `[wakes-on-event, wakes-on-schedule, only-when-used]`, excluding `always-on`. If `always-on` belongs, `metric.feasibility.power-risk` moves toward red (its red text is literally "always-on radio on a coin cell"). No typo-resolution rule exists.
3. Missed adaptive `q.fn.rate` spawns. 5 senses named, 2 spawned. `runner_errors[]` is defined as "multi-affirmation convention violations and missed ask-backs" — "ask-back" is the constraint-oracle's term; whether a missed *rate* spawn qualifies is unstated. Recorded it as `re.3`, and set the affected items' `rate: ABSENT` (unspawned) rather than UNKNOWN.
4. `rate.configurable` for tilt/motion. The `rate-attachment` convention mandates the bool; the founder stated configurability only for position. Recorded `false`. Nothing says whether an unstated mandatory bool is `false`, UNKNOWN, or ABSENT.
5. What `pir.environment.limits` blocks. R5 demands the UNKNOWN be listed "with the P1 metrics it blocks", but `q.followup.range`'s `feeds` contains no metric id — so formally it blocks nothing while plainly degrading `metric.feasibility.env-risk` (whose amber text names "extremes"). Recorded `blocks: []` + a note, and scored env-risk amber rather than unknown.
6. Which `q.phys.size` values are "tight". The adaptive says "tight picks open exact-dimension asks" without defining tight. Treated `phone` as not tight → no spawn was correct → `pir.physical.dims: ABSENT`. If `phone` is tight, this is a fourth runner error and dims is UNKNOWN.
7. `q.comp.safety` free text contradicting the enum. "no, just hopefully not explode lol" selects `none` while naming a Li-ion hazard on a vibrating, heat-soaked vehicle. No rule covers an enum answer whose prose undercuts the value. Recorded `none` (R4 records what was said); `metric.risk.safety` is `scored_by: derived` so it cannot re-rate. Carried the hazard as a P5 item in evidence.
8. G0 check 4 ("the graph itself lints clean"). A property of `30-p0-graph.yaml`, not of this PIR, and `sfd graph lint` was not run by this extraction. Recorded `not-evaluated` rather than assuming green (fail-not-skip).
9. No scorecard field for research tasks. The catalog seam mandates "the gap becomes a named research task"; `sfd.scorecard/v0` has no such key. Put them in each score's `evidence` lines *and* added a `recommendation.research_tasks` block — a schema extension the contract neither grants nor forbids.
10. `pir.modes` representation. The graph says duty "becomes the product-mode set {active/idle/sleep/ship} in P3" without saying whether P0 records the duty enum values or the translated mode set. Recorded the enum values as given.
11. The button's route into `pir.functions`. `q.phys.controls` feeds `pir.physical.controls` only. The button reaches `pir.functions` solely because `q.fn.shows`' verbatim happens to mention it. Recorded a second `hmi` item cited to `q.fn.shows`; a founder who mentioned the button only under `q.phys.controls` would yield a different functions list for the same product.
12. Speed vs velocity. The founder named both; no rule on de-duplicating near-synonymous senses. Kept both as separate sensing items (verbatim faithfulness over tidiness).
13. `metric.risk.schedule` green on two none-declared dates. The metric's own scale grants green, but the *global* green means "evidence supports it; no action needed" — which asserts low risk where the true state is no information. Followed the metric scale and used its "stated as such in evidence" clause to say so explicitly.
14. Two `scored_by: derived` metrics require judgment. `metric.viability.economics` ("plausible for the BOM class") and `metric.feasibility.power-risk` ("comfortably plausible", "aggressive sleep design") carry no `derivation:` key, unlike `metric.platform.compute-demand` which has a mechanical rule. Scoring them needed a BOM estimate and a sleep-current judgment from a scorer typed as "no judgment". Showed the reasoning in evidence.
15. Presentation state for the other five multi questions. Only `q.env.conditions` carries a `presented:` note. For `q.fn.talks`, `q.power.duty`, `q.time.exists`, `q.comp.region`, `q.comp.data` the transcript is silent, so I assumed full presentation. The `multi-affirmation` convention supplies no default for silence.
16. `metric.tech.exists` scope exceeds its `fed_by`. Declared `fed_by: [q.novel.magic]` (answer: "not really") yet `scored_by: agent+catalog`, and the seam requires mapping the product's *functions* onto capabilities. Scored across all of `pir.functions`, going beyond the declared feed.
17. Timestamps on events that never occurred. The `timestamps` convention allows date-only. Used `2026-08-26` throughout — including on `constraints[zephyr-rtos]`, where the thing being timestamped (a classification) was never asked. `quote: ABSENT` marks it, but the `at:` is fabricated precision the schema demands.
