# Underdetermination list — instance iH (run3)

Verbatim section (b) of the instance's return.

1. `pir.targets.unit_cost.sell` — ABSENT vs UNKNOWN. `conditional_fields` gives three cases (states intent to sell / explicit not-for-sale / silence) but never says whether `"its just for me"` — spoken to `q.intent.who`, about USERS — counts as an explicit not-for-sale. `q.comp.region` is itself phrased "Where will it be sold first?" and was answered "us only", cutting the other way. I chose silence → UNKNOWN. This single choice decides the verdict: UNKNOWN → `blocked` (rule 2); ABSENT → economics amber → `go`.
2. `q.constraint.strength` value mapping. The ask says "a requirement, or a preference?"; the enum values are `[hard, preference]`. "requirement"→`hard` is implied, never written. The founder's answer was also a typo ("requiemrnt").
3. Garbled enum token "akwas on schel" (`q.power.duty`). No rule for mapping mangled free text onto an enum. I read it as `wakes-on-schedule` and did NOT select `always-on`; that read is what fires `capability.time`.
4. No confirmation rule for enum/multi mapping. `conventions.multi-affirmation` governs what the runner PRESENTED, not how a free-text answer maps onto values. Every enum/multi in this transcript was answered in prose and mapped by me.
5. Adaptive spawn granularity for `q.fn.rate`. "once per named sense" against a founder who names five senses in one breath and ends with "things like that". Nothing says an open-ended list is or is not exhaustive, and nothing says a missed spawn is a runner error — I extended `runner_errors`' "missed ask-backs" to cover it.
6. State of never-presented multi values (`drops`, `"none"` on `q.env.conditions`). The convention says the answer cannot bind them and the error is recorded; it does not say whether they become UNKNOWN. I left them unselected plus a runner error.
7. "Tight picks" for `q.followup.range` on `q.phys.size` is undefined. I judged `phone` not tight → `pir.physical.dims: ABSENT` (unspawned adaptive), not a missed spawn.
8. `rate.configurable` when unstated (tilt). The convention types it `bool`; UNKNOWN vs `false` is undefined. I chose UNKNOWN (R5 forbids silent defaults) and addressed it as `pir.functions[sensing.tilt].rate.configurable`, a nested-list address the `unknown-addressing` convention only half anticipates.
9. `metric.feasibility.power-risk` is `scored_by: derived` but carries NO `derivation:` string (unlike `compute-demand`). A "mechanical" score required judgment; I applied the scale text literally (floor `none-declared` makes red unreachable → amber).
10. `metric.viability.economics` is `scored_by: derived` but its not-for-sale branch ("plausible for the BOM class") needs a BOM-cost judgment with no rule, no price data and no BOM-class table anywhere in the docs.
11. Partial-feed scoring. Nothing says whether one UNKNOWN sub-field (`unit_cost.sell`) makes the whole metric `unknown`, or whether the metric may be scored from its answered feeds (`q.scale.units` + the build half). I chose `unknown`, which triggers the severe rule-2 outcome.
12. `metric.platform.compute-demand` derivation speaks only of rate PERIODS and streams; "if an alert it must be as quick as possible" is an event LATENCY. I scored green mechanically and stated the caveat that a hard sub-second read moves it to amber.
13. No mechanism to override a founder's own safety enum. `q.comp.safety = none` despite "hopefully not explode lol" on a vehicle-mounted, vehicle-charged Li-ion pack with an unknown temperature range. The gate fires only on `serious`. I kept green + no gate, and carried the hazard in evidence and reasons.
14. `metric.viability.novelty` red boundary. Red is "an existing product already is this"; the founder himself says "yes, others exist". Amber vs red for a self-conceded existing category is undefined. I chose amber (the stated difference is incremental/unverified).
15. `metric.risk.self-assessed` green needs "matches the actual hard part AND has a plan" — no clause for "matches, and no plan exists anywhere in the PIR". I used the amber clause.
16. Catalog seam vs the depth rule. The evidence bar lets me NAME an upstream Zephyr implementation, but D1 requires a path @ SHA and the pinned tree is absent from this worktree (`ws/` holds only `west.yml`). Nothing says how to cite a named example without the tree. I named the GNSS/modem/IMU/charger examples and marked them explicitly unverified, with extraction as the research task.
17. `pir.users` has no shape (free text vs a typed user model). Kept verbatim.
18. G0 check 4 (`sfd graph lint`) is inside the G0 pass condition but is tool-side. No instruction for what an extraction instance records. I wrote `not-evaluated` rather than assuming green (FAIL-NOT-SKIP).
19. `pir.summary`: verbatim vs normalized — the spec shows only `...`. I used the verbatim.
20. `at:` for a never-asked item. The zephyr-rtos constraint has no answer timestamp; I used the transcript date with `state: UNKNOWN`.
21. Where a G0 blocker belongs on the scorecard. `recommendation.blockers` is defined as verdict-level (open human gates / core unknowns) and the G0 state "rides in `reasons[0]`" — so a G0-blocked PIR contributes nothing to `blockers` even though it plainly blocks progress. I followed the doc literally.
