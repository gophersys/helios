"""D_Schottky — typed component (ingestion-factory output).

GENERATED by src/ecad/ingest/codegen.py — DO NOT EDIT.
Edits go in d_schottky_overrides.py (applied by the src.ecad.library registry).
"""

from src.ecad.component import Component, Pin
from src.ecad.model import (
    ElectricalType,
    FootprintRef,
    PinRole,
    PinSpec,
    UnitDef,
    UnitStrategy,
)


class D_Schottky(Component):
    part_name = 'D_Schottky'
    lib_id = 'Device:D_Schottky'
    reference_prefix = 'D'
    description = 'Schottky diode'
    datasheet = ''
    footprint = FootprintRef(lib='Diode_SMD', name='D_SOD-123', source='kicad')
    unit_strategy = UnitStrategy.EXPLICIT
    unit_plan = (  # frozen at generation time
        UnitDef(name='D_Schottky', pads=('1', '2')),
    )
    _PIN_SPECS = (
        PinSpec(pad='1', name='K', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
        PinSpec(pad='2', name='A', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
    )

    value = 'D_Schottky'

    def __init__(self, value: str = 'D_Schottky',
                 footprint: FootprintRef | None = None) -> None:
        """Per-instance value and footprint (see class docstring)."""
        super().__init__()
        self.value = value
        if footprint is not None:
            self.footprint = footprint

    @property
    def A(self) -> Pin:
        """Pad 2 — 'A' (passive)."""
        return self._pin('2')

    @property
    def K(self) -> Pin:
        """Pad 1 — 'K' (passive)."""
        return self._pin('1')
