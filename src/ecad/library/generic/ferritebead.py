"""FerriteBead — typed component (ingestion-factory output).

GENERATED by src/ecad/ingest/codegen.py — DO NOT EDIT.
Edits go in ferritebead_overrides.py (applied by the src.ecad.library registry).
"""

from src.ecad.component import Component, Pin
from src.ecad.model import (
    ElectricalType,
    FootprintRef,
    PinRole,
    PinSpec,
    UnitDef,
    UnitStrategy,
)


class FerriteBead(Component):
    part_name = 'FerriteBead'
    lib_id = 'Device:FerriteBead'
    reference_prefix = 'FB'
    description = 'Ferrite bead'
    datasheet = ''
    footprint = FootprintRef(lib='Inductor_SMD', name='L_0603_1608Metric', source='kicad')
    unit_strategy = UnitStrategy.EXPLICIT
    unit_plan = (  # frozen at generation time
        UnitDef(name='FerriteBead', pads=('1', '2')),
    )
    _PIN_SPECS = (
        PinSpec(pad='1', name='P1', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
        PinSpec(pad='2', name='P2', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
    )

    value = '600R@100MHz'

    def __init__(self, value: str = '600R@100MHz',
                 footprint: FootprintRef | None = None) -> None:
        """Per-instance value and footprint (see class docstring)."""
        super().__init__()
        self.value = value
        if footprint is not None:
            self.footprint = footprint

    @property
    def P1(self) -> Pin:
        """Pad 1 — 'P1' (passive)."""
        return self._pin('1')

    @property
    def P2(self) -> Pin:
        """Pad 2 — 'P2' (passive)."""
        return self._pin('2')
