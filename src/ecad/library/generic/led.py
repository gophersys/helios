"""LED — typed component (ingestion-factory output).

GENERATED by src/ecad/ingest/codegen.py — DO NOT EDIT.
Edits go in led_overrides.py (applied by the src.ecad.library registry).
"""

from src.ecad.component import Component, Pin
from src.ecad.model import (
    ElectricalType,
    FootprintRef,
    PinRole,
    PinSpec,
    UnitDef,
    UnitStrategy,
)


class LED(Component):
    part_name = 'LED'
    lib_id = 'Device:LED'
    reference_prefix = 'D'
    description = 'Light emitting diode'
    datasheet = ''
    footprint = FootprintRef(lib='LED_SMD', name='LED_0603_1608Metric', source='kicad')
    unit_strategy = UnitStrategy.EXPLICIT
    unit_plan = (  # frozen at generation time
        UnitDef(name='LED', pads=('1', '2')),
    )
    _PIN_SPECS = (
        PinSpec(pad='1', name='K', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
        PinSpec(pad='2', name='A', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
    )

    value = 'LED'

    def __init__(self, value: str = 'LED',
                 footprint: FootprintRef | None = None) -> None:
        """Per-instance value and footprint (see class docstring)."""
        super().__init__()
        self.value = value
        if footprint is not None:
            self.footprint = footprint

    @property
    def A(self) -> Pin:
        """Pad 2 — 'A' (passive)."""
        return self._pin('2')

    @property
    def K(self) -> Pin:
        """Pad 1 — 'K' (passive)."""
        return self._pin('1')
