"""SW_Push — typed component (ingestion-factory output).

GENERATED by src/ecad/ingest/codegen.py — DO NOT EDIT.
Edits go in sw_push_overrides.py (applied by the src.ecad.library registry).
"""

from src.ecad.component import Component, Pin
from src.ecad.model import (
    ElectricalType,
    FootprintRef,
    PinRole,
    PinSpec,
    UnitDef,
    UnitStrategy,
)


class SW_Push(Component):
    part_name = 'SW_Push'
    lib_id = 'Switch:SW_Push'
    reference_prefix = 'SW'
    description = 'Push button switch, generic, two pins'
    datasheet = ''
    footprint = FootprintRef(lib='Button_Switch_SMD', name='SW_SPST_TL3342', source='kicad')
    unit_strategy = UnitStrategy.EXPLICIT
    unit_plan = (  # frozen at generation time
        UnitDef(name='SW_Push', pads=('1', '2')),
    )
    _PIN_SPECS = (
        PinSpec(pad='1', name='1', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
        PinSpec(pad='2', name='2', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
    )

    value = 'SW_Push'

    def __init__(self, value: str = 'SW_Push',
                 footprint: FootprintRef | None = None) -> None:
        """Per-instance value and footprint (see class docstring)."""
        super().__init__()
        self.value = value
        if footprint is not None:
            self.footprint = footprint

    @property
    def V1(self) -> Pin:
        """Pad 1 — '1' (passive)."""
        return self._pin('1')

    @property
    def V2(self) -> Pin:
        """Pad 2 — '2' (passive)."""
        return self._pin('2')
