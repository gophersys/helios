"""Capacitor — typed component (ingestion-factory output).

GENERATED by src/ecad/ingest/codegen.py — DO NOT EDIT.
Edits go in capacitor_overrides.py (applied by the src.ecad.library registry).
"""

from src.ecad.component import Component, Pin
from src.ecad.model import (
    ElectricalType,
    FootprintRef,
    PinRole,
    PinSpec,
    UnitDef,
    UnitStrategy,
)


class Capacitor(Component):
    part_name = 'Capacitor'
    lib_id = 'Device:C'
    reference_prefix = 'C'
    description = 'Unpolarized capacitor'
    datasheet = ''
    footprint = FootprintRef(lib='Capacitor_SMD', name='C_0402_1005Metric', source='kicad')
    unit_strategy = UnitStrategy.EXPLICIT
    unit_plan = (  # frozen at generation time
        UnitDef(name='Capacitor', pads=('1', '2')),
    )
    _PIN_SPECS = (
        PinSpec(pad='1', name='P1', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
        PinSpec(pad='2', name='P2', etype=ElectricalType.PASSIVE, role=PinRole.PASSIVE),
    )

    value = '100nF'

    def __init__(self, value: str = '100nF',
                 footprint: FootprintRef | None = None) -> None:
        """Per-instance value and footprint (see class docstring)."""
        super().__init__()
        self.value = value
        if footprint is not None:
            self.footprint = footprint

    @property
    def P1(self) -> Pin:
        """Pad 1 — 'P1' (passive)."""
        return self._pin('1')

    @property
    def P2(self) -> Pin:
        """Pad 2 — 'P2' (passive)."""
        return self._pin('2')
