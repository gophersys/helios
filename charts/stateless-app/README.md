# charts/stateless-app

Deployment + Service + optional Ingress/HPA/ServiceMonitor/ExternalSecret.
Target: 12-factor HTTP services with no persistent state.

Status: stub. See `charts/CONVENTIONS.md` for the values contract.

TODO: implement templates, values.yaml, values.schema.json, Chart.yaml,
project.json + ctl.sh when the first app consumes this chart.
