# v2 Development Journal — 02.04.26

## Summary

Lighter day. Wrapped up the Codebases module (codebases, releases, artifacts with MinIO upload/download), standardized all API responses into the `{"data": ..., "errors": [...]}` envelope, wired up OpenAPI 3.0 docs with Swagger UI, and wrote the previous day's journal. Not the most motivated but got the important stuff across the line.

---

## Since Last Journal (02.03.26)

| Metric | Count |
|--------|-------|
| New commits | 2 |
| Files changed | 70 |
| Files added | 14 |
| Files modified | 50 |
| Files deleted | 8 |
| Lines inserted | 4,640 |
| Lines deleted | 851 |
| **Net new lines** | **+3,789** |

---

## Commit Log

| # | Commit | Files | Insertions | Deletions |
|---|--------|-------|------------|-----------|
| 16 | Standardize API responses and add OpenAPI documentation | 14 | +1,250 | -174 |
| 17 | Add Codebases module, audit logging, UI components, and remove firmware | 56 | +3,390 | -677 |

---

## What Got Built

### Backend (Python / Flask)

- **Codebases module** — Full CRUD for codebases, releases, and artifacts with file upload/download via MinIO
- **Audit logging** — `log_audit()` helper, tracks all mutations with user, action, resource type, and diff
- **Background scheduler** — Periodic audit log cleanup (30-day retention)
- **API standardization** — Uniform `{"data": ..., "errors": [...]}` envelope applied across all existing endpoints
- **OpenAPI 3.0** — Auto-generated spec via `apispec`, Swagger UI at `/v2/docs` (935-line docs module)
- **Storage client** — MinIO integration for artifact binary upload/download with presigned URLs

### Frontend (React / TypeScript)

- **Codebases UI** — Codebase cards, release management, artifact upload/download
- **History page** — Searchable audit log viewer with detail expansion
- **Guides page** — Placeholder page for documentation/guides
- **Reusable components** — `ConfirmDeleteDialog`, `EmptyState`, `ErrorAlert`, `LoadingState`, `StatusBadge`
- **Sidebar** — Added Codebases, History, and Guides nav entries

### Removed

- Legacy firmware appid module (replaced by Codebases)
- Old Bruno API client files for firmware endpoints

---

## Notes

Mostly a "finish what I started yesterday" kind of day. The Codebases module was the bulk of it — 56 files across backend routes, types, frontend pages, and components. API standardization was satisfying though, nice to have every endpoint return the same shape. Swagger UI is a good quality-of-life win for testing.
