# Open Decisions Register

> Living document · created 2026-06-12. The only blocking human-interaction pattern (P13): items
> land here with options + a recommendation; rulings become ADRs and the row moves to Resolved.

## Open

| # | Decision | Options (recommendation first) | Needed by |
|---|---|---|---|
| OD-FLEET-FREEZE | **The four fleet contract DRAFTS are unfrozen, and freezing them is Mateo's §5 gate.** Each draft ends with the exact question and carries its own open-fork table — **41 forks across the four**, each with a recommendation. This row is the INDEX; the forks have one home each, in the contract that owns them, so they are not copied here. Four of them cannot be delegated, because each changes a frozen surface or rules on Mateo's own words: `fleetenvelope` **F1** (`PeerPlane` mesh-vs-tree — it may put the frozen `agentsession` contract into revision), **F3** (his 2026-08-26 "first-class traceparent fields" instruction, which the measured `propagation.MapCarrier` identity argues against), **F4** (a confirm request on "two verbs, two strengths"), **F8** (`ControlMessage.By` — a wire deletion `.apibaseline` cannot see). | ratify each draft as written, or rule its named forks differently; the freeze lands as contract revisions `agentruntime` R2 + `agentsession` R2 plus the NEW `agentcheckpoint` baseline, and **ADR-0030 (reserved, unwritten) should land in the same decision** so the authority chain stops dangling | before ANY fleet implementation lane starts against a moving target |
| OD-FLEET-SPAWN | ⚠️ **Has a real `Spawn` ever executed against the deployed orchestrator?** The `agentgateway` image comment says it "spawns no harness in-process", while the orchestrator binary opens the session in-process at `reconcile.go:159`, in a distroless image with no `claude` binary. Nobody has run it. | (A) execute one against the deployed orchestrator and record the result — the recommended and only honest option · (B) proceed and discover it mid-lane. **If the answer is "neither has ever run end to end", that is a bigger finding than the whole fleet design** and it outranks every row above. | BEFORE the first fleet implementation slice |
| OD-FLEET-CONNECTORS | **The production connectors path was never traced to a consumer** — "user connectors do not work in the shipped binary at all". Fix site: `main.go:142-160` + `prodserve.go:232-261`. This gates `AgentTeam.connectorScope`, and it is the program's one explicitly named deferral. | (A) land the fix with a test asserting `eden://connector/<id>` resolves in the PRODUCTION composition, BEFORE any per-team connector is claimed to work · (B) claim per-team connectors over a path with no consumer | before any per-team connector claim |
| OD-15-bom | **Services-BOM sourcing** (the http-gateway `internal/server/bom` manifest — which library + contract versions a running gateway was built from, ADR-0023) | (A) embed-at-build of the module graph (a `go:generate` snapshot) · (B) runtime `debug.ReadBuildInfo` read (no build step — the shipped default) · (C) hybrid (build-stamp the contract version, runtime-read the module set). Rule when a fleet operator first needs the manifest as a guarantee, not just a log line. | first multi-version fleet |
| OD-16-pagination | **Pagination home** for list routes in the http-gateway template | a shared `pagination` helper assembled at the resource's `execute` stage (cursor-based, recommended) · per-resource ad-hoc limit/offset · a generated pagination wrapper from the OpenAPI contract. Rule before the first list-heavy resource. | first list resource |
| OD-16-row-authz | **Row-level authorization** beyond the route-level `Required` Grant | the `execute` stage filters/owns the row check against the verified `Identity` (recommended — keeps authz in the one stage that touches persistence) · a persistence-layer policy (RLS-style) · a dedicated authorization library. The route `Required` grant stays the coarse gate regardless. | first per-owner resource |
| OD-16-roles | **Roles home** (where role→grant mappings live) | minted into the JWT at issue time by the identity provider (the dev-JWT signs grants directly today; recommended to keep grants on the token) · a server-side role→grant table in `persistence` · a `roles` library shared across templates. Rule when roles outgrow hand-issued dev-JWTs. | first non-Mateo principal |
| OD-16-chart | **Deploy chart granularity** | one chart per application template (rendered from its `deploy/servicespec`, recommended — isolation, independent versioning) · one umbrella chart across templates · a hybrid (per-template charts + a platform umbrella for the supporting stack). Rides ADR-0022 #2's two-axis renderer either way. | first multi-service deploy |
| OD-16-secrets-injection | **Secrets injection** into a gateway pod | the Vault sidecar token-file path (the ADR-0022 production default the template ships) · projected kubernetes Secrets via `valueFromSecretKeyRef` · the Vault Agent Injector annotations. The rendered manifests carry references only, never values, in every option. | production gateway deploy |
| OD-3 | **Client state/query layers** post-React: replaces the zustand/jotai/xstate/TanStack-Query choices of the former `/LIBRARIES.md` (now 10 §12) | Svelte 5 runes + TanStack Svelte Query; XState's Svelte adapter only if machine-mirroring of backend lifecycle survives review. | M3 |
| OD-4 | **CI executor v1 shape**: own runner on docker/kubernetes from day 1, or wrap an existing runner behind the executor port first | own runner (contract purity, content-aware caching) vs wrap (speed). | L2 |
| OD-13 | **Fleet deployment architecture** (C17): per-customer isolated environments, gated rollouts of one change across N deployments, fleet-wide observability — D4 modeling + orchestrator design | needs architecture work before the production milestone; rides ADR-0012's posture. | post-spine |
| OD-6 | **Hosted-tier billing**: Stripe adapter scope and metering granularity | defer until hosted milestone; F3 contract designed to carry it. | hosted milestone |
| OD-7 | **agentconfiguration open items** inherited from upstream (CLI module split, yaml v3 vs v4, harness plugin model, per-call key rotation, content-based routing) | per upstream agentcfg-architecture §open-questions; rule during WS2. | WS2 |
| OD-9 | **Transcript retention & privacy policy** (per-project retention, redaction verification) | needs ruling before any non-Mateo user exists. | L4 |
| OD-10 | **Visual editor scope** for v1 dashboard (read-only pipeline/artifact viewer first vs editable canvas) | viewer-first recommended; editing re-enters via phase artifacts, not canvas mutation — this constraint is the editor's architectural contract regardless of scope ruling. | L2/L4 |
| OD-11 | **S6 observability stack composition** (trace/metric/log storage, dashboarding) — the one v1 subsystem whose concrete components are unpinned while buf/Connect/kind/etc. are | candidates to be evaluated (e.g. Grafana LGTM stack, ClickHouse-backed, openobserve); must be minimally live by L1 exit (06 §2). | L0/L1 |
| OD-12 | **First managed-cloud adapter scheduling** (EKS/GKE/AKS/DO) — the ladder can complete on local substrates, leaving cloud adapters, billing polling, and cloud drift unexercised | schedule one managed-kubernetes adapter + its billing/drift surface immediately post-L4 (recommended), or accept all four as unscheduled backlog with explicit sign-off. | post-L4 planning |
| OD-14 | **Default-deny egress enforcement gap across v1 substrates** (07 §3, `workspaceprovider`). The contract's default-deny-+-declared-allow egress is only PARTIALLY enforced on the v1 substrates, and the `CapabilityManifest` declares the gap truthfully (the conformance suite verifies the enforced part against the real substrate and Skips honestly where absent — never a fake pass). Concretely: **docker** enforces genuine DEFAULT-DENY for a zero-egress (clean-room) workspace via a per-workspace `--internal` network (verified: off-host dial-out is "Network is unreachable") → `CapEgressPolicy=CapPartial`; it CANNOT do default-deny + SELECTIVE allow (an `--internal` bridge is all-or-nothing; per-destination allow needs daemon-level iptables we do not own), so a workspace that declares egress rules runs on the bridge (declared hosts reachable, but so is everything else). **kubernetes (k3d/kind)** ships flannel, which does NOT enforce `NetworkPolicy` → `CapEgressPolicy=CapAbsent`; the adapter installs the derived default-deny `NetworkPolicy` (inert on flannel) and the conformance egress case Skips honestly. Net v1 gap: NO substrate yet enforces default-deny + selective-allow, and the kubernetes path enforces NO egress at all. | options (recommendation first): (a) adopt a NetworkPolicy-enforcing CNI for the central/test kubernetes substrate (Cilium/Calico) so `CapEgressPolicy` becomes `CapFull` there and the suite's real dial-out assertion runs on kubernetes too; (b) move egress enforcement to the broker/dial-out-agent mesh (07 §3 `management` pattern) and treat the substrate NetworkPolicy as defense-in-depth; (c) accept docker-only default-deny for v1 with the gap declared, scheduling real kubernetes enforcement before any non-Mateo tenant runs on the central cluster. Real selective-allow enforcement is future work (07 §3). | before hosted/central multi-tenant exposure |
| OD-17-apca | **APCA: advisory or gate?** (the `@eden/theme` contrast policy) | (a) store APCA as a non-blocking warning, WCAG 2.x the only gate (recommended default) · (b) offer an opt-in "APCA-strict" tenant mode that *also* gates on \|Lc\|≥75 body / ≥60 large. WCAG 2.x is the mandatory legal gate regardless; adopting APCA *as* the gate carries legal risk (non-normative, no WCAG-3 finalization before ~2030). Corrected cross-walk (Lc 45/60/75 ≈ 3/4.5/7) is a light-bg heuristic, never a conversion. (Design-foundations Part D #1.) | before `@eden/theme` encodes the gate |
| OD-17-type-spacing | **Coupled vs decoupled type/spacing scales** | shared origin (16px) + separate generating functions — type = geometric ratio, spacing = 4px-quantized hybrid (recommended; Material/Carbon/Refactoring-UI) · Utopia-style spacing-coupled-to-type-ratio for fluid surfaces. (Part D #2.) | before `@eden/theme` encodes spacing |
| OD-17-breakpoint | **Breakpoint axis canonical form** | semantic Material window classes (600/840/1200/1600) canonical + device-width (Tailwind/Bootstrap) aliases on top (recommended 🧩) · device-width canonical. (Part D #5.) | before `@eden/theme` emits the breakpoint config |
| OD-17-shadow | **Shadow model** | the Comeau/Ahlin parametric layered generator with Material opacities (0.20/0.14/0.12) as anchors (recommended 🔶) · Material's literal 3-layer table. (Part D #6.) | before `@eden/theme` emits elevation/shadow tokens |
| OD-17-spring | **Spring constants provenance** | ship `.snappy` (bounce 0.15) / `.bouncy` (0.30) tagged as practitioner-measured-not-first-party (⚠️) · pin to a first-party-Apple-documented set only. (Part D #7.) | before `@eden/theme` emits spring tokens |
| OD-17-fluid-spacing | **Fluid vs fixed spacing** | fixed by default, fluid reserved for section-level gaps (`space-16`+) (recommended) · enable a fluid-spacing mode axis. (Part D #8.) | before `@eden/theme` emits the spacing ramp |
| OD-17-tone-table | **M3 tone-table spec revision to pin** | cite a frozen M3 revision for the tone→role table, not "latest" (M3's `On-Primary-Container` light tone moved 10→30 in the 2025 spec). **Ruling needed:** which revision. (Part D #9.) | before `@eden/theme` encodes the role table |
| OD-17-motion-personality | **Brand → motion-personality coupling in v1** | ship a single neutral motion personality and defer brand-driven personality (conservative) · let the brand seed pick a spring `personality` (calm/default/lively) + shadow tint in v1 (the one place brand touches motion). (Part D #10.) | before `@eden/theme` v1 motion slice |
| OD-17-devcontainer-ui | **Devcontainer UI-toolchain additions** (vitest, Playwright + browsers + system libs, eslint/svelte, prettier, axe, Stryker, size-limit) — a **shared-config change** | **hold-and-confirm** with the Go-backend track before it lands (the devcontainer image is a shared surface, deconfliction map §4.5); the toolchain is pinned at build time, "absent tool = FAIL not skip". (Handoff §4.4 / ADR-0024 #6.) | before Phase 0 of the TS pipeline |
| OD-CI-1 | **History-walk home** for the `codeinsight` temporal metrics (contract codeinsight §2) | grow a read-only History capability on `gitrepository.Inspector` (recommended — it owns git, "one concept one home"; additive `.apibaseline` change → contract revision) · a `codeinsight`-local reader over `gitrepository.Backend`. | before `codeinsight` implementation phase |
| OD-CI-2 | **Entity granularity** for the `Report` (contract codeinsight §3) | file-level default in v1, directory/package derived (recommended) · method-level X-Ray hotspots (CodeScene-style) deferred to a later refinement. | `codeinsight` architecture phase |
| OD-CI-3 | **Bus-factor algorithm** (contract codeinsight §3, research §3 / open-Q4) | >50%-line-ownership coverage (recommended default) · per-file primary-developer threshold · Minimum-Critical-Set (NP-hard). Resolved by research-note 06 round-2 finding. | before ownership view ships |
| OD-CI-4 | **Complexity source** for `codeinsight` (contract codeinsight §4 / research open-Q3) | **PROVISIONALLY RESOLVED** (research-note 06 §R2): native-AST per-language `MetricProvider` — Go provider computes cyclomatic+cognitive from `go/ast` (no subprocess); non-Go providers shell to the tool (dependency-cruiser/jscpd/knip). Ratify at the `codeinsight` architecture-phase gate (stays Open until ratified there). | `codeinsight` architecture phase |
| OD-EDITOR-2 | **Desktop ssh-remote read-only enforcement** (research note 07 §5/§7): how an ssh-remote VS Code (the desktop `vscode://vscode-remote/ssh-remote+<host><worktree>` path) is held read-only | a read-only bind / a restricted ssh user / an overlay (recommended — structural read-only, mirroring the sidecar's `readOnly` mount) · accept that desktop ssh is read-write by nature and flag it. Independent of the kubernetes sidecar (ADR-0027); shares only the `sshHost` wire field already on the editor contract. | before the desktop ssh editor endpoint lights up |

## Resolved (ruling → ADR)

| # | Decision | Ruling | ADR |
|---|---|---|---|
| RD-1 | System name | Eden, fully and everywhere; gophersys org kept; repos renamed | 0002 |
| RD-2 | Go floor | 1.26 (supersedes corpus 1.24) | 0003 |
| RD-3 | UI stack | Re-platform on Svelte; supersedes React 19/React Aria/vanilla-extract decisions at Eden level | 0004 |
| RD-4 | Photosphere fate | Re-founded on Svelte as the same standalone asset; theming engine retained | 0005 |
| RD-5 | Hosting posture | Local-first (`eden up`), hosted tier later on same ports | 0006 |
| RD-6 | Bootstrap path | Kernel-first ladder L0–L4 | 0007 |
| RD-7 | First agent connector | Claude Code; pi/omp+DeepSeek second (proves abstraction + cheap arm) | 0008 |
| RD-8 | Library-system open decisions A–F (10 §11) | dependencies slug · eden module root · root gitignored go.work · U1 as gated donor material · bare-host · docs-first | 0009 |
| RD-9 | Documentation scheme + in-repo migration pass | four doc classes, kebab-case naming, attic policy; root planning docs absorbed into doc 10 | 0010 |
| RD-10 | Project document schemas | JSON Schema 2020-12 · dual-surface canonical form (md+frontmatter / yaml → one JSON projection) · `schemas/document/v1/` · design system stays a separate linked F6 artifact | 0011 |
| RD-11 | Compute posture | hosted-default central multi-tenant cluster (an ordinary F1 adapter, metered); clients are control surfaces; local k3d/kind = just another cluster; BYO encouraged | 0012 |
| RD-12 | SCM integration (formerly OD-5) | eden-authority default · byo-authority advanced (org metadata only) · per-project enforcement {enforced via host app \| advisory} with permanently visible guarantees | 0013 |
| RD-13 | Upstream corpus (formerly OD-8) | copied verbatim into `docs/upstream/` as a fifth document class; all citations repointed in-repo; root README becomes the human front door | 0014 |
| RD-14 | Build order + app framework (formerly OD-2) | product-first: apps/frontend (SvelteKit + Svelte 5) hosting the document workspace is the spine; architecture is pulled by product need; ladder unchanged as proof framework | 0015 |
| RD-16 | Svelte behavior layer (formerly OD-1) | **Bits-UI-primary hybrid:** adopt Bits UI (`bits-ui@2.18.1`, Svelte-5-native, MIT) for overlays/menus/the ⌘K palette; **hand-roll the data grid** on Svelte 5 runes over `@tanstack/table-core` (the universal gap no candidate fills); compose the wizard from Bits `Tabs` + Eden runes. Evidence-based spike (fixed APG rubric → 3 candidate profiles → adversarial refutation): adversarially-adjusted weighted totals Bits 4.18 · hand-rolled 3.52 · Melt 3.13; confidence ≈ 0.8. Melt rejected (pre-1.0, single-maintainer, stalled, ships none of menu/Command/grid, classic-API corpus-hallucination trap). Carries the ADR-0004 a11y-evidence obligation: a three-layer stack per pattern (axe on Chromium+WebKit / Playwright keyboard assertions / manual SR matrix) in a version-pinned `a11y-evidence/<component>.md`, wired as a `phase-gate qa`/Stop-hook blocker; plus the `@eden/theme` "define tokens at/above the Portal host" constraint. **OD-1-spikes CONFIRMED GO (2026-06-15):** both runtime spikes passed on Chromium AND WebKit (26/26) — focus-trapped nested Dialog with correct LIFO/scroll-lock/focus-return, axe 0 serious/critical at every depth, Eden tokens verified rendering THROUGH the Bits Portal (non-vacuous: dialog computed color == cascade-resolved token); ⌘K palette 500 items → 40 virtualized rows, fuzzy scorer + grouped a11y clean. Integration lessons for `@eden/behavior`: Bits has no `Command.Dialog` → compose `Dialog.Portal/Content` wrapping `Command.Root`; Command needs two-way `bind:value`; a scrollable results region needs `tabindex=0`. Bits is GO. | 0024 |
| RD-15 | OOM-condition discriminator on the kubernetes Run path (formerly OD-15) | **adopt option (a): the workload-owned pod** — the `workspaceprovider` `Entrypoint`/workload-pod capability makes the container's MAIN process the workload (PID-1), so the kubelet attaches the `OOMKilled` container-status reason to the READABLE workspace container and `ConditionOOMKilled` surfaces NATIVELY on the kubernetes supervised Status. Closes the §7 Q14 exec-into-hold gap for Entrypoint specs (the exec-into-hold model remains for specs without an Entrypoint, with Q14's honest limitation). Delivered + really-tested on real docker (`ConditionOOMKilled` via `Supervised`, no skip) and real k3d (honest-skip where the node does not enforce the cgroup) in Milestone-B B2. | 0022 |
| RD-17 | OpenAPI spec-first vs emit-spec (formerly OD-16-openapi) | **adopt go-first-emit:** the five-file route packages' Go `Request`/`Response` types are the AUTHORING SURFACE, and `contract/openapi.yaml` is EMITTED from them by `tools/openapi` (`bash ./ctl.sh openapi`). `verify-openapi` re-emits and fails the architecture gate on drift (the routes changed but the contract was not regenerated); `gen-client` runs oapi-codegen over the emitted contract to produce the typed Go client the integration lane drives. Rejects spec-first (a hand-authored YAML drifts from the code it documents) and full emit-of-server-types (the routes ARE the typed surface — generating them from a YAML that is itself generated from them is circular). Delivered on the http-gateway reference `resource` CRUD: 6 operations emitted, drift-gated, client-generated, proven end-to-end on real postgres through the emitted client (Milestone-B). | 0023 |
| RD-19 | Read-only editor on kubernetes — workload shape + routing (formerly OD-EDITOR-1 + OD-EDITOR-3) | **Editor-as-SIDECAR, host-per-agent ingress, lib owns both siblings.** The kubernetes editor is a read-only code-server **sidecar** in the agent pod mounting the same workdir `emptyDir` `readOnly:true` (the `--volumes-from …:ro` analog) — NOT a shared service (an `emptyDir` is pod-scoped; a separate Deployment cannot see it without RWX storage). Selected by ONE additive, capability-gated `WorkspaceSpec.Editor *EditorSpec` field (nil ⇒ byte-identical pods), exactly mirroring `Entrypoint`/`CapWorkloadPod`; `EditorSpec` carries NO secret. **OD-EDITOR-1 routing → host-per-agent** (`<agent-id>.editor.<domain>`, wildcard DNS+TLS): cleanest origin isolation, no code-server base-path reconfiguration — rejects path-per-agent (`editor.<domain>/<id>/`) whose proxy/asset/websocket rewriting is more fragile and shares one origin. **OD-EDITOR-3 → YES:** the `workspaceprovider` library owns the local docker sibling too (the same `Editor` field drives the dockeradapter), removing the `deploy/ctl.sh` asymmetry. Client seam grows `CreateService`+`CreateIngress` (namespace-cascade rollback already covers them). | 0027 |
| RD-18 | Density / type-scale / touch-target policy (formerly OD-17-type-ratio + OD-17-density-default + OD-17-c21) | **Density is the user-facing richness axis, not the type ratio (founder ruling 2026-06-14).** `@eden/theme` ships a THREE-STEP user-facing density control — `DensityMode` = `relaxed` \| `standard` \| `dense`, with the MIDDLE (`standard`) the recommended default and `dense` the expert mode "directly applicable to Eden itself" (`EXPERT_DENSITY_MODE`). The modes resolve to engine tiers (`relaxed→spacious`, `standard→comfortable`, `dense→compact`) consistent with the per-surface `DEFAULT_DENSITY` map (application=comfortable, agent=compact), so OD-17-density-default is subsumed: agent surfaces earn `compact` = the dense mode. The **type ratio stays 1.20** (the minor-third math default) — heading *drama* is NOT the lever; density is. OD-17-c21 is resolved: the founder accepts the **re-derived** type sizes (16·1.20^i), the 5 colors + 3 fonts remain immutable seeds, no hand-set 48. Touch-target floor: **`targetContext` defaults to `touch` (44px AAA)** — the decoupled hit target clamps to 44 at any density (the visual box still shrinks), proven by a design-correctness weaken-to-confirm (pointer drops the densest below 44). Delivered + phase-gate-all GREEN in `libs/typescript/theme` (24 design-correctness assertions). | 0024 |
