All done. Deliverables:

**`cache.go`** — LRU cache using `container/list` + `map`, safe for concurrent use with `sync.RWMutex`. Exports `Config`, `New`, `Get`, `Put`, `Len` with the exact signatures from spec.md.

**`cache_test.go`** — 10 tests covering: invalid capacity, valid capacity, miss, hit, update, eviction (oldest evicted, Get refreshes LRU, Put refreshes LRU), Len invariance on update, and concurrent goroutine safety.

**Results:**
```
go build ./...       — passes
go vet ./...         — passes
go test -v -race -bench=. ./... — all 10 tests pass, 2 benchmarks OK:
  BenchmarkGet-10   2,448,127 ops  480.8 ns/op
  BenchmarkPut-10   3,252,542 ops  366.3 ns/op
```
