Done.

**Files:**
- `ratelimiter.go` — token-bucket implementation with Clock interface, Config/Dependencies structs, New constructor, Allow method. Sync mutex, fractional accumulation, capacity capping, context cancellation handling.
- `ratelimiter_test.go` — 11 tests covering invalid config, nil clock fallback, zero/negative tokens, cancelled/deadline contexts, sequential consumption, time-based refill, capacity capping, fractional accumulation, and concurrent safety (via `wg.Go`).

**Verification:**
- `go build ./...` — clean
- `go vet ./...` — clean
- `go test -race -count=1 -v ./...` — 14/14 subtests pass, no race
- `bash ./check.sh` — rulecheck: all rules satisfied, exits 0
