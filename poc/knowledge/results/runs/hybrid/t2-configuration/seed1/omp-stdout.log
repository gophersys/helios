Done. Deliverables:

**`configuration.go`** — package with `Source`, `Config`, and `Load`. Reads `HELIOS_PORT`/`HELIOS_NAME`/`HELIOS_DEBUG` through the injected source function, applies defaults (8080/"helios"/false), validates range and parse, wraps errors with `%w` so `errors.AsType[*strconv.NumError]` works.

**`cmd/configprint/main.go`** — CLI that loads from `os.LookupEnv` (only place `os.Getenv`/`os.LookupEnv` is called, satisfying `go/environment-confinement`).

**`configuration_test.go`** — 11 tests covering: defaults, individual overrides, all-three override, port range bounds (0, 70000), non-integer port, empty name, bad debug value, and `*strconv.NumError` preservation via `errors.AsType`.

Verification:
- `go build ./...` ✓
- `go vet ./...` ✓
- `go test ./... -v` — 11/11 pass ✓
- `bash ./check.sh` — exits 0, `rulecheck: all rules satisfied` ✓
