All deliverables complete.

**Files created:**

- `ring.go` — `Ring` implementation: ring-buffer `slog.Handler` with concurrent safety, eviction, `WithAttrs`/`WithGroup` support. `NewRing(capacity)` returns concrete `*Ring`. Uses shared `ringState` so clones share the same buffer and mutex.
- `multi.go` — `New(handlers ...slog.Handler)` using `slog.NewMultiHandler` (Go 1.26+), returns error on empty handlers.
- `ring_test.go` — 13 tests covering: invalid capacity, empty records, handle+retrieve, eviction order, returned-slice isolation, `WithAttrs`, `WithGroup`, `Enabled`, concurrent safety, `New` with zero/single/multiple handlers.

**Verification:**
- `go build ./...` — passes
- `go vet ./...` — passes
- `go test ./... -v` — 13/13 pass
- `bash ./check.sh` — `rulecheck: all rules satisfied` → exits 0

Export signatures match `spec.md` exactly: `NewRing(capacity int) (*Ring, error)`, `(*Ring).Records() []slog.Record`, `New(handlers ...slog.Handler) (*slog.Logger, error)`.
