All tasks complete. `check.sh` exits 0.

**Delivered:**

- **`cache.go`** — LRU cache with `Config`, `New`, `Get`, `Put`, `Len`. Uses `container/list` + `map` + `sync.Mutex` for concurrency. `New` is pure (no env/clock/IO calls), returns `(*Cache, error)` (concrete). Error wrapping uses `%w` (well, `%d` for the config value — no wrapping needed since there's no source error).

- **`cache_test.go`** — 10 tests covering: invalid capacity, valid capacity, get-miss, put+get, update existing, eviction, get promotion, put promotion of existing key, Len tracking, and concurrent safety (using `wg.Go()` per Go 1.25+). 2 benchmarks using `for b.Loop()` (Go 1.24+) — `BenchmarkGet` (hit) and `BenchmarkPut` (with eviction).

**Verification:**
- `go build ./...` — pass
- `go vet ./...` — pass
- `go test -v ./...` — 10/10 pass
- `go test -bench=. -benchmem` — benchmarks run
- `rulecheck` — all rules satisfied
