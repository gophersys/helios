Done. Implementation summary:

**`retry.go`** — Exported API matches spec exactly:
- `Sleeper` interface (1 method: `Sleep(ctx, duration)`)
- `TransientError` with `Err`/`RetryAfter` fields, `Error()`/`Unwrap()` methods
- `Config` (`MaxAttempts`, `BaseDelay`), `Dependencies` (`Sleeper`)
- `New` validates config, defaults nil sleeper to clock-based
- `Do` loops up to `MaxAttempts`, checks `errors.AsType[*TransientError]` for retryability, uses `RetryAfter` when >0 else `BaseDelay * 2^attempt`, returns sleeper errors or last failure with `errors.Is` matching

**`retry_test.go`** — 18 tests covering:
- New validation (bad MaxAttempts, bad BaseDelay, default sleeper, valid)
- Do immediate success, success after retry, non-retryable error, wrapped TransientError
- Attempt exhaustion, RetryAfter override, sleeper error, exponential backoff sequence
- Single attempt edge case, zero BaseDelay edge case
- TransientError invariants, default sleeper cancellation/timeout

All passing: `go build`, `go vet`, `go test` (18/18), `bash ./check.sh` (rulecheck: all satisfied).
