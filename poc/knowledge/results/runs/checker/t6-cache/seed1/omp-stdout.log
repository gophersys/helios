Done. Deliverables:

**`cache.go`** — LRU cache with:
- `Config{Capacity}` / `New` (validates `> 0`)
- `Get` (hit → move-to-front), `Put` (update/insert + evict LRU when full), `Len`
- Thread-safe via `sync.RWMutex`; O(1) ops via `map[string]*list.Element` + `container/list`

**`cache_test.go`** — 9 tests + `BenchmarkGet`/`BenchmarkPut`:
- Invalid capacity, empty get, put+get, update, eviction, eviction-with-recent-access, Len, concurrent safety (race detector clean)
- Both benchmarks use `RunParallel` with hit ratio guaranteeing evictions for `BenchmarkPut`

**Verification**: `go build`, `go vet`, `go test -race -bench=.` all pass; `bash check.sh` exits 0 (rulecheck lint fixed by using `sync.WaitGroup.Go`).
