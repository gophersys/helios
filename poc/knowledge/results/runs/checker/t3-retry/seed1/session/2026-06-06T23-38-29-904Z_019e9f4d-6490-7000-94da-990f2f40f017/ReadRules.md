{
  "concatenated": "--- /Users/mateo/helios/poc/knowledge/rules/01-constructor-purity.yaml ---\nid: go/constructor-purity\nclass: C1\ncategory: architecture\nstatement: >-\n  A constructor (any exported function whose name is New or starts with New)\n  must be pure: it must not directly call time.Now, time.Sleep, timers,\n  os.Getenv/LookupEnv/Environ, file I/O (os.Open/ReadFile/WriteFile/Create),\n  network I/O (net.Dial*, http.Get/Post/...), or randomness (math/rand,\n  crypto/rand.Read). All environmental effects enter through injected\n  dependencies.\nrationale: >-\n  New(configuration, dependencies) is the helios spine: a pure constructor\n  makes every component substitutable, deterministic to test, and forces the\n  composition root to own all wiring. A constructor that reads the clock or\n  the environment hides an input and makes behavior untestable without\n  patching globals.\napplicability: >-\n  Any non-main, non-test package that defines an exported constructor.\nexceptions:\n  - >-\n    Defaulting a missing dependency to a real adapter (e.g. if deps.Clock ==\n    nil { deps.Clock = systemClock{} }) is allowed: the adapter's methods may\n    touch the environment at call time, but the constructor itself stays pure.\n  - package main / composition roots are exempt — wiring is their job.\nexample:\n  good: |\n    type Clock interface{ Now() time.Time }\n    func New(cfg Config, deps Dependencies) (*Limiter, error) {\n        if deps.Clock == nil { deps.Clock = systemClock{} } // adapter default: allowed\n        return &Limiter{clock: deps.Clock, capacity: cfg.Capacity}, nil\n    }\n  bad: |\n    func New(cfg Config) (*Limiter, error) {\n        return &Limiter{last: time.Now(), capacity: cfg.Capacity}, nil // hidden input\n    }\noracle:\n  kind: analyzer\n  ref: constructorpurity\nfix_hint: >-\n  Inject the effect as a dependency (e.g. a Clock interface with Now()) and\n  default it to a real adapter when nil; never call the environment directly\n  inside New*.\nprovenance:\n  - source: \"helios LIBRARY-SYSTEM.md §1.5 (hexagonal everywhere; New(configuration, dependencies) pure)\"\n    tier: 4\n  - source: \"Google Go Style Guide — best practices: dependency injection over global state\"\n    tier: 2\n  - source: \"Dave Cheney, 'SOLID Go Design' (GolangUK 2016) — dependency inversion\"\n    tier: 3\nstaleness: { go_minimum: \"1.18\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: medium\n\n--- /Users/mateo/helios/poc/knowledge/rules/02-environment-confinement.yaml ---\nid: go/environment-confinement\nclass: C1\ncategory: architecture\nstatement: >-\n  os.Getenv, os.LookupEnv and os.Environ may be called only in package main\n  (the composition root). Library packages receive configuration as values or\n  through an injected source function — never by reading the process\n  environment themselves.\nrationale: >-\n  Environment reads scattered through libraries create hidden global inputs:\n  tests interfere with each other, behavior differs between hosts, and the\n  composition root loses authority over wiring. 12-factor \"config in the\n  environment\" applies to the deployable, not to every package.\napplicability: >-\n  Any library (non-main) package; binds whenever configuration values are\n  needed.\nexceptions:\n  - package main and cmd/* composition roots — reading the environment is exactly their job.\n  - test files using t.Setenv to exercise a composition root.\nexample:\n  good: |\n    // library\n    type Source func(key string) (value string, found bool)\n    func Load(source Source) (Config, error) { v, ok := source(\"HELIOS_PORT\"); ... }\n    // main\n    cfg, err := configuration.Load(func(k string) (string, bool) { return os.LookupEnv(k) })\n  bad: |\n    // library\n    func Load() (Config, error) {\n        port := os.Getenv(\"HELIOS_PORT\") // hidden global input in a library\n        ...\n    }\noracle:\n  kind: analyzer\n  ref: environmentconfinement\nfix_hint: >-\n  Accept a source function (func(key string) (string, bool)) or a resolved\n  Config value; let package main read the real environment and inject it.\nprovenance:\n  - source: \"helios LIBRARY-SYSTEM.md §2 (no `if environment == ...` in business code; stage selects injected wiring at the composition root only)\"\n    tier: 4\n  - source: \"The Twelve-Factor App, factor III — config (scoped to the deploy, not the package)\"\n    tier: 3\n  - source: \"Peter Bourgon, 'Go best practices, six years in' — flag/env parsing belongs in func main\"\n    tier: 3\nstaleness: { go_minimum: \"1.18\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: medium\n\n--- /Users/mateo/helios/poc/knowledge/rules/03-error-wrapping.yaml ---\nid: go/error-wrapping\nclass: C1\ncategory: idiom\nstatement: >-\n  When propagating an error with added context use fmt.Errorf with the %w verb\n  (or errors.Join for aggregation). Never format an error with %v/%s into a new\n  error and never concatenate err.Error() — both destroy the error tree, so\n  errors.Is/As/AsType stop working for callers.\nrationale: >-\n  Wrapped errors keep sentinel and typed errors inspectable across package\n  boundaries. A %v-formatted error is a dead string: callers can no longer\n  branch on the cause, which forces string matching — the canonical Go\n  anti-pattern.\napplicability: >-\n  Any code path that returns an error derived from another error.\nexceptions:\n  - >-\n    Deliberate boundary opacity: when an error must NOT be inspectable (e.g.\n    redacting internal detail at an API boundary), formatting with %v and\n    documenting the opacity is correct.\n  - Log messages (not returned errors) may format errors with %v freely.\nexample:\n  good: |\n    if err != nil {\n        return fmt.Errorf(\"load configuration: %w\", err)\n    }\n  bad: |\n    if err != nil {\n        return fmt.Errorf(\"load configuration: %v\", err) // breaks errors.Is/As\n    }\noracle:\n  kind: analyzer\n  ref: errorwrapping\nfix_hint: >-\n  Replace the %v/%s verb with %w (fmt.Errorf(\"context: %w\", err)), or use\n  errors.Join to aggregate multiple errors.\nprovenance:\n  - source: \"Go release notes 1.13 + errors package docs — error wrapping with %w\"\n    tier: 1\n  - source: \"Go blog, 'Working with Errors in Go 1.13'\"\n    tier: 2\n  - source: \"100 Go Mistakes and How to Avoid Them, mistake #49 (ignoring when to wrap an error)\"\n    tier: 3\nstaleness: { go_minimum: \"1.13\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: low\n\n--- /Users/mateo/helios/poc/knowledge/rules/04-interface-size.yaml ---\nid: go/interface-size\nclass: C1\ncategory: idiom\nstatement: >-\n  An exported interface declares at most 4 methods. Bigger contracts must be\n  split into composable single-purpose interfaces (io.Reader/io.Writer style).\nrationale: >-\n  The bigger the interface, the weaker the abstraction (Go proverb). Small\n  interfaces are satisfiable by fakes, decorators and adapters; fat interfaces\n  force every implementation to stub methods it does not care about and make\n  conformance suites combinatorial.\napplicability: >-\n  Any exported interface type in a library package.\nexceptions:\n  - >-\n    Interfaces mirroring an external wire contract (a generated client, a\n    protocol surface) may exceed the limit; isolate them in the adapter layer.\n  - Unexported interfaces are not scored — internal seams may be pragmatic.\nexample:\n  good: |\n    type Reader interface{ Read(p []byte) (int, error) }\n    type Closer interface{ Close() error }\n    type ReadCloser interface { Reader; Closer }\n  bad: |\n    type Store interface { // 7 methods: unfakeable, unsplittable\n        Get(...); Put(...); Delete(...); List(...); Watch(...); Compact(...); Stats(...)\n    }\noracle:\n  kind: analyzer\n  ref: interfacesize\nfix_hint: >-\n  Split the interface into single-purpose pieces and compose them by embedding;\n  consumers should declare the subset they actually use.\nprovenance:\n  - source: \"Go Proverbs (Rob Pike, Gopherfest 2015) — 'The bigger the interface, the weaker the abstraction.'\"\n    tier: 3\n  - source: \"helios LIBRARY-SYSTEM.md §9 — golangci-lint interfacebloat max 5; POC tightens to 4\"\n    tier: 4\nstaleness: { go_minimum: \"1.0\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: low\n\n--- /Users/mateo/helios/poc/knowledge/rules/05-return-concrete.yaml ---\nid: go/return-concrete\nclass: C1\ncategory: idiom\nstatement: >-\n  Exported functions and constructors return concrete types (or error), not\n  project-defined interface types. \"Accept interfaces, return structs\":\n  the consumer decides which interface to view the value through.\nrationale: >-\n  Returning an interface erases the concrete API surface, blocks method\n  additions without breaking callers, defeats inlining, and inverts ownership:\n  interfaces belong to consumers, not producers. Returning the struct keeps\n  the full API available and lets each caller declare its own narrow seam.\napplicability: >-\n  Any exported function in a library package whose result is a project-defined\n  named interface.\nexceptions:\n  - error and context.Context are interfaces by design — always allowed.\n  - >-\n    Standard-library interfaces (io.Reader, slog.Handler, http.Handler, ...)\n    are established contracts; returning them is allowed when the function's\n    purpose is to produce that contract.\n  - >-\n    Factories that genuinely select among implementations at runtime may\n    return an interface; document why.\nexample:\n  good: |\n    func New(cfg Config) (*Cache, error) { ... }   // caller sees the full *Cache API\n  bad: |\n    type Cacher interface{ Get(string) (string, bool); Put(string, string) }\n    func New(cfg Config) (Cacher, error) { ... }   // producer-owned interface\noracle:\n  kind: analyzer\n  ref: returnconcrete\nfix_hint: >-\n  Return the concrete *Type; if a caller needs a seam, the caller defines the\n  interface next to its own code.\nprovenance:\n  - source: \"Go Code Review Comments — interfaces (consumer side); CodeReviewComments wiki\"\n    tier: 2\n  - source: \"helios LIBRARY-SYSTEM.md §9 — 'accept interfaces, return concrete' + ireturn linter\"\n    tier: 4\n  - source: \"Jack Lindamood, 'Preemptive Interface Anti-Pattern in Go' (2016)\"\n    tier: 3\nstaleness: { go_minimum: \"1.0\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: medium\n\n--- /Users/mateo/helios/poc/knowledge/rules/06-context-first.yaml ---\nid: go/context-first\nclass: C1\ncategory: idiom\nstatement: >-\n  If a function takes a context.Context it is the first parameter and it is\n  named ctx. Contexts are never stored in structs and never replaced by a\n  package-level context.\nrationale: >-\n  A uniform context position makes cancellation auditable at a glance and\n  composes with every API in the ecosystem. Struct-stored contexts outlive\n  their request scope and leak cancellation semantics.\napplicability: >-\n  Any function or method that performs cancellable or deadline-bound work.\nexceptions:\n  - >-\n    Methods implementing a third-party interface whose signature fixes a\n    different parameter order.\n  - >-\n    A struct field holding a context is acceptable only for the documented\n    http.Request-style carrier pattern.\nexample:\n  good: |\n    func (r *Retrier) Do(ctx context.Context, operation func(ctx context.Context) error) error\n  bad: |\n    func (r *Retrier) Do(operation func() error, ctx context.Context) error // ctx buried\noracle:\n  kind: analyzer\n  ref: contextfirst\nfix_hint: >-\n  Move the context.Context to the first parameter position and name it ctx.\nprovenance:\n  - source: \"Go blog, 'Go Concurrency Patterns: Context' + context package docs\"\n    tier: 2\n  - source: \"Go Code Review Comments — contexts\"\n    tier: 2\nstaleness: { go_minimum: \"1.7\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: low\n\n--- /Users/mateo/helios/poc/knowledge/rules/07-errors-astype.yaml ---\nid: go/errors-astype\nclass: C1\ncategory: post-cutoff\nstatement: >-\n  To extract a typed error from an error tree use the generic\n  errors.AsType[E](err) (Go 1.26), not errors.As(err, &target). AsType is\n  type-safe at compile time, needs no pre-declared target variable, and is\n  faster.\nrationale: >-\n  errors.As takes any (interface{}) and panics at runtime on a mis-typed\n  target; the 1.26 generic form moves that to compile time and reads as a\n  single expression. The Go release notes describe it as a \"type-safe, faster\n  alternative to As\". New code on Go >= 1.26 has no reason to use the old form.\napplicability: >-\n  Code on Go >= 1.26 that branches on a typed error in an error tree.\nexceptions:\n  - Code that must build on Go < 1.26 keeps errors.As.\n  - >-\n    Matching against an interface type with errors.As where the target type\n    is only known dynamically.\nexample:\n  good: |\n    if transient, ok := errors.AsType[*TransientError](err); ok {\n        delay = transient.RetryAfter\n    }\n  bad: |\n    var transient *TransientError\n    if errors.As(err, &transient) { delay = transient.RetryAfter }\noracle:\n  kind: analyzer\n  ref: errorsastype\nfix_hint: >-\n  Replace `var t *T; errors.As(err, &t)` with `t, ok := errors.AsType[*T](err)`\n  (Go 1.26+).\nprovenance:\n  - source: \"Go 1.26 release notes — errors: new generic AsType function (go.dev/doc/go1.26)\"\n    tier: 1\nstaleness: { go_minimum: \"1.26\", review_by: \"2026-12\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: high\n\n--- /Users/mateo/helios/poc/knowledge/rules/08-slog-multihandler.yaml ---\nid: go/slog-multihandler\nclass: C1\ncategory: post-cutoff\nstatement: >-\n  To send log records to several slog handlers, use slog.NewMultiHandler\n  (Go 1.26). Do not hand-roll a fan-out type that holds a slice of handlers\n  and loops over them in Handle.\nrationale: >-\n  Hand-rolled multi-handlers were the standard workaround before 1.26 and they\n  routinely get the contract wrong: Enabled short-circuiting, WithAttrs/\n  WithGroup cloning, and error joining across children. The stdlib version\n  handles all four correctly and is the single obvious way.\napplicability: >-\n  Code on Go >= 1.26 that needs one logger writing to multiple sinks.\nexceptions:\n  - Code that must build on Go < 1.26 may hand-roll (or vendor) a fan-out handler.\n  - >-\n    A custom Handler that filters/transforms per child (not a plain fan-out)\n    is real logic, not a multi-handler — allowed.\nexample:\n  good: |\n    logger := slog.New(slog.NewMultiHandler(jsonHandler, ringHandler))\n  bad: |\n    type multiHandler struct{ handlers []slog.Handler }\n    func (m *multiHandler) Handle(ctx context.Context, r slog.Record) error {\n        for _, h := range m.handlers { h.Handle(ctx, r) } // wrong on 4 contract points\n        return nil\n    }\noracle:\n  kind: analyzer\n  ref: slogmultihandler\nfix_hint: >-\n  Delete the hand-rolled fan-out type and use\n  slog.New(slog.NewMultiHandler(h1, h2, ...)) (Go 1.26+).\nprovenance:\n  - source: \"Go 1.26 release notes — log/slog: new NewMultiHandler and MultiHandler (go.dev/doc/go1.26)\"\n    tier: 1\nstaleness: { go_minimum: \"1.26\", review_by: \"2026-12\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: high\n\n--- /Users/mateo/helios/poc/knowledge/rules/09-benchmark-loop.yaml ---\nid: go/benchmark-loop\nclass: C1\ncategory: post-cutoff\nstatement: >-\n  Benchmarks iterate with `for b.Loop()` (Go 1.24), not\n  `for i := 0; i < b.N; i++` and not `for range b.N`. b.Loop keeps setup and\n  cleanup outside the measured region exactly once and prevents the compiler\n  from optimizing the benchmarked call away.\nrationale: >-\n  The b.N pattern re-runs setup on every benchmark restart, silently measures\n  dead code when the compiler proves results unused, and is the single most\n  copied stale idiom in Go. b.Loop fixes all three; since Go 1.26 it no longer\n  even inhibits inlining.\napplicability: >-\n  Any *_test.go benchmark on Go >= 1.24.\nexceptions:\n  - Code that must build on Go < 1.24 keeps b.N.\n  - >-\n    Benchmarks that intentionally index by iteration (e.g. pre-generated\n    inputs[i]) may keep a manual counter alongside b.Loop.\nexample:\n  good: |\n    func BenchmarkGet(b *testing.B) {\n        c := mustCache(b)\n        for b.Loop() { c.Get(\"k\") }\n    }\n  bad: |\n    func BenchmarkGet(b *testing.B) {\n        c := mustCache(b)\n        for i := 0; i < b.N; i++ { c.Get(\"k\") } // stale idiom: setup re-runs, dead-code risk\n    }\noracle:\n  kind: analyzer\n  ref: benchmarkloop\nfix_hint: >-\n  Replace the b.N loop with `for b.Loop() { ... }` (Go 1.24+).\nprovenance:\n  - source: \"Go 1.24 release notes — testing: new B.Loop method (go.dev/doc/go1.24)\"\n    tier: 1\n  - source: \"Go 1.26 release notes — testing: B.Loop no longer prevents inlining (go.dev/doc/go1.26)\"\n    tier: 1\nstaleness: { go_minimum: \"1.24\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: high\n\n--- /Users/mateo/helios/poc/knowledge/rules/10-waitgroup-go.yaml ---\nid: go/waitgroup-go\nclass: C1\ncategory: post-cutoff\nstatement: >-\n  When spawning one goroutine per task, use sync.WaitGroup.Go(f) (Go 1.25)\n  instead of the wg.Add(1) / go func() { defer wg.Done(); ... }() triple.\nrationale: >-\n  The Add/go/Done triple has two classic failure modes — Add inside the\n  goroutine (race with Wait) and a forgotten Done on an early return (deadlock).\n  WaitGroup.Go makes both unrepresentable and reads as one line.\napplicability: >-\n  Code on Go >= 1.25 that spawns goroutines tracked by a sync.WaitGroup,\n  where the goroutine is created at the call site.\nexceptions:\n  - Code that must build on Go < 1.25 keeps Add/Done.\n  - >-\n    Decoupled lifecycles — when Add happens in one place and the goroutine is\n    started elsewhere (worker pools handing tasks to pre-started workers),\n    Add/Done remains the correct primitive.\nexample:\n  good: |\n    var wg sync.WaitGroup\n    for _, input := range inputs {\n        wg.Go(func() { process(input) })\n    }\n    wg.Wait()\n  bad: |\n    var wg sync.WaitGroup\n    for _, input := range inputs {\n        wg.Add(1)\n        go func() { defer wg.Done(); process(input) }()\n    }\n    wg.Wait()\noracle:\n  kind: analyzer\n  ref: waitgroupgo\nfix_hint: >-\n  Replace wg.Add(1) + go func(){ defer wg.Done(); ... }() with\n  wg.Go(func(){ ... }) (Go 1.25+).\nprovenance:\n  - source: \"Go 1.25 release notes — sync: new WaitGroup.Go method (go.dev/doc/go1.25)\"\n    tier: 1\nstaleness: { go_minimum: \"1.25\", review_by: \"2027-06\" }\nstatus: VERIFIED\nhypothesis_baseline_violation: high"
}