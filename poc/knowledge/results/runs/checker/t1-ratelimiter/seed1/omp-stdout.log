All done. Here's what was delivered:

**Files:**
- `ratelimiter.go` — token-bucket implementation with `Clock` interface, `Config`, `Dependencies`, `New`, `Allow`. Thread-safe via `sync.Mutex`, fractional refill accumulation, capped at capacity, system clock fallback.
- `ratelimiter_test.go` — 20 tests covering: invalid params, nil clock, context cancellation (both `Canceled` and `DeadlineExceeded`), full bucket start, sequential consumption, partial consumption denial, refill over time, cap at capacity, zero refill rate, fractional accumulation, multi-call refill correctness, concurrent safety, race detection with `-race`, and bulk consumption.

**Verified:**
- `go build ./...` — OK
- `go vet ./...` — OK
- `go test -race -count=1 ./...` — 20/20 PASS
- `bash ./check.sh` (build + vet + rulecheck) — exit 0, all rules satisfied
