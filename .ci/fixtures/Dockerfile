# The fixture hadolint reads. It must PASS at the pinned hadolint, so a green
# run says the linter worked and not that it found nothing to read.
FROM ubuntu:24.04

SHELL ["/bin/bash", "-o", "pipefail", "-c"]

RUN echo "nothing is built from this file"

CMD ["/bin/bash"]
