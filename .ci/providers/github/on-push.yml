name: on-push

on:
  push:
    # init/seed is the current working branch (the repo default today); main is the future
    # integration target. CI runs on a push to either so it fires NOW + keeps working after the
    # main-as-integration cutover (a tracked follow-up).
    branches: [init/seed, main]
  workflow_dispatch:

concurrency:
  group: on-push-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read

jobs:
  check:
    name: affected-check
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/gophersys/base:latest
      credentials:
        username: mateosegura
        password: ${{ secrets.GHCR_PULL_TOKEN }}
    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Run affected-check
        env:
          NX_BASE: ${{ github.event.before }}
        run: bash .ci/ctl.sh affected-check
