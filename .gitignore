# West workspace — everything under ws/ is fetched, except the manifest.
.west/
ws/*
!ws/west.yml

# Go build output
tools/sfd/sfd

# macOS Finder droppings — catalogScan flags any stray in catalog/, and a
# .DS_Store must not be able to turn the gate red
.DS_Store
