# =============================================================================
# mtib-server — Edge Test Bench Service
# =============================================================================
# Matches MtibEnvConfig in src/config.py (1:1 with EnvConfig annotations).
# =============================================================================

# ── Hardware ─────────────────────────────────────────────────────────────────
HARDWARE_VERSION=REV1.2

# ── Logging ──────────────────────────────────────────────────────────────────
LOG_LEVEL=4
LOG_PATH=logs

# ── Server ───────────────────────────────────────────────────────────────────
SERVER_PORT=50053

# ── Assets ───────────────────────────────────────────────────────────────────
ASSETS_PATH=assets

# ── Metrics (MQTT) ───────────────────────────────────────────────────────────
METRICS_ENABLED=false
METRICS_BROKER_URL=mqtt://kubecop.ad.corekinect.com:1883

# ── Motion (FluidNC linear rail) ─────────────────────────────────────────────
MOTION_ENABLED=false
