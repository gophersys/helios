services:
  mtib-server:
    image: containers.ad.corekinect.com/mtib-server:dev
    pull_policy: always
    privileged: true
    ports:
      - ${SERVER_PORT:-50053}:${SERVER_PORT:-50053}
    env_file:
      - ../.env
    volumes:
      - /dev:/dev
