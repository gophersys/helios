services:
  mtib-server:
    image: containers.ad.corekinect.com/mtib-server:dev
    pull_policy: always
    privileged: true
    ports:
      - 50053:50053
    env_file:
      - ../.env
    volumes:
      - /dev:/dev
