{
    "name": "proxy",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/fixtures/mtib_sigma5/proxy",
    "projectType": "application",
    "targets": {
      "build":{
        "command": "nx run proxy:containerize",
        "cwd": "{workspaceRoot}"
      },
      "containerize": {
        "executor": "@nx-tools/nx-container:build",
        "cwd": "{workspaceRoot}",
        "options": {
          "context": "{workspaceRoot}",
          "file": "apps/fixtures/mtib_sigma5/proxy/deploy/Dockerfile",
          "platforms": [
            "linux/amd64"
          ],
          "load": false,
          "push": true,
          "tags": [
            "ccr01.ad.corekinect.com/proxy:1.0"
          ]
        }
      }
    }
  }