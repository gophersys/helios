{
    "name": "executor",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/fixtures/mtib_sigma5/executor",
    "projectType": "application",
    "targets": {
      "build":{
        "command": "nx run operator:containerize",
        "cwd": "{workspaceRoot}"
      },
      "containerize": {
        "executor": "@nx-tools/nx-container:build",
        "cwd": "{workspaceRoot}",
        "options": {
          "context": "{workspaceRoot}",
          "file": "apps/fixtures/mtib_sigma5/operator/deploy/Dockerfile",
          "platforms": [
            "linux/arm64"
          ],
          "load": true,
          "push": false,
          "tags": [
            "control-plane:5000/executor:1.0"
          ]
        }
      }
    }
  }