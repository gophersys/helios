{
    "name": "mtib_posix",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/fixtures/mtib_sigma5/posix",
    "projectType": "application",
    "targets": {
      "build":{
        "command": "nx run mtib_posix:containerize",
        "cwd": "{workspaceRoot}"
      },
      "deploy":{
        "command": "kubectl apply -f deploy/deployment.yaml",
        "cwd": "{projectRoot}"
      },
      "teardown":{
        "command": "kubectl delete deployment mtib-posix",
        "cwd": "{projectRoot}"
      },
      "containerize": {
        "executor": "@nx-tools/nx-container:build",
        "cwd": "{workspaceRoot}",
        "options": {
          "context": "{workspaceRoot}",
          "file": "apps/fixtures/mtib_sigma5/posix/deploy/Dockerfile",
          "platforms": [
            "linux/arm64"
          ],
          "load": true,
          "push": false,
          "tags": [
            "control-plane:5000/mtib-posix:1.42"
          ]
        }
      }
    }
  }