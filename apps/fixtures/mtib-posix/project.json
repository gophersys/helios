{
    "name": "mtib-posix",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "",
    "projectType": "application",
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "commands": [
            "docker build ."
          ],
          "cwd": "{projectRoot}"
        }
      }
    }
  }