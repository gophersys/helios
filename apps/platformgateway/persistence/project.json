{
  "name": "platformgateway-persistence",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "apps/platformgateway/persistence",
  "tags": ["scope:apps", "type:codegen", "eden:axis=persistence"],
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": { "command": "bash ./ctl.sh generate", "cwd": "{projectRoot}" },
      "cache": false
    },
    "verify": {
      "executor": "nx:run-commands",
      "options": { "command": "bash ./ctl.sh verify", "cwd": "{projectRoot}" },
      "cache": false
    }
  }
}
