{
  "name": "validation-sigma5",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/validation/sigma5",
  "projectType": "application",
  "targets": {
    "build": {
      "command": "nx run validation-sigma5:containerize",
      "cwd": "{workspaceRoot}"
    },
    "push": {
      "dependsOn": [
        "build"
      ],
      "command": "docker push containers.ad.corekinect.com/concord-validation-sigma5:latest"
    },
    "containerize": {
      "executor": "@nx-tools/nx-container:build",
      "cwd": "{workspaceRoot}",
      "options": {
        "context": "{workspaceRoot}",
        "file": "apps/validation/sigma5/Dockerfile",
        "platforms": [
          "linux/arm64"
        ],
        "load": true,
        "push": false,
        "tags": [
          "containers.ad.corekinect.com/concord-validation-sigma5:latest"
        ]
      }
    },
    "setup": {
      "executor": "nx:run-commands",
      "cwd": "{projectRoot}",
      "options": {
        "cwd": "{projectRoot}",
        "commands": [
          {
            "command": "python3 -m venv .venv"
          },
          {
            "command": "bash -c 'source .venv/bin/activate && pip install -e .'"
          },
          {
            "command": "cp .env.example .env"
          }
        ],
        "parallel": false
      }
    }
  }
}