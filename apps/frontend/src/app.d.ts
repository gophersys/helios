// See https://svelte.dev/docs/kit/types#app.d.ts for the full set of declarations.
// The App namespace types the SvelteKit ambient contract (Error/Locals/PageData/etc.);
// entries are filled in as the document workspace pulls server-side state into existence.
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export {};
