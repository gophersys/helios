/**
 * Global teardown for the E2E story suite.
 * Placeholder — stages add their cleanup logic here as needed.
 */
export default async function globalTeardown(): Promise<void> {
  console.log('[e2e] Global teardown complete.');
}
