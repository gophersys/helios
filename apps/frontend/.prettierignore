# SvelteKit + Vite generated output and the embedded validator binary — never linted.
.svelte-kit/
build/
.eden/
node_modules/
static/
# Playwright run artifacts (results, traces, HTML report) — generated, never linted.
test-results/
playwright-report/
