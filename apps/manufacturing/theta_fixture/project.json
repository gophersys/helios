{
  "name": "manufacturing-theta-fixture",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/manufacturing/theta_fixture",
  "projectType": "application",
  "targets": {
    "build": {
      "command": "nx run manufacturing-theta-fixture:containerize",
      "cwd": "{workspaceRoot}"
    },
    "push": {
      "dependsOn": [
        "build"
      ],
      "command": "docker push containers.ad.corekinect.com/concord-manufacturing-theta:latest"
    },
    "containerize": {
      "executor": "@nx-tools/nx-container:build",
      "cwd": "{workspaceRoot}",
      "options": {
        "context": "{workspaceRoot}",
        "file": "apps/manufacturing/theta_fixture/Dockerfile",
        "platforms": [
          "linux/amd64"
        ],
        "load": true,
        "push": false,
        "tags": [
          "containers.ad.corekinect.com/concord-manufacturing-theta:latest"
        ]
      }
    },
    "setup": {
      "executor": "nx:run-commands",
      "cwd": "{projectRoot}",
      "options": {
        "cwd": "{projectRoot}",
        "commands": [
          {
            "command": "python3 -m venv .venv"
          },
          {
            "command": "bash -c 'source .venv/bin/activate && pip install -e .'"
          },
          {
            "command": "cp .env.example .env"
          }
        ],
        "parallel": false
      }
    }
  }
}
