apiVersion: v1
kind: Service
metadata: { name: flaresolverr, namespace: media, labels: { app: flaresolverr } }
spec:
  selector: { app: flaresolverr }
  ports: [ { name: http, port: 8191, targetPort: http } ]
