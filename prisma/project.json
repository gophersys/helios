{
    "name": "database",
    "$schema": "../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "prisma",
    "projectType": "application",
    "targets": {
        "generate-client": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "yarn prisma generate"
                    }
                ],
                "cwd": "{projectRoot}",
                "parallel": false
            }
        },
        "migrate": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "yarn prisma migrate dev"
                    }
                ],
                "cwd": "{projectRoot}",
                "parallel": false
            }
        },
        "migrate-deploy": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "yarn prisma migrate deploy"
                    }
                ],
                "cwd": "{projectRoot}",
                "parallel": false
            }
        },
        "setup": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "nx run database:generate-client"
                    },
                    {
                        "command": "nx run database:migrate"
                    }
                ],
                "cwd": "{workspaceRoot}",
                "parallel": false
            }
        },
        "reset": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "yarn prisma migrate reset --force"
                    }
                ],
                "cwd": "{projectRoot}",
                "parallel": false
            }
        },
        "seed": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "python3 seed.py"
                    }
                ],
                "cwd": "{projectRoot}",
                "parallel": false
            }
        },
        "studio": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    {
                        "command": "yarn prisma studio"
                    }
                ],
                "cwd": "{projectRoot}",
                "parallel": false
            }
        }
    },
    "tags": []
}
