# k3d-lane-budget

phase:    pr
repo:     gophersys/libs
branch:   fix/k3d-lane-budget
worktree: ~/code/.worktrees/libs-k3d
pr:       -
attempt:  1/2

## Goal

`go/workspaceprovider/kubernetesadapter` dies at exactly 600s in its integration
lane, because `go/_ctl/lib.sh` runs `go test -tags integration` with NO `-timeout`
and Go's default is 10 minutes per package. Give the substrate lanes a STATED
budget, so a real-cluster suite has room and a hang is still reported.

## Plan

APPROVED 2026-08-13. The planner REFUTED my brief on the point I was most sure of.

**I was wrong that splitting k3d from kind buys diagnosis.** Two reasons:
1. `.claude/rules/21-test-taxonomy.md` §d makes "the SAME ProviderSuite green on
   k3d AND kind" the distro-transparency PROOF. Splitting them splits the proof.
2. The panic ALREADY names the substrate: `running tests: TestKind_Conforms
   (2m28s)`. The diagnosis I said was missing is right there in the output I
   quoted.

So option (b) is out on its own merits. The plan is (a) — a stated budget as a
general rule — plus per-test cost reporting, which is what actually attributes it.

**THE REAL BUDGET IS NOT THE FLAG.** The package stands up **8 clusters, not 2**:
6 k3d + 2 kind, one per test. `harnessConfig.perTest`, set by `WithPerTest()`
whose own doc says "instead of the shared default", is **READ BY NOBODY** — the
shared default was never wired. Cluster bring-up is only ~25% of the package; the
suites are the rest.

### Measured, natively, and the reason is stated

`ghcr.io/gophersys/base:latest` is amd64-only and this host is arm64, so measuring
inside it means QEMU and meaningless seconds. Measured on the host instead.

```
k3d  bring-up + teardown alone   21.4s   (create 17.5 / kubeconfig+import 3.4 / delete 0.5)
kind bring-up + teardown alone   61.4s   (create 60.6 / load 0.4 / delete 0.4)
kubernetesadapter package        601.3s isolated, 544.1s in-lane, ceiling 600.0s
```

**It is not sailing past the wall — it is a coin flip at 91-100% of it.** That is
why it reads as flake.

Per test: `K3d_Conforms` 127s, `Kind_Conforms` 108s, `K3d_Editor` 64-106s,
`Kind_Editor` 69s, `K3d_Entrypoint` 114s and then SKIPS (busybox import failed),
3 others ~20s. CI is ~2x this host (`dockeradapter` 93s CI vs 46.1s local), so CI
needs ~18-20 min.

### It is a class, not a case

- `go/orchestrator` also declares `k3d kind` and spins a cluster.
- `go/agentsession` declares `k3d kind` and touches neither — an over-declared
  `require_cmd`, noted and out of scope.
- **The same landmine sits one verb over**: `_cover_profile` has no `-timeout`
  either, and both cluster libs set `EDEN_COVER_TAGS="lifecycle load integration"`,
  so `cover-floor` — a dimension of BOTH `phase-gate testing` and `qa` — runs the
  same 8 clusters under the same silent 10m.

## The shape

One home for the knob, matching `EDEN_COVERAGE_FLOOR`: default in
`go/_ctl/lib.sh`'s metadata block, override in the per-library dispatcher.

- `go/_ctl/lib.sh` — `: "${EDEN_SUBSTRATE_TIMEOUT:=10m}"`, REJECT `0`/`0s`/empty
  with a named error, pass `-timeout` in `cmd_integration`, `cmd_lifecycle`,
  `cmd_load` and `_cover_profile`, add `-v` to `cmd_integration` only, print the
  budget in `lib_usage`.
- `go/workspaceprovider/ctl.sh`, `go/orchestrator/ctl.sh` — `25m` (~1.4x the CI
  estimate).
- `.ci/ci.contract.yaml` — merge tier 30 -> 45 minutes, workflows REGENERATED by
  `cictl generate`, never hand-edited.
- 16 goldens re-recorded, diff reviewed line by line. NOT 17: my plan said 17 and
  that was wrong. `templates/go/http-gateway.txt` is byte-identical on purpose —
  its library is untouched, and that is the corroboration, not an omission.

The default `10m` is Go's own, so the 14 non-cluster libraries change behaviour
not at all — only the number stops being implied.

## THE RISK THAT EXPLAINS THE WHOLE DEFECT

**A pull request touching only `go/_ctl/lib.sh` runs ZERO library gates.**
`go/_ctl` has no `project.json` and `.ci/ctl.sh` skips it by name, so the
substrate tier would never exercise the fix. The 2 `ctl.sh` overrides MUST land in
the same pull request — that is what makes both libraries affected.

This is exactly how the defect survived, and I saw the same shape on libs #12
hours ago: both tiers green, zero libraries gated.

## Deliberately deferred — the REAL fix

Wire the shared cluster the harness already advertises. `WithPerTest`'s dead
`perTest` field, plus the already-written `uniqueClusterNamespace(t)` and per-test
`reap()`, are the machinery. 8 clusters -> 3 (`K3dClusterWithRegistry` needs
`--registry-config` and cannot share).

It is deferred for a stated reason, not from timidity: it changes a shipped
`<lib>test` package used by 2 libraries, carries real flake risk on the lane that
ALREADY flakes, and **there is no per-test cost measurement in CI to prove it
worked**. The `-v` in this change creates that baseline first.

## Proven

Everything under "Measured" was run by the planner NATIVELY — the base image is
amd64 on an arm64 host, so timing inside it is QEMU-emulated. The verifier could
NOT re-derive those seconds: no k3d or kind substrate is reachable in that
container. **That part of the verification is incomplete, and the absent thing is
a live cluster, not a tool.** The command that produces them belongs in this
section; see finding 2.

Run and reproduced by the verifier, in the container, both mounts:

- `bash go/_ctl/lib_test.sh` rc=0, `12 test(s) hold; 11 of 12 proven able to fail
  across 13 counter-stimuli; 1 stated no counter`
- `bash verb_conservation_test.sh` rc=0, `17 project record(s) hold; 3 mutant(s)
  caught`
- `bash ./ctl.sh validate` rc=0, over 31 scripts and 4 suites
- `shellcheck -S style` rc=0 on all 4 changed scripts
- The guard refuses `0`, `0s`, `""`, `0h0m0s`, `-5m`, `0m0s`, `" 0 "`,
  `notaduration` — rc=1 with the `go` stub recording ZERO invocations each time.
- THE ORDERING HYPOTHESIS IS REFUTED: both overriding dispatchers assign
  `EDEN_SUBSTRATE_TIMEOUT="25m"` BEFORE the `source` line, so a per-library
  override does reach the guard.
- Behaviour, not shape: the real lane returned in 2753ms with rc=1 against a 10s
  sleep and a 2s budget; under `integration-untimed`, 10756ms with rc=0.
- `.ci` drift verified by BUILDING the real cictl and running it: `drift` rc=0,
  then proven able to fail by editing `on-pr.yml` 45 -> 30, rc=1 naming the line,
  restored clean.
- No masked exit code anywhere in the added lines. All 6 added `|| rc=$?` take a
  redirect or a subshell, never a pipeline, and each is followed by a `die`.

## Phase 4 — 4 findings

**F1 (real hole, phase 2).** `lifecycle`, `load` and `-v` are pinned by the
GOLDEN and by nothing else. Proven: delete `-timeout` from `cmd_lifecycle` and
`lib_test.sh` stays GREEN; conservation goes red; then
`EDEN_CONSERVATION_RECORD=1` makes it green again and nothing anywhere is red.
The `integration` lane does NOT have this property — after a re-record its shape
and behaviour assertions still fail.

The distinction that makes this a finding: **the golden is the one check in this
repository with a sanctioned "make it match" button.** So 3 of the 5 things this
feature added are one `EDEN_CONSERVATION_RECORD=1` away from silent deletion.

**F2 (phase 3).** This `## Proven` named no command, and the unreproducible
`601.3s / 544.1s / 600.0s` is now load-bearing in 4 shipped files. The comments
attribute it honestly — "the planner's, measured natively, not re-derived here" —
so nothing is false; but nobody re-deriving `25m` after the first CI run has
anything to re-run.

**F3 (real defect, phase 3).** The guard is a CHARACTER FILTER, not a duration
parse: `-z "${VAR//[!1-9]/}"` passes anything holding a digit 1-9. So
`EDEN_SUBSTRATE_TIMEOUT=0.4ns` passes, reaches `go test -timeout=0.4ns`,
truncates to 0, and the lane runs UNBOUNDED. Proven against a real `go` and a
6-second test:

```
EDEN_SUBSTRATE_TIMEOUT=2s      rc=1  elapsed=3s  panic: test timed out after 2s
EDEN_SUBSTRATE_TIMEOUT=0.4ns   rc=0  elapsed=7s  --- PASS: TestSleeps (6.01s)
```

Contrived as an input, not as a defect: the error message claims "There is no
value that means 'no budget'", and that claim is false.

**F4 (mine).** This state file still read `phase: plan` after 3 implementation
commits, and 2 of its lines were untrue. Fixed here.

## Blocked

Nothing. No open questions.

## Next

Phase 7 fix, in order: implementer for F2 and F3, then test author for F1.


## Phase 7 — all 4 findings closed

**F3, the real defect.** The guard was a CHARACTER FILTER, not a duration parse.
Reproduced first against a real `go` and a 6-second test:

```
0.4ns          accept  rc=0 / 7s   UNBOUNDED — the 6s test ran to completion
0.0000000001s  accept  rc=0 / 7s   UNBOUNDED
2s             accept  rc=1 / 2s   bounded
```

FIXED WITHOUT A PARSER, and the reasoning is exact rather than approximate:
refuse any value containing `.`. Go's grammar puts digits only inside numeric
components and every unit is >= 1ns, so with NO `.` present one digit in 1-9
guarantees a total >= 1ns; with a `.` present it guarantees nothing, because Go
truncates to whole nanoseconds. Everything surviving both tests goes to Go, which
rejects a malformed duration loudly.

`1ns` is the load-bearing row: the smallest value the digit test admits, and it
REALLY BOUNDS — rc=1 in 729ms against a 10s sleep. That is what makes ">= 1ns" a
proof and not a hope.

The cost is stated in the message rather than hidden: `1.5h` is refused and you
write `90m`. A refusal is loud; accepting `0.4ns` was silent.

Two false statements also went: the absolute "There is no value that means 'no
budget'", and a message calling `notaduration` "zero or negative" — the same
defect one size down.

**F1, the hole the test author flagged in its own work.** `lifecycle`, `load` and
`-v` were pinned by the GOLDEN and by nothing else — and the golden is the one
check in this repository with a sanctioned "make it match" button. Proven, then
proven closed:

```
throwaway tree, -timeout removed from cmd_lifecycle
  BEFORE the new assertions: all 12 tests ok        <- the hole
  AFTER:  FAIL t_the_lifecycle_lane_states_its_budget
          plus phase 2 aborts — the mutant "changed nothing", so the
          counter-stimulus could not even be applied
```

**F2.** The command that produces `601.3s / 544.1s / 600.0s` is now recorded where
the `25m` lives, with the substrate it needs, why it cannot reproduce in the amd64
base image on an arm64 host, the date and host, and the line "if a re-run
disagrees, the 25m moves, not the comment".

**F4.** This state file was stale and 2 of its lines were untrue. Fixed.

## Proven

```
bash go/_ctl/lib_test.sh        rc=0   17 test(s) hold; 16 of 17 proven able to fail
                                       across 20 counter-stimuli; 1 stated no counter
bash verb_conservation_test.sh  rc=0   17 project record(s) hold; 3 mutant(s) caught
bash ./ctl.sh validate          rc=0   all checks passed, 4 suites
bash .ci/ctl.sh ci-drift        rc=0   no drift (host cictl; absent from the base image)
shellcheck -S style             rc=0   on every changed script
```

Every refused value runs ZERO `go` invocations — asserted, not assumed. A budget
that will be rejected must be rejected BEFORE the lane spends a substrate.

## Next

Phase 5 — the pull request.
