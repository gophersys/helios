apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: media
  labels:
    app: filebrowser
    app.kubernetes.io/part-of: music
spec:
  selector:
    app: filebrowser
  ports:
    - name: http
      port: 80
      targetPort: http
