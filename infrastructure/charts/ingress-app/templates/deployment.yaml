{{- /*
  templates/deployment.yaml — ingress-app

  STATUS: stub.

  When implemented, emits a Deployment identical to stateless-app plus
  templates/ingress.yaml (mandatory), templates/middleware.yaml
  (Traefik security + rate-limit + optional SSO middleware),
  templates/certificate.yaml (cert-manager Certificate CR).

  Because this archetype exists specifically to host the opinionated
  ingress wiring, the Deployment is identical to stateless-app's — the
  archetype distinction lives entirely in the extra ingress templates
  and the strict schema (ingress.enabled must be true, host required).
*/ -}}
