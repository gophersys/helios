{{- /*
  templates/job.yaml — job archetype

  STATUS: stub.

  When implemented, emits a Job with:
    - optional Helm hook annotations from .Values.job.helmHook
    - command/args, activeDeadlineSeconds, backoffLimit,
      ttlSecondsAfterFinished, completions, parallelism, completionMode
    - standard security + ExternalSecret + network-policy plumbing
    - nodeSelector + tolerations from .Values.scheduling
*/ -}}
