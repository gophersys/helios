# A FIXTURE, not a waiver file. See good.yaml.
#
# The defect: id is the only field trivy REQUIRES, and an entry without one
# waives nothing. It reads as a waiver — it has a reason and an expiry, so a
# reviewer sees a considered exception — while the finding it was meant to cover
# still fails the scan, or worse, the whole file is rejected and every other
# waiver in it stops applying too.
#
# The first entry is complete on purpose, for the reason no-expiry.yaml gives.

vulnerabilities:
  - id: CVE-2026-00007
    statement: fixture only — complete, and must not be reported
    expired_at: 2026-11-30
  - statement: fixture only — this entry names no finding at all
    expired_at: 2026-11-30
