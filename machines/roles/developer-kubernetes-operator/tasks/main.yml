---
# roles/developer-kubernetes-operator/tasks/main.yml
#
# STATUS: NOT IMPLEMENTED.
#
# TODO: implement the following when the first k8s-operator host is enrolled:
#   - ensure kubectl, helm are installed (platform-linux-* currently has a
#     conditional for this; needs upstream repos added first)
#   - install k9s via release tarball pin
#   - install argocd CLI via release tarball pin
#   - install krew (kubectl plugin manager) and a base set of plugins:
#       ctx, ns, neat, tree
#   - install kustomize binary
#   - drop a ~/.kube/config stub pointing at $KUBECONFIG env var (actual
#     kubeconfig lives in BW and is fetched via secrets-load)
#   - install a consistent zsh kubectl plugin line (optional)

- name: developer-kubernetes-operator is a stub
  ansible.builtin.debug:
    msg: "TODO: developer-kubernetes-operator not implemented — see role comments"
  tags: [developer, kubernetes, stub]
