// Package parallelfixture is a minimal module used only by lib_test.sh to run
// several golangci-lint instances at the same time against the shared config.
package parallelfixture

// Marker is a constant that gives the package something to compile.
const Marker = "parallelfixture"
