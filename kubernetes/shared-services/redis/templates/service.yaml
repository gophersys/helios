apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    {{- include "shared-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "shared-redis.selectorLabels" . | nindent 4 }}
