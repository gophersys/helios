apiVersion: v1
kind: Service
metadata:
  name: minio
  labels:
    {{- include "shared-minio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.apiPort }}
      targetPort: api
      protocol: TCP
      name: api
    - port: {{ .Values.service.consolePort }}
      targetPort: console
      protocol: TCP
      name: console
  selector:
    {{- include "shared-minio.selectorLabels" . | nindent 4 }}
