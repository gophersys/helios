# cert-manager Helm values
# Install: helm install cert-manager jetstack/cert-manager -n cert-manager -f values.yaml
installCRDs: true

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi
