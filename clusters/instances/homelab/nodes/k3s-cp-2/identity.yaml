# identity.yaml — k3s-cp-2 (homelab cluster member)
#
# Proxmox VM on pve-03, Ubuntu 24.04, joined to the homelab k3s cluster + Tailscale.
# Values from live kubectl (2026-07-05). See clusters/instances/homelab/identity.yaml.

name: k3s-cp-2
os: linux-ubuntu
purpose: "k3s control-plane — devops role"
tailscale_hostname: k3s-cp-2
status: active
location: proxmox-pve-03
owner: mateo.segura413@gmail.com

roles:
  - common
  - platform-linux-ubuntu          # NOTE: role to be created (nodes are Ubuntu 24.04, not Debian)
  - networking-tailscale
  - secrets-bitwarden-client
  - cluster-node-labels

hardware:
  provider: proxmox                 # NOTE: providers/proxmox/ module pending (R12)
  compute_unit:
    arch: amd64
    cores: 2
    memory_gb: 3
    storage_gb: 80                  # root VM disk (thin LV on the host NVMe); grown 60->80 live 2026-08-18
    storage_class: ssd
    network: tailnet
  proxmox:
    host: pve-03
    # vmid: TODO (confirm on pve-03)

kubernetes:
  role: server                      # k3s role
  cluster_role: devops
  labels:
    role: devops
  taints:
    - key: devops
      value: "true"
      effect: PreferNoSchedule   # soft — control-plane/platform prefers not to run apps
  extra_flags: []

irreplaceable: false                # Proxmox VM — recreatable (unlike OCI A1.Flex)

ssh:
  port: 22
  allowed_tailnet_tags:
    - "tag:admin"
