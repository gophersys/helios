# The repository path of your local development host.
# 
# Docker in Docker requires host's path to properly mount volumes
CONCORD_MONOREPO_ROOT=/path/to/concord

# Set to true to enable sandbox mode
IS_SANDBOX=1