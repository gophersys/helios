{
  "name": "ci-devcontainer",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": ["scope:shared", "type:ci-orchestration"],
  "targets": {
    "ci-.devcontainer-validate": {
      "//": "This verb delegates to the repository-root ctl.sh, so it inherits that verb's runtime file discovery and can carry no complete inputs list either. The list that stood here repeated the root's fault. See .claude/rules/00-identity.md, 'Nx caching'.",
      "executor": "nx:run-commands",
      "options": {
        "command": "bash ./ctl.sh validate",
        "cwd": "{projectRoot}"
      },
      "cache": false
    }
  }
}
