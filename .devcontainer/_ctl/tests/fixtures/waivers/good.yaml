# A FIXTURE, not a waiver file. It lives under _ctl/tests/fixtures/, so no
# scanner reads it. Its only reader is _ctl/tests/scheduled-workflows.test.sh.
#
# Every entry here carries the 3 fields the rule asks for, so every detector
# must stay QUIET on this file. It is also what proves the reader parses at all:
# the test asserts this file holds exactly 3 entries, so a parser that stopped
# matching reports 0 and fails here instead of reporting a clean real file it
# never read.
#
# The CVE identifiers are invented. This file waives nothing.

vulnerabilities:
  - id: CVE-2026-00001
    statement: fixture only — this identifier does not exist
    expired_at: 2026-09-30
  - id: CVE-2026-00002
    paths:
      - "usr/lib/python3.12/site-packages/fixture-1.0.dist-info/METADATA"
    statement: fixture only — the vulnerable path is not installed in this image
    expired_at: 2026-10-31

misconfigurations:
  - id: AVD-DS-0001
    statement: fixture only
    expired_at: 2026-12-01
