# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Build artifacts (none expected at repo root — Docker has its own cache)
# Keep intentionally sparse: this repo holds Dockerfiles + ctl scripts only.
