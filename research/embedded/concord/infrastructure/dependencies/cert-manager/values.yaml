# cert-manager Helm values — shared defaults.
# Cluster-specific overrides go in clusters/<name>/dependencies/cert-manager.yaml
crds:
  enabled: true
replicaCount: 1
resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi
