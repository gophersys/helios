# Underdetermination list — instance iI (run3)

Verbatim section (b) of the instance's return.

1. `pir.targets.unit_cost.sell`: UNKNOWN vs ABSENT — the highest-leverage ambiguity on the whole record. `conditional_fields` says "present iff the founder states an intent to sell; an explicit not-for-sale is ABSENT; silence is UNKNOWN and the runner must ask." "its just for me" + "2, and maybe 10 for my freinds" entails no sale but never declares it, and it answers `q.intent.who`/`q.scale.units`, not `q.scale.cost`. I took the strict reading (silence → UNKNOWN). Consequence: the ABSENT reading scores economics amber and yields verdict go; my reading yields blocked. One sentence of contract text decides the verdict.
2. `unknowns[].blocks` when the feeding question feeds no metric. `q.constraint.strength` and `q.followup.range` feed only PIR fields, so `blocks: []` — yet the docs describe `blocks` as "the P1 metrics it blocks." I used empty lists plus `gate: G0` (which the spec sanctions) and a prose note. Whether `pir.environment.limits` should list `metric.feasibility.env-risk` (which it materially degrades but does not feed) is undecided.
3. Is `phone` a "tight pick" for `q.phys.size`? The graph says only "tight picks open exact-dimension asks" with no threshold, and the transcript shows no dimension spawn. I recorded `pir.physical.dims: ABSENT` (unspawned adaptive). If `phone` is tight, that becomes a sixth runner error and an UNKNOWN.
4. The button and `pir.functions`. The contract's coverage table says hmi is fed by "`q.fn.shows` + `q.phys.controls`", but `q.phys.controls`'s `feeds` list omits `pir.functions`. I followed `feeds` (R2's authority) and left the button out of `functions`, recording it only in `physical.controls`. The two statements conflict.
5. `q.env.conditions` unpresented values. `multi-affirmation` says a partial presentation is "a runner error recorded in the PIR" but does not say what becomes of `drops`/`"none"` — an UNKNOWN per value, or nothing. I recorded the runner error only and stated that `drops`'s state is not established.
6. Whether an ungrouped `q.fn.rate` spawn is a defect. "once per named sense/act" vs a runner that plausibly grouped altitude/speed/velocity into the position family. I logged RE-004 with the mitigating reading stated rather than assuming the grouping.
7. `rate.configurable` when unstated. The `rate-attachment` convention types it `bool`, leaving no room for UNKNOWN. For the tilt rate I wrote `false` (not stated as configurable) rather than inventing a third state.
8. "as quick as possible" against the compute-demand derivation. The rule reads "every stated rate period >= 1 s"; an unbounded latency wish is not a period. I scored green on the stated period ("within seconds") and recorded the caveat.
9. `q.comp.safety` = `none` vs `minor`. The verbatim answer is "no" (→ `none`), but "hopefully not explode lol" names a real Li-ion-on-a-vehicle hazard. A `derived` scorer may not overrule a stated enum value, so I scored green and surfaced the hazard as a line addressed to the G1 human. `minor` would have made this amber (still no gate — the gate is `serious` only).
10. `q.power.duty`'s "akwas on schel". No convention covers transcription repair. I read it as "always on schedule" → `wakes-on-schedule`; reading it as `always-on` + schedule would add a fourth mode and push power-risk toward red. Logged as RE-005.
11. `q.scale.units` "2, and maybe 10 for my freinds" — the question asks y1 and y3; the answer names no years. I mapped 2→y1, 10→y3.
12. G0 condition 4 (`sfd graph lint`) has no recording slot. The PIR spec fixes `g0: {status, blockers[]}`, which offers nowhere to say a condition was *not evaluated*. I added a `g0.checks[]` block rather than let silence read as a pass — a documented extension to the record shape, flagged here.
13. `metric.feasibility.power-risk` with a `none-declared` floor. Green tests the floor ("floor comfortably plausible") and amber tests the delight target. A declared-none floor makes green vacuously true while the binding target (1 month) is untested. I let the amber clause win; a literal floor-only reading gives green.
14. The catalog seam's D1 rule vs. `metric.tech.exists`'s green. Every SoC record is D1 by construction ("declared ≠ supported"), so no capability resting on a SoC record can ever score green under this catalog. Whether that ceiling is intended is not stated.
15. F3's "cited at D1+" for named research tasks. The evidence bar requires the scorer to NAME an upstream Zephyr implementation, but naming from model knowledge cannot be cited at D1 against the pinned SHA. I marked those lines explicitly UNVERIFIED at 413b789d and made the verification the research task's content.
16. `scores[].verdict_weight` for P2-class metrics. The registry lint says the key is present iff `consumer` is P1-verdict, while the scorecard spec says entries mirror `verdict_weight`. I omitted the key on the three P2-class metrics.
17. `recommendation.reasons` ordering on a blocked verdict. The spec pins only `reasons[0]` (the G0 state) and says the rest carries "everything rules 3–4 would have said". I ordered: G0 → rule 3 → rule 4 → minor unknowns → P2 ambers → other unknowns → runner defects.
18. `decision` "empty". No shape given for empty. I wrote `decision: {}` with a comment rather than null-valued keys.
