# corpus seed — axis-sprawl. Default audit.Rules(); no [rules] override, and in
# particular no lowered axis_budget_floor: a seed that needs a loosened rule
# proves nothing about the shipped predicate.
[probe]
root = ".panel"

[probe.containers]
plate = ".plate"

[probe.parts]
dial = ".dial"
