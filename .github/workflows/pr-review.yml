name: pr-review

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: pr-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    runs-on: arc-review
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: gophersys/cictl/review@4313c9f10ee9e90dde6585dcdba224563d272826 # merged PR #26
        with:
          pr: ${{ github.event.pull_request.number }}
          harness: codex
          model: default
          budget-usd: "25"
          max-rounds: "4"
          stream-store: none
